# OCR 增强 · 第十七轮（需求攫取 → 剪贴板取字生产级规整/完善/修复）

> 执行时间：2026-07-14　|　轮次：17（自动化 hourly 触发）　|　聚焦：OCR 需求发现 + 剪贴板取字「规整 / 完善 / 修复」到生产级

---

## 一、需求攫取与发觉（OCR 文字识别端到端复盘）

前 16 轮已交付的能力（收敛在 OCR 面板 + 隔离 localStorage + 4 个取字入口 + 实体可点击打开）：

| 维度 | 已交付 |
|---|---|
| 取字入口 | ① 截图取字 ② 结果条取字 ③ 历史网格取字 ④ 剪贴板取字 |
| 识别质量 | 语言选择(auto/zh/en/ja)、置信度感知+阈值、智能阅读顺序(多列/竖排)、合并为一行 |
| 编辑/提取 | 逐行内联纠错、逐行勾选、智能实体提取(URL/邮箱/电话)、智能文本清洗、实体可点击打开 |
| 导出/复制 | 复制全部、带坐标导出(txt/md/JSON/TSV)、自动复制、面板内重识别、空间预览联动 |
| 体验 | 偏好持久化、耗时统计、本地历史(最近5)、面板字号、搜索高亮+命中跳转、飞行中锁防重入 |

### 剩余候选需求 × 可用性 × 风险矩阵（本轮评估）

| 候选需求 | 可用性 | 风险 | 本轮决策 |
|---|---|---|---|
| **剪贴板取字：规整/完善/修复到生产级**（按钮永久卡死隐患、文件回退误选非图片、按钮禁用不一致） | 高 | 低 | ✅ **本轮落地** |
| 剪贴板 Cmd+V 直接粘贴取字 | 中 | 中（需隔离文本输入框，避免全局快捷键回归） | ⏸ 留评估 |
| 翻译（OCR 结果多语翻译） | 中 | 高（需离线模型或联网，超出本地工具定位） | ⏸ 持续延后 |
| 多区域批量 OCR | 中 | 中 | ⏸ 延后 |
| PDF / 长图整页 OCR | 低 | 中 | ⏸ 超出定位 |

**结论**：面板内增强已高度收敛，最高价值的增量是**把用户点名的「剪贴板取字」做彻底的生产级规整**——纯增量、零耦合、不影响其它功能。本轮不做跨组件/网络边界项。

---

## 二、本轮落地：剪贴板取字生产级规整/完善/修复

> 仅改动 `read_clipboard_image` 后端命令 + `startOcrFromClipboard` 前端函数 + 2 个按钮的禁用态。**未触碰**任何截图/标注/历史/钉图/主题/其它 3 个 OCR 入口逻辑。返回契约（PNG data URL 字符串）不变，前端零改造依赖。

### 2.1 后端 `src-tauri/src/commands/edit.rs`：`read_clipboard_image` 文件回退完善

- **文件回退只挑真正的图片**：剪贴板里可能复制了文件夹、或混入了文本/PDF 等非图片文件。原逻辑直接取 `files.first()`，`image::open` 会报「不是可识别的图片」，误导用户以为图片坏了。
  - 新增 `IMG_EXTS`（png/jpg/jpeg/gif/bmp/webp/tiff/tif/heic/heif），在回退路径里 `find` 第一个扩展名命中的文件；若没有任何图片文件，返回精准文案「剪贴板里没有可识别的图片文件」。
  - 覆盖「Finder 复制图片文件」「资源管理器复制图片文件」场景，且对混入非图片的复制不再误报。
- 零新 crate / 零新权限；命令注册行不变；arboard `get().file_list()` API 不变。

### 2.2 前端 `EnhancedScreenshotApp.tsx`：`startOcrFromClipboard` 规整/修复

- **【修复·核心】按钮永久卡死隐患**：原函数把 `ocrClipBusy` 的重置放在 `await runOcr(...)` 之后。虽然 `getImageDims` 永不 reject、`runOcr` 内部已兜底，但任何意外异常（如 `setCurrent` 在极端并发下抛错）都会跳过重置，使「读取中…」永久卡死、按钮永久禁用、该功能彻底不可用。
  - 重写为 `try { 读图 + 进编辑器 + runOcr } catch { 错误分级文案 } finally { 必重置 ocrClipBusyRef / setOcrClipBusy(false) }`，**无论成功/读取失败/任何意外异常都解锁按钮**。这是生产级健壮性的关键保证。
- **错误分级保留**：仅 `read_clipboard_image` 可能 reject，按「没有图片 / 不是图片 / 无可用图片」归 `clipEmpty`，其余归 `clipFailed` 附原因——行为与旧版一致，文案精准。

### 2.3 按钮禁用态一致性（规整）

- 主屏 `home-sec-btn` 原 `disabled={ocrClipBusy}`，编辑器工具栏按钮 `disabled={ocrBusy || ocrClipBusy}`——两处不一致。
  - 统一为主屏 `disabled={ocrBusy || ocrClipBusy}`，与编辑器按钮完全一致；识别进行中两处均禁用，避免「识别时又点剪贴板取字」的边缘重入。

---

## 三、验收

| 项 | 命令 | 结果 |
|---|---|---|
| 后端类型/编译 | `cargo check`（src-tauri） | ✅ exit 0（2.07s） |
| 前端构建 | `pnpm build`（tsc && vite build） | ✅ exit 0（125 模块） |
| 契约不变 | `read_clipboard_image` 返回 PNG data URL 字符串 | ✅ 前端零改造 |
| 耦合检查 | 截图/标注/历史/钉图/主题/其它 3 个取字入口逻辑 | ✅ 未改动 |
| 回归检查 | 4 列截图网格、编辑器工具栏、结果条取字、历史网格取字 | ✅ 逻辑未动 |

### 生产级行为对照（修复前 → 修复后）
- 读图阶段任何意外异常：按钮可能永久「读取中…」卡死 → **`finally` 保证必解锁，功能始终可用**。
- Finder 复制「文件夹 + 一张 PNG」的混合选择：原逻辑可能挑到非图片文件报「不是可识别的图片」 → **只挑图片扩展名文件，精准识别或精准报「没有可识别的图片文件」**。
- 识别进行中再点主屏剪贴板按钮：仅 `ocrClipBusy` 拦截、与编辑器按钮策略不一致 → **两处统一 `ocrBusy || ocrClipBusy`，一致禁用**。

---

## 四、仍留评估（延续，本轮不做）
- 剪贴板 Cmd+V 直接粘贴取字（需谨慎隔离文本输入框，避免全局快捷键回归）
- 翻译、多区域批量 OCR、PDF/长图整页 OCR（风险/定位边界）

剪贴板取字经本轮规整/完善/修复后已达生产级：大图保护、文件回退（精准）、零尺寸守卫、错误分级、防重入、按钮永不卡死。OCR 4 入口 + 面板增强高度收敛。
