# OCR 功能增强 · 第十五轮（需求攫取 → 实体可点击打开 URL/邮箱/电话）

> 时间：2026-07-14 09:46。状态：代码完成，`cargo check`（后端）与 `pnpm build`（前端 tsc + vite，125 模块，exit 0）均通过。**改动极小、收敛在 OCR 实体区块 + 1 个薄后端命令**，未触碰截图/标注/历史/钉图/主题等任何其它功能。

## 一、需求攫取与发觉（本轮起点）

对前 14 轮已落地能力做盘点（结构化识别/语言/区域/内联纠错/导出/搜索高亮/置信度/合并/持久化/自动复制/统计/历史/阅读顺序/重识别/快捷键/字号/勾选/空间预览联动/4 个取字入口/智能实体提取/预览框框选范围/智能清洗/实体整块复制）。

**关键发觉**：被连续延后 14 轮、标记为「中可用性 / 中风险、需 shell 插件、跨组件风险」的 **#1 缺口「实体项可点击打开（URL/邮箱/电话）」**，其唯一阻塞条件（缺打开外部 URL 的能力）**已在项目中被满足**——
- `src-tauri/Cargo.toml` 已依赖 `tauri-plugin-opener = "2"`；
- `src-tauri/capabilities/default.json` 已授予 `opener:default` 权限；
- `src-tauri/src/lib.rs` 已 `.plugin(tauri_plugin_opener::init())` 注册。

即「无 shell 插件」的前提已不成立，原风险评估失效。重新评估：**可用性 高（截图取字后直接点开链接/发邮件/拨号，是最高频真实诉求）/ 风险 低（复用既有插件，薄封装、零新 crate、零新权限）**。满足「集成/改善/优化且不影响其它功能」约束，落地。

## 二、实现（增量、零回归）

### 1. 后端新增命令 `open_external`（薄封装，零新依赖）
- 新文件 `src-tauri/src/commands/open.rs`：
  - `open_external(target: String)`：按前缀路由——
    - `http(s):` / `mailto:` / `tel:` → `open_url`；
    - `www.` 裸域名 → 补 `https://` 后 `open_url`；
    - 本地存在的文件路径 → `open_path`；
    - 其它（裸域名等）→ 兜底 `https://` 后 `open_url`；
  - 复用已集成的 `tauri_plugin_opener::{open_url, open_path}`，**不引入任何新 crate**；只读、不写、不申请额外权限；失败返回中文错误供前端 toast。
- `src-tauri/src/commands/mod.rs` 加 `pub mod open;`；
- `src-tauri/src/lib.rs` 的 `invoke_handler!` 注册 `commands::open::open_external`（仅新增一行，未改任何现有命令签名）。

### 2. 前端：实体项文字改为可点击打开
- 在实体区块 IIFE 内新增 `openExternalEntity(kind, item)`：
  - `urls` → 原样（自动补 `https://` 若 `www.` 开头）；
  - `emails` → `mailto:` + 原文；
  - `phones` → `tel:` + 仅保留数字；
  - 调 `invoke('open_external', { target })`，失败 `flash(t('ocr.openEntityFailed'), 'error')`。
- 实体 chip 内的 `<span className="ocr-entity-text">` 改为 `<button className="ocr-entity-text ocr-entity-link">`，`title={item}`（悬停看全文）、`aria-label={t('ocr.openEntity')}`、`onClick` 打开；复制按钮保持不变（复制与打开并存）。

### 3. i18n（zh-CN / en-US 同步）
- `ocr.openEntity`：「点击用系统默认程序打开」/「Open with the default app」；
- `ocr.openEntityFailed`：「打开失败：{msg}」/「Failed to open: {msg}」。

### 4. CSS
- 新增 `.ocr-entity-link`：复用 `.ocr-entity-text` 的省略样式，叠加链接观感（`color:var(--accent)`、`cursor:pointer`、hover 下划线、`focus-visible` 焦点环），主题变量自适应、亮/暗统一。

## 三、验证
- `cargo check` → Finished，exit 0（首编 `None` 类型推断报错，按 `open_url<P,S:AsRef<str>>` 签名改为 `None::<&str>` 修正后通过）。
- `pnpm build` → exit 0（tsc 通过，125 模块，vite 产物正常）。
- `dist/assets/*.js` 含 `open_external`/`openEntity`/`openEntityFailed`/`ocr-entity-link`；`dist/assets/*.css` 含 `.ocr-entity-link` 定义；locale 已含中英文案。
- 旧行为保留：`ocr-entity-text` 仍出现在产物（复制按钮与文字省略样式均未动）。

## 四、零回归 / 不影响其它功能
- **仅改 OCR 实体区块**：1 个新后端命令（薄封装既有 opener 插件）+ 前端 1 个 helper + 实体 chip 文字变按钮 + 2 个 locale 键 + CSS；未改任何命令签名、截图/标注/历史/钉图/主题/权限流程。
- **默认行为完全一致**：实体区块本身由 `ocrExtract` 开关控制（默认关，零回归）；开启后文字由纯文本变为可点击链接，复制按钮并存，复制/导出/贴回/历史数据链完全未被触碰。
- 新增命令仅被 OCR 面板调用，与其它功能无任何耦合。

## 五、仍留后续评估（延续前几轮，本轮不做）
- 翻译（网络/离线边界偏高，或超出本地工具定位）；
- 多区域批量 OCR（需改造框选流程，改动面大）；
- OCR 全文（非实体区块）中 URL 自动可点击——本轮聚焦实体区块（需求原始定义即「实体项可点击打开」），全文内联 URL 高亮点击为另一增量，留待后续评估。
- **OCR 面板纯前端增强已高度收敛；被延后 14 轮的「实体可点击打开」本轮已闭环。**
