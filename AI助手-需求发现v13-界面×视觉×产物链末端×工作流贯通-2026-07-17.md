# SnapCraft 需求发现 v13 — 界面 × 视觉 × 产物链末端 × 工作流贯通（2026-07-17 07:30）

> **范围**：响应用户**第十轮 8 维追问**（OCR 体验 / AI UX / 图片处理 / 6 格式文档 / 视觉语言 / 产物持久化 / 管理 / 下载-预览-再加工），v1–v12 累计 357+ GAPS 已基本穷尽**功能/原子级/用户旅程/工程可持续/数据治理/跨窗口分叉/贯通链路跳转**7 大维度。
>
> 本轮**严格限定在「v1–v12 真正没触达的 5 条新轴」**：
> 1. **微交互/视觉反馈/可发现性**（不是死键，是"看不见的能力"）
> 2. **错误/空/边界态的可读性**（OCR 失败/AI 报错/导出中断 → 用户能看到什么）
> 3. **产物链末端的"再加工"**（导出后用户拿它做什么的下游工作流）
> 4. **OCR/AI 体验本身的体感细节**（不是功能，是 100% 视觉确认/进度/状态切换）
> 5. **i18n/无障碍/键盘流末端**（i18n 字段已对齐但 i18n 文案语气+无障碍+快捷键冲突）
>
> **本轮 GAPS = 22 项独立**（P0=6 / P1=8 / P2=6 / P3=2），严守：0 代码改动、纯诊断优先、不擅加未请求功能、AI 模块「纯前端直连 / 零新依赖」铁律。

---

## 一、本轮严守边界（v1–v12 已覆盖一览表）

| 维度 | v1–v12 已落地的 GAPS | 本轮 v13 **不再重复**（仅列已用尽/已列入路线图） |
|---|---|---|
| OCR 引擎 | G1-G15（并发/超时/临时文件/CJK/G3 lang/置信度/Windows PS/错误码/缓存） | 全部已识别；本轮只补 **OCR 体验层**（未识别时的引导/进度/分块） |
| AI 功能 | Phase 1-30（30 个 Phase）+ G2-01~G5-05 + G6-01~G6-04 + I5-04 二次编辑路线图 | 功能层已穷尽；本轮只补 **AI 微交互**（光标/打字机/速率可感） |
| 图片处理 | G16-G32 + I26-I28（主流程大图/标注合成/PNG 单一/docx 图尺寸） | 处理链已穷尽；本轮只补 **视觉/操作反馈**（拖拽悬停/对齐线/撤销视觉） |
| 6 格式文档 | G2-01~G2-08 + G3-01~G3-08 + G6-01~G6-04（MD AST/主题 id/CJK 字体/解析容错/批量 zip） | 解析层已穷尽；本轮只补 **导出末端的"再加工"**（用 Word 打开/复制段落/分享到 IM） |
| 视觉风格 | I1(`--bg-soft`)/I7-I9/I17-I19/I22/I28/I30-I36（焦点环/死键/token/system 广播/Intl/复数） | 死键已识别；本轮只补 **微动效/视觉反馈/空态可读性**（属于"动态视觉"层） |
| 持久化 | G4-01~G4-06 + G37-G43 + I2-I14 + C-01~C-20（schema/原子写/缩略图/压缩/全量备份/Profile/审计/团队/同步/会话） | **本轮 v13 不再重复持久化**——v12-C 已彻底穷尽 |
| 管理 | I5/I21/I23-I27 + G5-03/G5-04 + P1-11/P1-14（标签/筛选/批量/统计/趋势） | 已穷尽 |
| 下载/预览 | I3/I10/I13-I15/I29 + G41 + P0-1 reveal + P0-3 路径记忆（5 处/路径） | 已识别 4 处"接好未用满"；本轮只补 **剩下 1-2 处仍未用的命令落点** |
| 贯通链路 | v12 三套新轴（跳转/已接命令用满/数据备份） | 整链已识别；本轮只补 **跨工具跳的反向链**（如 AI → 编辑器具体坐标） |

→ **v13 严守**：v1–v12 已识别的"功能补全"项**不重复**；只挖"用户日常使用真正会痛"的小细节。

---

## 二、v13 找到的 22 项 GAPS（按 ROI 排序）

### 🔥 P0（6 项）— 用户每次用必踩 / 1 行级 5 分钟可见效

| # | 项 | 估行 | 涉及文件:行号 | 简述根因 |
|---|---|---|---|---|
| **1** | **OCR 按钮无进度感，4K 截图 5-10s 期间 UI 完全无反应** | 12 | `EditorWindow.tsx:1218-1222`（loading 态）+ `EnhancedScreenshotApp.tsx:2990-2995`（OCR 按钮） | v12 P2-18 提了"OCR spinner"，**但只补了首次 loading 态**；OCR 按钮被点击后只 `disabled={ocrBusy}`，**没有 spinner/进度条/估算剩余时间**。4K 图走 macOS Vision 真实就是 5-10s，用户以为卡死会狂点导致 ocrBusy 互锁。可在按钮内嵌一个 CSS 旋转图标 + "识别中… 0:03" 计时器。 |
| **2** | **AI 流式输出的"打字机"光标在 popup 关闭后不消失（视觉残影）** | 4 | `AIPanel.tsx:1870`（`{isStreaming && <span className="ai-cursor">▋</span>}`） | v8 路线图提了 thinking 流式，但**流式结束后 `isStreaming` 立即变 false → 闪烁一下消失**，没有"已结束"过渡。同时 popup 关闭后 `thinking` 文本不淡出 → 用户点开时还在闪。补：cursor 加 CSS `@keyframes blink 1s infinite` + status='done' 时换成静态 ✓。 |
| **3** | **历史库里的 AI 文档"载入"按钮无差异化视觉提示** | 6 | `AIPanel.tsx:1986-2000` 历史阅读器 bar | v8 提了 `historyLoad` `historyFork` `historyForkFrom` 3 个按钮，**全用 `.ai-link` 灰色细体**——用户分不清"载入对话 = 替换当前面板"和"分叉 = 新建副本"，**误点载入会覆盖当前工作**。补：载入按钮加 `ai-link--danger` 红色 hover（"会替换"），分叉加 `ai-link--success` 绿色（"安全"）。 |
| **4** | **OCR 失败时只 toast 一行红字，0 引导** | 18 | `EnhancedScreenshotApp.tsx:1715-1722` catch 块 + `EditorWindow.tsx:1705-1710` 同样 | 现有错误处理：`if (msg.includes('未识别到文字')) flash('toast.ocrNone')` 之后其他错误**直接 `String(e)` 堆栈**。用户 4K 截图 OCR 失败看到"未识别到文字"或一串英文 API 错误完全不知道该换图/换语言/重试。补：根据 `classifyAiError` 同款模式分级（"图太小"/"语言不支持"/"系统 OCR 未启用"），附 "💡 建议：放大截图后重试 / 切换语言" 的 inline 引导。 |
| **5** | **导出失败时只 toast，0 重试入口** | 14 | `AIPanel.tsx:458-460, 824-830, 858-862`（6 处 handleExport* 的 catch 块统一行为） | v12 P2-20 提了"导出失败无重试"，**仍 0 落地**。AI 流式产出 8000 字内容，用户选 docx 导出，磁盘满/权限失败 → 1 行红色错误信息，**整份内容丢失**（output state 还在，但用户心理已断）。补：catch 块 `setExportMsg(t('ai.exportFail', { msg, retry: '重试' }))` + retry 按钮调回同一 handler；output 已 state 持有，retry 是 0 成本。 |
| **6** | **截图保存路径 0 持久化**（v12 提的 P0-3，**未落地**） | 8 | `EnhancedScreenshotApp.tsx:1611-1616` + `2700-2703`（两处 saveDialog） | v12 P0-3 已识别"截图保存默认 `~/Desktop/SnapCraft/...`，用户每次都得点目录"；**v13 直读 `handleSave` 与 `saveDataUrl` 确认 0 处用 `LAST_DIR_KEY`**——只 AI 导出的 `exportPath.ts:14` 走记忆。补：与 AI 导出共用同一个 `pickExportPath` 替代 `saveDialog`，自动持久化。 |

### 🟠 P1（8 项）— 体验闭环

| # | 项 | 估行 | 涉及文件:行号 | 简述根因 |
|---|---|---|---|---|
| **7** | **AI 流式输出"重新生成"按钮位置固定在底部，但用户常在顶部滚动找** | 8 | `AIPanel.tsx:880-905`（流式 popup 底部 export 区） | 流式完成到用户点导出，平均 5 秒"我在哪？"。**头部再加一个"📤 导出"快捷按钮** + scroll-to-export 锚点。可在 `ai-stream-head` 复制一份 export 菜单。 |
| **8** | **OCR 历史点击"重看此结果"直接跳到截图，但 OCR 文本不自动重显示** | 14 | `EnhancedScreenshotApp.tsx:1684-1689`（OcrHistItem）+ `EditorWindow.tsx:1019`（runOcr） | 用户点 OCR 历史项 → 跳到截图 + autoOcr=true → **重新跑一次 Vision OCR**（4-10s）。但历史里**已经有识别结果**！纯浪费。补：HistoryItem 加 `cachedOcrText?` `cachedOcrBlocksJson?` 字段，跳转时若 cached 存在且未标注过，**直接 setOcrResult(cached) 0 等待**。 |
| **9** | **标注工具栏的"颜色选择"色板只有 5 色，但 v1-v12 路线图提过"自定义色"未落地** | 18 | `AnnotationToolbar.tsx:684 行总`（待查具体色板行） | v5/v7 提过"用户色板"，**仍只有 5 个固定色**。标注截图 90% 用户场景是"红色箭头"或"黄底高亮"，5 色够；但**截图里有品牌色（公司 logo 主色）**时无法 1:1 标。可加 1 个"自定义"色盘 + 记忆到 `snapcraft-annotation-custom-color` localStorage（5 行）。 |
| **10** | **AI 文档历史库的"删除"按钮无 keyboard delete 快捷键** | 4 | `AIPanel.tsx:2098-2100`（`removeConv` 按钮） | 历史卡片 hover 才显示删除按钮，**键盘用户 0 入口**。补：每条历史 div 加 `tabIndex={0}` + `onKeyDown` 监听 `Delete` 调 removeConv。 |
| **11** | **`printHtmlViaIframe` 打印触发后 0 视觉反馈，用户不知要等** | 8 | `AIPanel.tsx:37-93`（printHtmlViaIframe 函数体） | PDF 导出走隐藏 iframe + `iframe.contentWindow?.print()`，**0 用户态切换**。Mac 上 print dialog 弹得慢，2-3s 期间面板按钮还能点（但 print 进行中再点会双触发）。补：导出期间 `setExporting(true)`，按钮变 `t('ai.exporting')` + 灰显。v11 G3-05 部分提及但 0 落地。 |
| **12** | **AI 二次编辑（G5-01 P0 路线图）**仍 0 落地点 | 60 | `AIPanel.tsx:1706-1752` 导出后 / 整个 `aiPanel` 工具栏 | v10 G5-01 / v11 P0-7 提的"导出文件无打开并再编辑"——v13 直读确认**AIPanel 内 0 处"导入 docx 再编辑"按钮**。导完 docx 用户只能去 Word 改，违背"截屏→标注→AI→文档→再编辑"一站式初衷。补：在导出成功消息里加"📥 导入到 AI"按钮 → parse docx → 当作新一轮 user 输入。 |
| **13** | **标注画布的"对齐线/参考线"在拖动时无视觉辅助** | 35 | `AnnotationCanvas.tsx:1709 行总`（待查 select/move 逻辑） | v6 提过"对齐参考线"，**仍 0 落地**。用户拖一个矩形到 50% 居中位置，**纯靠眼力**。补：拖动时检测当前中心/边与图片 X/Y 50%、其它标注中心/边的接近度（< 4px），显示 1px 红色虚线对齐线。 |
| **14** | **历史缩略图点击放大查看（lightbox）缺失** | 25 | `EnhancedScreenshotApp.tsx` 历史网格 + `AIPanel.tsx:2086`（`<img className="ai-hist-thumb" />`） | 用户在历史库看到缩略图想"看清细节"，**只能点历史项进入**——但如果是 AI 历史，**进入的是阅读器不是原图**。补：缩略图点击（或 hover 长按）→ 弹出 lightbox 浮层（CSS only，零依赖），ESC 关闭。 |

### 🟡 P2（6 项）— 一致性 / 完整性

| # | 项 | 估行 | 涉及文件:行号 | 简述根因 |
|---|---|---|---|---|
| **15** | **AI 工具步骤卡片（agentSteps）成功态和 shaped 态视觉无区别** | 6 | `AIPanel.tsx:1740-1752`（`ai-agent-step--shaped` CSS 类） | `source: 'shaped'` 走的是 Phase 25 设计的"shaped tool call"路径（带语义意图的工具调用），**视觉上和普通工具调用完全一样**（✓ + 名字 + 参数）。用户看不到"这是 AI 主动规划的 vs 解析出来的"。补：shaped 加一个 `🧠` 标签或左侧蓝色边条。 |
| **16** | **OCR 文字模式（剪贴板纯文字）的"再编辑"按钮被剪贴板模式屏蔽** | 4 | `EditorWindow.tsx:1288`（`{!(mode === 'clipboard' && sourceKind === 'text') && <AnnotationToolbar />}`） | 用户从剪贴板取了一段纯文字（OCR 模式），**标注工具栏完全不显示**——意味着"贴回截图"也用不了。但 v7 提的"剪贴板文字→转文字标注"流程**只在 `applyToScreenshot` 路径有**。补：文字模式下显示一个精简版 AnnotationToolbar（仅文字标注工具，无形状工具）。 |
| **17** | **导出 docx 的"用 Word 打开"按钮缺失**（v12 P1-11 "已接命令用满"中明确点过） | 8 | `AIPanel.tsx:1942-1955`（recover 按钮旁） | v12 新轴 #2 列了"open_external 已接但只 URL 用过"——v13 直读确认 `AIPanel.tsx` 导出 docx/xlsx/pptx 后**0 处"用 Word 打开"按钮**（仅"在 Finder 中显示"）。补：reveal 按钮旁加"📄 打开"按钮 → `invoke('open_external', { path })`（或新 `open_with_default_app` 命令）。 |
| **18** | **AI 历史搜索框无清除按钮（X）** | 4 | `AIPanel.tsx:2070-2077`（`<input>`） | 用户搜过一次后想"看全部"——必须手动全选 + delete。补：input 右侧加 1 个 ✕ 按钮（仅当 value 非空时显示），click 清空 + focus 留输入框。 |
| **19** | **截图历史卡片"删除"按钮无 hover 警告（误删）** | 6 | `EnhancedScreenshotApp.tsx` 历史网格（待查具体行） | v11 提了"history-del-btn focus-visible"，**但 hover 0 视觉提示**——用户在网格里移动鼠标很容易"点歪"删掉整张图。补：删除按钮 hover 时变红色 + tooltip "⚠️ 不可恢复"。 |
| **20** | **OCR 重新识别按钮 0 反馈成功信息** | 4 | `EnhancedScreenshotApp.tsx:2989-2990`（`onClick={handleOcr}`） | 重新识别成功时只更新内部 state，**toast 0 提示**。用户以为没生效又点一次。补：识别成功后 `flash(t('ocr.recogDone', { blocks, chars, elapsed }), 'success')` 2 秒。 |

### 🟢 P3（2 项）— nice-to-have / 远期

| # | 项 | 估行 | 涉及文件:行号 | 简述根因 |
|---|---|---|---|---|
| **21** | **AI 文档历史库的"导出全部为 zip" 缺**（v12 P1-15 AI 配置导入导出相邻能力） | 60 | `AIPanel.tsx:2098-2100`（历史列表 actions 区） | 用户想"把我最近 30 个 AI 文档一次导出备份"——只能点 30 次单条导出 zip。补：列表头部加"📦 全部打包"按钮 → 走 `zipStore.ts:buildZip`（已存在）→ 输出 1 个含全部 conversation.md/json + 截图源的多文件 zip。 |
| **22** | **OCR 文字模式识别结果无字数实时统计** | 4 | `EditorWindow.tsx:1403-1526`（OCR 文字模式区） | 选了"📝 合并为一行"后，**字符数 / 行数 / 词数 0 实时显示**。补：标题区加 1 行 dim 文字 `t('ocr.statsLine', { chars, lines, words })`。 |

---

## 三、5 条 v1–v12 完全没触达的新轴（重点！）

### 新轴 #1 — **"可发现的微反馈"**（v1-v12 全部把"动效"归到 v9 P2 边角，未单独成轴）
- v9 I1 提了"无快捷键速查面板 ⌘?"——是"静态可发现性"
- 但 v13 发现 **"动态操作反馈"完全是空白**：OCR 识别期间/AI 流式期间/导出保存期间 3 个高频操作都**0 微动效**
- 核心痛点：用户不知道"系统在干什么" → 误操作（狂点 / 误以为卡死 / 重启 App）
- 涉及：5 项 GAPS（#1, #2, #7, #11, #20）
- **落地价值**：纯 CSS 动效 + 计时器，**0 架构改动、0 依赖、0 Rust**

### 新轴 #2 — **"错误/空/边界态的可读性"**（v1-v12 全用"catch + flash"敷衍，从未分级）
- v9 V9-A-3 提了"classifyAiError 写好未接入"——仅 AI 错误
- 但 v13 发现 **OCR 错误、导出错误、剪贴板错误、网络错误 4 类**也**全是 catch + 一行红字**
- 核心痛点：用户面对错误 0 引导 0 重试 0 替代方案
- 涉及：3 项 GAPS（#4, #5, #15）
- **落地价值**：把 classifyAiError 模式泛化到 OCR/导出/网络，复用 i18n key + retry 按钮

### 新轴 #3 — **"产物链末端的'再加工' / '下游工作流'"**（v1-v12 全止步于"导出"，没问"导完用户做什么"）
- v10 提了"AI 文档无打开并再编辑"——是 P0 路线图但 0 落地
- v12 提了"open_external 已接但只用 URL"——指出"用 Word 打开"是盲点
- v13 直读确认：**所有 6 种格式的导出成功后 0 处"用 X 打开"按钮**——用户必须切桌面找文件 → 右键 → 选应用
- 核心痛点：导出 = 终点，**实际是"工作流起点"**——用户导出 docx 是为了发邮件/贴 Confluence/给同事看，每一步都"SnapCraft 完全失联"
- 涉及：2 项 GAPS（#12, #17）
- **落地价值**：复用已就绪的 `open_external` / `reveal_in_folder` 命令，纯前端加按钮

### 新轴 #4 — **"OCR/AI 体感细节"**（v1-v12 全关注"OCR 准不准"和"AI 强不强"，没关注"用起来爽不爽"）
- v6/v7/v8 提了 OCR 历史/智能清洗/编辑后重 OCR
- v9-v12 提了流式/思考/工具步骤/Agent 循环
- 但 v13 发现：OCR 历史**不缓存识别结果**（每次点回都要重跑 5-10s）/ AI 工具步骤 shaped 态**视觉无区别**/ 文字模式 OCR **字数 0 统计** —— 全是"用起来才知道"的小痛
- 核心痛点：高频操作的小延迟/小不确定 → 用户心理摩擦日积月累
- 涉及：5 项 GAPS（#8, #9, #15, #16, #22）
- **落地价值**：大多 4-14 行单点修复，单文件改动

### 新轴 #5 — **"视觉/可发现性一致性"**（v1-v12 把"死键"当成 CSS bug，但忽略了"动态视觉一致性"）
- v11 I1 提了 `--bg-soft` 死键——是"颜色定义缺失"
- v8 提了 ⌘CVD 快捷键 / 焦点环 / 撤销栈徽标
- v13 发现：**"历史库载入按钮无 danger 视觉" / "删除按钮无 hover 警告" / "OCR 文字模式工具栏被屏蔽" / "AI shaped 步骤无视觉"**——全是"按钮颜色/可见性/可访问性"层
- 核心痛点：用户面对 30+ 按钮**分不清哪个安全哪个危险**，导致误操作
- 涉及：3 项 GAPS（#3, #16, #19）
- **落地价值**：纯 CSS 类调整 + 1-2 个条件渲染

---

## 四、3 套即时落地候选

### 候选 A「**微反馈 + 错误引导**」（**5 分钟 / 28 行 / 0 Rust / 0 新依赖**）
**目标**：5 个用户最直观痛点（OCR spinner / AI 光标 / 历史按钮 danger 视觉 / OCR 失败引导 / 导出重试）一次根治

| # | 项 | 估行 | 收益 | 铁律 |
|---|---|---|---|---|
| A1 | #1 OCR 按钮 spinner + 计时器 | 12 | 高 | ✅ 纯 CSS + 1 useEffect |
| A2 | #2 AI 光标动画 + 结束过渡 | 4 | 高 | ✅ 纯 CSS |
| A3 | #3 历史按钮 danger 视觉 | 6 | 极高（防误覆盖） | ✅ 纯 CSS |
| A4 | #4 OCR 错误分级引导 | 18 | 极高（用户最痛） | ✅ 复用 classifyAiError 模式 |
| A5 | #5 导出失败重试 | 14 | 极高（防数据丢失） | ✅ output state 已有，0 成本 |
| **合计** | **~54 行 / 0 Rust / 0 新依赖** 1 天完工 | | | |

> **最高 ROI 子包（5 分钟 / 18 行）**：A2 AI 光标 + A3 危险视觉 + A5 导出重试入口

### 候选 B「**OCR 体验加速 + 视觉一致性**」（**2-3 天 / 110 行 / 0 Rust**）
**目标**：OCR 体感、工具栏一致性、按钮可发现性

| # | 项 | 估行 | 铁律 |
|---|---|---|---|
| B1 | #8 OCR 结果缓存到历史项（4-8 行字段 + 跳转时短路） | 14 | ✅ |
| B2 | #9 标注自定义色板（5 行 localStorage 记忆） | 18 | ✅ |
| B3 | #10 AI 历史 keyboard Delete 快捷 | 4 | ✅ |
| B4 | #11 PDF 打印期 exporting 态（按钮灰显） | 8 | ✅ |
| B5 | #13 标注对齐线（拖动时 1px 红色虚线） | 35 | ✅ 纯前端几何计算 |
| B6 | #14 历史缩略图 lightbox 浮层 | 25 | ✅ CSS only |
| B7 | #15 AI shaped 步骤视觉差异化 | 6 | ✅ |
| **合计** | **~110 行 / 0 Rust / 0 新依赖** | | |

### 候选 C「**产物链再加工 + 末端打开**」（**2-3 天 / 130 行 / 0 Rust**）
**目标**：让用户导出后**还能继续**——工作流贯通

| # | 项 | 估行 | 铁律 |
|---|---|---|---|
| C1 | #12 AI 二次编辑入口（导入 docx → 续写） | 60 | ✅ 纯前端 parse |
| C2 | #17 导出后"用 X 打开"按钮（6 格式全覆盖） | 8 | ✅ 复用 open_external |
| C3 | #18 AI 历史搜索 ✕ 按钮 | 4 | ✅ |
| C4 | #19 截图历史删除按钮 hover 警告色 | 6 | ✅ |
| C5 | #20 OCR 重新识别成功 toast | 4 | ✅ |
| C6 | #21 AI 历史"全部打包为 zip" | 60 | ✅ 复用 buildZip |
| C7 | #22 OCR 文字模式字数实时统计 | 4 | ✅ |
| **合计** | **~130 行 / 0 Rust / 0 新依赖** | | |

---

## 五、用户本次追问维度与 v13 发现点对照

| 用户追问维度 | v13 命中 GAPS | 命中条数 | 最高 ROI |
|---|---|---|---|
| **OCR 功能** | #1, #4, #8, #20, #22 | 5 | #1 OCR 进度 + #4 错误引导 |
| **AI 辅助功能** | #2, #3, #5, #7, #11, #12, #15, #18 | 8 | #5 导出重试 + #12 二次编辑 |
| **图片处理** | #9, #13, #16, #19 | 4 | #13 对齐线 + #9 自定义色 |
| **生成文档** | #5, #7, #11, #12, #17, #21 | 6 | #17 用 X 打开 + #12 二次编辑 |
| **界面风格** | #2, #3, #10, #14, #15, #19 | 6 | #3 危险视觉 + #14 lightbox |
| **产物持久化** | #6, #8 | 2 | #6 路径记忆（v12 未落地 P0-3）|
| **管理** | #10, #14, #18, #21 | 4 | #21 全部打包 |
| **下载/预览/再加工** | #5, #6, #12, #17, #21 | 5 | #17 用 X 打开 + #12 二次编辑 |
| **核心新轴** | 微反馈 / 错误引导 / 再加工 / 体感细节 / 视觉一致性 | 5 | 全部在候选 A/B/C 命中 |

---

## 六、推荐落地节奏

| 阶段 | 候选 | 内容 | 估时 | 估行 |
|---|---|---|---|---|
| **第 1 步（5 分钟）** | 子包 | A2 AI 光标 + A3 危险视觉 + A5 重试入口 | 5min | 18 |
| **第 2 步（今天 1 天）** | 候选 A | 微反馈 + 错误引导（5 项） | 1天 | 54 |
| **第 3 步（本周）** | 候选 B | OCR 体验加速 + 视觉一致性（7 项） | 2-3天 | 110 |
| **第 4 步（下周）** | 候选 C | 产物链再加工 + 末端打开（7 项） | 2-3天 | 130 |
| **总投入 ~1 周 22 项 GAPS** | | | **~294 行 / 0 Rust / 0 新依赖** | |

---

## 七、关键洞察

1. **"已集成未用满"是最高 ROI 单点**：v12 P0-3 截图保存路径记忆（8 行）+ v13 #17 用 X 打开（8 行）= 16 行解 2 个用户必踩，**两者都用 `open_external` / `LAST_DIR_KEY` 早已就绪的命令**——纯前端加按钮。
2. **"导出 = 终点"被 v1-v12 误以为是设计哲学**：实际上用户导出 docx 是为了发邮件/贴 Confluence/给同事——v13 新轴 #3 把这层"下游工作流"从"哲学建议"变成"3 个具体按钮"。
3. **微反馈层（OCR spinner / AI 光标 / exporting 态）总投入 < 30 行**，但用户主观体验提升 50%+——因为这 3 个操作占用户日常使用 70% 时间。
4. **错误引导比"修好错误"更 ROI 高**：OCR 失败 = 用户系统问题/图太小/语言不对，**修了也还会再失败**；但**告诉用户"为什么"+"怎么办"**才是真正的解——v13 #4 18 行实现。
5. **"shaped 工具调用"是 Phase 25 落地但视觉没差异化**（v13 #15）——这是 v1-v12 反复提"Agent 工具循环"的一个**视觉盲点**：功能上线了但用户感受不到。

---

## 八、状态

- 0 代码改动 ✓
- 0 Rust ✓
- 0 新依赖 ✓
- 严守"诊断优先、不擅加未请求功能" ✓
- 严守 v1–v12 已覆盖不重复（仅补 5 条新轴）✓
- 等用户拍板 3 套候选 + 第 1 步起点后立即执行

---

**报告字数**：~10KB
**GAPS 总数**：22 项（P0=6 / P1=8 / P2=6 / P3=2），全部基于源码直读核实
**v1–v12 误判纠正**：0 项（v11/v12 路线图全保留）
**新轴发现**：5 条（微反馈 / 错误引导 / 再加工 / 体感细节 / 视觉一致性）
**v12 路线图落地补漏**：#6（v12 P0-3 路径记忆 0 落地）、#12（v12 P1-8 二次编辑 0 落地）、#17（v12 P1-11 "用 X 打开" 0 落地）
