# AI 助手 Phase 4 — 多轮对话（迭代式文档生成）

> 参考 `privdoc-ai` 的 **agent 多轮会话模型**（conversations / messages / memory / 缓存清理），
> 把 SnapCraft AI 从「单轮生成」升级为「可多轮追问、逐步打磨成稿」的对话式助手。
> 纯前端实现（localStorage 持久化），零 Rust 改动，不影响截图 / 标注 / OCR / 历史等现有功能。

## 需求发掘（对照 privdoc-ai）

privdoc-ai 的 agent 不是「一次问答」，而是一个有状态的会话：它维护 `conversations` + `messages`，
并在多轮中保留上下文。回看 SnapCraft 现有 AI 能力：

- Phase 1：单张截图 → 单轮生成（一次性输出，无法追问）
- Phase 2a：导出 .md/.txt + 一键润色（润色是「整段重写」，非自然对话）
- Phase 2b：多截图成稿 + 自定义模板 + .html
- Phase 2c：真实 .docx（内嵌截图）+ .pdf
- Phase 3：AI 文案回写截图标注（产出落回图片）

**断点**：用户生成首稿后，如果想「把第二节写详细点」「再加一段结论」「译英文」，
只能反复改 goal 重打整段，或点固定润色 chip——缺少最自然的「多轮对话」形态。
而 privdoc-ai 的会话模型正好补这块。截图工具的独有价值在于：**截图即「文档上下文」**，
首轮携带视觉 + OCR，后续轮纯文本迭代即可，token 也更省。

## 实现（纯前端，零 Rust 改动）

### 1. `aiTypes.ts`
- 新增 `AiChatTurn { role: 'user' | 'assistant'; content: string }`，对齐 privdoc-ai 的 message 模型。

### 2. `aiStore.ts`
- 新增对话线程状态 `conversation: AiChatTurn[]` 与 `convKey: string`（按截图内容哈希的持久化键）。
- `convHash(s?)`：把可能数 MB 的 dataURL 压缩成稳定短键（djb2 取前缀 + 长度），碰撞可忽略。
- `loadConversation / saveConversation`：以 `snapcraft-ai-conv:<hash>` 为键持久化，**每个截图一份独立对话线程**，切换截图互不干扰——对齐 privdoc-ai「conversation per document」。
- `setConvKey(key)`：切换截图时加载对应线程。
- `chat(input, ctx)`：多轮核心。
  - 首轮（`conversation` 为空）：`preset.buildUser(goal+OCR)` + 视觉图片（当前图 + 附加多图）。
  - 后续轮：仅追加文本追问，**不再重复发送图片**（截图上下文已在首轮给出）。
  - 消息构造 `[system, ...history, userMsg]`，流式输出，`output` 始终等于最后一条 AI 回复。
  - 完成后把 `user + assistant` 两条记录写回 `conversation` 并持久化。
- `clearConversation()`：开始新对话（清空当前线程）。
- `generate()` 改为委托 `chat` 首轮——老入口不变，自动并入对话线程，向后兼容。
- `refine()` 润色结果回写对话线程最后一条 AI 回复，保持上下文一致。

### 3. `AIPanel.tsx`
- `useEffect` 监听 `imageDataUrl` → 调 `setConvKey(convHash(...))`，自动切换/恢复对话线程。
- 操作区新增「🗨 新对话」按钮（清空当前线程）。
- 新增**追问输入栏** `.ai-follow`：生成首稿后，输入后续指令（Enter 发送 / Shift+Enter 换行）即发起下一轮。
- 原「单一 output 区」替换为**对话记录区** `.ai-chat`：逐条渲染 user / AI 气泡（AI 用 Markdown 渲染），
  流式时显示「正在思考…」+ 光标；首轮上下文提示文案 `ai.chatEmpty`。
- 导出 / 润色 / 回写截图按钮仍作用于 `output`（= 最新一轮 AI 成稿），链路不变。

### 4. i18n（zh-CN + en-US）
新增 `newChat / chatEmpty / chatPlaceholder / send / thinking / you` 六键。

### 5. CSS（`src/index.css`）
新增 `.ai-follow*` / `.ai-chat*` / `.ai-msg*` / `.ai-think` 等，沿用既有主题变量（accent / border / surface），
气泡区分 user（主题色浅底）与 AI（surface 描边），滚动区自适应。

## 验证
- `pnpm build`（tsc + vite）exit 0，**134 模块，零类型错误**。
- dist 含全部新 token：CSS `ai-chat / ai-follow / ai-msg-user / ai-msg-ai / ai-think`；
  JS `convHash / clearConversation / chatEmpty / newChat / chatPlaceholder`。
- 纯前端改动，`./start.sh dev` 或 `pnpm build && ./start.sh app` 立即生效，现有截图/标注/OCR/历史功能不受影响。

## 与 privdoc-ai 的差异 & 后续候选
- 差异：privdoc-ai 用 Rust + SQLite 持久化会话与 `memory`（带 importance/source 的键值记忆）；
  本方案用前端 localStorage 按截图分桶，更轻、零后端风险，且天然把「截图」作为记忆锚点。
- 后续候选（未做，避免影响现有功能）：
  - **AI 记忆压缩**：长对话超限时自动摘要前 N 轮（privdoc-ai 的 `run_cache_cleanup` / oversized 裁剪思路）。
  - **对话历史列表**：跨截图管理多个会话线程（类似 privdoc-ai 的 conversation 列表）。
  - **后端代理隐藏 Key**（首版直连，密钥在渲染层）。
  - **批量截图一键成报告**：多张截图合并为一个会话上下文一次性成稿。
  - **.xlsx 导出**（表格数据）。
