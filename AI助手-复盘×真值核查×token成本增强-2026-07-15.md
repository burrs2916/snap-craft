# AI 助手 · 整体复盘 × 真值核查 × 顶级项目对标 × 底层增强（2026-07-15 15:38）

> 本轮为「接入 AI 助手」自动化第 N 次复盘。方法：**只读审计当前代码 + 真读两份顶级项目源码**，并**实修 4 项**（2 bug + 1 UX + 1 AI 底层增强）。严守「增量、不推翻重构、零 Rust 改动、不影响现有功能」。

---

## 1. 真值核查结论（对照 Agent 审计 + 直读码）

| 维度 | 结论 |
|---|---|
| 空/假实现 | **无**。AIPanel 全部按钮、aiClient 直连、导出链均接真实逻辑 |
| 前后端断联 | **3/3 对齐**：`get_history` / `save_text_file` / `save_binary_file` 与 Rust `#[tauri::command]` 入参（snake↔camel 默认转换）100% 一致 |
| 流式健壮性 | Phase 13 仍在：`findFrameEnd` 双层 `\n\n`/`\r\n\r\n` 帧切分 + 尾部 flush + 退避重试(≤3)+Retery-After 尊重 + 预算护栏(120k)。**无丢字** |
| 记忆检索 | 确为多因子(0.6 相关+0.2 时间衰减+0.2 重要性)+MMR 贪心，非纯词法（Phase 9 已升级） |
| i18n | 中英 `ai.*` 键对称、无孤儿、无缺失 |

### 仍待修 gap 复核
- 🟡 `docx` 导出**无视** Phase 12 的 5 套主题/封面/TOC（HTML/PDF 美、docx 朴素不一致）——**本轮未做**（需重构 markdownDocx，风险中）。
- ⚪ 历史阅读器导出 docx/pdf 无像素为**设计限制**（历史线程不存来源截图像素），非 bug。
- ✅ 「新对话」误删归档文档 **已确认修复**（仅重置工作态）。

---

## 2. 本轮回填实修（纯前端 / `pnpm build` exit0 / 136 模块 / 零回归）

### 🐞 ① 回写截图泄漏 `<!--SNAP:k-->` 标记
- **根因**：`handleApplyToScreenshot`(AIPanel.tsx:668) 用 `mdToPlainText(output)` 直接贴回，图文报告的章节锚点 `<!--SNAP:k-->` 被原样写成图上文字标注。
- **修复**：先 `stripSnapMarkers(output)` 再转纯文本。`stripSnapMarkers` 已 import，零新增依赖。

### 🐞 ② 「成报告」无空目标守卫
- **根因**：`handleMakeReport`(AIPanel.tsx:401) 不像 `handleGenerate` 那样拦截空目标，空 goal 也会发请求（浪费 token）。
- **修复**：开头加 `if (!goal.trim()) return;`，与生成守卫一致。

### 🐞 ③ 多截图章节顺序关闭面板即丢失（gap ⑦）
- **根因**：`selectedOrder` 是组件 `useState`，面板 `!open` 即 `return null` 卸载，选择顺序无法跨开关保留——而「拖拽/箭头重排章节顺序」是上轮刚做的差异化卖点，丢顺序直接削弱它。
- **修复**：新增 `SEL_PREFIX` + `loadSel/saveSel`（localStorage，按截图哈希分桶）；新增恢复 `useEffect([imageDataUrl])` 在打开/切换截图时还原；`toggleHistoryItem/moveSel/removeSel/onDropAt/清空` 全部写入持久化。关闭面板再打开，图文报告章节顺序仍在。

### ✨ ④ AI 底层增强：token / 成本实时透明（对齐 claw-code `usage.rs`）
- **动机**：用户对「AI 成为真正卖点」的核心诉求之一是**可控、可量化**。顶级项目都把 token 用量当作一等公民。
- **实现**：
  - `aiClient.ts`：SSE 解析新增 usage 捕获——Anthropic 取 `message_start.input_tokens` + `message_delta.output_tokens`；OpenAI 兼容取末帧 `usage`（请求时带 `stream_options:{include_usage:true}`）。`StreamOpts.onUsage` 实时回调。
  - `aiStore.ts`：新增 `usage{AiUsage}` 状态 + `setUsage`，`chat`/`refine` 流式期间写入。
  - `AIPanel.tsx`：结果区底部新增 `🔤 N↑ · M↓ tokens · ≈$x.xxxx` 一行（`estimateCost` 内置常见模型价目表，未知模型仅显 token、不显成本，避免误报）。
  - `AiUsage` 类型归一到 `aiTypes.ts`（消除循环定义）。
- **价值**：用户每次生成都可见真实消耗与估算花费——成本透明本身就是「专业工具」的体感卖点。

---

## 3. 对照顶级项目（claw-code / openclaw）能力映射

| 能力 | claw-code | openclaw | 我们现状 | 差距 |
|---|---|---|---|---|
| 流式/SSE | `sse.rs:26` 帧切分 | `parseAnthropicSseBody` 归一+flush | ✅ Phase 13 追平 | 持平 |
| 重试/预算 | `anthropic.rs:401` 退避 | `compaction.ts` 预算 | ✅ 已具备 | 持平 |
| 记忆检索 | 无 | 六因子+MMR+衰减 | ✅ 多因子+MMR | 持平（缺语义向量/consolidation 循环） |
| **token/成本** | `usage.rs` ModelPricing | — | ✅ **本轮落地** | 追平 |
| Agent 工具循环 | `conversation.rs:342 run_turn` + 熔断 | `tool-loop-detection.ts:498` 失控检测 | ❌ 未做 | **最大差距=最大卖点** |
| prompt caching | `prompt_cache.rs` 命中诊断 | `cache_control` 注入 | ❌ 未做 | 易补、降成本 |
| 记忆 consolidation | 无 | `dreaming.ts` cron 周期 | ❌ 未做 | 中 |
| 会话 fork | `session.rs:260 fork` | `session-key.ts` 隔离 | ❌ 未做 | 低（桌面端 localStorage 够用） |
| 错误分类 | `is_retryable_status` 按码 | — | ⚠️ 有 RETRYABLE_STATUS，缺 401 Key 提示 | 易补 |

---

## 4. 卖点挖掘路线图（按优先级）

**P0（胜负手，需先确认工具契约）**
- **AI Agent 化（工具循环）**：让模型直接调 `addAnnotation` 做智能标注 / 隐私哨兵自动打码 / 圈选重点 / 一键教程 / 选区 OCR。竞品只产文字，我们能让 AI **直接操作截图**=真正差异化。落地形态参考 claw-code `run_turn`(max_iterations 熔断) + openclaw `detectToolCallLoop`(未知工具/无进展/乒乓 三检测器)。

**P1（纯前端、低成本、高收益）**
- **docx 套主题封面/TOC**（gap①）：让 6 格式导出视觉一致，消除「docx 朴素」割裂感。
- **prompt caching**（Anthropic `cache_control` 打到稳定 system/记忆前缀）：重复调用降本，零 UI 改动。
- **401 Key 误用友好提示** + 错误分类细化。

**P2（增强「卖点」厚度）**
- **语义记忆**：本地 embedding 替换词法重叠，跨长对话更准确 recall。
- **选区/划词 AI**：在编辑器选区上直接「解释/翻译/追问」，把 AI 融入截图工作流而非孤立面板。
- **后端代理隐藏 Key**（需 Rust）：密钥不下发渲染层，企业/团队场景刚需。
- **批量工厂**：多截图队列一键成稿/批量打码。

**文档润色剩余**
- docx 主题封面/页面尺寸；中英 typography 细则；PPT 导出（用现有 markdown→PDF 思路延伸）。

---

## 5. 验证与交付
- `pnpm build`：**exit0**，tsc + vite，136 模块，零类型错误，无回归。
- `dist` 含新 token：`ai-usage`(CSS) / `stream_options`(JS) 已确认。
- 本轮严守「增量、不影响现有功能」；重启 App（`./start.sh dev` 或 `pnpm build && ./start.sh app`）即见：① 回写不再带锚点；② 成报告空目标不发请求；③ 多截图顺序跨开关保留；④ 结果区显示 token/成本。

> 下一步建议从 **P0 AI Agent 化** 切入（动手前需与用户确认「前端暴露给模型的标注操作工具契约」），并行推进 P1 的 docx 主题与 prompt caching。
