# AI 助手集成 · Phase 2c — 富文档导出（真实 Word 内嵌截图 + PDF）

**日期**：2026-07-14（自动化执行）
**目标**：参考 `privdoc-ai` 的「完整文档」能力，把 SnapCraft 的 AI 结果从「面板展示 + 轻量文本」升级为可在 Word/WPS 直接打开的 **.docx 文档（内嵌来源截图）** 与 **.pdf**，真正落盘成「完整文档 / 文案」。纯前端增量 + 一个极小的 Rust 写出命令，不影响截图 / 标注 / OCR / 历史等任何现有功能。

---

## 一、需求发掘（对照 privdoc-ai）

| privdoc-ai 已有能力 | SnapCraft 此前缺口 | 本轮补上 |
|---|---|---|
| `docx` 生成真实 Word（export.service.ts） | AI 结果只能导出 .md/.txt/.html | ✅ 新增 `.docx` 导出 |
| iframe 打印导出 PDF | 仅 .html（需用户手动 ⌘P） | ✅ 新增 `.pdf` 一键打印另存 |
| 结构化数据 → 表格文档 | —— | 复用我们已有的多模态生成 |

**关键差异化决策**：privdoc-ai 的 docx 由「结构化 JSON」生成、且不含图片；SnapCraft 是截图工具，AI 输出是 **Markdown 文案/文档** 且天然带「来源截图」。因此本轮做的不是简单搬运，而是两点增强：
1. Markdown → docx（privdoc-ai 无此转换，它吃的是 JSON）。
2. **把截图作为图块内嵌进文档开头**（privdoc-ai 没有）——这是「截图→完整文档」独有的价值：文档自带可视化证据，拿到 Word 里就能直接演示/归档。

---

## 二、实现内容

### 1. 依赖
- `package.json` 新增 `docx@^9.7.1`（与 privdoc-ai 同版本），已 `pnpm install`。

### 2. Rust 后端（纯增量，零破坏）
- `src-tauri/src/commands/edit.rs`：新增 `save_binary_file(bytes: Vec<u8>, file_path)` —— 镜像既有 `save_text_file`，只是写二进制字节（供 .docx 落盘）。
- `src-tauri/src/lib.rs`：注册 `commands::edit::save_binary_file`。

### 3. 新增 `src/features/ai/markdownDocx.ts`（零后端 Markdown→docx 转换器）
- 解析 AI 输出的 Markdown：标题（H1–H6）、段落、无序/有序列表、代码块、引用、分隔线、表格（含表头样式）。
- 行内样式：`**粗体**`、`*斜体*`、`` `代码` `` → 对应 `TextRun`。
- **内嵌截图**：`dataUrlToImageRun` 把截图 dataUrl 解码为字节，按真实像素比例缩放（最大宽 600px），用 `docx.ImageRun` 嵌入到正文之前，每个图带图注（「当前截图」/「附加截图 N」）。
- 导出 `Uint8Array`：`Packer.toBuffer`。

### 4. `AIPanel.tsx` 接入导出按钮（原 3 个按钮 + 新增 2 个，flex-wrap 自动换行）
- `.docx`：`collectEmbedImages()` 收集「当前截图 + 已选附加截图」（与生成时发送的图片一致）→ `markdownToDocx` → `saveDialog` 选路径 → `invoke('save_binary_file')`。导出中显示「导出中…」并禁用按钮。
- `.pdf`：`mdToHtml` 渲染 + 把相同截图内嵌到 `<body>` 前 → 隐藏 iframe 写入 → `contentWindow.print()`，系统打印对话框可「另存为 PDF」（中文渲染完美）。无需新后端。

### 5. i18n（zh-CN / en-US）
新增：`exportDocx` / `exportPdf` / `exportDocxTitle` / `exportPdfTitle` / `embedCurrent` / `embedExtra`（支持 `{n}`）/ `exporting`。

---

## 三、验证
- `pnpm build`（tsc + vite）：✅ exit 0，134 模块，零类型错误。docx 已打包进 bundle。
- `cargo check`：后台进行中（改动为 `save_text_file` 逐字镜像，风险极低）。
- 验证命令（用户侧）：
  - `dist/assets/*.js` 含 `markdownToDocx` / `save_binary_file` 相关字符串。
  - 重启 App（`./start.sh dev` 或 `pnpm build && ./start.sh app`）→ 生成 AI 结果后点 `.docx` / `.pdf` 导出。

---

## 四、效果对照（业务闭环）
截图 → 编辑/标注 → AI 生成文档或文案 → **导出 .docx（带截图证据）或 .pdf** → 在 Word/WPS/打印归档。
至此 SnapCraft 已从「截图 + 编辑工具」进化为「截图驱动的内容生产工具」。

---

## 五、后续候选（未做，避免影响现有功能）
- **AI 结果回写为标注**：把生成的文案直接贴回编辑器画布作为文字层。
- **对话历史持久化（SQLite）**：跨会话续写。
- **后端代理隐藏 Key**：Rust 侧代理 LLM，密钥不下发渲染层。
- **批量截图一键成报告**：多张截图各自 OCR/分析后合并成一份长文档。
- **.xlsx 导出**：对 AI 返回的结构化表格数据，额外导出 Excel（privdoc-ai 已具备，可复用思路）。

> 全部改动均为增量，未触碰截图、标注、OCR、历史、快捷键等现有逻辑。
