# OCR 增强 · 第 9 轮（需求攫取 → 空间预览联动 + 结果条快捷取字）

> 实施时间：2026-07-14。状态：代码完成，`pnpm build`（tsc + vite）exit 0（125 模块），dist 已含新类与 locale。
> 约束：纯前端增量，复用既有 `ocr_image` / `openEditor` / `runOcr`，**零后端改动**，**未触碰截图/滚动/标注/裁剪等其它功能**。

## 一、需求攫取与可用性评估

对已完成 8 轮增强的 OCR 面板做了「还缺什么」的需求回顾。当前已具备：结构化识别（坐标+置信度）、语言选择、区域框选、逐行内联纠错、导出 txt/md/json/tsv、结果内搜索+命中高亮跳转、置信度阈值、合并为一行、偏好持久化、自动复制、耗时统计、本地历史、智能阅读顺序、面板重识别、面板快捷键、字号调节、逐行勾选（精准提取）。

**仍欠服务的用户需求（按可用性排序）：**

| # | 需求 | 可用性 | 落地风险评估 | 本轮处理 |
|---|------|--------|--------------|----------|
| 1 | **看到文字「落在图上哪」**：当前面板只有文字列表，看不到识别块在截图中的空间位置，无法核对 OCR 是否认错行/错位 | 高 | 低（self-contained 缩略图，复用 `current.dataUrl`+块坐标，不动编辑画布） | ✅ 实现 |
| 2 | **截图后立刻取字**：结果条只有 复制/编辑/保存/钉图，取字要先点「编辑」再点「取字」两步 | 高 | 低（仅新增一个按钮，复用 `openEditor`+`runOcr`） | ✅ 实现 |
| 3 | 跨视图快速取字（从历史网格直接取字） | 中 | 中（历史网格已有 4 按钮，再加易拥挤；编辑入口已可覆盖） | 暂缓 |
| 4 | 文字↔图块跨视图联动高亮（在编辑画布上高亮原图块） | 中 | 高（需跨 `AnnotationCanvas`，且面板遮罩已盖住画布，本就看不到） | 用需求#1 的面板内缩略图安全替代 |
| 5 | 翻译 / 离线词典 | 中 | 高（需网络或离线资源，超出本地工具定位） | 暂缓 |
| 6 | 去重/去空行、表格化等文本清洗 | 低 | 低 | 暂缓（避免面板进一步膨胀） |

结论：本轮落地 **需求#1 + #2**——两者都是「高频刚需 + 零风险（纯面板内/结果条按钮）」，最符合「提升可用性且不影响其它功能」的约束。

## 二、实现项

### ① 面板内空间缩略图 + 文字块边界框双向联动
- **新增状态** `ocrHoverLine: number | null`：记录当前联动的原始块下标（鼠标悬停逐行列表或图块框时写入，离开置 null）。仅驱动视觉联动，不进入任何数据链（复制/导出/贴回/历史均不受影响）。
- **预览块** `.ocr-preview`：在统计行下方插入 self-contained 缩略图（`current.dataUrl`），用 `display:inline-block` 包裹 `img` 使 `max-width/max-height` 保比例、不产生黑边；其上的 `.ocr-preview-boxes` 绝对覆盖，按块归一化坐标 `x/y/w/h → 百分比` 定位每行的彩色边界框。
- **双向联动**：
  - 列表行 `onMouseEnter/Leave` → 设 `ocrHoverLine=i` → 对应图块框加 `.active`（加粗描边+光晕）；
  - 图块框 `onMouseEnter/Leave` → 同写 `ocrHoverLine` → 对应列表行加 `.focused`（左侧 accent 竖条+淡底）；
  - 点击图块框 → `focusOcrLine(i)`：高亮 + `scrollIntoView({block:'nearest'})` 把列表滚到该行，便于就地修正。
- **语义着色**（与既有置信度体系一致）：低置信块（0<conf<0.7）边框琥珀色 `.low`；被搜索/置信度过滤隐藏的块 `.hidden`（虚线+18% 透明度，保留空间上下文但不喧宾夺主）；激活态 `.active` 最强调。
- **复用**：`ocrVisSet`（由既有 `ocrVisibleLines()` 推导）判断隐藏框；`ocrTextAt(i,b)` 作框 `title` 悬浮提示；`ocrResult.blocks` 原始顺序（空间坐标本就应按原序绘制，不受阅读顺序重排影响）。

### ② 截图后结果条一键取字
- **新增** `startOcrFromShot(shot)`：先 `openEditor(shot)`（设置 current + 切编辑视图），再 `runOcr(shot.dataUrl)`（`runOcr` 收 `imageData` 入参，不依赖 `current` 闭包）。
- **结果条新增按钮** `🔍 取字`：置于 复制 与 编辑 之间，点击即进编辑器并直接出 OCR 面板。省去「编辑→取字」两步，命中「截完图马上要文字」最高频场景。
- 仅新增一个按钮，结果条其它 复制/编辑/保存/钉图/关闭 行为完全不变。

## 三、关键设计 / 安全边界
- **零回归**：`ocrHoverLine` 默认 `null`，预览框在 `null` 时无任何 `.active`；列表 `.focused` 仅在悬停时出现；所有复制/导出/贴回/历史路径与历史行为完全一致。
- **零后端改动**：未改 `ocr.rs` / `commands/mod.rs` / `lib.rs`；新增逻辑 100% 在前端 `EnhancedScreenshotApp.tsx` + `index.css` + 两个 locale 文件。
- **不影响其它功能**：改动收敛在 OCR 面板（预览+联动）与截图结果条（新增按钮）；截图/滚动长截/区域/窗口/标注/裁剪/历史/钉图/权限/主题/多语言均未被触碰。
- **主题兼容**：预览框/边界框全部用 CSS 变量（`--accent`/`--border`/`--surface-*`）着色，`prefers-color-scheme` 切换无需额外处理。

## 四、验证
- `pnpm build` → `tsc && vite build` exit 0，125 模块，dist CSS/JS 已含 `ocr-preview-imgwrap` / `ocr-box` / `ocr-block.focused` 等新类与 `preview` / `previewTitle` / `result.ocr` / `result.ocrTitle` 等 locale 键。
- 类型检查通过（tsc 无报错），无未定义引用。
- 因无后端改动，`cargo check` 非必需；未运行以避免无关噪音。

## 五、后续可评估（延续前几轮，本轮不做）
- 跨视图/历史网格直接取字（需求#3，拥挤风险）
- 翻译 / 离线词典（需求#5，网络/资源边界）
- 文本清洗（去重/去空/表格化，需求#6，面板膨胀风险）
- OCR 面板内空间预览已基本替代「跨视图联动高亮」（需求#4），且更安全（不依赖被遮罩的编辑画布）。

## 六、验收方式
```bash
./start.sh app        # 打开已含新前端的 SnapCraft.app（改完前端 pnpm build 后重跑）
```
1. 截图 → 结果条出现 `🔍 取字` → 点击直接进编辑器并弹出 OCR 面板（无需先点编辑）。
2. OCR 面板内统计行下方出现「识别区域预览」缩略图，图上彩色框对应每行文字。
3. 鼠标悬停某行 → 对应框高亮；悬停某框 → 对应行高亮并（必要时）滚动入视；点击框 → 列表滚到该行。
4. 低置信行框为琥珀色；搜索/调高置信度阈值后，被过滤的行对应框变虚线淡化。
