# SnapCraft · OCR 取字功能增强（第五轮 · 智能阅读顺序校正）

> 实施时间：2026-07-14。状态：前端 `pnpm build` 通过（125 模块，exit 0），未改动后端 Rust，未触碰截图/标注/滚动/钉图/历史等其它功能。
> 本轮在已有 OCR（语言选择 / 逐行结果 / 区域框选 / 贴回标注 / 内联纠错 / 导出 / 搜索 / 即时重识 / 置信度 / 合并为一行 / 偏好持久化 / 自动复制 / 耗时统计 / 本地历史）基础上，
> 做**一项纯前端、且全部收敛在 OCR 面板内 + 隔离 localStorage** 的增强：**智能阅读顺序校正**。

## 一、需求的攫取与发觉（本轮聚焦：版面还原）

前四轮已解决「能取字、能改、能搜、能导出、能按质量过滤、能留存复用」。从真实使用路径继续看一个**一直存在但被跳过的硬伤**——

### 已知限制（来自 `OCR增强-overview.md`）
> "逐行块的「阅读顺序」对多列/竖排版面 Vision 可能仍按读序，未作额外重排"

Apple Vision / WinRT 的逐行块顺序在遇到**多栏排版（论文、新闻、说明书）、竖排文字（古籍/日语）**时，
常把"右栏第一行"排在"左栏末行"之前，导致「复制全部 / 导出 / 贴回」的文字是乱序的，用户只能手工重排。
这是 OCR 在**整个一大类截图场景（学术、文档、漫画、日文）**上的可用性断点。

| # | 痛点 | 为什么影响可用性 | 本轮方案 |
|---|------|----------------|----------|
| 1 | **多列/竖排乱序**：Vision 读序≠人类阅读序 | 论文/新闻/日文的「复制全部」结果是错位的，用户得逐行挑 | **智能阅读顺序校正**：按块坐标重排（多列 左→右·上→下；竖排 上→下·右→左） |
| 2 | **无开关、无记忆**：每次都得手动处理 | 高频多列用户每次重蹈覆辙 | 开关 + **偏好持久化**（写入既有 `snapcraft.ocr.prefs`，跨会话记住） |

> 上述改动**不引入跨视图渲染、不改后端命令、不碰截图历史体系**，完全符合「增量增强、不影响其它功能」的硬约束。
> 之前轮次记录的其余候选（翻译、跨视图快速取字）仍因成本/超出本地工具定位**本轮不做**。

## 二、本轮实现的需求

选取 **#1 阅读顺序校正 + #2 偏好持久化**。直接命中 OCR 在多列/竖排场景的最高频「版面还原」痛点，
且改动全部收敛在 OCR 面板 + 既有隔离 localStorage key，零后端改动。

## 三、具体改动

### 前端 `src/features/screenshot/EnhancedScreenshotApp.tsx`
- **模块级纯函数 `ocrReadingOrder(blocks): number[]`**（组件外，无依赖、易测）：
  - 输入块数组，输出「原始块下标」的排序数组（返回下标而非新块，保证 `ocrEdits` 编辑映射不被破坏）。
  - **竖排判定**：多数块 `h > w * 1.2` 视为竖排 → 同列（x 相近）按 y 上→下、不同列按 x 右→左。
  - **横排多列**：先按中心 x 排序，依相邻块水平间隙（`> 平均块宽*0.6`）切分列；列内按 y 上→下、列间按起始 x 左→右。
  - 单栏/两行等简单情形退化为「按 y 上→下」，自然修正 Vision 偶发的行间错位。
- **类型与状态**：新增 `OcrLayout = 'none' | 'reading'`；`OcrPrefs` 增 `layout` 字段（默认 `'none'`）；
  新增 `ocrLayout` 状态（默认读偏好）。`runOcr` 重识别时**不重置**该偏好（与 `ocrMerge`/`ocrAutoCopy` 一致）。
- **`ocrVisibleLines` 增强**（依赖增 `ocrLayout`）：遍历顺序改为
  `ocrLayout === 'reading' ? ocrReadingOrder(blocks) : blocks.map((_,i)=>i)`；
  过滤逻辑不变。于是**复制全部 / 导出 / 贴回标注 / 逐行列表 / 全文框**全部自动按重排后顺序呈现（WYSIWYG）。
- **面板 JSX**：选项行新增「按版式重排」勾选（与「合并为一行」「识别后自动复制」并列）+ 说明 tip。
- **偏好持久化**：`saveOcrPrefs` 现写入 `layout`，跨会话记住。

### 文案 `src/locales/{zh-CN,en-US}.json`（ocr 组新增 2 键）
`layoutReading` / `layoutReadingTitle`（中：「按版式重排」/「对多列/竖排截图按坐标重排阅读顺序（默认关，不影响原有读序）」；
英：`Reorder by layout` / 对应英文说明）。

### 样式 `src/index.css`
新增 `.ocr-layout`（镜像 `.ocr-merge` 的 label 样式）+ `.ocr-layout input`；`.ocr-opts-row` 加 `flex-wrap: wrap` 防窄面板溢出。

## 四、如何验收

```bash
pnpm build                       # 前端 tsc+vite，已通过（125 模块，exit 0）；后端无改动
./start.sh app                  # 打开含最新前端的 SnapCraft.app（内嵌 dist）
```

编辑器内点「取字」对一张**多列/竖排**图：
- **默认行为零变化**：未勾「按版式重排」时，阅读顺序与历史版本完全一致（已确认显示/复制/导出/贴回全部走 `ocrVisibleLines`，默认走 identity 顺序）。
- **勾选「按版式重排」后**：逐行列表、全文框、复制全部、导出 .txt/.md、贴回标注 全部按「左栏上→下 → 右栏上→下」（竖排则上→下、右→左）的正确阅读序呈现。
- **偏好记忆**：勾选后关闭 .app 重新打开，该开关仍保持开启（存于 localStorage `snapcraft.ocr.prefs`）。
- **搜索/置信度仍叠加生效**：重排与搜索、置信度阈值过滤互不冲突，统一在 `ocrVisibleLines` 内完成。

## 五、回归确认

- 截图 / 区域 / 窗口 / 滚动长截图 / 钉图 / 文字标注 / 马赛克：均未改动，行为不变。
- 区域框选 OCR、内联纠错、导出、结果内搜索、切换语言即时重识别、置信度阈值、合并为一行、自动复制、耗时统计、本地历史：链路未动，
  新增的「阅读顺序校正」只改变 `ocrVisibleLines` 的**遍历顺序**（默认 `none` = 原顺序）。
- 后端 `ocr_image` / `OcrResult` / `OcrBlock` 签名与行为零变化（本轮纯前端）。
- OCR 偏好使用**既有**隔离 localStorage key（`snapcraft.ocr.prefs`），与截图历史 store 完全隔离。
- `ocrLayout` 默认 `'none'`，**对所有既有用户与既有截图零行为改变**（向后兼容，无回归风险）。
- `ocrReadingOrder` 为纯几何启发式：单栏退化为按 y 排序；多列/竖排按坐标聚类，不开网络、不依赖平台。
