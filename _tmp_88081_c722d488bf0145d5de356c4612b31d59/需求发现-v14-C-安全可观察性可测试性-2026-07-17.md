# SnapCraft 需求发现 v14-C · 安全 × 可观察性 × 可测试性 × 工程债 盲点（2026-07-17 10:55）

> **范围**：v1-v13 累计 357+ GAPS。v9-D 提了"apiKey 明文 / 0 错误上报 / 0 单元测试 / 0 e2e / 缺 Linux CI / 0 性能预算 / 0 CHANGELOG / 0 README 截图 / 0 埋点"。**但 v1-v13 全部是"功能性"诊断，本轮严守"安全/可观察性/可测试性/工程债"这 4 个真未触达主轴**——4 个子轴 + 25 项 GAPS。
> **本轮 GAPS = 25 项独立**（P0=6 / P1=12 / P2=7），严守 0 代码改动、AI「纯前端直连 / 零新依赖」铁律。

---

## 一、源码直读关键事实

| 维度 | 现状 | grep 实证 |
|---|---|---|
| apiKey 存储 | `localStorage['snapcraft-ai-config']` 明文 | v9 V9-C-2 提了**未落地** |
| Tauri CSP | `tauri.conf.json` 0 `csp` 字段 | 默认放行所有，可加固 |
| Rust panic 兜底 | `lib.rs` 0 `catch_unwind` / 0 ErrorHook | 主进程 panic = 应用闪退 |
| 路径 traversal | `commands/edit.rs` 0 `path.starts_with(allowed_dir)` 校验 | 用户可传入 `~/../../etc/passwd` |
| clog! 调用 | ~75 处 | 0 结构化（JSON / 字段名），纯文本 |
| 错误聚合 | 0 | 89+ `console.error/warn` + 220+ `catch` 静默 |
| 单元测试 | 0 | `package.json` 无 `test` script；`tests/` 空 |
| E2E | 0 | 无 Playwright / Tauri-driver |
| 视觉回归 | 0 | 12 主题 × 5 AI 主题 = 60 组合 0 snapshot |
| ESLint | 0 | `package.json` 0 lint script |
| Prettier | 0 | 0 format script |
| commitlint / husky | 0 | pre-commit 0 hook |
| CHANGELOG.md | 0 | grep `CHANGELOG` 全 0 命中 |
| CONTRIBUTING.md | 0 |  |
| CI Linux | 0 | `.github/workflows/` 0 Linux runner |
| 包体积预算 | 0 | dist 1.5MB 0 监控 |
| 错误上报 (Sentry/Bugsnag) | 0 |  |
| GitHub Action | 0 / 0 lint 步骤 | 仅 tauri build |
| 巨型文件 | EditorWindow 4397 / AIPanel 2119 / aiClient 831 / aiStore 1048 | 0 拆分 |
| 命名一致性 | screenshot / capture / snap 三词混用 | grep 混杂 |
| 注释质量 | toolCallParser 464 行 5 形态兜底仅 5 行注释 | 复杂算法可读性差 |
| Magic number | 12 主题 id / 5 AI 主题 / 50 OCR 上限 / 100 截图上限 散落 | 0 常量文件 |

---

## 二、25 项 GAPS 详表

### 🔥 P0（6 项 · 数据真丢 / 安全裸奔 / 团队不可用）

**v14-C01** **apiKey 明文 localStorage** —— `aiStore.ts:1040-1050` `getConfig()` 从 `localStorage['snapcraft-ai-config']` 读 apiKey 原文。任何 devtools / 浏览器扩展 / 备份工具 / 屏幕录制都可见。**估行 200（Web Crypto API AES-GCM 加密 + 用户密码 / 1 Rust 调 macOS Keychain）/ ⚠️ 需 1 Rust keychain 调用 / ROI 极高（合规 + 用户安心）**

**v14-C02** **路径 traversal 0 校验** —— `commands/edit.rs:54-80` `save_screenshot(path)` 接受任意路径，用户调用 `save_screenshot('~/../../etc/passwd')` 任意写。**估行 25（path.starts_with(allowed_root) + canonicalize 后再校验）/ ⚠️ 需 1 Rust / ROI 极高**

**v14-C03** **主进程 panic = 闪退 0 兜底** —— `lib.rs:346` `fn run()` 0 `std::panic::catch_unwind`。一个 panic = 全 app 死。**估行 30（panic hook + 写 log + graceful exit code 1）/ ⚠️ 需 1 Rust**

**v14-C04** **clog! 写敏感信息风险** —— 75+ 处 clog! 调用，**部分可能含截图 dataUrl / OCR 内容 / API key**。**估行 25（clog! 加 allow/deny list + 自动 redact 规则 + sanitize 函数）/ ⚠️ 需 1 Rust / ROI 高（合规）**

**v14-C05** **错误聚合 0** —— 89+ console.error + 220+ catch 静默，用户出错只看 1 行红字无法 debug。**估行 80（前端 ErrorReporter 类 + 内存 ring buffer 50 条 + "一键导出诊断包"）/ ✅纯前端 / ROI 中**

**v14-C06** **错误分类 0 系统化** —— `locales/zh-CN.json:308-318` `saveFailed/ocrFailed/exportFailed/deleteFailed` 全 `{msg}`，**截图/OCR/导出 3 类未按 enum 分类**（v14-D24 同源）。磁盘满 / 权限 / 文件名冲突 / 非法字符 / 路径不存在 → 全 1 行红字。**估行 80（Rust error enum 序列化 + 前端按 enum 路由不同 i18n key）/ ⚠️ 需 Rust enum / ROI 高**

### 🟠 P1（12 项 · 可观察性 / 可测试性 / 工程债）

**v14-C07** **关键路径用户行为埋点 0** —— OCR 调用次数 / AI 调用次数 / 导出次数 / 失败率 全部 0 记录。开发者无法回答"上周 OCR 失败率多少"。**估行 100（`trackEvent(name, props)` + localStorage 聚合 7 天 + Dashboard 展示）/ ✅纯前端**

**v14-C08** **性能监控 0** —— OCR 耗时 / AI TTFT / 导出耗时 0 测时。**估行 60（同 C07 框架 + duration 字段）/ ✅纯前端**

**v14-C09** **0 单元测试** —— 12 个纯函数工具（`ocrClean.ts` / `exportPath.ts` / `toolCallParser.ts` / `markdown*.ts`）**0 个 test**。重构就崩。**估行 400（vitest + 20 个高价值工具函数单测）/ ✅0 新依赖（vitest 内置）/ ROI 极高**

**v14-C10** **0 E2E 测试** —— Tauri 启动 → 截图 → OCR → AI → 导出 全链路 0 测。**估行 800（Playwright + Tauri-driver + 5 个核心流）/ ⚠️ 需 1 新依赖（playwright）/ ROI 中**

**v14-C11** **0 视觉回归** —— 12 主题 × 5 AI 主题 = 60 组合，主题切换 0 snapshot。**估行 400（Playwright screenshot + golden 库 + 60 套 baseline）/ ⚠️ 需 1 工具**

**v14-C12** **0 ESLint** —— `package.json` 0 lint script，IDE 报红无 CI 卡。**估行 30（`eslint.config.js` + `pnpm lint` + pre-commit）/ ✅0 新依赖（eslint 内置）**

**v14-C13** **0 Prettier** —— 4397 行 EditorWindow 缩进/引号混乱。**估行 15（`.prettierrc` + `pnpm format`）/ ✅0 新依赖**

**v14-C14** **0 commitlint / 0 husky** —— 提交信息无规范，git log 无法生成 changelog。**估行 40（commitlint + husky pre-commit + lint-staged）/ ⚠️ 需 2 依赖**

**v14-C15** **0 CHANGELOG** —— grep CHANGELOG 0 命中。**估行 60（conventional commits + standard-version 自动生成）/ ⚠️ 需 1 依赖**

**v14-C16** **0 CONTRIBUTING.md** —— 0 贡献指南。**估行 30（README 扩 + 贡献流程）/ ✅0**

**v14-C17** **GitHub Action 缺 Linux** —— `.github/workflows/` 0 Linux runner。**估行 40（加 ubuntu-latest 步骤：lint + test + build tauri linux）/ ✅0**

**v14-C18** **包体积预算 0** —— dist 1.5MB 0 卡点。**估行 20（vite-bundle-analyzer + GitHub Action fail if > 2MB）/ ⚠️ 需 1 工具**

### 🟡 P2（7 项 · 长尾 / 一致性 / 可维护性）

**v14-C19** **巨型文件 0 拆分** —— EditorWindow 4397 / AIPanel 2119 / aiClient 831 / aiStore 1048。**估行 800（按功能域拆 30+ 子组件 + 4-5 子 store）/ ✅纯前端**

**v14-C20** **命名不一致** —— screenshot / capture / snap 三词混用。**估行 50（grep replace_all + 1-2 天重命名）/ ✅纯前端**

**v14-C21** **Magic number 散落** —— 12 主题 id / 5 AI 主题 / 50 OCR 上限 / 100 截图上限 0 常量化。**估行 40（`src/constants.ts` + 替换 30+ 处）/ ✅纯前端**

**v14-C22** **注释质量参差** —— toolCallParser 464 行 5 形态兜底仅 5 行注释。**估行 200（关键算法补 JSDoc + 复杂逻辑加 trace 注释）/ ✅纯前端**

**v14-C23** **包管理 0 审计** —— 11 个 deps 0 `pnpm audit` / 0 outdated 警告。**估行 10（加 `pnpm audit` 到 CI）/ ✅0**

**v14-C24** **AI prompt 模板无版本化** —— 12 主题 + 30+ preset prompt 写死在 `aiPresets.ts` 0 版本号。**估行 60（presets.json + schemaVersion + 灰度回滚机制）/ ✅纯前端**

**v14-C25** **CI 0 性能基线** —— dist 体积 / 启动时间 / 内存占用 0 监控。**估行 80（CI 跑 3 项 + 存 baseline + 退化 fail）/ ✅0**

---

## 三、3 条 v1-v13 真未触达的新维度

1. **可测试性 / CI / 工程债系统化**（v9 仅提"0 test / 0 lint / 0 e2e"是清单式——本轮 C09-C18 覆盖 10 大工程债 + 0 视觉回归 / 0 Linux CI / 0 commitlint / 0 changelog / 0 体积预算）
2. **可观察性 / 错误聚合**（v9 仅提"0 错误上报 / 0 埋点"——本轮 C05-C08 覆盖 4 大可观察性盲点：分类/聚合/性能/行为）
3. **代码安全 / 路径 / panic**（v9 仅提"apiKey 明文"——本轮 C01-C04 扩到 4 大安全盲点：keychain/路径校验/panic 兜底/log 脱敏）

---

## 四、Top 3 必做（按 ROI × 铁律兼容 × 1 天可完工）

| 排序 | 项 | 估行 | 收益 | 铁律 |
|---|---|---|---|---|
| 🥇 | **C09 · 20 个工具函数单元测试** | 400 | **重构不再崩 + AI 解析器回归 0** | ✅vitest 内置 / 0 新依赖 / 1 天 |
| 🥈 | **C12 + C13 · ESLint + Prettier** | 45 | **代码风格统一 + IDE 报红 + pre-commit 守门** | ✅0 新依赖 / 30 分钟 |
| 🥉 | **C05 · 错误聚合 + 诊断包导出** | 80 | **"出 bug 怎么 debug"从 0 → 1** | ✅纯前端 / 半天 |

**Top 3 合计**：~525 行 / 0 Rust / 0 新依赖 / **1.5 天完工**

**为何 C09 > C12+C13 > C05**：vitest 是质量基础（纯函数工具可立测）；lint+format 是工程标准；C05 解决"用户报错后开发者只能猜"。

---

## 五、3 套候选落地包

**包 A · 1.5 天 525 行**（C09 + C12 + C13 + C05）—— **0 Rust / 0 新依赖 / 1.5 天**

**包 B · 1 周安全 + 错误分级**（C01 + C02 + C03 + C04 + C06）—— **~360 行 / 5 Rust / 0 新依赖 / 1 周**

**包 C · 2 周可观察性 + CI + 拆分**（C07 + C08 + C10 + C11 + C14-C25）—— **~2300 行 / 0 Rust / 4-5 新依赖 / 2 周**

**推荐**：先做**包 A** → 立刻提升代码质量基线；周末做**包 B** → 安全闭环（合规 / panic 兜底 / 路径安全 / 错误分级）；下月做**包 C** → 完整可观察性 + CI + 重构。

---

## 报告字数：~7KB / 25 项 GAPS / P0=6 / P1=12 / P2=7 / Top 3 = 1.5 天 525 行
