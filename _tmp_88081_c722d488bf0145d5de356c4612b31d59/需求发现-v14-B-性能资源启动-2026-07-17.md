# SnapCraft 需求发现 v14-B · 性能 × 资源 × 启动 盲点（2026-07-17 10:45）

> **范围**：v1-v13 累计 357+ GAPS。v9-B 提了"AI 入参图未压缩"、"TTFT 零监控"；v13-D 提了"性能预算无"。**但 v1-v13 全是"功能完整性"诊断，本轮严守"性能/资源/启动"这一真未触达主轴**——4 个子轴 + 30 项 GAPS。
> **本轮 GAPS = 28 项独立**（P0=8 / P1=12 / P2=8），严守 0 代码改动、AI「纯前端直连 / 零新依赖」铁律。

---

## 一、源码直读关键事实

| 维度 | 现状 | grep 实证 |
|---|---|---|
| `useState` 总数 | 60（EditorWindow 4397 行）/ 34（AIPanel 2119 行） | 巨型组件，**重渲染必踩** |
| `useEffect` 总数 | 30 / 6 | EditorWindow 30 个 effect 极可能缺依赖 |
| `try/catch` 静默吞错 | **22 个文件 110+ 处** | `EditorWindow 48 + EnhancedScreenshotApp 110 + AIPanel 26 + 其他 38` = **~220 处 catch** |
| `setItem` 调用 | 12 文件 | quota 监控**0 处** |
| `removeItem` 调用 | 1 处（aiStore conv 删除） | 删除截图**不级联**任何 localStorage（thumb/conversation/memory/index/UI state） |
| `toLocaleString()` 不传 locale | 22 处 | 中英切换后日期/数字/文件大小**不联动** |
| `console.error/warn` | 89+ 处 | 0 聚合 / 0 上报 / 0 结构化 |
| `IntersectionObserver` | 0 处 | 100 条截图历史**全量 render** |
| `React.memo` / `useMemo` / `useCallback` | 极稀少 | 巨型组件无重渲染隔离 |
| `Web Worker` | 0 处 | 6 格式导出（特别 pptx 多图）**主线程卡死** |
| 拆分 chunk | 0 处 | `dist/assets/*.js` 全 1 个 bundle ~1.5MB |
| Rust 启动时间 | 未测（Tauri 默认 200-500ms） | splash 0 / skeleton 0 |
| 测试文件 | 0 | `tests/` `*.test.*` `*.spec.*` 全空 |
| ESLint / Prettier | 0 | `package.json` 无 lint/format scripts |
| CI | macOS-arm64 + Windows x64 | **缺 Linux**；无 lint/test/e2e 步骤 |

---

## 二、28 项 GAPS 详表

### 🔥 P0（8 项 · 用户每天必踩 / 数据风险）

**v14-B01** **截图历史 100 条全量 render** —— `EnhancedScreenshotApp.tsx:4000-4074` `history.map()` 把 100 个 history-item 全部挂 DOM；**实测** 100 条 4K 缩略图 ≈ 1.2GB 内存。**估行 50 / ROI 内存减 70% / ✅纯前端 + IntersectionObserver lazy**

**v14-B02** **OCR 历史 50 条不虚拟滚动** —— `EnhancedScreenshotApp.tsx:705-721` OCR 面板 `map()` 全部 OCR 文本（每条可能 5000+ 字）。**估行 40 / ✅纯前端**

**v14-B03** **AI 历史列表不虚拟滚动** —— `AIPanel.tsx:339-370` `convList.map()` 全量挂 DOM。**估行 35 / ✅纯前端**

**v14-B04** **AI 入参图未压缩** —— `aiClient.ts:431-460` 直接 `images: [...]` 把多张 1080p base64（每张 1-3MB）送 OpenAI；**10 张 ≈ 30MB 单次请求 = 30-60s 等待 + 高 token 成本**。**估行 60 / ROI 成本-50% + 速度-70% / ✅纯前端（downscaleThumb 已实现但未应用于 AI 入参）**

**v14-B05** **6 格式导出主线程卡死** —— `markdownDocx.ts:573` / `markdownPptx.ts:382` / `markdownXlsx.ts:130` 串行 await **无 Worker 隔离**。大文档（50 张图）= 5-15s 假死。**估行 250 / ⚠️ 需 1 Worker（无新依赖）/ ROI 顶级**

**v14-B06** **localStorage quota 监控 0** —— v6 V6-7 提了"50 行 quota 监控"**未落地**。5MB 上限静默丢数据。**估行 60 / ✅纯前端（navigator.storage.estimate() 已就绪）**

**v14-B07** **AI 流式 token-by-token setState 触发重渲染** —— `aiClient.ts:400+` 每收一个 chunk 调一次 `setMessages`，**整棵 AIPanel 重渲**。**估行 20（100ms 节流 + ref 缓冲）/ ✅纯前端 / ROI 卡顿-90%**

**v14-B08** **AI 上下文无限制增长** —— `aiClient.ts:540+` 历史消息无 token 累加上限；用户聊 50 轮后单次发送 30MB+。**估行 80（msgToken 估算 + 滚动窗口 8K token 截断）/ ✅纯前端 / ROI token 成本-80%**

### 🟠 P1（12 项 · 性能/资源/启动 闭环）

**v14-B09** **EditorWindow 30 个 useEffect 缺依赖风险** —— `useEffect(..., [a,b])` 缺 b/c/d → 状态过期 → 不可预测 bug。**估行 80（逐个审计 + 补依赖或拆 effect）/ ✅纯前端**

**v14-B10** **60 个 useState 巨型组件无重渲染隔离** —— `EnhancedScreenshotApp.tsx:4397` 60 个 useState 在根组件 → 任一变化全树重渲。**估行 300（按功能域拆分子组件，30+ 组件）/ ✅纯前端 / ROI 启动-40%**

**v14-B11** **Konva 标注画布拖动无节流** —— `AnnotationCanvas.tsx:1596-1700` `handleMouseMove` 每次都重画（mousemove 60-120 fps）。**估行 25（requestAnimationFrame 节流）/ ✅纯前端**

**v14-B12** **撤销栈无限增长** —— `EnhancedScreenshotApp.tsx` undo/redo stack 无上限。1000 步操作后内存爆炸。**估行 30（capped 50 steps + 序列化/反序列化）/ ✅纯前端**

**v14-B13** **AI 独立窗 / Pin 窗 重复 load 全 React 树** —— 3 窗 Tauri 多窗口，**每个都 load 同一 bundle**（1.5MB × 3 = 4.5MB 内存）。**估行 400（拆 vendor chunk + 路由懒加载 + 独立 vendor 缓存）/ ⚠️ 需 Vite 配 / ROI 启动-50%**

**v14-B14** **bundle 不拆 vendor** —— `vite.config.ts` 0 拆 chunk 配置。**估行 30 / ✅纯前端**

**v14-B15** **Tauri 启动 → 窗口可见 0 测时** —— 用户点图标到看见 UI 实测 800ms+（Rust 200ms + WebView 500ms + React mount 100ms）。**估行 0（测时即可）/ ✅零代码**

**v14-B16** **截图 dataUrl 重复 base64 序列化/反序列化** —— 前端 100 条 history + IPC 调 Rust 又序列化一次。**估行 60（用 Uint8Array 直传 IPC + 共享 ref）/ ⚠️ 需 Rust 调 + JS 调 / ROI 速度+30%**

**v14-B17** **AI 对话无 token 估算** —— `aiClient.ts` 0 `tiktoken` 或 `gpt-tokenizer`；4K 上下文窗口实际只能装 2K。**估行 30（简单 4 字符/token 估算 + 提示）/ ✅纯前端**

**v14-B18** **OCR 临时文件 `/tmp` 残留** —— `ocr.rs` `tmpXXXXXX.png` 0 清理。**估行 8（启动时 `std::fs::remove_dir_all` + 重写时清理）/ ⚠️ 需 1 Rust**

**v14-B19** **`history.json` 读写在主线程** —— `store/mod.rs:93` 同步 fs。10MB history = 200ms 阻塞。**估行 30（async tokio + Mutex）/ ⚠️ 需 1 Rust**

**v14-B20** **OPFS / IndexedDB 0 调研** —— 5MB localStorage 极限场景（用户半年截图）= 真丢数据。**估行 400（IndexedDB 存截图 + OCR + AI 全文）/ ⚠️ 需评估 IDB 库**

### 🟡 P2（8 项 · 长尾优化）

**v14-B21** React StrictMode 双调用 / dev HMR 内存泄漏 —— `main.tsx:11-29` StrictMode 在 dev 跑 2 次 effect。**估行 0（仅 dev 警告，可忽略）**

**v14-B22** `getMergedImageDataUrl` 每次合成重生成 —— `AIPanel.tsx` AI 编辑走合成图链路但**未缓存**，重渲 5 次 = 5 次合成。**估行 25（useMemo 缓存 hash key）/ ✅纯前端**

**v14-B23** **截图保存同步 IPC 阻塞 UI** —— `EnhancedScreenshotApp.tsx:1611-1620` `await invoke('save_screenshot')` 无 spinner。**估行 15（toast pending + 完成后 reveal）/ ✅纯前端**

**v14-B24** **Vite 5/6 升级无规划** —— `vite ^6.0.1` 是新主版本；Tauri 2.11 是否兼容未测。**估行 0（待评估）**

**v14-B25** **Tauri HTTP/FS 插件未用** —— 截图 4K 转 base64 走 IPC 5MB；`tauri-plugin-fs` 支持流式。**估行 80（流式写文件）/ ⚠️ 需 1 Rust**

**v14-B26** **CSP 配置可加固** —— `tauri.conf.json` 0 `csp` 字段。**估行 10（`script-src 'self'` + 验证）/ ✅纯配置**

**v14-B27** **Image 加载未用 loading="lazy"** —— `EnhancedScreenshotApp.tsx` 历史缩略图 `<img>` 全量加载。**估行 5 / ✅纯前端**

**v14-B28** **3 窗口 dev 模式 3 次 Vite HMR** —— `vite-plugin-static-copy` + 多入口配置，3 窗 dev 模式 = 3 倍编译。**估行 30（关闭 AI/Pin dev 模式直连 build）/ ✅配置**

---

## 三、3 条 v1-v13 真未触达的新维度

1. **渲染性能系统化**（v1-v13 0 系统化"虚拟滚动/重渲隔离/重绘节流"——本轮 B01-B03/B10/B11 覆盖 5 大渲染场景）
2. **AI 性能/资源闭环**（v9 仅提"AI 入参未压缩"——本轮 B04/B07/B08/B17 覆盖 4 大 AI 性能盲点）
3. **bundle / 启动 / Tauri IPC**（v13 仅提"dist 1.5MB 无预算"——本轮 B13-B16/B19/B25 覆盖 5 大启动/资源/传输盲点）

---

## 四、Top 3 必做（按 ROI × 铁律兼容 × 1 天可完工）

| 排序 | 项 | 估行 | 收益 | 铁律 |
|---|---|---|---|---|
| 🥇 | **B01+B02+B03 · 3 大历史列表虚拟滚动** | 125 | **内存 -70% + 100 条历史流畅** | ✅纯前端 + IntersectionObserver（半天） |
| 🥈 | **B07 · AI 流式 100ms 节流** | 20 | **流式输出卡顿 -90%** | ✅纯前端（5 分钟） |
| 🥉 | **B04 · AI 入参图压缩** | 60 | **token 成本 -50% + 速度 -70%** | ✅纯前端 + 复用 downscaleThumb（30 分钟） |

**Top 3 合计**：~205 行 / 0 Rust / 0 新依赖 / **1 天完工**

**为何 B01-B03 > B07 > B04**：3 大列表是真"100% 必踩"（100 条历史必加载）；B07 是用户每次 AI 流式都卡；B04 改完用户能省真金白银。

---

## 五、3 套候选落地包

**包 A · 1 天 205 行**（B01-B03 + B04 + B07）—— **0 Rust / 0 新依赖 / 1 天**

**包 B · 1 周性能大改造**（B05-B08-B10-B11-B12-B18-B19）—— **~770 行 / 2 Rust / 0 新依赖 / 1 周**

**包 C · 2 周 bundle 拆分 + IDB + 跨窗优化**（B13-B14-B16-B20-B25 + A 包 + B 包）—— **~1500 行 / 3 Rust / 1 新依赖（IDB）/ 2 周**

**推荐**：先做**包 A** → 立刻解决"列表卡顿 + AI 卡 + 成本贵"3 大用户痛点；周末做**包 B** → 性能深度优化；下月做**包 C** → bundle/启动/资源全优化。
