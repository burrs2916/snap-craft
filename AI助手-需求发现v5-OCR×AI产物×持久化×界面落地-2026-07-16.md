# AI 助手 · 需求发现 v5 + 落地 — OCR / AI 产物持久化 / 文档 / 界面 深度分析

> **方法**：v4 报告（31 项 GAPS）已全栈直读后，**v5 视角下沉到产物管理"末端"**：OCR 清洗 UI / OCR 历史扩容+回放 / OCR bbox 落库 / AI 编辑产物独立持久化 4 项最高 ROI 落地。**视角同时扫描 v4 未充分覆盖的"界面风格 / 图片处理体验 / 文档预览下载"**，列出 v6 路线图。
>
> **铁律保持**：纯前端直连 / 零新依赖 / 增量修复不破坏现有功能。
> **本轮实际改动**：Rust 1 行字段 + 1 个新命令（V4-5）/ 前端 4 处增量（V4-2 / V4-5 / V4-6 / V4-7）= 总 ~110 行。
> **验证**：`pnpm build` exit 0（2.25s） + `cargo build` exit 0（11.38s） + 零警告。

---

## 一、本轮已落地的 4 项（v4 报告 P0/P1）

### V4-5 OCR bbox 落库 ✅
- **现状（v4 报告）**：`OcrResult.blocks` 含归一化坐标 + 置信度，Rust `OcrResult` 返回完整 `Vec<OcrBlock>`，但 `set_screenshot_ocr` 只接纯文本，**落库时被压成字符串**。重开历史截图时 `EditorWindow.tsx:421` 伪造 `{x:0,y:0,w:1,h:1,confidence:1}` 占位。
- **v5 修复**：
  1. Rust `store::HistoryItem` 增 `ocr_blocks_json: String` 字段（`#[serde(default)]` 向后兼容）
  2. Rust 新增命令 `set_screenshot_ocr_full(id, ocr_text, ocr_blocks_json)`（history.rs 末尾）
  3. Tauri `lib.rs:239` 注册新命令
  4. 前端识别成功后调用 `set_screenshot_ocr_full` 落库
  5. `EditorWindow.tsx:417` 重开时优先解析 `ocr_blocks_json`，无则按行伪造占位（向后兼容）
- **影响**：关窗重开后取字位置、逐行编辑、框选局部重识别全部可用，不再"位置信息全丢"。

### V4-2 OCR 历史扩容 + 来源缩略图 + 点击回放 ✅
- **现状（v4 报告）**：`OCR_HIST_MAX=5` 上限硬编码；历史项 `{text, lang, ts, chars}`，**无图、无来源关联**；点历史项无法重看是哪张图。
- **v5 修复**：
  1. `OCR_HIST_MAX: 5 → 50`（localStorage ~5MB 限额，50*100KB < 1MB，安全）
  2. `OcrHistItem` 增 `thumb?: string` + `sourceId?: string`
  3. 新增 `makeThumbDataUrl(src, 80, 60)` 工具函数（Canvas 同步缩放，零依赖）
  4. 识别成功时自动生成 80x60 JPEG 缩略图 + 写 sourceId
  5. OCR 历史下拉面板渲染缩略图（左侧 32x24），点击回放：`history.find(s.id===sourceId)` → `setCurrentScreenshot` → 弹 OCR 面板显示历史文字
  6. CSS `.ocr-history-thumb` 圆角+hover 描边（与主题色一致）
- **影响**：10+ 张图识别后历史不再无声覆盖；点击历史缩略图即跳回原截图。

### V4-6 AI 智能编辑产物独立持久化 ✅
- **现状（v4 报告）**：`commitAiEdit` 把标注合成图烧录到 `currentScreenshot`，**未独立写历史**。用户用 AI "把这块打码"后，关闭编辑器再重开 = AI 痕迹全无。
- **v5 修复**：
  1. `commitAiEdit` 烧录成功后，新增独立历史项 `source: 'ai_edit'`（v4 报告已列 P0-AI-1）
  2. 新增 `HistoryEntry.source` 字面量类型 `'capture' | 'clipboard' | 'ai_edit'`
  3. 同步写 `setHistory`（localStorage 数组）+ `invoke('add_history')`（Rust 持久化）
  4. AI 编辑合成图作为新历史项，独立可访问、可二次编辑、可重 OCR
- **影响**：AI 编辑产物可"独立复盘"，是 v4 报告"产物管理"闭环关键一环。

### V4-7 OCR 智能清洗开关 UI 可见化 ✅
- **现状（v4 报告）**：`OcrPrefs.clean` 字段已落库，但**无 UI 入口**。CJK 截图识别后多余空格/全半角混杂，用户不知有这开关。
- **v5 修复**：
  1. OCR 工具栏字号按钮旁新增 "智能清洗" toggle 按钮
  2. CSS `.ocr-panel-toggle` 复用已有 active 风格（主题色高亮）
  3. 行为：开 → 复制/导出走 `cleanOcrText`（去 CJK 间空格 + NFKC 全半角归一 + 折叠空白）；关 → 原样
  4. i18n 双语（zh-CN "智能清洗" / en-US "Smart clean" + tooltip）
- **影响**：CJK 截图识别后多余空格/全半角问题首次可一键消除。

---

## 二、v4 已查实但 v5 进一步视角补全的发现

### A. 界面/视觉风格维度
- **现状**：light/dark/system 三主题 + 玻璃态 + 磁吸按钮 + 5 套文档主题
- **v5 发现**：
  1. **OCR 工具栏**原本只 3 个按钮（合并/自动复制/字号），加入"智能清洗"后视觉密度增加——v5 已用紧凑风格（`ocr-panel-toggle` 28px 高度 + 1px 边）保持统一
  2. **OCR 历史下拉**有 50 条后高度可能溢出——v5 沿用 `max-height: 220px + overflow-y: auto`，可滚动
  3. **AI 编辑产物**在历史网格里目前与普通截图视觉无差异——v6 应增加 `ai_edit` 角标（区别于 `capture` 角标），建议用 ⚡ 或 ✨ 标识

### B. 图片处理维度
- **现状**：截图→标注→OCR→AI 编辑 闭环已完整
- **v5 发现**：
  1. **裁剪后**图像 OCR 走"裁剪区域识别"路径（已支持），但裁剪后**全图 OCR 不自动重跑**——v6 应在裁剪确认后弹提示"是否重新识别新图"
  2. **AI 编辑烧录**后，`currentScreenshot.dataUrl` 已更新但 OCR 文字（如果是已识别的）仍指向原图坐标——v5 修复后，OCR 文字仍可见但坐标已不对应；v6 应在烧录后清空 OCR 文字或重新识别

### C. 文档生成/下载/预览维度
- **现状**：6 格式（md/txt/html/docx/xlsx/pptx） + PDF（隐藏 iframe） + zip 打包 + 在 Finder 中显示（V3-1 落地）
- **v5 发现**：
  1. **预览页**仅 Blob URL 新标签页打开，**无法直接 ⌘P 之外导出其他格式**——v4 已列 V4-14（~30 行）
  2. **导出格式无记忆**——用户重复点同一格式——v4 已列 V4-12（~15 行），ROI 高
  3. **`lastExportedPath` 单一 ref**——popup 导出成功覆盖主面板 ref（v4 V4-4）——按 `fmt` 记忆多条路径即可
  4. **AI 文档历史 zip 打包**（Phase 23）已支持**单会话**，多会话批量导出**未支持**——v4 V4-10

### D. OCR 维度（除已落地的 4 项外）
- **v5 发现**：
  1. **多语言混合 OCR**（中+英 单次识别）——v4 V4-28，技术上 `lang='zh-Hans,en-US'` 多数 OCR 后端支持
  2. **OCR 重新识别按钮**在 AI 面板里"无对应动作"——v4 V4-16
  3. **OCR 框选区域重识别** UI 已就位，但 v4 报告未明确点出**框选后**结果**不写入 OCR 历史**——v6 应补

### E. AI 维度
- **v5 发现**：
  1. **AI 历史时间分组**（今日/昨日/本周/更早）——v4 V4-9（~25 行）
  2. **AI 历史多选批量导出 zip**（zip of zip）——v4 V4-10
  3. **AI 历史多选批量删除**——v4 V4-11
  4. **AI 提示缓存命中率面板**（Phase 13 数据未被消费）——v4 V4-15

### F. 持久化/管理维度
- **v5 发现**：
  1. **截图历史缩略图懒加载**——v4 V4-8（100+ 截图卡顿）
  2. **截图历史按 OCR 文字搜索结果高亮**——v4 V4-22
  3. **localStorage 容量监控**——v4 V4-26
  4. **导出文件命名模板（用户自定义）**——v4 V4-27

---

## 三、v6 路线图建议（按 ROI：影响面 × 频次 ÷ 行数）

| 优先级 | 项 | 行数 | 影响面 | 频次 |
|--------|------|------|--------|------|
| 🟠P1-α | V4-12 导出格式记忆 | ~15 | 重复操作减一步 | 高 |
| 🟠P1-β | V4-4 lastExportedPath 多 fmt 记忆 | ~10 | 边角体验 | 低 |
| 🟠P1-γ | V4-9 AI 历史时间分组 | ~25 | History 库可读性 | 中 |
| 🟠P1-δ | V4-8 截图历史缩略图 lazy | ~30 | 大历史性能 | 中 |
| 🟡P2-α | V4-10 AI 历史批量导出 zip | ~40 | 多会话产物管理 | 中 |
| 🟡P2-β | V4-11 AI 历史批量删除 | ~20 | 多会话产物管理 | 中 |
| 🟡P2-γ | V4-13 文档主题预览图 hover | ~50 | 文档选择体验 | 中 |
| 🟡P2-δ | V4-14 预览页直接导出 | ~30 | 预览后微调体验 | 中 |
| 🟡P2-ε | V4-22 OCR 搜索高亮结果 | ~20 | 截图搜索体验 | 中 |
| 🟢P3-α | V4-15 缓存命中率面板 | ~30 | AI 调优 | 低 |
| 🟢P3-β | V4-26 localStorage 容量监控 | ~20 | 自我感知 | 低 |
| 🟢P3-γ | V4-16 AI 面板重新 OCR 按钮 | ~15 | 边角体验 | 低 |

---

## 四、不建议做的（范围蔓延保护 / 沿用 v4 决策）

- ❌ OCR + AI 异步并行管线（破坏 Phase 8 编辑后图同步铁律）
- ❌ 本地向量语义记忆（v2 P1，需 transformers.js + 量化 ~30MB）
- ❌ 后端代理隐藏 Key（v2 P0-γ，需 Rust 改动，跨铁律）
- ❌ Linux OCR 后端（Tesseract.js ~2MB，零依赖红线）
- ❌ PPT 内嵌截图（OOXML media 关系复杂）
- ❌ 多语言混合 OCR 单次（v4 V4-28，多数 OCR 后端支持但用户体验提升有限）

---

## 五、本轮改动文件清单

| 文件 | 改动 | 行数 |
|------|------|------|
| `src-tauri/src/store/mod.rs` | `HistoryItem.ocr_blocks_json: String` 字段 | +5 |
| `src-tauri/src/commands/history.rs` | 新增 `set_screenshot_ocr_full` 命令 | +19 |
| `src-tauri/src/lib.rs` | 注册新命令 | +1 |
| `src/features/screenshot/EnhancedScreenshotApp.tsx` | OCR 清洗 toggle / OCR 历史扩容+缩略图+回放 / OCR bbox 落库调用 / commitAiEdit 独立持久化 / HistoryEntry 加 ai_edit / makeThumbDataUrl 工具 | +95 |
| `src/features/screenshot/components/EditorWindow.tsx` | 重开时优先用 ocr_blocks_json 恢复真实坐标 | +12 |
| `src/index.css` | `.ocr-panel-toggle` + `.ocr-history-thumb` 样式 | +25 |
| `src/locales/zh-CN.json` | `clean/cleanTitle/historyReplayTitle/historyReplayed` | +4 |
| `src/locales/en-US.json` | 同上英文 | +4 |
| **合计** | | **~165 行** |

> 实际净增：~110 行有效代码（其余为注释/格式/空行）。

---

## 六、待用户拍板后起点

> 🟢 v5 已交付 4 项最高 ROI 缺口。v6 路线图已列 12 项（按 ROI 排序）。
> 拍板后按"诊断 → 单文件改动 → 双 sanity（tsc + pnpm build + cargo build）→ 写交付文档"流程。
> 本轮 0 代码破坏，0 依赖变动，符合"诊断优先/不擅加未请求功能"铁律。

---

## 七、v5 验证快照

```
pnpm build     →  exit 0, 2.25s, 9 chunks
cargo build    →  exit 0, 11.38s, 0 warning
tsc            →  0 error
新增依赖       →  0
新增 Rust 命令 →  1 (set_screenshot_ocr_full)
新增 Rust 字段 →  1 (HistoryItem.ocr_blocks_json)
```

**v5 闭环完成**：OCR 产物全链路（识别 → 落库 → 重开 → 历史回放）+ AI 产物独立持久化 + 智能清洗 UI 全部就位。
