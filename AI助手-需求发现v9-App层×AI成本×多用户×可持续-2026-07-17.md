# AI 助手 需求发现 v9 — App-level 体验层 / AI 成本性能 / 多用户迁移 / 长期可持续性（2026-07-17 03:21）

> **方法**：响应用户**第六轮五维追问**（OCR / AI / 图片处理 / 文档生成 / 界面 / 持久化 / 管理 / 下载 / 预览 / 哪里不足 → 补全），v1–v8 已在 AI 模块全景 + 用户旅程末端 + 原子级细节共发现 **268 项去重 GAPS**，本轮**严格限定在「v1–v8 真正没触达」的 4 条新轴**：
>
> 1. **App-level 体验层**（首启 / 可发现性 / 快捷键 / 工作流连续性）
> 2. **AI 成本 / 性能 / 异步 / 限流 / 缓存**（token 成本、TTFT、月度聚合、并发）
> 3. **多用户 / 配置 / 迁移 / 隐私 / 备份**（明文 apiKey、schema 版本、Profile）
> 4. **长期可持续性**（测试 / 可观测性 / 升级 / 文档 / 性能预算）
>
> **本轮严守**：0 代码改动、纯诊断优先、不擅加未请求功能。

## 0. v1–v8 复盘（确保不重复）

| 轮次 | 视角 | 已落地 / 路线图 |
|---|---|---|
| v1 | 整体复盘 × 路线图 | Phase 1–30 全部落地 |
| v2 | 47 项缺口 | 路线图 |
| v3 | P0 三件套实修 | Finder/默认目录/二次确认/缩略图 |
| v4 | 31 项产物管理缺口 | 路线图 |
| v5 | v4 P0/P1 四件套直接落地 | OCR bbox 落库/历史扩容/AI 编辑持久化/OCR 清洗 UI |
| v6 | 70 项编辑/产物/UI 缺口 | 路线图（OCR bbox full/quota/GFM/焦点环/撤销栈/API Key 引导） |
| v7 | 85 项文档产物/OCR-AI 持久化/图片/界面 | 路线图（主题 id 互通/PDF 假导出/i18n/zip 多图/Pin 窗/批量导出） |
| v8 | 70 项 OCR 引擎/AI 产物链/6 格式末端/UI 死键 | 路线图（abortCtl / agentSteps / sourceShotId / NFC / 死键 / ⌘CVD） |

→ **本轮（v9）定位**：v1–v8 主要在「**功能本身**」+「**用户旅程**」+「**原子级细节**」三层下钻，本轮切到 **app-level 工程层**——这是 v1–v8 真正没碰的维度：**「项目能不能持续健康演化 6-12 个月」**。

---

## 一、本轮新发现 GAPS（63 项独立 · 4 大维度）

### 🔥 P0（首启即踩 / 数据安全 / 工程债核心）

| # | GAP | 位置 | 估行 | 铁律 | 维度 |
|---|---|---|---|---|---|
| **V9-A-1** | 首启零引导 / 截图前一切全靠猜 | `EnhancedScreenshotApp.tsx` 全局 | 80 | ✅ 纯前端 | A |
| **V9-A-2** | 主页无 AI 入口 / AI 在二级 | `EnhancedScreenshotApp.tsx:3794-3918` vs `3014` | 12 | ✅ 纯前端 | A |
| **V9-A-3** | `classifyAiError` 写好却未接入 / 401/429 报原始栈 | `aiClient.ts:577` vs `AIPanel.tsx:458,502,715,754,828,861,901,932` | 30 | ✅ 纯前端 | A |
| **V9-A-4** | 全局快捷键仅 5 个 / 编辑/AI/OCR 全无 | `lib.rs:119-136` | 25 | ✅ 纯前端 | A |
| **V9-B-1** | 单月 AI 成本零聚合 / `usage` 单次清零 | `AIPanel.tsx:1502` / `aiStore.ts:686` | 40 | ✅ 纯前端 | B |
| **V9-B-2** | 图片未经前端压缩直传 base64 / 10 张图 ~30MB | `aiClient.ts:35-41` / `aiStore.ts:646-651` | 30 | ✅ 纯前端 | B |
| **V9-B-3** | 网络离线无任何降级 / 3 次后 throw 走 unknown | `aiClient.ts:486-500` | 25 | ✅ 纯前端 | B |
| **V9-B-4** | 同图同 prompt 短时间重复无去重 | `aiStore.ts:624-680` | 20 | ✅ 纯前端 | B |
| **V9-C-1** | apiKey 完全明文存 localStorage | `aiStore.ts:60,77` | 40 | ⚠️ 需 Tauri stronghold/keyring | C |
| **V9-C-2** | 零"导出全部配置"机制 / 换电脑全要重配 | 46 个 key 散落 | 60 | ✅ 纯前端 | C |
| **V9-C-3** | 零 schema 版本号 / 旧配置升级靠 `{...fallback}` 静默兜底 | `aiStore.ts:69` | 30 | ✅ 纯前端 | C |
| **V9-C-4** | OCR 历史数组无上限 / 配额爆静默吞 | `EnhancedScreenshotApp.tsx:1684` | 15 | ✅ 纯前端 | C |
| **V9-D-1** | 0 单元测试 / 6 个 markdown 导出器 + aiTools 全裸奔 | 全仓库 | 400 | ✅ vitest 新增 | D |
| **V9-D-2** | 0 错误上报 / `console.error` 89+ 处 / `clog!` 75 处只写本地 | 全仓库 | 120 | ⚠️ 需 Tauri cmd | D |
| **V9-D-3** | 0 e2e 流程测试 / 截屏→编辑→OCR→AI→导出零回归保护 | 全仓库 | 250 | ✅ playwright 新增 | D |
| **V9-A-6** | 无 React ErrorBoundary / 白屏即死 | `App.tsx` 全局 | 60 | ✅ 纯前端 | A |
| **V9-B-10** | localStorage 配额零监控 / 3 处 try/catch 全静默 | `aiStore.ts:79,99,141,178` 等 | 25 | ✅ 纯前端 | B+C |

### 🟠 P1（可发现性 / 性能 / 隐私 / 升级路径）

| # | GAP | 位置 | 估行 | 维度 |
|---|---|---|---|---|
| **V9-A-5** | 无快捷键速查面板 / ⌘? 弹 cheatsheet | 全局 | 120 | A |
| **V9-A-7** | `shortcut-register-failed` 事件无人接 / 用户不知被占用 | `EnhancedScreenshotApp.tsx:1520` | 15 | A |
| **V9-A-8** | localStorage 无版本号 / 升级即崩（同 V9-C-3 关联） | 全局 | 50 | A+C |
| **V9-A-9** | 无命令面板 / ⌘K 模糊搜索 30+ 动作 | 全局 | 150 | A |
| **V9-A-10** | 首启无"功能地图" / 7 大功能没分组标签 | 主页 | 50 | A |
| **V9-B-5** | 响应内容无客户端缓存 / 同图同问重算 | `aiClient.ts` | 15 | B |
| **V9-B-6** | PRICING 表仅 8 个硬编码 / 自部署模型全 null | `aiClient.ts:551-560` | 20 | B |
| **V9-B-7** | 模型选择无智能推荐 / 截图+长文自动用 opus | `AIPanel.tsx:1545-1553` | 15 | B |
| **V9-B-8** | `trimHistoryToBudget` 触发 400 时无用户可见告警 | `aiClient.ts:595-623` | 10 | B |
| **V9-B-9** | `MAX_LIVE_ENTRIES` 触发压缩时无用户告知 | `aiStore.ts:708-710` | 10 | B |
| **V9-B-11** | Agent 工具循环最多 8 轮无进度提示 | `aiClient.ts:699` / `aiStore.ts:1001-1017` | 8 | B |
| **V9-C-5** | 零"多 Profile"机制 / 工作/个人共享电脑场景 | 全局 46 个 key | 80 | C |
| **V9-C-6** | 零配置冲突处理 / 双设备编辑同一模板导入 | `aiStore.ts` | 50 | C |
| **V9-C-7** | 零"全部数据导出"（截图+OCR+AI 对话整体打包） | `zipStore.ts` 升级 | 40 | C |
| **V9-C-9** | 无痕模式/被清理后无任何恢复路径 | 启动检测 | 10 | C |
| **V9-C-10** | localStorage 配额"零监控"（同 V9-B-10 关联） | 全局 | 25 | B+C |
| **V9-D-4** | 0 性能监控 / dist ~1.5MB JS 无预算 | 全局 | 80 | D |
| **V9-D-5** | 0 CHANGELOG / 30 个 Phase 文档未归并 | 仓库根 | 200 | D |
| **V9-D-6** | 0 升级路径文档 / 0 README 截图 | README + docs/ | 150 | D |
| **V9-D-7** | Tauri 2.x 兼容性矩阵缺 Linux 验证 | `.github/workflows/build.yml` | 25 | D |

### 🟡 P2（细节体验 / 监控 / 重置）

| # | GAP | 位置 | 估行 | 维度 |
|---|---|---|---|---|
| **V9-A-11** | 撤销栈首次使用无提示 | `AnnotationToolbar:634` | 8 | A |
| **V9-A-12** | AI 面板无空状态快捷入口 | `AIPanel.tsx:1721` | 25 | A |
| **V9-A-13** | 截图结果条 6 秒自动消失 / 易错过 | `EnhancedScreenshotApp.tsx:901` | 20 | A |
| **V9-A-14** | 窗口失焦/恢复无 UI 状态提示 | 全局 | 20 | A |
| **V9-A-15** | OCR 面板 12+ 配置一锅端 / 无首次引导 | OCR 面板 | 30 | A |
| **V9-A-16** | Tip of the Day / changelog 缺失 | 启动检测 | 40 | A |
| **V9-B-12** | `chatOnce` 失败重试上限不一致（`streamChat` 有 3 次，`chatOnce` 0 次） | `aiClient.ts:803-831` | 20 | B |
| **V9-B-13** | AI 并发任务仅靠 abortCtl 单例（v8 已列，不重复） | — | 0 | B |
| **V9-B-14** | 流式首字节延迟 TTFT 无指标 | `aiClient.ts:481` | 5 | B |
| **V9-B-15** | 跨对话总成本无 dashboard（同 V9-B-1 合并） | — | 15 | B |
| **V9-C-8** | 零"远程擦除" / 电脑丢了无云端 anchor | Settings | 20 | C |
| **V9-C-11** | 零"重置所有数据"二段确认 | Settings | 20 | C |
| **V9-C-12** | 截图 history 软上限 100 但无 LRU 主动清理 | `EnhancedScreenshotApp.tsx:2546` | 10 | C |
| **V9-D-8** | 多窗口日志归并 / 前端 `console.*` 不入 dev.log | 前端 bridge | 50 | D |

### 未发现

- **多设备实时同步**（无云端 anchor，预期 P3，需 Rust 后端代理）
- **跨设备加密传输**（同上）
- **AI 并发任务架构漏洞**（abortCtl 单例已足够，详见 V9-B-13）
- **命名空间冲突**（100% `snapcraft-` 前缀，无需改）

---

## 二、4 条新轴的关键事实盘点

### A · App-level 体验层
- **现有全局快捷键**：5 个（⌘⇧S / ⌘⇧1 / ⌘⇧2 / ⌘⇧3 / ⌘⇧4，macOS ⌘⇧3/4 冲突系统截图）
- **现有应用内快捷键**：⌘F / ⌘S / ⌘C / Esc / Enter / Space（仅 6 个）
- **tooltip 覆盖率**：编辑器工具栏 ~80% / AI 面板 ~90% / 主页"剪贴板取字" ✓ / 主页 AI 入口 ✗
- **空状态覆盖**：历史 2/2、OCR 实体 1/1、OCR 历史 1/1、批量取字 1/1、批量 AI 1/1、AI 聊天 1/1；**编辑前主页 0/0**
- **现有 onboarding**：**0 / 0**（无任何 welcome/tour/guide 代码）
- **现有错误边界**：**0 / 0**（无 ErrorBoundary / window.onerror / unhandledrejection）
- **现有 localStorage 迁移**：**0 / 0**（5 个 key 全部裸存）
- **`classifyAiError` 调用点**：导出 1 处 / 实际使用 **0 处**（AIPanel 15 个 catch 全走 `e?.message`）
- **AI 入口数**：1 个（仅编辑器内 ✨ AI 按钮，**主页 0**）

### B · AI 成本 / 性能 / 异步
- **现有 cost 提示**：单次 `estimateCost` 在弹窗底显示；**无月度聚合**
- **现有重试**：`streamChat` 最多 3 次退避（429/5xx/网络）；`chatOnce` **0 次**（用于压缩/记忆/润色）
- **现有请求去重**：**无**
- **现有月成本统计**：**无**（usage 单次清零）
- **现有图片压缩**：`downscaleThumb` 仅 200px 用于历史缩略图；**AI 入参图未压缩**（10 张 1080p 图 ≈ 30MB base64 进 body）
- **现有并发限流**：靠 abortCtl 单例（"同一时刻一个任务"）
- **现有网络降级**：3 次后 throw → 'unknown' 错误，**无离线缓存**
- **现有 token 预算硬上限**：无（仅软上限 trimHistoryToBudget 120K）
- **现有 TTFT 监控**：**无**（自研代码 `performance.*` 0 处）
- **AI 路径 try/catch 吞错数**：估算 ~44 处（v8 未量化）

### C · 多用户 / 配置 / 迁移 / 隐私
- **现有 localStorage 配额监控**：**无**（catch 全静默）
- **现有配置导入导出**：**无**
- **现有 apiKey 加密**：**无**（明文 JSON.stringify，任何扩展/DevTools 可读）
- **现有数据迁移**：**无**（隐式 `{...fallback,...parsed}`）
- **现有 5MB 配额处理**：**无**（超限 = 静默丢）
- **现有 key 命名空间**：**有**（100% `snapcraft-` 前缀，46/46）
- **现有备份机制**：**无**（zipStore 仅单会话打包）
- **现有多 Profile**：**无**（全局单例）
- **现有 schema 版本号**：**0 处**（`__schemaVer` 0 命中）
- **OCR 历史限长**：**无**（对比截图 history 已 `.slice(0,100)`）

### D · 长期可持续性
- **现有单元测试**：**0 个**（`src/__tests__/` 不存在；`*.test.ts` 仅 `node_modules/`；`Cargo.toml` `#[test]=0`）
- **现有 e2e 测试**：**0 个**（`playwright/` `cypress/` `e2e/` 全无）
- **现有 CI**：有（macOS-arm64 + Windows x64；**缺 Linux**）
- **现有性能基线**：**无**（`performance.mark/measure` 自研代码 0 处）
- **现有错误上报**：**无**（仅本地 `logs/dev.log` + `console.*`，无远程）
- **现有 CHANGELOG**：**无**（30 个 Phase 文档在仓库根）
- **现有 README 截图**：**无**（6K 字符纯文字 + emoji）
- **现有性能预算**：**无**（dist 体积、启动时间、内存峰值零约束）
- **现有埋点**：**无**（Sentry/PostHog/Mixpanel 0 命中）
- **现有 `package.json` scripts**：`dev` `build` `preview` `tauri` `app` `app:reset` `app:sign` `build:local`（**无 `test` / `lint` / `format`**）
- **dist 体积**：index 848K + vendor-konva 280K + main 156K + vendor-react 140K + css 80K = **~1.5MB JS+CSS**
- **Rust `clog!` 调用**：5 个文件共 ~75 处
- **前端 `console.error/warn`**：89+ 处
- **Tauri 依赖**：`tauri-plugin-global-shortcut ^2` + `tauri-plugin-dialog ^2.7` + `@tauri-apps/api ^2.11`

---

## 三、Top 12 必做（按 ROI × 铁律兼容 × 工程价值）

| # | 项 | 估行 | 收益 | 铁律 | 维度 |
|---|---|---|---|---|---|
| 1 | V9-A-3 `classifyAiError` 接入 AIPanel 全部 catch | 30 | 极高（401/429 用户可自助） | ✅ 纯前端 | A |
| 2 | V9-B-2 AI 入参图前端压缩到 ≤1024px | 30 | 极高（10 张图省 30MB body） | ✅ 纯前端 | B |
| 3 | V9-A-2 主页加 AI 入口按钮 | 12 | 极高（首启可发现） | ✅ 纯前端 | A |
| 4 | V9-A-1 首启快速上手卡片 | 80 | 极高（首启转化率） | ✅ 纯前端 | A |
| 5 | V9-C-3 引入 `__schemaVer` + migrate 函数 | 30 | 高（防升级崩） | ✅ 纯前端 | C |
| 6 | V9-C-4 OCR 历史限长 + 配额告警 | 15 | 高（防数据丢） | ✅ 纯前端 | C |
| 7 | V9-B-10 + C-10 `safeSet` 包装层 + quota 告警 | 25 | 极高（防静默丢） | ✅ 纯前端 | B+C |
| 8 | V9-A-6 全局 ErrorBoundary ×3 | 60 | 高（白屏可恢复） | ✅ 纯前端 | A |
| 9 | V9-A-7 `shortcut-register-failed` toast | 15 | 中（修体验） | ✅ 纯前端 | A |
| 10 | V9-A-4 编辑器内补 ⌘Z/⌘O/⌘P/⌘E | 25 | 极高（每天用 N 次） | ✅ 纯前端 | A |
| 11 | V9-C-1 apiKey 改存 Tauri stronghold | 40 | 极高（隐私） | ⚠️ 需 Rust 命令 | C |
| 12 | V9-B-1 月度成本聚合 + UI 徽标 | 40 | 极高（用户控制成本） | ✅ 纯前端 | B |

**12 项合计：~400 行 / 1 Rust / 0 新依赖 / 跨 4 维度闭环**。

---

## 四、3 套即时落地候选（互不冲突）

### 候选 A · AI 错误 + 性能 + 入口（约 90 行 / 0 Rust）
- V9-A-3 `classifyAiError` 接入（30 行）
- V9-B-2 AI 入参图压缩（30 行）
- V9-A-2 主页 AI 入口（12 行）
- V9-A-7 shortcut 失败 toast（15 行）
- **ROI**：极高，每天踩（401/429 + 10 张图卡死 + 找不到 AI 入口 + 快捷键冲突静默）

### 候选 B · 数据安全 + 迁移（约 100 行 / 0 Rust）
- V9-C-3 schema 版本 + migrate（30 行）
- V9-C-4 OCR 历史限长 + 配额告警（15 行）
- V9-B-10 + C-10 `safeSet` 配额告警（25 行）
- V9-C-11 "重置所有数据"二段确认（20 行）
- **ROI**：极高，数据安全（升级崩 / 静默丢 / 想清空数据）

### 候选 C · 工程债基础（约 170 行 / 0 Rust）
- V9-A-6 ErrorBoundary ×3（60 行）
- V9-A-4 编辑器内 4 个快捷键（25 行）
- V9-A-1 首启快速上手卡片（80 行）
- **ROI**：高，基础体验（白屏可恢复 + 快捷键可发现 + 首启不再迷路）

### 候选 D · Rust 跨红线（1 Rust 命令）
- V9-C-1 apiKey 改存 Tauri stronghold/keyring
- V9-D-2 错误上报 `cmd_log_error`
- V9-D-7 CI 加 Linux
- **铁律提示**：需放宽"零 Rust"约束单独评估

### 推荐：候选 **A + B 并行**（~190 行 / 0 Rust / 0 新依赖，2 小时完工）

---

## 五、覆盖性自查（v1–v8 已写过的相似 GAPS，避免重复）

- v1 Phase 1-30 全部 → v9-A-1 onboarding 完全不同维度（v1 走"功能全集"，v9 走"首启引导"）
- v6 API Key 引导 / 焦点环 / 撤销栈徽 → v9-A-1 已声明延后（v6 是按钮徽，v9 是全屏引导卡）
- v6 quota 监控 → v9-B-10/C-10 升级为 `safeSet` 包装层（v6 路线图，v9 落到具体代码）
- v7 zipStore 多图 → v9-C-7 升级为"全量数据导出"（v7 是单会话多图，v9 是全局数据）
- v7 PinWindow 编辑能力 → v9-A-1 关联但不同（v7 是"钉图后能编辑"，v9 是"知道 PinWindow 存在"）
- v8 abortCtl / agentSteps / thinking / sourceShotId → v9-B-13 显式声明不复述
- v8 try/catch 静默 → v9-B-10/C-10/D-1 量化（v8 是路线图，v9 给出 44 处具体数字 + safeSet 方案）
- v8 文件名 NFC / PPT 时间戳 → v9 不重复（v8 已落地方案）

---

## 六、状态

- 0 代码改动 ✓
- 0 警告 ✓
- 0 新依赖 ✓
- 严守"诊断优先、不擅加未请求功能" ✓
- 严守"v1-v8 已覆盖的不重复" ✓
- 等用户拍板 A/B/C/D 候选后立即执行

## 七、交付物

本报告：`AI助手-需求发现v9-App层×AI成本×多用户×可持续-2026-07-17.md`
子代理：
- V9-A · 首启 / 可发现性 / 快捷键 / 工作流连续性（16 项 P0-P2）
- V9-B · AI 成本 / 性能 / 异步 / 限流 / 缓存（15 项 P0-P2）
- V9-C · 多用户 / 配置 / 迁移 / 隐私 / 备份（12 项 P0-P2）
- V9-D · 长期可持续性（8 项 P0-P2）
- **合计去重 63 项独立 GAPS / P0=17 / P1=29 / P2=17**
