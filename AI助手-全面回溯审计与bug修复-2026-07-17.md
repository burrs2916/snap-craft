# SnapCraft 全面回溯审计 + 真实 Bug 修复（2026-07-17）

## 背景
用户指令：全面回溯现有功能，找出**空实现 / 假实现 / 隐藏 bug**，然后修复。

方法：3 路并行**只读**审计（AI 模块 / 截屏编辑器 / i18n+Rust+导出器），所有结论均 **file:line 实证**；并严格剔除"可疑"误报（用 grep 全仓库验证，避免历史轮次"数据滞后"式误判）。

> 审计中已实证**非 bug**（逐一澄清，未误改）：马赛克保存仍是原图、`Esc` 取消后状态复位、多屏负坐标、HiDPI 导出分辨率、`t()` 冻结在 useState、`CaptureOverlay` 孤儿引用、`display_id`→`displayId` 映射、pptx/docx/html 假实现、Rust `unimplemented!()`/裸 `unwrap()`/空返回。

## 修复清单（11 项真实 bug · 全增量 · 零 Rust 改动 · 零新依赖 · `pnpm build` exit 0）

| # | 严重度 | 问题 | 文件:行 | 修复 |
|---|---|---|---|---|
| 1 | 🔴P0 | **隐私哨兵打码坐标失效**：prompt 要求模型传嵌套 `r:{x,y,w,h}`，但 `redact_area` 的 schema/executor 读扁平 `x/y/w/h` → 坐标 `undefined`→`clamp01`→0，打码区域塌成 (0,0) 零尺寸矩形却回报"已打码"（**隐私泄漏**） | `aiTools.ts:237` + `:167` | executor 加 `args.x ?? args.r?.x` 兜底；prompt 示例/说明改为扁平坐标 |
| 2 | 🟠P0 | **流式每 token 触发 setState**：`onChunk:(d)=>set(s=>({output:s.output+d}))` 长文生成时数千次重渲染卡顿 | `aiStore.ts` chat/refine/runAgent 三处 | 新增 `makeStreamSink`：闭包缓冲 + 100ms 批量 flush；流结束 `stop()` 仅清定时器，最终 `set(output:full)` 校正，杜绝重复追加 |
| 3 | 🟠P1 | **批量 AI system 提示词缺键**：`t('ocr.batchAiSystem')` 键不存在 → 批量 AI 实际收到字面量 "ocr.batchAiSystem" 作系统指令 | `EnhancedScreenshotApp.tsx:2862` | 改为真实英文指令常量（模型据用户 prompt 语言回复） |
| 4 | 🟡P2 | **5 处 i18n 缺键露 key**：`ai.copyResult`/`ai.stopTitle`/`ocr.copyTitle`/`history.select`（属 `tool` 命名空间，确缺）/ 移除 `ocr.batchAiSystem` 引用 | AIPanel/EditorWindow/EnhancedScreenshotApp + locales | `ai.copyResult`→`ai.copy`、`ai.stopTitle`→`ai.stop`、`ocr.copyTitle`→`copy`、`history.select` 补键 |
| 5 | 🟡P2 | **OCR 批量复制静默吞错**：`navigator.clipboard.writeText(...).catch(()=>{})` 无 await、无反馈，写入失败用户无感知 | `EnhancedScreenshotApp.tsx:4170/4276` | 改 `async + await + flash(copied/copyFail)` |
| 6 | 🟡P2 | **涂黑/画笔打码 UI 不可达**：`maskSolid`/`maskBrushSize` 状态与 canvas 逻辑存在但工具栏无控件（**死功能**） | `AnnotationToolbar.tsx` | 加「涂黑」开关 +「笔刷大小」滑块 + `applySolid/applyBrushSize`，三者互斥高亮；补 `tool.solidMode`/`tool.brushSize` 键 |
| 7 | 🟡P2 | **unlisten Promise 无 .catch**：可能变 unhandled rejection | `EnhancedScreenshotApp.tsx` 2 处 | 补 `.catch(()=>{})` |
| 8 | 🟡P2 | **classifyAiError 恒返回 context**：400/413/422 非上下文类错误（如请求格式错）被误标为"上下文溢出" | `aiClient.ts:583-586` | 非上下文关键词 → 返回 `unknown` |
| 9 | 🟡P2 | **xlsx CJK 列宽写死**：`String(v).length` 中文按 1 计 → 中文列被 `####` 截断 | `markdownXlsx.ts:122` | CJK 字符计 2 单位 |
| 10 | 🟡P2 | **EditorWindow 保存失败仍关窗丢标注**：`saveAnnotations` 吞错 → 标注未落盘即 `destroy()` | `EditorWindow.tsx:955/965` | `saveAnnotations` 返回 boolean；关闭时失败 `flash(saveFail)` 并**中止关闭**；补 `editor.saveFail` 键 |
| 11 | 🟡P2 | **bridge callTool 无超时**：主窗未响应（崩溃/未监听）时 Promise 永久挂起，整段 AI 生成卡死 | `ai-window/bridge.ts:251` | 加 15s 超时兜底（resolve isError 结果，让 Agent 循环可恢复） |

## 有意未动（文档化，非 bug / 超出低风险修复范围）
- **多图层 API 死代码**（`screenshotStore.layers` 每次 mutation 被塌缩为单 default 层）：不崩溃，移除=重构，风险高于收益
- **`stitch.ts` 低对比误检**：纯色/白底长截图灰度差普遍 <12 易误判重叠，属算法调优，改动有回归风险
- **AI 3 个孤儿导出**（`setUsage`/`cleanOcrTextWithStats`/`toAiToolCalls`）：全仓库零调用，非用户可感知
- **pptx 不内嵌图**：代码注释自认的已知限制（OOXML media 关系复杂），属大功能非 bug
- **Rust `cleanup_temp_files` 未调用**：死命令，不造成崩溃/错误行为

## 验证
- `pnpm build`（`tsc && vite build`）→ **exit 0**，零类型错误
- 改动横跨 11 个文件，纯前端增量；严守铁律：零 Rust 大改、零新依赖、不擅加未请求功能

## 修复文件清单
- `src/features/ai/aiTools.ts`
- `src/features/ai/aiStore.ts`
- `src/features/ai/aiClient.ts`
- `src/features/ai/markdownXlsx.ts`
- `src/features/ai/AIPanel.tsx`
- `src/ai-window/bridge.ts`
- `src/features/screenshot/EnhancedScreenshotApp.tsx`
- `src/features/screenshot/components/AnnotationToolbar.tsx`
- `src/features/screenshot/components/EditorWindow.tsx`
- `src/locales/zh-CN.json`
- `src/locales/en-US.json`
