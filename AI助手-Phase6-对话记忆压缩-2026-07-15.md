# AI 助手 Phase 6 — 长对话记忆压缩（对齐 privdoc-ai Agent Memory）

> 自动化执行轮次：2026-07-15 | 参考项目：`/Users/liwenchao/GithubProSpace/privdoc-ai`

## 需求发掘（本轮）

复盘 Phase 1–5 后确诊：多轮对话（Phase 4）已能让用户「逐步把截图打磨成完整文档/文案」，但存在**上下文窗口溢出**的隐性断点——对话越长，每轮要回传的历史越多，最终超出模型上下文上限，导致后续追问丢失早期要点、成稿质量下降甚至报错。

对照 privdoc-ai，其 `agentStore` / `agent.service.ts` 的核心能力正是 **Agent Memory**：带 `importance` 评分的记忆条目 + `runCacheCleanup`（oversized messages trimmed）。这正是 SnapCraft 多轮对话成熟化所缺的一环。

**结论**：把「对话线程过长 → 自动压缩最早几轮为长期记忆」补上，让截图工具的「截图即文档上下文」模式在长迭代下依然稳健。

## 实现（纯前端，零 Rust 改动，不影响截图/标注/OCR/历史/导出）

- `aiTypes.ts`：新增 `AiMemory { summary, importance(1-5), createdAt, turnsCovered }`，语义对齐 privdoc-ai 的 `MemoryEntry`（含 importance）。
- `aiStore.ts`：
  - 新增 `memories: AiMemory[]`，按截图上下文哈希持久化到 `localStorage: snapcraft-ai-mem:<hash>`（与对话线程同键，切换截图自动恢复/清空）。
  - 阈值：`MAX_LIVE_ENTRIES=16`（实时对话条目数），`COMPACT_ENTRIES=4`（每次压缩最早 2 轮）。
  - `chat()`：在构建消息时把长期记忆作为**最前面的 system 消息**注入（`buildMemoryNote`），延续早期要点。
  - `compactMemory()`（非流式 `chatOnce` 摘要）：把最早 `COMPACT_ENTRIES` 条转录发给模型压缩；解析可选 `IMPORTANCE: N` 前缀得重要性评分（缺省 3）；生成 `AiMemory` 并 splice 掉实时轮次、持久化；**压缩失败静默回退，不阻断主流程**。
  - 自动触发：`chat` 每轮成功后若 `conversation.length > MAX_LIVE_ENTRIES` 即自动压缩。
  - `setConvKey` / `clearConversation` 同步记忆（切换/新建对话不串台）。
- `AIPanel.tsx`：记忆栏 `🧠 长期记忆 · n`（仅 `memories.length>0` 显示），可展开查看每条摘要 + 重要性圆点（`●○○○○`）+ 覆盖轮数；提供「压缩记忆」手动按钮（对话 ≥4 条时可用）。
- i18n（zh-CN + en-US）：`memTitle` / `memCompact` / `memCompactTitle` / `memImp` / `memCov`。
- CSS：`src/index.css` 新增 `.ai-mem*` 一套样式。

## 验证

- `pnpm build`（tsc + vite）exit 0，**134 模块，零类型错误**（与 Phase 5 一致，无回归）。
- `dist` 含全部新 token：CSS `ai-mem*` 9 个类；JS `memTitle/memCompact/memCov/memImp/snapcraft-ai-mem:/compactMemory`。

## 与 privdoc-ai 的差异

- privdoc-ai 的记忆由 Rust + SQLite 持久化、带 `importance` 与 `source`、可 `searchMemories` 检索；本方案前端 `localStorage` 按截图分桶，更轻、零后端风险，且天然把截图哈希作为记忆锚点。
- 本方案暂不实现 `searchMemories`（前端记忆条目少，全量注入即可）；如需可后续加「按当前追问相关性筛选记忆」。

## 生效方式

纯前端改动，`./start.sh dev` 或 `pnpm build && ./start.sh app` 生效。

## 后续候选（未做，按价值排序）

1. **记忆相关性检索**：多截图场景下按当前追问语义筛选要注入哪些记忆（对齐 privdoc-ai `searchMemories`）。
2. **跨截图会话列表**：在面板内列出本机所有对话线程，可回看/切换/删除。
3. **后端代理隐藏 Key**：Rust 侧代理转发，密钥不下发渲染层（用户尚未确认，先保留纯前端）。
4. **.xlsx 导出**：把 AI 生成的表格数据导出为真实 Excel。
5. **拖拽重排选择顺序**：图文报告里手动调整章节顺序。
