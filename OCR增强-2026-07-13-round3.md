# SnapCraft · OCR 取字功能增强（第三轮 · 置信度感知 + 去换行合并）

> 实施时间：2026-07-13 22:27。状态：前端 `pnpm build` 通过（125 模块，exit 0），未改动后端 Rust。
> 本轮在已有结构化 OCR（语言选择 / 逐行结果 / 区域框选 / 贴回标注 / 内联纠错 / 导出 / 结果内搜索 / 切换语言即时重识别）基础上，
> 做**两项纯追加、且全部收敛在 OCR 面板内**的增强，未触碰截图 / 标注 / 滚动 / 历史 / 钉图等其它功能。

## 一、需求攫取与发觉（本轮聚焦的可用性问题）

在"截图 + OCR 取字"的真实使用路径中，前两轮已解决了「能取字、能改、能搜、能导出」。
剩下最扎心的两个体验痛点（来自 OCR 必然的不完美特性）：

| # | 痛点 | 为什么影响可用性 | 本轮方案 |
|---|------|----------------|----------|
| 1 | **OCR 必有漏识/误识**，长文档里几行识别错的字混在正确结果里，用户难以一眼定位该复核哪几行 | 误识行和正确行视觉无差别，复核成本高 | **置信度感知**：低置信行自动高亮（琥珀色左边框）+ 可调阈值滑块，一键隐藏低置信行 |
| 2 | **复制/导出的换行**常不合场景：贴进搜索框、表单、聊天框时，多行 `\n` 会变成怪异的换行或占位 | 用户只能手动删换行，或去编辑器二次处理 | **合并为一行**开关：复制全部 / 导出时去除换行、用空格连接 |

> 注：置信度数据 Apple Vision 已返回（macOS），旧代码只取 `.text` 丢弃了它；本轮回收这份数据。
> Windows WinRT 不返回置信度（记 `0`=未知），故阈值控件在 Windows 下**自动不显示**，符合"不影响其它功能 / 不制造无用 UI"的约束。

## 二、本轮选择实现的需求（按"可用性↑ / 风险↓ / 范围收敛"筛选）

选取 **#1 置信度感知**、**#2 复制去换行合并**。
两者都直接命中 OCR 最高频的"结果质量 / 结果可用性"痛点，且**完全在 OCR 面板内**（新状态 + 新控件 + 新样式），
不引入跨视图、不改存储格式、不碰截图/标注链路，符合「增量增强、不影响其它功能」的硬约束。

之前轮次记录的其余候选，本轮**仍不做**（留后续评估，理由同前）：
- 跨视图快速取字（#6，跨视图渲染成本高）
- OCR 历史持久化（#7，需新增存储 + 命令，触碰历史体系）
- 翻译（#9，超出本地工具定位）
- 多列/竖排阅读顺序校正（#10，Vision 已较准，边际收益低）

## 三、具体改动

### 前端 `src/features/screenshot/EnhancedScreenshotApp.tsx`
- 新增状态 `ocrConf`（置信度阈值 0..100，默认 0=全部）、`ocrMerge`（合并为一行，默认 false）。
  - `runOcr` 重新识别时重置 `ocrConf=0`（与本次结果质量相关）；`ocrMerge` 作为格式偏好保留。
- 新增 `ocrVisibleLines()`：`useCallback` 计算「搜索 + 置信度阈值」双重过滤后的可见行集合，
  并贯穿**复制全部 / 导出文本 / 贴回标注**（所见即所得：你看到的过滤结果就是复制/导出/贴回的内容）。
  - 过滤语义：`置信度<=0`（未知）永远通过；`阈值=0` 不过滤；否则仅保留 `conf*100 >= 阈值` 的行。
- OCR 面板新增：
  - **置信度滑块行**（仅当存在 `confidence>0` 的块，即 macOS Vision 下显示）：`<input type=range 0..100 step=5>` + 当前阈值 `≥N%`；拖动即时过滤逐行列表。
  - **隐藏提示**：当阈值过滤掉若干行时，显示「已隐藏 N 行低置信文字，调低阈值可显示」。
  - **逐行低置信高亮**：`confidence>0 且 <0.7` 的行加 `.low-conf`（琥珀色左边框 + 淡底色），提醒用户复核。
  - **合并为一行开关**：`<label><input type=checkbox> 合并为一行</label>` + 说明 tip；开启后全文预览 / 复制全部 / 导出均用空格连接、去换行。
- `handleExportOcr`：改用 `ocrVisibleLines()` + 尊重 `ocrMerge` 的换行策略。
- `applyOcrAsAnnotations`：仅贴回可见集合（被隐藏的低置信行不进截图）。

### 文案 `src/locales/{zh-CN,en-US}.json`（ocr 组新增 6 键）
- `ocr.confLabel / confAll / confTitle / confHidden / copyMerge / copyMergeTitle`

### 样式 `src/index.css`
- `.ocr-conf-row / .ocr-conf-label / .ocr-conf-range / .ocr-conf-val / .ocr-conf-hint`
- `.ocr-block.low-conf`（inset 左边框 + 淡底色，无布局抖动）
- `.ocr-opts-row / .ocr-merge / .ocr-merge-tip`

## 四、如何验收

```bash
pnpm build                       # 前端 tsc+vite，已通过（125 模块，exit 0）
# 后端本轮无改动；如需运行最新前端，重建 .app：
pnpm tauri build --bundles app  # 或 ./start.sh app（若 .app 不存在会重建并嵌入新 dist）
./start.sh app                  # 打开含最新前端的 SnapCraft.app
```

编辑器内点「取字」（macOS 走 Apple Vision 才有置信度）：
- 逐行列表里**疑似误识的行**左侧出现琥珀色标记（置信度 < 70%）→ 优先复核/手动改。
- 拖动「置信度 ≥」滑块（如设 80%）：低于 80% 的行立即从列表消失；全文预览 / 复制全部 / 导出 / 贴回同步只取可见的高置信行。
- 出现「已隐藏 N 行低置信文字」提示 → 调低滑块即可恢复。
- 勾选「合并为一行」：全文预览变为单行空格连接；点「复制全部」「导出文本」均输出去换行版本，便于贴进搜索框/表单。

> Windows 侧：WinRT 不返回置信度，故「置信度」滑块**不显示**（无多余 UI），「合并为一行」开关仍可用。

## 五、回归确认

- 截图 / 区域 / 窗口 / 滚动长截图 / 钉图 / 历史 / 文字标注 / 马赛克：均未改动，行为不变。
- 区域框选 OCR（`ocrRegionMode`）、内联纠错、导出、结果内搜索、切换语言即时重识别：链路未动，新增过滤只作用于"可见集合"的呈现与复制/导出/贴回。
- 后端 `ocr_image` / `OcrResult` / `OcrBlock` 签名与行为零变化（本轮纯前端）。
- 新增状态均随重新识别安全重置（除 `ocrMerge` 作为格式偏好保留），不会跨结果串味。
