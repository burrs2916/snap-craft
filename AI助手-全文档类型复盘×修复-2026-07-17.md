# AI 助手 · 全部文档类型复盘 × 修复（2026-07-17 第五轮）

> 范围已从第一个 `doc` 标签扩展到 **全部 14 个文档/文案预设**：`doc / copy / summary / tutorial / bullet / translate / free / bug / prd / compete / meeting / insight / report / table`。
> 共享管线（表格/截图/元信息/富文本复制/打开/二次编辑）前几轮已修，本轮回溯各预设**特有**缺陷，并发现两个跨文档类型的真实 bug。

## 一、发现的真实缺陷（已修复）

### 缺陷 1（跨文档类）· GFM 任务清单 `- [ ]` / `- [x]` 在 4 个渲染器全不被识别
- **现象**：`prd` 预设明确要求模型输出「可勾选清单 `- [ ]`」（验收标准），`bug`/`meeting` 也常产出行动项清单。但 **AiMarkdown 预览 / HTML 导出 / DOCX 导出 / PPTX 导出 四个渲染器无一支持任务清单**——清单退化为带字面 `[ ]` 的普通项目符号。典型「所见≠所得」。
- **根因**：四个渲染器的列表解析只匹配 `[-*+] 文本`，未识别 `[-*+] \[[ xX]\]` 任务标记。
- **修复**（纯前端、0 依赖、4 渲染器一致）：
  - `aiMarkdown.tsx`：任务项渲染为 `<input type=checkbox disabled>` + 文本。
  - `markdownHtml.ts`（HTML/PDF + 富文本复制）：`<li class="md-task"><input type=checkbox disabled></li>`，5 套主题补 `.md-task` 样式。
  - `markdownDocx.ts`：用 ☑/☐ 字形呈现勾选态（Word 全版本兼容，无需内容控件）。
  - `markdownPptx.ts`：用 ☑/☐ 字形。
  - `index.css`：内联预览补 `.md-task` 主题化样式。

### 缺陷 2（跨文档类）· PPTX 导出把表格渲染成裸 `|` 管道文本
- **现象**：`table`（提取表格）、`compete`（维度对比表）、`meeting`（行动项表）等预设产出的 GFM 表格，导出 PPTX 时显示成原始 `| 列 | 列 |` 管道符号——而 DOCX / HTML / 预览都渲染成真表格。唯一漏网的渲染器。
- **根因**：OOXML 中表格必须是 `<p:graphicFrame>` 内的 `<a:tbl>`，不能塞进文本框 `<txBody>`；原 `buildSlideXml` 把整段正文塞进单个 `<p:sp>` 文本框，遇到 `|` 行直接降级成等宽文本。
- **修复**（需重构幻灯片布局，纯前端 0 新依赖）：
  1. 新增 `parseBodyBlocks()`：把正文拆成「文本块 / 表格块」。
  2. `buildSlideXml` 改为按块自上而下堆叠：文本块 → `<p:sp>` 文本框；表格块 → 独立 `<p:graphicFrame>` 内嵌 `<p:graphic><a:graphicData><a:tbl>`，含单元格边框 + 表头靛蓝底色 + 列对齐（`:---`/`:---:`/`---:`）。
  3. 修正了 XML 两处非法结构：`<a:graphic>` → 正确包装 `<p:graphic>`；`<a:rPr>` 内误嵌 `<a:t>` → 改为 `<a:rPr/>` 闭合后再放 `<a:t>`。

### 缺陷 3（预存在空实现 · 本轮暴露并修复）· 无图纯文档导出 PPT 正文被整体清空
- **现象**：纯文本/纯表格文档（不带截图）导出 PPTX 时，幻灯片**只剩标题、正文全空**。
- **根因**：SNAP 标记处理循环里 `slide.body = newBody` 把 `newBody` 初始为 `[]`，而「无标记且无非报告截图」时两个分支都不执行 → `newBody` 保持空数组 → 正文被覆盖清空。此前只在「带图的报告」场景验证过，掩盖了该 bug。
- **修复**：循环默认保留 `newBody = slide.body`，仅在 SNAP 分支剥离标记行、在兜底分支前置截图时才改写。

## 二、专项核对（确认无问题 / 前轮已修）
- **`report` 图文报告（多图 SNAP 流程）**：`orderedImages()` 当前图优先 → SNAP:1，附加图按 `selectedOrder` 顺序 → SNAP:2/3…；`parseInt(marker)-1` 索引对齐正确（前轮 off-by-one 修复生效）。本轮新增表格块后，SNAP 图片与表格块可共存（已用含图+表+清单的样例验证）。
- **`table` 纯表格文档**：现 PPTX 渲染为真表格；DOCX/HTML/预览早已支持。
- **`doc`/`copy`/`summary`/`tutorial`/`bullet`/`translate`/`free`/`bug`/`prd`/`compete`/`meeting`/`insight`**：共享管线（截图内嵌、元信息、复制富文本、打开、二次编辑）均生效，本轮回溯的清单/表格缺陷已补齐。

## 三、验证方式（真实代码 + 产物校验，非臆测）
1. 用 esbuild 转译 `markdownPptx.ts` 真实源码 → 在 Node 调用 `markdownToPptx` 生成 PPTX。
2. 解压后用 Python `xml.dom.minidom` 解析每张 `slideN.xml` 确认 **XML 良构**（无标签失配）。
3. 断言：含表格样例 → `slide1` 出现 `<a:tbl>` + `<p:graphicFrame>`；含清单样例 → 出现 ☑/☐；含 `<!--SNAP:1-->` + 截图 → `slide1` 出现 `<p:pic>` 且 `ppt/media/image1.png` 正确产出。
4. 全工程 `pnpm build`（tsc + vite）绿，0 错 0 警；0 Rust、0 新依赖。

## 四、剩余待办（未做，待拍板）
- **④ `doc`/`prd` 等单图预设的图文穿插锚点**：导出链路已支持 `<!--SNAP:k-->`，缺口在「预设 prompt 引导模型在文中埋锚点」属模型行为层；`report` 多图穿插已完整可用。
- 可选增强：列表嵌套、删除线任务态等更细的 GFM 覆盖（当前优先级低）。

## 五、交付物
- 本报告：`AI助手-全文档类型复盘×修复-2026-07-17.md`
- 改动文件：`aiMarkdown.tsx`、`markdownHtml.ts`、`markdownDocx.ts`、`markdownPptx.ts`、`index.css`（全部纯前端）。
