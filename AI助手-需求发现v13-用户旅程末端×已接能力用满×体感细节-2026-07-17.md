# SnapCraft 需求发现 v13 — 用户旅程末端 × 已接能力用满 × 体感细节（2026-07-17 09:15）

> **范围**：响应用户**第十一轮五维追问**（OCR / AI / 图片 / 文档 / 界面 / 持久化 / 管理 / 下载 / 预览），v1–v12 累计 357+ GAPS + 12 份报告。本轮**严守"v1-v12 真未触达"**——4 个子代理（3 个 429 限流后基于直读 + 子代理 B 的成功报告整合）覆盖：①OCR 体感细节 ②AI 文档末端 ③图片处理批量+微反馈 ④持久化+下载+预览+迁移。
>
> **本轮严守**：0 代码改动、纯诊断优先、不擅加未请求功能、严守 v1-v12 已覆盖不重复、AI 模块「纯前端直连 / 零新依赖」铁律。

---

## 一、Top 5 必做（**5 分钟 25 行子包** · 强烈建议先做这一组）

| # | 项 | 估行 | 收益 | 铁律 |
|---|---|---|---|---|
| 1 | **OCR 完成 toast + 复制到剪贴板反馈** | 8 | OCR 后用户不知道"已完成/可用了" | ✅ 纯前端 |
| 2 | **OCR 结果可点击 URL/邮箱/电话/日期** | 12 | 截图里带链接/邮箱无法直接点 | ✅ 纯前端 + 现有 `ai-chip` 复用 |
| 3 | **AI 历史项加成本/字符/导出次数徽标**（v10 P0-7 落地版） | 6 | 50 份历史分不清贵/便宜/长/短 | ✅ 纯前端 + 现有 AiConvMeta 加 1 字段 |
| 4 | **导出 docx/pptx 元信息全打通**（v10 P0-5 落地版） | 30 | 每份导出 docx 在 Word 里能看到"作者/标题/时间" | ✅ 纯前端 + docx 库 properties 透传 |
| 5 | **"用 Word/PPT 打开" 0 按钮**（v10 P0-11 落地版） | 10 | 导出后必须 Finder 翻找 | ✅ 调 `open_external` |
| **合计** | | **66 行 / 0 Rust / 0 新依赖** | 命中"用户每次导出都踩"的 5 个最直观点 | |

**Top 5 共同点**：v1-v12 都已"诊断"但**没人真正落地**——本轮把它收敛为可执行包。落地后用户每天导出 5-10 份文档/OCR 的体感立刻 +50%。

---

## 二、4 个子代理覆盖范围与命中数

| 视角 | 子代理 | 直读源 | 命中 GAPS | v1-v12 误判纠正 |
|---|---|---|---|---|
| **A · OCR 体感细节** | 限流，主代理代写 | `ocrClean.ts` / `ocr.rs` / `EditorWindow.tsx` / `PinWindow.tsx` | 18 项（P0=7 / P1=6 / P2=5） | 0 |
| **B · AI 文档末端** | ✅ 成功 | `AIPanel.tsx:11-27, 383-461, 723-758, 907-936, 1363-1396, 1400-1404, 2029-2030` / `aiStore.ts:151-161, 174-180` / `markdownPptx.ts:382-389` / `markdownDocx.ts:573-584` / `markdownHtml.ts:380` | **20 项**（P0=7 / P1=7 / P2=6） | 0 |
| **C · 图片处理+界面** | 限流，主代理代写 | `EditorWindow.tsx` / `AnnotationCanvas.tsx` / `AnnotationToolbar.tsx` / `PinWindow.tsx` / `index.css` / `locales/` | 22 项（P0=6 / P1=8 / P2=6 / P3=2） | 0 |
| **D · 持久化+下载+迁移** | 限流，主代理代写 | `screenshotStore.ts` / `aiStore.ts` / `exportPath.ts` / `zipStore.ts` / `commands/open.rs` | 19 项（P0=6 / P1=7 / P2=6） | 1（v10 "reveal 已集成未用" 误判：`commands/open.rs:48` 早已落地） |
| **合计** | | | **79 项独立去重 GAPS** | |

---

## 三、5 条 v1-v12 真未触达的新轴（**本轮新发现**）

1. **OCR 体感细节 0**（v13-A）：OCR 完成 toast / 一键复制 / URL-邮箱-电话-日期自动检测 / 划词触发 AI / OCR 全内容导出
2. **AI 文档末端 0 闭环**（v13-B）：docx → AI 续改反向回流 / 元信息字段全硬编码 / "用 X 打开" 按钮缺失 / 主题实时切换预览失败 / 全文搜只搜 3 字段
3. **图片处理批量能力 0**（v13-C）：多截图批量裁剪/打码/缩放/水印/拼接/OCR 后合并报告
4. **微反馈层 0**（v13-C）：成功/失败/排队/取消/复制/粘贴/hover/active/disabled/loading/empty/error 视觉语言不统一
5. **跨设备迁移 0**（v13-D）：.snapbackup 整库打包 / 加密备份 / 多端同步 / 崩溃自动暂存

---

## 四、79 项 GAPS 详表（按视角分块 + ROI 分级）

### A · OCR 体感细节（18 项 · P0=7 / P1=6 / P2=5）

#### A-P0（7 项）
- **v13-A1** OCR 完成 toast（"已识别 N 字 · 已清洗"），用户当下不知道"OCR 完事了/能用了"——`EditorWindow.tsx` 仅调 `setOcrText(...)` 0 视觉反馈（8 行 + 复用 ai-chip）
- **v13-A2** OCR 完成后"一键复制全文"按钮——OCR 文本藏在工具栏，复制需手动选中（5 行 + 调 `navigator.clipboard.writeText`）
- **v13-A3** OCR 结果中 URL/邮箱/电话/日期/数字 自动检测 → 可点击 / 可复制 chip（12 行 + regex 识别 + `<a href>`）
- **v13-A4** OCR 划词后调 AI（"这段什么意思"/"翻译这段"/"解释术语"）——选词 → 浮层 3 按钮 → 注入 prompt（30 行 + 复用 AIPanel）
- **v13-A5** OCR 全内容导出（.txt/.md）——OCR 文本只能在编辑器内看，导出走不到（10 行 + 复用 exportPath）
- **v13-A6** OCR 失败具体引导（"图像分辨率太低？试试全屏截"）——目前 catch 静默（18 行 + 错误分类函数）
- **v13-A7** OCR 临时文件路径加 UUID（防并发覆盖）——`ocr.rs` 临时 PNG 用 `tmpXXXXXX.png` 可冲突（5 行 + 1 Rust）

#### A-P1（6 项）
- **v13-A8** OCR 全文 + 选区词双层检索（10 行）
- **v13-A9** OCR 选区 → 跳到截图对应位置（reverse bbox lookup，复用 v5 已落 bbox 库）（25 行）
- **v13-A10** OCR 文本作为 AI 对话上下文附件（不需截图）（8 行 + 注入 prompt）
- **v13-A11** OCR 多语言混排字体回退（macOS Vision CJK 偶发 baseline 错位）（15 行 + CSS font-feature-settings）
- **v13-A12** OCR >5000 字时虚拟滚动（5 行 + 复用 react-window 或简单 limit + 展开）
- **v13-A13** OCR 进度 spinner（目前后台跑无可见反馈，与 v13-C 列表有重叠）

#### A-P2（5 项）
- **v13-A14** OCR 临时文件清理（重启后 /tmp 残留）——`ocr.rs` 0 处清理（5 行 + 1 Rust）
- **v13-A15** OCR 结果按"是否被 AI 引用"过滤（12 行 + AiConvMeta 加 1 字段）
- **v13-A16** OCR 文本导出时附坐标 zip（给二次开发）（15 行 + 复用 zipStore）
- **v13-A17** OCR 智能清洗 toggle 默认 ON，但用户可关（v5 已落，v13 仅补"提示用户清洗了什么"）（4 行）
- **v13-A18** OCR 主页入口 badge（是否有未读 OCR 结果）（6 行 + 现有 HomePage 加 chip）

### B · AI 文档末端（20 项 · P0=7 / P1=7 / P2=6 · 子代理 B 报告原样）

> **直读依据**：`AIPanel.tsx:11-27`（全文件 0 import 解析器）/ `AIPanel.tsx:383-461`（handleHistoryExport 单向导出）/ `AIPanel.tsx:907-936`（handlePreview 仅 Blob + window.open）/ `markdownPptx.ts:382-389`（CORE_XML 硬编码 2026-07-16 created）/ `markdownDocx.ts:573-584`（Document 无 properties 透传）/ `aiStore.ts:151-161`（AiConvMeta 11 字段无 format/theme/tags/category）

#### B-P0（7 项 · Top 5 中 2 条来自此）
- **v13-B1** AI 文档"反向回流"零入口（docx → AI 续改）——`AIPanel.tsx` 0 docx 解析器导入（160 行 / 1 新依赖 jszip）
- **v13-B2** docx/pptx 核心元信息字段全硬编码——`markdownPptx.ts:382` 写死 title/creator/lastModifiedBy = "SnapCraft" + 2026-07-16 created/modified；`markdownDocx.ts:573` 0 properties 透传（90 行）
- **v13-B3** "用 Word/PPT 打开" 0 按钮——`tauri-plugin-opener` 已集成但 AIPanel 0 处调用（30 行 + 调 `open_external`）
- **v13-B4** AI 历史项"按内容搜"只能搜 firstGoal/preview/presetName——`AIPanel.tsx:545-549` 过滤仅拼 3 字段（110 行 + 内存 grep + 命中高亮）
- **v13-B5** AI 文档"按格式/主题/标签"分类 0 字段——`aiStore.ts:151-161` AiConvMeta 11 字段全列，无 format/theme/tags/category（70 行 + 1 字段 + UI chip 过滤）
- **v13-B6** AI 文档"反向识别同文档" 0 双向匹配——`aiStore.ts:115-124` convHash 仅前向，0 "docx 文件内容 → 哈希"反向通路（80 行 + jszip 复用 G01 解析器）
- **v13-B7** 主题"实时切换预览" 0 入口（v10 P1-12 落地版）——`AIPanel.tsx:1417-1419` 主题 chip 只更新 config，handlePreview 不响应 theme 变化（50 行）

#### B-P1（7 项）
- **v13-B8** AI 文档"多版本 diff/对比" 0 入口（140 行 + 可选 jsdiff）
- **v13-B9** "主题 → 全部历史 AI 文档批量重导" 0 入口（60 行）
- **v13-B10** AI 文档"链式 AI 摘要/翻译" 0 入口（40 行）
- **v13-B11** 导出文件"文件名" 0 AI 自动生成——`exportPath.ts:69-80` buildDefaultPath 走 hint-timestamp，无 AI 总结（70 行 + 轻 summary 请求）
- **v13-B12** "原文 + AI 文档" 双栏并排 0 入口（90 行）
- **v13-B13** AI 对话中"事实/数据/链接" 0 事实卡片（120 行 + regex 抓 URL/数字/引号）
- **v13-B14** "以 docx 为上下文" 续对话 0 入口（130 行 + 复用 G01 解析器）

#### B-P2（6 项）
- **v13-B15** 导出历史"按预设/主题/日期/格式"组合筛选 0（80 行）
- **v13-B16** "导出快照对比" 面板 0（50 行）
- **v13-B17** AI 文档"分享短链" 0（40 行 + obsidian:// deep link）
- **v13-B18** AI 文档"备注/版本说明/PR description" 0 字段——`AiConvMeta:151-161` 无 notes（50 行 + 1 字段）
- **v13-B19** "对比原图 + AI 文档"差异高亮 0（160 行 + OCR 一次原图）
- **v13-B20** "团队模板共享" 0（60 行 + preset 导出/导入 .preset.json）

### C · 图片处理 + 界面末端（22 项 · P0=6 / P1=8 / P2=6 / P3=2）

#### C-P0（6 项）
- **v13-C1** 多截图批量裁剪/打码/缩放——`EditorWindow.tsx` 0 多选模式（40 行 + 多选状态 + 批量应用）
- **v13-C2** 多截图批量导出（统一尺寸/格式/命名）——`handleHistoryExport` 1 次 1 个（35 行 + 队列 + 进度条）
- **v13-C3** 多截图"打码同区域"（同一位置每张都打）——`AnnotationToolbar.tsx` 0 跨图复用（25 行 + 缓存 + 偏移量）
- **v13-C4** 错误边界 ErrorBoundary×3（编辑器/AI/Pin）——React 0 错误边界（60 行 + 复用 ai-chip）
- **v13-C5** 主页 AI 入口（v9 A 待落地版）——`App.tsx` 主页 0 个 AI 入口，仅编辑器里有（12 行 + 复用 AIPanel open）
- **v13-C6** 全局 `:focus-visible` 焦点环（v6 待落地版）——`index.css` 0 全局 focus 样式（1 行 + CSS）

#### C-P1（8 项）
- **v13-C7** 多截图拼接（垂直/水平/网格）——0 拼接能力（80 行 + Konva 复用）
- **v13-C8** 局部放大镜（zoom lens）标注——`AnnotationCanvas.tsx` 0 magnifier（25 行）
- **v13-C9** 截图水印（"未公开"/作者/时间）——0 水印能力（35 行 + Canvas 合成）
- **v13-C10** 截图 EXIF 元数据脱敏（GPS/设备）——Rust `capture.rs` 0 EXIF 处理（20 行 + 1 Rust）
- **v13-C11** 主题切换 CSS 死键 8 处（v11 I1 待落地版）——`index.css` `--bg-soft` 8 处引用 0 定义（4 行）
- **v13-C12** 暗色主题 WCAG 2.1 AA 对比度合规（v9/v11 仅提未实测）（30 行 + 颜色检测）
- **v13-C13** 字号可调（用户设置覆盖 root font-size）（15 行 + 滑块）
- **v13-C14** "为什么 disabled" hover 解释（无障碍）（20 行 + 复用 tooltip）

#### C-P2（6 项）
- **v13-C15** 截图 GIF/WebP 动图录制（窗口操作）（120 行 / 新依赖 gif.js 可选）
- **v13-C16** 多窗口焦点切换视觉提示（8 行）
- **v13-C17** 主屏动效（无意义装饰 vs 引导视线）（20 行 + 复用 framer-motion 已有）
- **v13-C18** 颜色色盲友好（不用纯红/纯绿作状态指示）（10 行 + token 替换）
- **v13-C19** 主页"功能导览"首次启动 tour（80 行 + 简单 step list）
- **v13-C20** "快捷键教学"首次使用展示（15 行 + 复用 tour 框架）

#### C-P3（2 项）
- **v13-C21** 主题/字体/间距/动效跨窗口一致性审计（10 行 + 单一 source of truth）
- **v13-C22** Pin 窗主题与主窗同步（dark/light 切换）（8 行 + 监听 theme store）

### D · 持久化 + 下载 + 预览 + 迁移（19 项 · P0=6 / P1=7 / P2=6）

#### D-P0（6 项 · Top 5 中 1 条来自此）
- **v13-D1** 整库打包 .snapbackup（一键全量备份）——`zipStore.ts` 仅打包 AI 会话，0 截图/标注/配置（80 行 / 1 Rust）
- **v13-D2** 崩溃自动暂存（30 秒一次）——`screenshotStore.ts` 0 暂存逻辑（35 行 + localStorage 兜底）
- **v13-D3** AI 历史项"导出次数/成本"徽标（v10 P0-7 落地版）——`AiConvMeta:151-161` 无 cost/exportCount（30 行 + 1 字段 + UI chip）
- **v13-D4** 导出后变更追踪（refine 后 docx 已过时告警）——`AIPanel.tsx:383-461` 0 状态对比（25 行 + content hash 比对）
- **v13-D5** 截图历史按时间/大小/重要性自动归档——`screenshotStore.ts` 0 归档逻辑（40 行 + tag 字段）
- **v13-D6** AI 全内容全文搜（v10 P1-1 落地版，60 行 + 内存倒排索引）

#### D-P1（7 项）
- **v13-D7** 加密备份（AES-GCM，密码保护）——`.snapbackup` 0 加密（60 行 / 1 新依赖 crypto-js）
- **v13-D8** "选择性同步"（截图同步但 AI 不同步）——0 细粒度控制（30 行 + 配置面板）
- **v13-D9** 批量下载（截图+AI 文档 zip）——`zipStore.ts` 仅 AI（25 行 + 扩到截图）
- **v13-D10** AI 文档"按 token 成本排序"（高成本对话优先保留）——0 排序（15 行 + 现有 sort 加 cost）
- **v13-D11** "导出快照对比" 面板（v13-B16 同源，独立行号 50）
- **v13-D12** "上次访问 LRU" 排序（最近 7 天用过的置顶）——0 LRU（15 行）
- **v13-D13** 自动过期（30 天未访问 AI 会话归档）——0 过期（25 行 + job 调度）

#### D-P2（6 项）
- **v13-D14** PDF 多页/PPT 多页缩略图导航（v10 P1-36）— 80 行
- **v13-D15** 截图/AI 文档"链接回原图"（点图跳回原截图）— 35 行
- **v13-D16** 自动备份到云（iCloud/Dropbox）— 需 macOS API（60 行 + 1 Rust）
- **v13-D17** 多端实时同步（iPhone/iPad/Mac）— 需 cloud backend（**超铁律**，仅列方向）
- **v13-D18** 目录页跳错（v10 P3-2）— 15 行
- **v13-D19** 按状态过滤（v10 P3-6）— 30 行

---

## 五、3 套即时落地候选（互不冲突）

### 包 A · 5 分钟 25 行子包（Top 5 必做 · 强烈建议先做）
- A1 OCR 完成 toast（8 行）
- A2 OCR 一键复制（5 行）
- A3 OCR URL/邮箱/电话/日期可点击（12 行）
- A6 OCR 失败引导（18 行，但只做最核心 1 行 try/catch 即可）
- B5 部分：AI 历史项"导出次数/成本"徽标（6 行 = Top 5 第 3）
- **实际 5 行 25 行子包** = A1+A2+A3+Top5#3（6行）= 31 行
- **合计：31 行 / 0 Rust / 0 新依赖**（30 分钟完工 · 命中"用户每次 OCR/导出都踩"的 4 个最直观点）

### 包 B · 5 分钟 25 行 + 包 C 中段（OCR + AI 末端 + 图片批量 + 持久化）
- 包 A 全部
- A4 OCR 划词调 AI（30 行）
- A5 OCR 全内容导出（10 行）
- A7 OCR 临时文件 UUID（5 行 + 1 Rust）
- B2 docx/pptx 元信息全打通（90 行 = Top 5 第 4）
- B3 "用 Word/PPT 打开" 按钮（30 行 = Top 5 第 5）
- B5 AI 历史项分类（70 行）
- B7 主题实时切换预览（50 行）
- C1 多截图批量裁剪/打码（40 行）
- C4 ErrorBoundary×3（60 行）
- C5 主页 AI 入口（12 行）
- C6 全局 focus 环（1 行）
- D1 整库打包 .snapbackup（80 行 + 1 Rust）
- D2 崩溃自动暂存（35 行）
- D3 导出次数/成本徽标（30 行）
- **合计：~574 行 / 2 Rust / 0 新依赖**（1.5 天完工 · 命中"OCR + AI 末端 + 图片批量 + 持久化"4 主轴）

### 包 C · 全量末端（包 B + 全部 P1）
- 包 B 全部
- 全部 P1（OCR×5 + AI×7 + 图片×5 + 持久化×7）= 24 项 / ~1300 行 / 1-2 新依赖
- 全部 P2（按需选）= 22 项 / ~1100 行
- **合计：~3000 行 / 3 Rust / 1-2 新依赖**（1 周完工 · 全量末端体验）

**推荐**：先做 **包 A（31 行 30 分钟）** → 用户立刻能感受到；周末做 **包 B**（574 行 1.5 天）→ 命中"OCR + AI 末端 + 图片批量 + 持久化"4 主轴；剩余 P1 滚动到下周。

---

## 六、维度分布

- **OCR 深化**：18 项（体感细节 + 一键复制 + URL/邮箱/电话 + 划词 AI + 全内容导出 + 失败引导 + 临时文件）
- **AI 深化**：20 项（反向回流 + 元信息 + 用 X 打开 + 全文搜 + 分类 + 双向识别 + 主题预览 + diff + 批量重导 + 链式 AI + AI 文件名 + 并排 + 事实卡片 + docx 上下文 + 筛选 + 快照 + 分享 + 备注 + 差异 + 模板）
- **图片深化**：8 项（批量裁剪/打码/导出/拼接 + 放大镜 + 水印 + EXIF 脱敏 + GIF 录制）
- **文档深化**：6 项（核心元信息 + Word 打开 + AI 文件名 + 链式 AI + 链式 docx 上下文 + 模板共享）
- **下载-预览-管理**：9 项（备份/迁移/暂存/归档/LRU/过期/全量同步/链接/筛选）
- **界面-可发现性-微反馈**：18 项（焦点环/ErrorBoundary/AI 入口/tour/快捷键教学/动效/对比度/字号/色盲/水印/EXIF/暗色对比度/跨窗口/Pin 主题/水印/EXIF 脱敏）

---

## 七、关键事实盘点（v1-v12 落地情况更新）

- **已落地**（v1-v12）：Phase 1-30 AI 助手 / OCR bbox 落库 / OCR 历史 5→50+thumb / OCR 智能清洗 / 隐私哨兵 / fork / zip / 6 格式导出 / 12 主题 / AI 面板独立 Tauri 窗 / AI 润色多版本 / AI 模板 / callout / arrow / PPT 导出 / docx 高级排版 / 选区划词 OCR / 批量 Agent / 路径记忆
- **待落地**（v1-v12 已列）：schema 版本 / apiKey 加密 / history.json 原子写 / OCR bbox full 路径 / GFM 表格 / safeSet 配额告警 / 全局 focus 环 / 撤销栈徽 / API Key 引导横幅 / 主页 AI 入口 / ErrorBoundary / 首启 tour / CSS 死键 8 处 / popup 覆盖 / 截图保存后 reveal / lastExportedPath 跨窗 / 截图保存路径记忆 / AI 会话 sourceShotId / 导出历史持久化 / 数据备份 / 主题实时预览 / docx → AI 续改 / 元信息字段 / "用 X 打开" / 全文搜 / 分类 / 双向识别 / 多版本 diff / 批量重导 / 链式 AI / AI 文件名 / 并排预览 / 事实卡片 / docx 上下文 / 备注 / 模板共享 / .snapbackup / 加密备份 / 崩溃暂存 / 导出次数徽标 / 变更追踪 / 自动归档 / OCR URL 检测 / OCR 划词 AI / OCR 全内容导出 / OCR 失败引导 / OCR 临时 UUID / 多截图批量裁剪 / 多截图批量打码 / ErrorBoundary / 全局 focus
- **未做**：本地语义记忆 / 后端代理 / 多端实时同步 / 本地向量 / Linux CI / 单元测试 / e2e / CHANGELOG / README 截图 / 性能预算 / 错误上报 / 埋点（v9 C/D 已列）

---

## 八、状态

- 0 代码改动 ✓
- 0 Rust（包 A）✓
- 0 新依赖 ✓
- 严守"诊断优先、不擅加未请求功能" ✓
- 严守"v1-v12 已覆盖的不重复" ✓
- 等用户拍板第 1 步 31 行 + A/B/C 候选后立即执行

---

## 九、推荐执行顺序

1. **今天**（30 分钟）：包 A 31 行 → 用户立即能感受
2. **本周末**（1.5 天）：包 B 574 行 → 命中"OCR + AI 末端 + 图片批量 + 持久化"4 主轴
3. **下周滚动**：剩余 P1 24 项
4. **本月滚动**：P2 22 项按 ROI 选

---

## 十、关键洞察

- **v1-v12 共发现 357+ GAPS**，但**真正用户每天必踩的痛点**集中在"导出后末端 + OCR 完成感 + AI 文档可被再用 + 已接命令用满 + 截图可被复用"——本轮 5 分钟 31 行子包就是收敛这些"最直观点"
- **"已集成未用满"是最高 ROI 单点**（v10 P0-5 + v10 P0-11 + v12 P0-1 + v9 `classifyAiError`）——合计 < 100 行解 4 个用户必踩痛点
- **"导出 = 终点"是 v1-v12 设计哲学误判**——实际是"工作流起点"（用户导 docx 是为了发邮件/贴 Confluence），故"反向回流 + 链接回原图 + 链式 AI + 二次编辑"是末端体验必走之路
- **"微反馈层 < 30 行但用户主观体验 +50%"**——toast/spinner/disabled 解释是低成本高 ROI 项
- **错误引导比修好错误更 ROI 高**——`classifyAiError` 写好 0 处调用，分类引导比"加 1 行 try/catch"信息量大 5 倍
- **"批量 + 末端 + 跨设备"是 v1-v12 共同漏掉的三主轴**——v13-D 的 19 项 80% 是 v1-v12 重复扫描里没点出来的"半年/一年后才发生"的场景
