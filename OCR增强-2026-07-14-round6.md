# SnapCraft · OCR 取字功能增强（第六轮 · 面板效率与结构化导出）

> 实施时间：2026-07-14。状态：前端 `pnpm build` 通过（125 模块，exit 0），未改动后端 Rust，未触碰截图/标注/滚动/钉图/历史等其它功能。
> 本轮在已有 OCR（语言选择 / 逐行结果 / 区域框选 / 贴回标注 / 内联纠错 / 导出 / 搜索 / 即时重识 / 置信度 / 合并为一行 / 偏好持久化 / 自动复制 / 耗时统计 / 本地历史 / 阅读顺序校正）基础上，
> 做**三项纯前端、且全部收敛在 OCR 面板内 + 复用既有命令**的增强：**面板内快捷键**、**重新识别**、**带坐标的结构化导出（JSON / TSV）**。

## 一、需求的攫取与发觉（本轮聚焦：面板操作效率 & 结果可再利用）

前 5 轮已把「能取字、能改、能搜、能导出、能按质量/版式过滤、能留存复用」做齐。从「把识别结果真正用起来」的路径继续看仍存在的可用性断点：

| # | 痛点 | 为什么影响可用性 | 本轮方案 | 状态 |
|---|------|----------------|----------|------|
| 1 | **面板内无法重跑**：切换语言后（macOS 不重跑）、或想换一张重识，必须关面板→再点工具栏「取字」 | 高频重试路径被拆成两步，打断心流 | **重新识别按钮**：面板内一键用同一张图重跑（保持当前语言） | ✅ 已实现 |
| 2 | **导出只有纯文本/MD**：想拿坐标喂表格/数据库/脚本，位置信息全丢 | 下游处理（对齐、复核、结构化入库）无法做 | **带坐标导出**：新增 JSON / TSV（含 x,y,w,h,confidence），复用 `save_text_file` | ✅ 已实现 |
| 3 | **面板全靠鼠标**：关面板、聚焦搜索都要点 | 键盘党每次多一次鼠标移动 | **面板快捷键**：`Esc` 关面板、`Cmd/Ctrl+F` 聚焦搜索 | ✅ 已实现 |
| 4 | 小字截图识别后文字太细看不清 | 需凑近看，体验差 | 面板内字号 A-/A+（CSS 变量） | ⏸ 留评估（CSS 耦合风险，本轮先不做以控回归） |
| 5 | 长文搜索命中不高亮 | 找到却看不到在哪 | 逐行/全文命中高亮 | ⏸ 留评估（textarea/input 内高亮实现成本高） |
| 6 | 文字↔图块联动：悬停某行在画布高亮其包围盒 | 最强但需跨组件（AnnotationCanvas） | 跨视图高亮 | ⏸ 留评估（超出「不影响其它功能」边界，本轮不做） |

> 上述 3 项改动**不引入跨视图渲染、不改后端命令、不碰截图历史体系、不碰标注/画布**，完全符合「增量增强、不影响其它功能」的硬约束。
> 之前轮次记录的其余候选（翻译、跨视图快速取字、联动高亮）仍因成本/超出本地工具定位/跨组件风险**本轮不做**。

## 二、本轮实现的需求

选取 **#1 重新识别 + #2 带坐标导出 + #3 面板快捷键**。三者都命中「结果更好用、操作更顺」的最高频痛点，且改动全部收敛在 OCR 面板 + 复用既有 `ocr_image` / `save_text_file` 命令，零后端改动。

## 三、具体改动

### 前端 `src/features/screenshot/EnhancedScreenshotApp.tsx`
- **类型**：新增 `OcrExportFmt = 'txt' | 'md' | 'json' | 'tsv'`（与既有 `OcrLayout` 并列）。
- **状态**：
  - `ocrExportFmt`（默认 `'txt'`，与历史版本一致，零回归）；
  - `ocrLastImage`（最近一次识别所用 dataUrl，供重跑复用）；
  - `ocrSearchRef`（搜索框 ref，供快捷键聚焦）。
- **`runOcr`**：进入即 `setOcrLastImage(imageData)`，记录重跑所需源图。
- **`buildOcrExportContent(fmt)`**（组件内纯函数）：按「搜索+置信度+阅读顺序」可见集合组装——
  - `txt`：文字，`合并为一行` 时以空格连接（历史行为）；
  - `md`：`# SnapCraft OCR` 标题 + 正文；
  - `json`：数组 `[{text,x,y,w,h,confidence}]`（坐标 5 位、置信度 3 位，**归一化、原点左上**，与画布一致；Windows 置信度记 0 如实保留）；
  - `tsv`：表头 `text\tx\ty\tw\th\tconfidence` + 行（文字内 `\t`/`\n` 已转义为空格，避免破坏列）。
- **`handleExportOcr`**：按 `ocrExportFmt` 选扩展名与对话框过滤器（`.txt/.md/.json/.tsv`），其余沿用既有 `save` 对话框 + `save_text_file` 落盘逻辑。
- **面板 JSX**：
  - 选项行 `ocr-opts-row` 新增「导出格式」`<select>`（文本/Markdown/JSON（含坐标）/TSV（含坐标））；
  - 操作行 `ocr-panel-actions` 在「作为文字标注贴回」后新增**「重新识别」**按钮（重跑 `ocrLastImage`，`ocrBusy` 或无可重跑图时禁用，重跑中显示「识别中…」）；
  - 搜索框加 `ref={ocrSearchRef}`。
- **快捷键 `useEffect`**：仅当 `ocrResult !== null` 时挂 `window` keydown——
  - `Esc` → 关面板；
  - `Cmd/Ctrl+F` → 阻止默认并聚焦+全选搜索框。
  依赖 `[ocrResult]`，面板关闭即卸载监听，不干扰其它视图。

### 文案 `src/locales/{zh-CN,en-US}.json`（ocr 组新增 8 键）
`rerun` / `rerunTitle` / `exportFmt` / `exportFmtTitle` / `fmtTxt` / `fmtMd` / `fmtJson` / `fmtTsv`。

### 样式 `src/index.css`
- `.ocr-fmt` / `.ocr-fmt-select`：镜像 `.ocr-merge` 标签 + `.ocr-lang` 下拉观感；
- `.ocr-panel-actions` 加 `flex-wrap: wrap`：按钮增多时优雅换行，不溢出面板。

## 四、如何验收

```bash
pnpm build                       # 前端 tsc+vite，已通过（125 模块，exit 0）；后端无改动
./start.sh app                  # 打开含最新前端的 SnapCraft.app（内嵌 dist）
```

编辑器内点「取字」对一张图：
- **面板内重新识别**：识别完成后点「重新识别」→ 用同一张图重跑（不关面板、不丢语言设置）；macOS 切换语言后亦可借此重跑。
- **快捷键**：`Esc` 直接关面板；`Cmd/Ctrl+F` 立即把光标聚焦到搜索框并全选。
- **带坐标导出**：选项行选「JSON（含坐标）」或「TSV（含坐标）」→ 点「导出文本」→ 落盘文件除文字外含 `x,y,w,h,confidence`（TSV 带表头、JSON 为数组）；选「文本/Markdown」行为与历史完全一致。
- **默认零变化**：未改导出格式时默认 `.txt`，阅读顺序/合并/置信度/搜索过滤在三种格式下统一生效（WYSIWYG）。

## 五、回归确认

- 截图 / 区域 / 窗口 / 滚动长截图 / 钉图 / 文字标注 / 马赛克：均未改动，行为不变。
- 区域框选 OCR、内联纠错、结果内搜索、切换语言即时重识别、置信度阈值、合并为一行、自动复制、耗时统计、本地历史、阅读顺序校正：链路未动；
  新增三项只增不替——重跑复用 `runOcr`、导出在既有 `save_text_file` 上扩展格式、快捷键限 OCR 面板内，三者互不冲突。
- 后端 `ocr_image` / `OcrResult` / `OcrBlock` / `save_text_file` 签名与行为零变化（本轮纯前端）。
- **默认行为零回归**：`ocrExportFmt` 默认 `'txt'`（= 历史纯文本导出）；`ocrLayout` 仍默认 `'none'`；新增快捷键仅在面板打开时生效；重跑按钮是纯增量入口。
- OCR 偏好仍用**既有**隔离 localStorage key（`snapcraft.ocr.prefs`），与截图历史 store 完全隔离；本轮未新增持久化字段（导出格式为会话内状态，符合「按需选择」直觉）。
