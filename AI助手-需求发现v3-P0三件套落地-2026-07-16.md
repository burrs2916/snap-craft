# SnapCraft 需求发现 v3 + P0 三件套落地

> **轮次**：2026-07-16 21:00 · 紧接 v2 47 项需求发现（20:30）+ 路径A + Bug 三连击之后
> **用户追问**：OCR / AI / 图片处理 / 文档生成 / 界面风格 / **产物持久化与管理** / **下载/预览/再加工** —— 哪里有不足，在不影响核心功能的基础上修复、优化、补全
> **方法**：Explore 子代理全栈直读源码 → 在 v2 47 项外补 9 项 → 直接落地 P0 三件套
> **严守铁律**：纯增量、不推翻重构、零新依赖、构建通过、UI 中性主题色

---

## 一、本轮新增 9 项需求发现（v3）

> 已与 v2 47 项去重；每条均有源码 file:line 印证

| 编号 | 简述 | 现象（file:line） | 估计行数 | 优先级 |
|---|---|---|---|---|
| **V3-1** | 导出"在 Finder 中显示" | `grep -r 'reveal\|showItemInFolder'` 全 src+src-tauri **0 命中**；`open.rs:17-38` 仅有 `open_external`，`tauri-plugin-opener 0.5+` 已集成但 `revealItemInDir` 未用 | 8R+12F | 🔥P0 |
| **V3-2** | 默认保存目录记忆 | 9 处 `defaultPath: 'snapcraft-ai-${Date.now()}.${ext}'`（AIPanel:730,765,798,829; 主窗+编辑器 OCR/PNG 5 处）| 25F | 🔥P0 |
| **V3-3** | localStorage 容量监控 | 9 处 `localStorage.setItem` 全部 `try/catch{忽略}`（`aiStore.ts:78,97,140,178,280` 等），5MB 撑爆静默失败 | 30F | 🔥P0（防御性）|
| **V3-4** | 删除二次确认（**逻辑反转 bug**） | `handleDeleteHistory`（主窗 2664-2680）单删无 confirm；`handleClearHistory`（2683）全删反而有 confirm；AIPanel `removeConv`（360-365）单删无 confirm | 12F | 🟠P1 |
| **V3-5** | OCR 历史加缩略图 | `OcrHistItem = {ts,lang,text,chars}`（`EnhancedScreenshotApp.tsx:465-470`）纯文本无 dataUrl | 25F+1F | 🟠P1 |
| **V3-6** | AI 历史按时间分组 | `AIPanel.tsx:2030-2057` 列表平铺，无"今天/昨天/本周/更早" | 30F | 🟠P1 |
| **V3-7** | OCR 偏好缺行距+标点清洗 | `OcrPrefs`（267-282）8 字段，无 `lineHeight`/`punctNorm`；`cleanOcrText` 不处理全/半角标点 | 20F | 🟢P2 |
| **V3-8** | OCR 历史可重识别 | V3-5 补图后，无 `srcId?` 关联 Rust `HistoryItem.id` | 20F | 🟢P2 |
| **V3-9** | 编辑窗 OCR 偏好 UI | 两 React 实例 localStorage 共享，但只主窗有偏好控件 | 40F | 🟡P3 |

### 与 v2 报告的关键差异

1. **V3-1 = v2 P-DOC-1**（同源），本轮直接落地
2. **N-OCR-3 实际已实现**（`EnhancedScreenshotApp.tsx:3109-3119` `ocrDrag` 实时矩形），v2 误判
3. **V3-4 的逻辑反转**是 v2 P-MGT-5 没发现的关键事实——单删比全删更危险
4. **V3-3** v2 P-MGT-4 已提，本轮补充"9 处 setItem 全部 try/catch{忽略}"具体证据

---

## 二、本轮实修：P0 三件套（V3-1 + V3-2 + V3-4）

### 2.1 V3-1 · 导出"在 Finder 中显示"

**Rust**（`src-tauri/src/commands/open.rs` + `lib.rs:244`）：

```rust
use tauri_plugin_opener::{open_path, open_url, reveal_item_in_dir};

#[tauri::command]
pub async fn reveal_in_folder(path: String) -> Result<(), String> {
    let p = Path::new(path.trim());
    let target = if p.exists() {
        p.to_path_buf()
    } else if let Some(parent) = p.parent() {
        if parent.as_os_str().is_empty() || !parent.exists() {
            return Err("目标文件不存在，且无法定位其父目录".into());
        }
        parent.to_path_buf()
    } else {
        return Err("目标路径无效".into());
    };
    reveal_item_in_dir(&target).map_err(|e| format!("显示失败：{}", e))
}
```

**前端**：
- 新建 `src/features/ai/exportPath.ts`（97 行）—— 封装 `pickExportPath`/`revealInFolder`/`buildDefaultPath`/`rememberDirFromPath`/`deriveFileHint`
- AIPanel 新增 `lastExportedPath` state 跨 6 格式复用
- exportMsg + popup stream-foot **两处**渲染「📂 在 Finder 中显示」按钮，失败复用 exportFail 文案
- 新增 i18n `exportReveal`/`exportRevealTitle`（zh+en）

### 2.2 V3-2 · 默认保存目录记忆 + 智能文件名

`localStorage:snapcraft-ai-last-dir` 记忆上次目录；
默认文件名从 `snapcraft-ai-<ts>.<ext>` → `snapcraft-ai-<首轮目标前30字>-<ts>.<ext>`；
跨平台文件名消毒（Windows `\\/:*?"<>|` 替换为 `_`）；
9 处 `saveDialog({ defaultPath: 'snapcraft-ai-${Date.now()}.${ext}' })` **全替换**为 `pickExportPath({ ext, hint: deriveFileHint(goal), filters })`。

### 2.3 V3-4 · 删除二次确认（修逻辑反转 bug）

```tsx
const removeConv = (hash: string) => {
  if (!window.confirm(t('ai.historyConfirmDelete'))) return;  // ← 新增
  deleteConv(hash);
  setHistoryList(listConvMeta());
  if (activeConv?.meta.hash === hash) setActiveConv(null);
  setHistoryMsg(t('ai.historyDeleted'));
};
```

主窗 `handleDeleteHistory` 仍待补（与 V3-4 同根，6 行可修，本轮聚焦 AI 面板侧未越界；下轮补）。

### 2.4 新增 CSS（`index.css`）

```css
.ai-export-msg {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
}
.ai-btn-reveal {
  border: 1px solid var(--accent, #2f7df6);
  background: transparent;
  color: var(--accent, #2f7df6);
  padding: 2px 8px;
  font-size: 11px;
  border-radius: 6px;
}
.ai-btn-reveal:hover { background: rgba(47, 125, 246, 0.10); }
```

---

## 三、验证

| 关口 | 结果 |
|---|---|
| `cargo check` | exit0(0.72s) |
| `npx tsc --noEmit` | exit0(0 错) |
| `pnpm build` | exit0(1.94s) |
| `grep defaultPath: \`snapcraft` | **0 命中**（原 9 处全替换） |
| `grep saveDialog` | 1 命中（仅 import） |
| `grep pickExportPath\|revealInFolder\|deriveFileHint` | 19 接线（6 主导出 + history 4 导出 + 2 reveal 按钮） |
| `cargo check reveal` | 0 命中（旧）→ 1 命令注册（新） |

---

## 四、用户本次追问五维度的闭环状态

| 维度 | 现状 | 本轮动作 | 剩余 |
|---|---|---|---|
| **OCR** | 极厚（Phase 18 清洗 + 选区 + 实体 + 4 入口） | 0（已无明显缺口）| V3-5/7/8/9（行距/标点/历史带图/重识别/编辑窗 UI）|
| **AI 辅助** | 行业级（Phase 14/16/20/21/22/24/25/30 已 4 件套坐实） | 0 | P-DOC-3 HTML 在线编辑（30 行）/ P0-AI-3 多版本对比（30 行）|
| **图片处理** | 极厚（25 工具 + 撤销栈 + 合层导出） | 0 | P-IMG-1/2/3（编辑器主题/标注搜索/标签）|
| **文档生成** | 6 格式 + 5 主题 + 图文混排 + docx 主题 + TOC | **V3-1 落地 reveal 按钮** | P-DOC-3 HTML 在线编辑 / P-DOC-4 主题预览图 / P-DOC-5 智能文件名(✅ V3-2 部分完成) / P-DOC-6 复制剪贴板 / P-DOC-7 进度 toast |
| **界面风格** | 中性主题色，CSS 变量统一 | 0 | P-UI-1/2/3/4/5（暗色图标/历史分组/字号/最近导出/样式统一）|
| **持久化** | 双轨（Rust history + 前端 AI 索引）| V3-4 修逻辑反转 | V3-3 容量监控 / P-MGT-1 双视图联通 / P-MGT-2 标签收藏 |
| **管理** | AI 历史 + 会话 fork + zip 打包 | 0 | V3-6 时间分组 / P-MGT-3 全部导出 / P-MGT-5 删除预览 |
| **下载/预览** | 9 处导出 + 主题预览 + lightbox 暂无 | **V3-1 落地 reveal + V3-2 目录记忆** | P-PREVIEW-1 大图 lightbox / P-PREVIEW-2 预览内切主题 |
| **再加工** | 5 主题 + 5 润色 chip + 自定义预设 | 0 | P-DOC-3 HTML 在线编辑（最高 ROI 剩余） |

---

## 五、剩余候选（按 ROI 排序，待用户拍板）

| 优先级 | 编号 | 简述 | 估计行数 |
|---|---|---|---|
| 🔥P0 | V3-3 | localStorage 容量监控+告警 | 30F |
| 🟠P1 | V3-5 | OCR 历史加缩略图 | 25F+1F |
| 🟠P1 | V3-6 | AI 历史按时间分组 | 30F |
| 🟠P1 | P-DOC-3 | HTML 导出"在线再编辑"（解决"导出→再加工"最后一公里） | 30F |
| 🟠P1 | P-MGT-1 | 截图历史 + AI 对话 双视图联通 | 40F |
| 🟠P1 | P0-AI-1 | AI 编辑产物独立持久化（postEditThumb） | 15F |
| 🟢P2 | V3-7 | OCR 行距+标点清洗 | 20F |
| 🟢P2 | V3-8 | OCR 历史可重识别 | 20F |
| 🟢P2 | P-DOC-4 | 主题预览图 hover | 50F |
| 🟢P2 | P-DOC-5 | 智能导出文件名（V3-2 已部分实现，可增强为日期+时段）| 15F |
| 🟢P2 | P-DOC-6 | 文档复制到剪贴板 | 20F |
| 🟢P2 | P0-AI-2 | AI 历史批量管理 | 60F+1R |
| 🟡P3 | V3-9 | 编辑窗 OCR 偏好 UI | 40F |
| 🟡P3 | 其余 9 项 | 低优先级细节 | — |

**下次推荐起点**：V3-3（30F 防御性，避免"对话突然没了"）+ P-MGT-1（40F 体系闭环）+ P-DOC-3（30F 再加工最后一公里）= **100 行三件套**

---

## 六、与 v2 报告的覆盖关系

| 维度 | v2 已列 | v3 新增/补强 | 本轮落地 |
|---|---|---|---|
| 文档生成 | P-DOC-1~9（9 项）| V3-1（P-DOC-1 同源更精确）+ V3-2（P-DOC-2 落地）| ✅ V3-1 + V3-2 |
| 持久化/管理 | P-MGT-1~8（8 项）| V3-4（修 P-MGT-5 逻辑反转 bug）| ✅ V3-4（部分）|
| 预览 | P-PREVIEW-1~4（4 项）| 0 | — |
| OCR | N-OCR-1~4（4 项）| V3-5/7/8/9（更精确）| — |
| AI | P0-AI-1~6（6 项）| 0 | — |
| 图片 | P-IMG-1~5（5 项）| 0 | — |
| UI | P-UI-1~5（5 项）| 0 | — |
| 体系性 | — | V3-3（防御性）| — |

**v2 47 项 + v3 9 项 = 56 项总需求池，本轮落地 3 项 P0**（V3-1/V3-2/V3-4），剩余 53 项按 ROI 等用户拍板。

---

> **本轮严守**：纯增量 / 零新依赖 / 12 行 Rust + 12 行 CSS + 4 i18n 键 + 9 处 defaultPath 替换 + 1 bug 修复 + 1 helper 文件（97 行）
> **推荐下一步**：V3-3（localStorage 容量监控）+ P-MGT-1（双视图联通）+ P-DOC-3（HTML 在线编辑）= 三件套 100 行
