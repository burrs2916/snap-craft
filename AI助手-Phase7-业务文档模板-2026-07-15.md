# AI 助手 Phase 7 — 预置业务文档模板（截图 → 完整文档）

> 自动化执行轮次：2026-07-15｜参考 privdoc-ai｜纯前端、零 Rust 改动、不影响截图/标注/OCR/历史

## 本轮需求发掘

复盘 Phase 1–6（面板/流式 → 导出 md·txt·html·docx·pdf·xlsx → 回写标注 → 多轮对话 → 图文报告 → 记忆压缩），
确认「截图 → 文档」的工具链已较为完整，但**预设仍偏通用**（文档/文案/周报/教程/要点/翻译/提问/报告/表格）。
用户愿景是「通过编辑截图，让 AI 帮我们生成**完整的文档或文案**」——而真实业务里「完整文档」往往有固定形态：
缺陷单、需求文档、竞品分析、会议纪要、数据解读。此前用户只能自己写 prompt 或建自定义模板（Phase 2b），
门槛偏高。本轮直接把 5 个高频业务文档形态**预置为内置预设**，开箱即用。

## 需求发现（重要，建议列为下一 Phase）

- **🔴 关键断点：AI 当前只看「原始截图」，看不到用户的标注/编辑。** 经核查，`EnhancedScreenshotApp` 传
  `current?.dataUrl`、`EditorWindow` 传 `imageData`，二者都是**原始捕获图**；箭头/打码/文字/高亮等标注是独立
  `annotations` 状态、作为覆盖层渲染，并未合入传给 AI 的图。这与「通过**编辑**截图生成文档」的愿景相悖，
  且有**隐私隐患**：用户对敏感信息打码后，AI 仍可能基于原图像素产出含敏感内容的文稿。
  - 修复方向：把「底图 + 全部标注」合成为一张图再传给 AI（即让 AI 看到用户最终编辑结果）。
  - 风险：需忠实重绘所有标注类型到 canvas，逻辑复杂、易与屏幕上渲染结果不一致；属设计型改动，**不宜在自动化轮次中盲目动手**，
    建议单独 Phase 并由用户确认方案（可复用标注渲染层或导出合成逻辑）。
- 其余候选（沿用 Phase 6）：① 记忆相关性检索；② 跨截图会话列表；③ 后端代理隐藏 Key；④ 拖拽重排多图选择顺序。

## 本轮实现（纯前端增量）

在 `src/features/ai/aiPresets.ts` 的 `AI_PRESETS` 中新增 5 个内置预设（无需改 UI/store——`allPresets()` 自动聚合）：

| id | 中文 | 英文 | 产出形态 |
|----|------|------|----------|
| `bug` | 缺陷报告 | Bug report | 标题/环境/复现步骤/预期·实际/严重度/上报信息 |
| `prd` | 需求要点 | PRD outline | 背景目标/目标用户/功能点/交互边界/验收清单/待确认 |
| `compete` | 竞品分析 | Competitive analysis | 分析对象/对比维度表格/优劣/机会建议 |
| `meeting` | 会议纪要 | Meeting notes | 主题/结论/行动项表格(事项·负责人·时限)/待跟进/计划 |
| `insight` | 数据解读 | Data insight | 概览/趋势异常(引数字)/原因假设/洞察建议/风险 |

- 每个预设：`vision:true` + 中文 system 指令（要求 Markdown、引截图文字同语言、信息缺失标「待补充」），
  复用共享 `buildDefaultUser` 构造（兼容单图与多图 OCR 注入）。
- i18n（zh-CN + en-US）：`ai.preset.{bug,prd,compete,meeting,insight}` 与对应 `Desc` 共 10 键已加。
- 预设 chips 容器 `.ai-presets` 为 `flex-wrap`，新增 5 个 chip 自动换行，不破坏布局。

## 验证

- `pnpm build` exit0（136 模块，零类型错误，无回归）。
- `dist/assets/index-*.js` 含全部新 token：中文标签（缺陷报告/需求要点/竞品分析/会议纪要/数据解读）、
  英文标签（Bug report/PRD outline/Competitive analysis/Meeting notes/Data insight）、
  系统提示关键句（产品需求要点文档/竞品分析/行动项/数据洞察与建议）。

## 生效方式

重新构建并启动即可：`./start.sh dev` 或 `pnpm build && ./start.sh app`。
AI 面板预设栏将出现 5 个新业务文档模板，点击即按该形态生成完整文档，并照常支持导出 md/txt/html/docx/pdf/xlsx、回写标注、多轮打磨。
