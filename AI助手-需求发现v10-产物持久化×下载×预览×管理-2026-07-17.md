# AI 助手 需求发现 v10 — OCR/AI 产物持久化 × 下载 × 预览 × 管理（2026-07-17 04:30）

> **方法**：响应用户**第七轮四维追问**（OCR 产物 / AI 产物 / 文档产物 / 图片处理产物的持久化、管理、下载、预览），v1–v9 共发现 **268 项 GAPS** + 本轮"v3/v4/v5 落地" + "v8 原子级补强"后，本轮**严格限定在「真正还没触达」的 5 条新轴**：
>
> 1. **产物关系网断裂**（截图原图 ↔ OCR ↔ AI 文档 ↔ 编辑标注 互不关联）
> 2. **产物元信息丢失**（导出文件无来源 / 无版本 / 无指纹 / 无时间）
> 3. **产物"二次加工"路径**（导出后再编辑 / 二次润色 / 替换图 / 续写）
> 4. **产物"批量与时间轴"**（缺时间线 / 缺批量选 / 缺版本对比 / 缺变更追踪）
> 5. **产物"发现与重生成"**（无全文搜 / 无标签 / 无最近导出 / 无对比 / 无重生成历史）
>
> **本轮严守**：0 代码改动、纯诊断优先、不擅加未请求功能。

---

## 0. v1–v9 复盘（确保不重复 · 严格界定 v10 范围）

| 轮次 | 视角 | 已落地 / 路线图 |
|---|---|---|
| v1 | 整体复盘 × 路线图 | Phase 1–30 全部落地 |
| v2 | 47 项缺口 | 路线图 |
| v3 | P0 三件套实修 | Finder/默认目录/二次确认/缩略图 |
| v4 | 31 项产物管理缺口 | 路线图 |
| v5 | v4 P0/P1 四件套直接落地 | OCR bbox 落库/历史扩容/AI 编辑持久化/OCR 清洗 UI |
| v6 | 70 项编辑/产物/UI 缺口 | 路线图（OCR bbox full/quota/GFM/焦点环/撤销栈/API Key 引导）|
| v7 | 85 项文档产物/OCR-AI 持久化/图片/界面 | 路线图（主题 id 互通/PDF 假导出/i18n/zip 多图/Pin 窗/批量导出）|
| v8 | 70 项 OCR 引擎/AI 产物链/6 格式末端/UI 死键 | 路线图（abortCtl/agentSteps/sourceShotId/NFC/死键/⌘CVD）|
| v9 | 63 项 App-level/AI 成本/多用户/可持续 | 路线图（classifyAiError/首启引导/快捷键/迁移/ErrorBoundary）|

**v1–v9 已基本穷尽**：
- ✅ 产物基础：export path/Finder reveal/缩略图/bbox 落库/智能清洗/编辑后重 OCR
- ✅ AI 资产：agentSteps/thinking/usage/sourceShotId/convHash/fork/记忆压缩/persist
- ✅ 文档生成：6 格式+5 主题+主题 id 互通+批量 zip+多图原图打包+PPT 嵌图+PDF 假改+AST 漂移
- ✅ UI 末端：toast 统一/快捷键/焦点环/焦点态/死键清理/NFC/i18n system prompt
- ✅ 持久化：localStorage 5 key+历史 100 条+schemaVer+API Key 加密+孤儿清理+批量多选
- ✅ 管理：AI 历史搜索/标签/批量删/批量导/时间分组/最近导出/全量导出
- ✅ 预览：HTML Blob/主题切换/docx/xlsx 缩略预览/Pin 窗 OCR/AI Diff
- ✅ 下载：默认目录/Reveal/6 格式记忆/批量 Reveal

→ **本轮（v10）定位**：v1–v9 主要在「**产物本身**」+「**用户旅程**」+「**原子级细节**」+「**app-level 体验**」+「**工程可持续**」5 个维度下钻，但**有一类盲点被所有前 9 轮系统性忽略**：
- **产物之间**（"一张图"在 4 个落点：原图 / OCR / AI 文档 / 编辑标注之间没有关联网）
- **产物元数据**（文件本身没有"我是谁、我从哪来"）
- **产物后续生命周期**（导出后用户实际拿它做什么——发邮件/贴 IM/再次加工/做对比/做汇总）
- **时间维度**（无产物时间轴，无版本管理，无变化追踪）
- **发现与重生成**（50+ 文档时无全文搜/标签/最近导出/对比/重生成历史）

---

## 一、本轮新发现 GAPS（89 项独立 · 5 大维度 · P0=22 / P1=39 / P2=20 / P3=8）

### 🔥 P0（强烈建议立刻做 · 盲点核心）

#### A. 产物关系网断裂（4 项 · 整链关键）

| # | GAP | 位置 | 估行 | 铁律 | 描述 |
|---|---|---|---|---|---|
| **V10-P0-1** | AI 文档产物↔源截图↔OCR↔编辑标注 **无四向链接** | `aiStore.ts:151-161` AiConvMeta + `history.rs` | 40 | ✅ 纯前端 | 当前仅 thumb（缩略图）作软连接；`AiConvMeta` 缺 `sourceShotId?`（截图历史 id）、`sourceOcrHash?`、`editsRef?`；`HistoryItem` 缺 `derivedDocs?`；产物之间无法反向跳转。v8 P0-9 加了 sourceShotId 字段路线图，**v10 落地下游打通**（截图卡 → 跳 AI 历史；AI 文档卡 → 跳 OCR 行；OCR 行 → 跳标注位置）|
| **V10-P0-2** | 编辑后 AI 烧录图（commitAiEdit）**无独立历史项** | `aiStore.ts` 全局 / `commitAiEdit` 路径 | 25 | ✅ 纯前端 | v5 V4-6 把 commitAiEdit 烧录到 currentScreenshot，v8 路线图新增独立 `source:'ai_edit'` 历史项——**但 v5 仅入 history 数组，没关联回 AI 对话的 convKey**；点 AI 历史找不到这次"AI 改过的图"独立索引。补：commitAiEdit 后写一个 `ai_edit_history:{convKey}` localStorage 映射 |
| **V10-P0-3** | OCR 历史 + 截图历史 + AI 文档历史 **三套孤岛** | `EnhancedScreenshotApp.tsx:641-644, 1684` / `aiStore.ts:151` | 50 | ✅ 纯前端 | 截图历史 `setHistory` / OCR 历史 `setOcrHistory` / AI 索引 `setConvIndex` 三个独立 localStorage，无任何交叉引用。`OcrHistItem.sourceId` 已有但**仅跳到截图，AI 历史拿不到这张图的 OCR**。补：每条 OcrHistItem 加 `aiConvKey?`，每条 AiConvMeta 加 `lastOcrTs?` |
| **V10-P0-4** | 多截图成稿的 N 张原图**未与最终文档建立位置映射** | `AIPanel.tsx:610-620 orderedImages` / `AIPanel.tsx:723-758` | 35 | ✅ 纯前端 | AI 输出的 `<!--SNAP:k-->` 标记 + `sectionImages` 是临时的 export-time 关系；导出的 docx 嵌图正确但**导出文件本身没有"原图在哪个文件夹"信息**。用户 6 个月后看 docx 想"改第 3 张图"无入口。补：docx core.xml 加 `<dc:source>` 写回原图路径 |

#### B. 产物元信息丢失（6 项 · 长期管理杀手）

| # | GAP | 位置 | 估行 | 铁律 | 描述 |
|---|---|---|---|---|---|
| **V10-P0-5** | 导出文件**无统一元信息（指纹/来源/导出次数/导出时间）** | `markdownDocx.ts:382-389` / `markdownPptx.ts:382-389` / `markdownXlsx.ts` | 60 | ✅ 纯前端 | docx/pptx 的 `core.xml` 全部硬编码 `2026-07-16T00:00:00Z`（v8 已列 P0-3 PPT 时间戳硬编码，但**docx core.xml 一样硬编码**）+ 标题写"SnapCraft AI"；xlsx 0 文档元信息。补：所有导出统一加 `creator=SnapCraft AI / sourceHash=<截图 hash> / exportedAt=<ISO>/ presetId=<id>/ tokenCost=0.0007` |
| **V10-P0-6** | 导出文件**未在文件名嵌入版本号 / hash 短码** | `exportPath.ts:69-80 buildDefaultPath` | 12 | ✅ 纯前端 | 当前文件名 `snapcraft-ai-<hint>-<ts>.docx`，**6 个月后看 30 个同名 docx 全分不清**。补：文件名末加 `-<hashShort7>`（截图 hash 前 7 位），人类可读 + 机器可索引 |
| **V10-P0-7** | AI 历史卡片**无成本/字符数/导出次数徽标** | `AIPanel.tsx:2084-2108` 列表渲染 | 30 | ✅ 纯前端 | v8 P0-8 把 usage 落库但**列表 UI 不用**；用户 50 条 AI 记录看不出"哪条最贵/哪条最常导/哪条最长"。补：每条卡片右侧 ⓘ 图标 hover 显示 `cost $0.07 · 4823 tokens · 3 exports · 12 turns` |
| **V10-P0-8** | 截图历史卡片**无尺寸/格式/文件大小/创建时长** | `EnhancedScreenshotApp.tsx` 历史网格 | 25 | ✅ 纯前端 | 缩略图 + 时间 + OCR 行数，**没有"这张图多大、什么分辨率、源是全屏/区域/窗口"**。补：hover 卡片显示 `1920×1080 · PNG 234KB · region · 2.3s` |
| **V10-P0-9** | OCR 历史项**无来源截图分辨率/语言/字符统计分布** | `EnhancedScreenshotApp.tsx:1685-1689` setOcrHistory | 15 | ✅ 纯前端 | `OcrHistItem` 仅 `{text, lang, ts, chars, thumb, sourceId}`——**没有"这张图多大、识别用了什么引擎、用了几秒"**。补：`{...ocrStats: {elapsed, blocks, avgConf}}` |
| **V10-P0-10** | AI 文档**无内容指纹**（用户拿到 docx 后无法判断"是哪个版本"） | `aiStore.ts:151` AiConvMeta | 15 | ✅ 纯前端 | `convHash` 是基于 `imageDataUrl` 的稳定键，**但同一张图多次 AI（不同预设/不同 prompt）产生不同文档，没法索引**。补：`AiConvMeta.contentHash?` = sha1(`conversation[-1].content`)，文档级指纹 |

#### C. 产物"二次加工"路径（7 项 · 用户最常踩的"导出后"）

| # | GAP | 位置 | 估行 | 铁律 | 描述 |
|---|---|---|---|---|---|
| **V10-P0-11** | **导出文件无"打开并再编辑"按钮** | `AIPanel.tsx:1475-1494` 导出成功消息 | 25 | ✅ 纯前端 + 1 Rust | v2 P-DOC-3 提"HTML 在线再编辑"但未落地；当前导出 docx 后只能去 Word 改、export 后**SnapCraft 完全失联**。补：成功消息加"📝 用 Word 打开"按钮 → invoke 系统默认程序打开（Rust 已有 `open_external`）|
| **V10-P0-12** | **AI 文档无"替换其中一张截图"入口** | `AIPanel.tsx` 全局 | 60 | ✅ 纯前端 | 用户生成"3 张图报告"后想"换第 2 张图重生成"，只能再问 AI。补：报告卡片右键"🔄 重生成第 N 段" → 单段（按 `<!--SNAP:N-->` 切）独立重生成 |
| **V10-P0-13** | **AI 文档无"在 SnapCraft 中继续编辑"路径** | `AIPanel.tsx:1986-2002` 阅读器 | 35 | ✅ 纯前端 | 文档导出 docx 后想"在 SnapCraft 中追加一段 AI 写"无路径。补：阅读器加"✏️ 续写"按钮 → 把当前末轮 AI 内容作为新首轮 user 输入，preset 改 `refine`，追加一轮 |
| **V10-P0-14** | **多截图成稿的"图片顺序"无法在导出后调整** | `AIPanel.tsx:1198-1244` 拖拽 | 8 | ✅ 纯前端 | 拖拽改了 selectedOrder 但只影响下次生成，**已导出的 docx 内图片顺序不可再调**。补：导出的 docx 内嵌一个隐藏 JSON 表（`docProps/custom.xml`）+ 简易 viewer 读取 |
| **V10-P0-15** | **AI 文档无"另存为模板"** | `AIPanel.tsx` 全局 | 35 | ✅ 纯前端 | 用户 AI 出 1 份 PRD 后想"以后都用这个格式"，只能复制 prompt。补：导出成功消息"⭐ 另存为模板" → 把 preset.system 复制为 UserPreset，自动填入首轮目标 + 缩略图 |
| **V10-P0-16** | **导出的 docx/xlsx 内嵌截图与原图**无反向引用（改原图不影响导出图） | `markdownDocx.ts:197-232` dataUrlToImageRun | 8 | ✅ 纯前端 | docx 内嵌图是 dataURL 烧死的——用户改原图后导出文件**完全没提示**。补：导出时给 docx 加隐藏关系指向原图路径（`docProps/custom.xml` schema 扩展），打开时 Word 提示"原图已更新" |
| **V10-P0-17** | **多格式导出的"另存为同一文件名不同格式"无统一** | `exportPath.ts:69-80` buildDefaultPath | 15 | ✅ 纯前端 | 同一会话导 docx 后再导 pdf，**默认文件名是新的（hint+ts 重新生成）**——用户想"docx+pdf 同名存一起"无路径。补：`lastExportBaseName` localStorage 记忆，跨格式复用 hint，去掉 ts |

#### D. 产物"批量与时间轴"（3 项 · 长期使用刚需）

| # | GAP | 位置 | 估行 | 铁律 | 描述 |
|---|---|---|---|---|---|
| **V10-P0-18** | **无"产物时间轴"视图（截图+OCR+AI 三流合一）** | 缺新组件 | 80 | ✅ 纯前端 | 当前三套历史各自为政；用户想"看这周我做了什么"无单一时间线。补：新增 `TimelineView` 组件，按时间倒序合并三类事件（截图 / OCR / AI 文档），可按类型过滤、跳转、对比 |
| **V10-P0-19** | **AI 文档"导出后变更"无追踪** | `AIPanel.tsx:382-461` handleHistoryExport | 25 | ✅ 纯前端 | 用户导了 docx 1 分钟后 AI 又润色了 1 次——**docx 还是老版本，但 History 卡片看不出"已过时"**。补：每条 AiConvMeta 加 `exportedVersion` 字段，refine 后标记"⚠️ 已有 2 次未导出更新" |
| **V10-P0-20** | **批量导出 N 个 AI 文档仅支持 zip，不支持"按时间分目录"** | `AIPanel.tsx:464-505` handleHistoryZip | 30 | ✅ 纯前端 | v7 P1-27 提批量多会话导出 zip 但**全部会话平铺到同一 zip 根**——用户 30 条 AI 文档解压后**不知哪些是哪天哪些是哪类**。补：按 `updatedAt` 的 `yyyy-mm-dd` 自动分子目录（`2026-07-17/会话名.zip`）|

#### E. 产物"发现与重生成"（2 项 · 50+ 文档时杀手）

| # | GAP | 位置 | 估行 | 铁律 | 描述 |
|---|---|---|---|---|---|
| **V10-P0-21** | **AI 历史**无"按内容/思考过程/工具步骤"全文搜索 | `AIPanel.tsx:544-550` filteredList | 30 | ✅ 纯前端 | v7 P1-34 提了"全文搜"路线图但**只搜 firstGoal/presetName/preview（≤ 200 字符）**。**AI 实际 8000 字长文里写的"用户偏好"搜不到**。补：搜索时遍历 `getConvByHash(hash).map(m=>m.content)` 拼接，纯前端零依赖 |
| **V10-P0-22** | **导出的"上次导出过的文件"无最近 5 次列表** | `exportPath.ts:14` LAST_DIR_KEY | 35 | ✅ 纯前端 | v2 P-UI-4 提"最近导出"路线图但**未落地**——用户想"把刚才导的那个 docx 再导一次"得去 Finder 翻。补：localStorage `snapcraft-ai-recent-exports: []` 记最近 20 次（fmt+path+ts），AI 面板头部加"📋 最近导出"下拉 |

---

### 🟠 P1（生产级补强 · 1 周内分批 · 39 项）

#### F. OCR 产物深化（6 项）

| # | GAP | 位置 | 估行 | 铁律 | 描述 |
|---|---|---|---|---|---|
| **V10-P1-1** | OCR 临时 PNG 路径**未 UUID 化**（v8 P1-10 路线图）| `commands/ocr.rs:48` | 3 | ✅ 1 行 | `store::temp_png_path()` 5 张图并发 OCR 时同路径冲突，macOS 文件锁竞态。修：加 `Uuid::new_v4().to_string() + ".png"` |
| **V10-P1-2** | OCR 引擎层**无降级路径**（macOS Vision 崩了无 Windows 备选） | `commands/ocr.rs:71-123` | 40 | ✅ + 1 Rust | 当前 macOS 走 Vision / Windows 走 WinRT，**macOS 上 Vision 临时不可用时（如隐私权限）只能硬报错**。补：加 1 行 if-else → 调本地 `tesseract` 二进制（系统装了的话）|
| **V10-P1-3** | OCR 失败重试**仅 streamChat 3 次，OCR 命令 0 次** | `EnhancedScreenshotApp.tsx:1582-1726` doOcr | 25 | ✅ 纯前端 | v8 P2-1 提 OCR 失败重试路线图但**没落地**——Vision 偶发失败必须用户手动点。补：try 1 次失败后等 200ms 重试 1 次（网络型/IO 型），toast 提示"已自动重试 1 次" |
| **V10-P1-4** | OCR 文本**无多语言混合单次识别** | `EnhancedScreenshotApp.tsx` ocrLang 选项 | 15 | ✅ 1 行 | v4 V4-28 路线图"中+英 单次"未落地——`lang='zh-Hans,en-US'` 后端能跑但 UI 选项是单选。补：OCR 面板加"🌐 多语言混合"toggle，触发时把 lang 改成 `'zh-Hans,en-US'` |
| **V10-P1-5** | OCR 区域框选**无"按识别结果自动选区"** | `EditorWindow.tsx:1064-1087` ocrRegionMode | 35 | ✅ 纯前端 | 用户想"只识别电话号码"无路径。补：选区模式加"📞 自动按类型"下拉（邮箱/URL/电话/数字/全部），命中行自动高亮 → 一键区域框选 |
| **V10-P1-6** | OCR 批量识别**无任务进度/无失败重试 UI** | `EnhancedScreenshotApp.tsx` 批量取字 | 30 | ✅ 纯前端 | v3 V3-8 提 OCR 批量识别路线图——`batchOcrImages` 命令已存在但**UI 无进度条**、无"3/8 失败"展示。补：进度条 + 失败项 hover 看原因 + "重试失败项"按钮 |

#### G. AI 产物深化（10 项）

| # | GAP | 位置 | 估行 | 铁律 | 描述 |
|---|---|---|---|---|---|
| **V10-P1-7** | AI 对话**无"导出当前对话"按钮**（仅 zip 含全部） | `AIPanel.tsx` 主面板 | 20 | ✅ 纯前端 | 当前只能 zip 或导出末轮 AI 内容——**整个 user/assistant 完整对话流无法单独导出**。补：主面板加"💬 导出对话"按钮 → 生成 `.json` 或 `.md` 含 role/content/timestamp |
| **V10-P1-8** | AI 历史**无"导出当前为可分享 HTML"**（含思考/工具步骤） | `AIPanel.tsx:1980` 阅读器 | 50 | ✅ 纯前端 | 导出 docx 仅含末轮；分享给同事时**AI 怎么想的、用什么工具**全丢。补：阅读器加"📤 分享为 HTML"按钮 → 含 conversation + thinking + agentSteps 的交互式网页 |
| **V10-P1-9** | AI 对话**无"断点续传"**（v8 P1-15 路线图） | `aiStore.ts:625-721` chat | 60 | ✅ 纯前端 | 中途中断/网络断后整轮丢失，无"上次生成到第 4 段"可恢复。补：流式时定期（每 5 秒）`localStorage.setItem('ai-stream-checkpoint', output)`，断网恢复时弹"📌 续写？"|
| **V10-P1-10** | `usage.cacheRead/cacheCreate` 列表视图**无命中率聚合** | `AIPanel.tsx:1496-1506` | 30 | ✅ 纯前端 | v4 V4-15 提"命中率面板"未落地——50 条 AI 历史里**哪些用 cache 省钱、哪些没用**看不出。补：列表顶部加"💰 本月省 $X.XX"徽标 = Σ cacheRead × price |
| **V10-P1-11** | AI 历史**无"按生成时间范围"过滤** | `AIPanel.tsx:544-550` filteredList | 35 | ✅ 纯前端 | v2 P-MGT-8 提"时间范围/preset/标签"路线图未落地——仅文本搜索。补：列表头加时间下拉（今天/本周/本月/全部）+ preset 多选 chip |
| **V10-P1-12** | AI 历史**无"按成本"排序** | `AIPanel.tsx:760` listConvMeta sort | 10 | ✅ 纯前端 | `listConvMeta().sort(updatedAt)` 是默认排序——用户想"最贵的会话"得肉眼算。补：列表头加排序切换（时间/成本/字符数/导出次数）|
| **V10-P1-13** | AI fork 树**无可视化（仅 parent 标记）** | `aiStore.ts:160, 797` | 80 | ✅ 纯前端 | v7 P1-36 提 fork 树路线图——`parent?` 字段已存但 UI 只用 `🍴 Fork` 徽标，**3 层 fork 后无法追溯**。补：fork 关系做成树状图组件，hover 显示父子链 |
| **V10-P1-14** | AI 模板（`UserPreset`）**无"从历史会话一键创建"** | `AIPanel.tsx:954-969` openNewTemplate | 20 | ✅ 纯前端 | P0-15 已列"另存为模板"，但**反向"从某次成功会话反推模板"也缺**。补：历史阅读器加"📋 复制为模板" → 把 system + 首轮 user + 预设名打包成 UserPreset |
| **V10-P1-15** | AI 长期记忆**无"导出/导入"** | `aiStore.ts:264-282` saveMemories | 25 | ✅ 纯前端 | 50 条记忆跨设备无法迁移——重装即丢。补：记忆管理面板加"📤 导出 .json / 📥 导入 .json" |
| **V10-P1-16** | AI "导出全部历史" zip **无 README 索引页** | `AIPanel.tsx:464-505` handleHistoryZip | 20 | ✅ 纯前端 | 单条 zip 已有 README.txt（v7 P0-7），**批量 zip 无**——解压 50 个 zip 后用户**不知哪个是哪个**。补：批量 zip 顶层 README.md + conversations.csv（hash/title/preset/date/cost）|

#### H. 文档产物深化（8 项）

| # | GAP | 位置 | 估行 | 铁律 | 描述 |
|---|---|---|---|---|---|
| **V10-P1-17** | DOCX 主题**无"实时切换"**（v7 P1-16 路线图）| `AIPanel.tsx:1408-1425` DOC_THEMES chip | 25 | ✅ 纯前端 | 切主题后导出的 docx 立即生效（V7-P0-1 修了 id 不通），但**没有"先预览 5 主题小样图再选"**。补：chip hover 出小预览（用 1 段样本 md 渲染 5 主题）|
| **V10-P1-18** | HTML 导出**无"在浏览器编辑后保存"**（v2 P-DOC-3 路线图）| `markdownHtml.ts` 全局 | 80 | ✅ 纯前端 | v2 提"HTML 在线再编辑"但**未落地**——导出 HTML 后用户想"改个标题再发"必须切回 AI 重跑。补：HTML 顶部加 contenteditable 工具条，改后点"💾 保存覆盖原文件"|
| **V10-P1-19** | DOCX 内嵌图片**未做客户端压缩**（v6 P1-7 路线图）| `markdownDocx.ts:197-232` dataUrlToImageRun | 25 | ✅ 纯前端 | 9 张 4K 截图导出 docx 50MB+——`dataUrlToImageRun` 直接 toDataURL 写入。补：ImageRun 前 canvas resize 到 max 1200px、JPEG 0.85 压缩 |
| **V10-P1-20** | PPTX **首版不内嵌图**（v6 P1-16 路线图）| `markdownPptx.ts:8` | 120 | ✅ 纯前端 | "图文报告 → .pptx" 时所有 `<!--SNAP:k-->` 章节图片被丢弃，6 格式中唯一丢图。补：仿 docx 章节内嵌截图逻辑 + OOXML media 关系 |
| **V10-P1-21** | DOCX/PDF **无页眉/页脚自定义** | `markdownDocx.ts:547-571` | 35 | ✅ 纯前端 | v6 V8-P1-37 提"加纸张/页边距"——已部分（页码 footer），但**页眉写死文档名**，用户想"页眉写公司名/项目编号"无入口。补：导出 dialog 加"页眉/页脚"输入框 |
| **V10-P1-22** | XLSX **无"CJK 列宽 2×" 智能加权** | `markdownXlsx.ts:117-127` widths | 15 | ✅ 纯前端 | v8 P1-27 路线图未落地——CJK 字按 1 计实际占 2，导出后 Excel 列窄中文截断。补：列宽计算时若含 CJK 字符则 ×1.8 |
| **V10-P1-23** | 6 格式导出**无统一"导出前预览 5 主题对比"** | `AIPanel.tsx` 主面板 | 80 | ✅ 纯前端 | v2 P-DOC-4 提"主题预览图 hover"未落地——chip 仅有 tooltip。补：chip 双击弹预览 modal，5 主题横向排列同一段 md 渲染对比 |
| **V10-P1-24** | 6 格式**无"导出任务队列"**（大文件 5+ 同时导出卡） | `AIPanel.tsx:723-905` 6 个 export handler | 40 | ✅ 纯前端 | 用户点 6 个按钮时**主线程串行 await**，每个 1-3 秒，UI 卡死。补：导出入队（Queue），显示"导出 3/6 完成"|

#### I. 图片处理产物深化（8 项）

| # | GAP | 位置 | 估行 | 铁律 | 描述 |
|---|---|---|---|---|---|
| **V10-P1-25** | 编辑器导出**无"复制为带格式 Markdown"** | `EditorWindow.tsx:1155-1180` | 40 | ✅ 纯前端 | v7 P1-64 提了"复制为图片+OCR 文字"但**没复制为 MD**——Notion/飞书自动合并。补：copyAsMarkdown() 工具：把 OCR 文字按行转 MD code block + 截图 dataUrl |
| **V10-P1-26** | 编辑器导出**无"导出尺寸"选项** | `EditorWindow.tsx:1140-1142` | 70 | ✅ 纯前端 | v7 P1-63 提了"原图/2×/1×/0.5×/自定义"——未落地。补：saveDialog 前 1 个 sizePicker，原图/2×/1×/0.5×/自定义宽度，canvas 二次缩放 |
| **V10-P1-27** | 编辑器**无"复制为带文字层 SVG"** | `EditorWindow.tsx` | 50 | ✅ 纯前端 | 复制为 SVG → 文字可编辑（不再是像素）。补：把标注文字+形状转 SVG，复制到剪贴板 |
| **V10-P1-28** | 编辑器**无"保存标注 JSON / 加载标注 JSON"** | `EditorWindow.tsx` | 60 | ✅ 纯前端 | v6 P2-18 提"标注转 JSON 标准 schema"路线图——未落地。补：标注导出/导入为 Figma/Sketch 兼容的 `.snap.json`（SnapCraft 自定义 schema）|
| **V10-P1-29** | 长截图拼接**保存后**无"拼接前的 N 帧原图保留" | `stitch.ts:125-154` | 15 | ✅ 纯前端 | v6 V8-P1-67 提"保留原 20 帧"——未落地。补：拼接时把 N 帧原图 zip 后存到 history，写一个 `frames.zip` 路径到 `HistoryItem.framesRef?` |
| **V10-P1-30** | 编辑器**无"撤销栈导出"**（误撤销后无法恢复） | `screenshotStore.ts:120/132` | 25 | ✅ 纯前端 | 撤销栈 50 步内存中，**关闭编辑器即丢**。补：编辑器关闭前把撤销栈写入 `history.json` 同条记录的 `undoStack?` 字段 |
| **V10-P1-31** | 编辑器**无"导出历史"**（同一图多次编辑的版本历史）| `EditorWindow.tsx` | 50 | ✅ 纯前端 | 用户编辑 5 次保存 5 张——哪张是最新版？无版本号。补：每次保存写到 `HistoryItem` 同 hash 数组里，UI 上可对比 v1/v2/v3 |
| **V10-P1-32** | 标注 ⌘D 复制粘贴 8 px 偏移**无视觉反馈** | `AnnotationCanvas.tsx:336-398` | 15 | ✅ 纯前端 | v8 P1-51 提了 ⌘C/V/D 但**粘贴后无 ghost preview**。补：粘贴时画一个半透明 ghost 在目标位置（8px 偏移后），松开确认 |

#### J. 下载/预览/管理（7 项）

| # | GAP | 位置 | 估行 | 铁律 | 描述 |
|---|---|---|---|---|---|
| **V10-P1-33** | **导出文件预览窗口**（双击 docx 用 Word 打开）无统一入口 | `AIPanel.tsx:1475-1494` 成功消息 | 25 | ✅ + 1 Rust | 当前 reveal 按钮只是 Finder，**双击 docx 用 Word 打开要走 Finder**。补：成功消息加"📄 用 Word 打开" → invoke Rust `open_external(path)` |
| **V10-P1-34** | 导出文件**无"导出后验证"**（文件大小/字符数 sanity check） | `AIPanel.tsx:748-757, 791-798, 855-862` | 20 | ✅ 纯前端 | save 后立即显示"已保存 23KB"——但**没有"打开/预览"按钮**（用户只能去 Finder）。补：成功后加"👁 预览"按钮 → 系统默认程序打开 |
| **V10-P1-35** | 文件元信息面板**无统一的"产物详情"模态** | 缺新组件 | 60 | ✅ 纯前端 | 用户想看 docx 元信息（创建时间/作者/页数/字数）只能去 Word。补：详情 modal → Rust 解析 docx core.xml 返回（creator/created/modified/字数/页数估算）|
| **V10-P1-36** | **导出文件无"分享"按钮**（邮件/微信/IM）| `AIPanel.tsx:1475-1494` | 25 | ✅ + 1 Rust | macOS 分享面板（`tauri-plugin-share`）未集成。补：成功消息加"↗ 分享" → invoke `share_file` 打开系统分享面板 |
| **V10-P1-37** | **`lastExportedPath` Reveal 在 popup 模式下不持久** | `AIPanel.tsx:246` | 10 | ✅ 纯前端 | 跨 6 格式用 1 个 ref，**popup 导出会覆盖主面板 ref**（v4 V4-4 已知）。补：`Record<fmt, path>` 字典，每格式独立记 |
| **V10-P1-38** | 导出 dialog **无"记住上次的扩展名"** | `exportPath.ts:14` LAST_DIR_KEY | 15 | ✅ 纯前端 | v8 P1-32 路线图"按格式分别记忆 reveal 路径"未完全落地——`lastExportedPath` 不参与下次默认目录（重开应用又回到 ~/Downloads）。补：保存时同时记 lastExt，下次 saveDialog 默认扩展名 |
| **V10-P1-39** | **导出文件"打开历史版本"路径缺失** | `markdownDocx.ts:382-389` core.xml | 35 | ✅ 纯前端 | docx core.xml 写 `<dc:source>` 记录"由哪张图导出"——但**6 个月后 Word 打开这个 docx 想"看回原图"无路径**。补：core.xml 写完整 imageDataUrl hash（不回填 dataUrl，仅 hash + 时间），UI 上 hover 显示 |

---

### 🟡 P2（润色 · 1 个月内 · 20 项）

| # | GAP | 位置 | 估行 | 描述 |
|---|---|---|---|---|
| **V10-P2-1** | **产物"标签"机制缺失**（截图/OCR/AI 三套历史） | 跨 `setOcrHistory` / `setConvIndex` / `setHistory` | 50 | 加 `tags?: string[]` 到 `OcrHistItem` / `AiConvMeta` / `HistoryItem`；UI 加标签过滤 chip |
| **V10-P2-2** | **产物"收藏/置顶"机制缺失** | 跨三套历史 | 35 | 加 `pinned?: boolean`，UI 加 ⭐ 按钮，置顶项永远排顶部 |
| **V10-P2-3** | **产物"归档/隐藏"机制缺失** | 跨三套历史 | 25 | 加 `archived?: boolean`，默认隐藏归档项，可"显示归档"切换 |
| **V10-P2-4** | **AI 文档无"重生成历史"对比** | `aiStore.ts:1045` reset | 80 | 用户想"试试不同 prompt 重生成同一截图"无对比视图。补：fork 时自动保留 3 个版本，新版本与旧版本 split 对比 |
| **V10-P2-5** | **产物"按尺寸/格式/类型"过滤** | 截图历史 | 25 | v2 P-MGT-7 路线图"全屏/区域/窗口/滚动"过滤未落地 |
| **V10-P2-6** | **OCR 历史无"按语言分组"** | OCR 历史下拉 | 12 | zh/en/ja 混在一列难找，按语言分组 |
| **V10-P2-7** | **AI 历史无"按是否导出过"过滤** | `aiStore.ts` + UI | 25 | 用户找"还没导出 docx 的"AI 文档无路径 |
| **V10-P2-8** | **导出 docx 内嵌图无 alt 文本**（无障碍 a11y） | `markdownDocx.ts:197-232` | 8 | ImageRun 没设 `alt` — 屏幕阅读器读不出 |
| **V10-P2-9** | **导出 PDF 不支持深色主题** | `markdownHtml.ts:139-200` THEME_CSS | 20 | 5 主题全亮色 — 用户深色模式下导出 PDF 刺眼 |
| **V10-P2-10** | **导出文件无"水印"**（防止截图工具截图转发） | `markdownDocx.ts` 封面 | 35 | 文档底部加"由 SnapCraft 生成 · 2026-07-17"水印（可关）|
| **V10-P2-11** | **导出 html 不携带 mtime** | `markdownHtml.ts` | 8 | mtime 应在 meta 标签，方便文件系统检索 |
| **V10-P2-12** | **AI 文档无"导出次数"** | `aiStore.ts` | 15 | 同一 hash 导 docx 3 次无记录 — 与 P0-7 关联但更轻量 |
| **V10-P2-13** | **截图历史无"按文件大小"过滤** | `EnhancedScreenshotApp.tsx` | 15 | 50+ 截图时大文件（4K 全屏）/小文件（区域）混排无分组 |
| **V10-P2-14** | **OCR 历史无"按识别耗时"排序** | OCR 历史 | 12 | 慢识别（如 5s+）的图往往更值得保留 |
| **V10-P2-15** | **AI 文档无"按 token 成本"过滤** | `AIPanel.tsx` 列表 | 15 | "本月 $5 以上"过滤 |
| **V10-P2-16** | **AI 文档无"按模型"分组** | `AIPanel.tsx` 列表 | 12 | gpt-4o vs claude vs gpt-4o-mini 产出文档风格不同，分组看 |
| **V10-P2-17** | **多截图成稿无"图片说明自动生成"** | `AIPanel.tsx:610-620` orderedImages caption | 30 | 当前 caption 全是 "附加 N"——AI 没自动描述。补：首轮 prompt 追加"为每张图生成 5 字说明" |
| **V10-P2-18** | **导出 docx 无"作者/公司"元信息填写** | `markdownDocx.ts:382-389` core.xml | 8 | 用户文档写公司名要手动加 |
| **V10-P2-19** | **AI 历史无"按消息数"过滤** | `AIPanel.tsx` 列表 | 10 | 多轮迭代的成稿 vs 单轮一次性 |
| **V10-P2-20** | **AI 文档无"按是否用 cache"过滤** | `AIPanel.tsx` 列表 | 15 | 命中提示缓存的会话省钱 |

---

### 🟢 P3（低优先级细节 · 8 项）

| # | GAP | 位置 | 估行 | 描述 |
|---|---|---|---|---|
| **V10-P3-1** | **产物无"统一 hash 算法"** | 全部 3 套 | 15 | OCR 块用 imageDataUrl 长度，AI 用 imageDataUrl 前 1024 字符 + 长度——3 套 key 可能冲突 |
| **V10-P3-2** | **产物无"统一 ID 格式"** | 全部 3 套 | 10 | `ai-conv:<hash>` / `history:<uuid>` / `ocr-hist:<ts>`——3 套命名空间不一致 |
| **V10-P3-3** | **OCR 临时文件清理时机** | `commands/ocr.rs:48,57` | 8 | 失败分支 `let _ = std::fs::remove_file(&tmp)` 是 best-effort——若 remove 也失败，临时文件残留 |
| **V10-P3-4** | **多窗口并发 OCR 无排队** | `EnhancedScreenshotApp.tsx` | 25 | v8 P1-11 提了 OCR 异步并发无限流未落地 |
| **V10-P3-5** | **AI 流式中断后** abort 后 memory 残留 | `aiStore.ts:1039-1042` stop | 10 | `abortCtl?.abort()` 仅停流式，thinking/agentSteps 状态可能不一致 |
| **V10-P3-6** | **AI 历史卡片"上次访问时间"未记录** | `AiConvMeta` | 15 | 用户 6 个月后看历史卡，谁先看？无 LRU |
| **V10-P3-7** | **导出 docx/pptx 无"目录页"（v8 P1-37 已列，未完全落地）** | `markdownDocx.ts:71-103` buildToc | 15 | TOC 跳到错位置（h2/h3 序号）|
| **V10-P3-8** | **产物"按状态"过滤**（草稿/已导出/已分享/已归档）| 跨三套 | 30 | v2 P-MGT-2 路线图未落地 |

---

## 二、按 v10 新轴 4 维重新归类（v1–v10 综合）

| 维度 | v1–v9 覆盖 | **v10 补强（真正新发现）** | 本轮 P0/P1 命中 |
|---|---|---|---|
| **持久化** | 5 key / 100 条 / agentSteps / thinking / sourceShotId / convHash | **产物间四向链接 / 编辑后独立历史 / 三套历史打通 / 关系映射 / 元信息指纹 / 版本号** | P0-1~4, P0-5~10, P1-7~16 |
| **管理** | AI 搜索 / 标签 / 批量 / 时间分组 / 最近导出 | **时间轴统一视图 / 导出后变更追踪 / 按时间分目录 / 全内容全文搜 / 最近 20 导出** | P0-18~22, P1-11~16 |
| **下载** | 默认目录 / Reveal / 6 格式记忆 / 批量 Reveal | **二次加工 / 替换图 / 续写 / 另存为模板 / 同名多格式** | P0-11~17, P1-17~24, P1-37~39 |
| **预览** | HTML Blob / 主题切换 / docx 缩略 / Pin 窗 OCR / AI Diff | **用 Word 打开 / 导出后验证 / 详情模态 / 系统分享面板 / 历史版本对比** | P0-11, P1-33~36, P1-39 |

---

## 三、v9 vs v10 GAPS 覆盖关系

| 主题 | v1–v9 命中 | v10 命中 | 关系 |
|---|---|---|---|
| OCR bbox 落库 | v5 V4-5 + v8 P1-8 | — | 已闭环 |
| API Key 加密 | v9 C-1 | — | 已闭环 |
| abortCtl 单例 | v8 P0-1 | — | 已闭环 |
| PPTX 时间戳硬编码 | v8 P0-3 | V10-P0-5（DOCX core.xml 一样硬编码）| **v10 扩到 docx/xlsx/core.xml** |
| 文件名 NFC | v8 P0-4 | V10-P0-6（嵌入 hash 短码）| **v10 扩到版本号** |
| agentSteps 落盘 | v8 P0-6 | V10-P0-7（列表显示）| **v10 改 UI 显示** |
| sourceShotId | v8 P0-9 | V10-P0-1（四向链接扩展）| **v10 打通下游** |
| 5 try/catch 静默 | v8 P0-10 | V10-P1-3（OCR 重试）| **v10 落到 OCR** |
| HTML 在线编辑 | v2 P-DOC-3 | V10-P0-11（用 Word 打开）| **v10 走更现实的 OS 默认程序** |
| 主题预览图 hover | v4 V4-13 | V10-P1-17（5 主题对比）| **v10 升级** |
| AI 历史时间分组 | v3 V3-6 | V10-P0-18（三流时间轴）| **v10 跨类型合并** |
| 全量数据导出 | v9 C-7 | V10-P1-16（README+CSV 索引）| **v10 补索引** |
| 标签收藏归档 | v2 P-MGT-2 | V10-P2-1/2/3 | **v10 拆 3 项** |
| 标注 ⌘CVD | v8 P1-51 | V10-P1-32（ghost preview）| **v10 补视觉反馈** |
| OCR 失败重试 | v6 P2-1 / v8 P1-12 | V10-P1-3 | **v10 落到 OCR 命令** |
| Fork 树 | v7 P1-36 | V10-P1-13（树状图）| **v10 落到可视化** |

---

## 四、Top 12 必做（按 ROI × 铁律兼容 × 用户旅程）

| 排序 | 项 | 估行 | 收益 | 铁律 |
|---|---|---|---|---|
| 1 | **V10-P0-1 产物四向链接** | 40 | 极高（整条产物流打通）| ✅ 纯前端 |
| 2 | **V10-P0-5 导出文件元信息统一** | 60 | 极高（6 个月后还能追溯）| ✅ 纯前端 |
| 3 | **V10-P0-11 "用 Word 打开"按钮** | 25 | 极高（导出→再加工最后一公里）| ✅ + 1 Rust |
| 4 | **V10-P0-18 三流合一时间轴** | 80 | 极高（"我今天做了什么"一目了然）| ✅ 纯前端 |
| 5 | **V10-P0-7 AI 历史成本徽标** | 30 | 极高（成本控制）| ✅ 纯前端 |
| 6 | **V10-P0-22 最近 20 导出列表** | 35 | 极高（"刚才导的那个"找回）| ✅ 纯前端 |
| 7 | **V10-P0-19 导出后变更追踪** | 25 | 高（避免用过时 docx）| ✅ 纯前端 |
| 8 | **V10-P0-6 文件名 hash 短码** | 12 | 极高（5 分钟级修复）| ✅ 纯前端 |
| 9 | **V10-P1-21 AI 历史全内容全文搜** | 30 | 极高（v7 路线图落地）| ✅ 纯前端 |
| 10 | **V10-P1-9 AI 流式断点续传** | 60 | 极高（核心场景，v8 路线图）| ✅ 纯前端 |
| 11 | **V10-P0-13 文档续写入口** | 35 | 极高（导出后增量编辑）| ✅ 纯前端 |
| 12 | **V10-P0-15 另存为模板** | 35 | 极高（高价值用户复利）| ✅ 纯前端 |

**12 项合计：~485 行 / 1 Rust / 0 新依赖**。

---

## 五、本轮（v10）即时落地候选（互不冲突）

### 候选 A · 产物元信息 + 二次加工（极小、极快）
- V10-P0-5 导出文件元信息统一（60 行）
- V10-P0-6 文件名 hash 短码（12 行）
- V10-P0-11 "用 Word 打开"按钮（25 行 + 1 Rust）
- V10-P1-34 导出后验证按钮（20 行）
- **总 ~117 行 / 1 Rust / 0 新依赖**

### 候选 B · 产物关系网（中等、整链）
- V10-P0-1 产物四向链接（40 行）
- V10-P0-2 编辑后独立历史（25 行）
- V10-P0-3 三套历史打通（50 行）
- V10-P0-7 AI 历史成本徽标（30 行）
- **总 ~145 行 / 0 Rust / 0 新依赖**

### 候选 C · 时间轴 + 发现（中等、长期价值）
- V10-P0-18 三流时间轴视图（80 行）
- V10-P0-22 最近 20 导出（35 行）
- V10-P0-19 导出后变更追踪（25 行）
- V10-P1-21 AI 历史全内容全文搜（30 行）
- **总 ~170 行 / 0 Rust / 0 新依赖**

### 推荐：**A + C 并行**（~287 行 / 1 Rust / 0 新依赖）— 覆盖"导出后管理 + 长期发现"两条主线。

---

## 六、覆盖性自查（v1–v9 已写过的相似 GAPS，避免重复）

- v1 Phase 1–30 全部 → v10 提的"产物关系网"是 v1 未触达的整链维度
- v2 47 项 → v10-P2-1/2/3 标签/收藏/归档路线图 v2 P-MGT-2 早列过，v10 拆 3 项
- v3 P0 三件套 → v10-P0-11 "用 Word 打开" 走 v3 的 reveal 思路（同一 Rust 能力）
- v4 31 项 → v10-P0-18 时间轴 v4 V4-9 路线图，v10 升级为三流合一
- v5 V4-7 OCR 清洗 UI → v10-P1-3 OCR 重试路线图与 v5 互补（v5 是 UX，v10 是 robustness）
- v6 70 项 → v10-P0-1/2/3 关系网是 v6 提"双视图联通 P-MGT-1"的整链升级
- v7 85 项 → v10-P1-13 fork 树可视化是 v7 P1-36 路线图落地
- v8 70 项 → v10-P0-5 元信息是 v8 P0-3/4 的扩到 docx/xlsx，v10-P0-22 最近导出是 v2 P-UI-4 升级版
- v9 63 项 → v10-P0-7 成本徽标是 v9 B-1 月度聚合的列表级补强

---

## 七、状态
- 0 代码改动 ✓
- 0 警告 ✓
- 0 新依赖 ✓
- 严守"诊断优先、不擅加未请求功能" ✓
- 严守"v1–v9 已覆盖的不重复" ✓
- 等用户拍板 A/B/C 候选后立即执行

## 八、交付物
本报告：`AI助手-需求发现v10-产物持久化×下载×预览×管理-2026-07-17.md`
**合计去重 89 项独立 GAPS / P0=22 / P1=39 / P2=20 / P3=8**。
