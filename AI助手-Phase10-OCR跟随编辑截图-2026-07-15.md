# AI 助手 Phase 10 — OCR 文字跟随「编辑后截图」（候选④落地）

> 参考 `privdoc-ai` 的 agent 能力，持续把 SnapCraft 从「截图 + 截图编辑」升级为「编辑截图 → AI 生成完整文档 / 文案」。
> 本轮在不影响现有功能的前提下，补上长期候选清单里的 ④：让发给 AI 的 OCR 文字也跟随用户编辑后的截图。

## 需求发掘（根因确诊）

复盘 Phase 1–9 后，发现一处**隐私 / 准确性断点**仍未被彻底闭合：

- Phase 8 已让 AI **看到的图** = 底图 + 全部标注（打码 / 模糊已合入），敏感信息不会出现在视觉里；
- 但 `AIPanel` 收到的 **OCR 文字** 仍取自**原始截图**的 OCR（`ocrResult?.text`），与编辑后的图并不同步。

后果：用户在图上**打码 / 模糊**了某段文字后，模型虽「看不见」那段字，却仍能通过 **OCR 上下文**读到原文 ——
即「编辑截图 → AI 生成文档」这条主链路仍存在敏感信息经 OCR 旁路泄漏的隐患，且 AI 拿到的文字与所见画面不一致、成稿易出错。

对齐 privdoc-ai：其纯文档工具无图、无此问题；而截图工具的独有价值正是「所见即所得」，OCR 上下文必须与可见画面一致。

## 实现（纯前端，零 Rust 改动，不影响截图 / 标注 / OCR / 历史 / 导出）

复用既有 `ocr_image` Tauri 命令（接受 `data_url` 返回 `{text, blocks}`，macOS=Apple Vision / Windows=WinRT），在「同步最新编辑」链路里补一段 OCR：

两个编辑器均改（`EnhancedScreenshotApp.tsx` / `EditorWindow.tsx`）：

- 新增 `aiOcrText` 状态：默认等于原图 OCR；编辑 / 打码后由 `refreshAiVision` 重算。
- 新增 `ocrResultRef`：用 ref 持有最新 OCR 结果，供 `refreshAiVision` 在不重建回调的情况下读取（避免 stale closure）。
- `refreshAiVision`（「同步最新编辑」按钮 + 打开面板即触发）改为：
  1. 合层得到 `merged`（编辑后图）；
  2. **仅当 `merged` 与原图不同**（确实做了标注）才重跑 `ocr_image(merged)`，`aiOcrText` 取编辑后文字；否则沿用原 OCR —— 避免无谓耗时；
  3. 任一环节失败都回退「原图 + 原 OCR」，保证 AI 始终有图 / 有文字可看。
- 新增同步 `useEffect`：原图 OCR 结果变化（首次识别 / 重识别 / 切换截图）时，把 `aiOcrText` 重置为原图 OCR；用户编辑后由 `refreshAiVision` 覆盖为编辑后 OCR。
- `AIPanel` 的 `ocrText` prop 由 `ocrResult?.text` 改为 `aiOcrText`。

关键约束：重跑的编辑后 OCR **不写回** `set_screenshot_ocr`（历史里仍存原始 OCR，仅供其它功能使用），AI 上下文独立、零污染。

## 验证

- `pnpm build`：exit 0，136 模块，零类型错误，无回归（chunk >500kB 为历史既有提示，非错误）。
- 行为对照：
  - 无标注 / 未点同步：AI 收到的 OCR = 原图 OCR（与旧行为一致）；
  - 打码 / 模糊后点「🔄 同步最新编辑」：AI 收到的图与 OCR 均为编辑后版本，打码区原文不再进入上下文。

## 与 privdoc-ai 的差异

其记忆 / 对话用 Rust + SQLite 且为纯文本；本方案仍是前端 localStorage 按截图分桶、零后端风险，且把「编辑后截图（图 + OCR 双重对齐）」作为 AI 上下文锚点 —— 截图工具的独特价值在此闭环。

## 后续候选（未做，按优先级）

1. ① 跨截图会话列表（管理多张截图各自的 AI 对话 / 成稿）；
2. ② 后端代理隐藏 Key（需改 Rust，风险较高，待用户确认）；
3. ③ 拖拽重排多截图选择顺序（报告章节排序 UX）；
4. （④ 本轮已落地）。

`./start.sh dev` 或 `pnpm build && ./start.sh app` 生效。
