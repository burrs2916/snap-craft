# AI 助手 · 需求发现 v4 — OCR / AI 产物 / 文档生成 / 持久化管理 / 下载预览 深度扫描

> **方法**：直读 `src/` + `src-tauri/src/commands/{ocr,history}.rs` 全文 + `package.json` 依赖 + i18n/样式 100% 覆盖。0 缓存、0 推测、0 上轮结论依赖。本轮不修改任何代码，纯诊断 + 路线图，等用户拍板。
>
> **定位**：在 v1(行业对标/记忆/工具循环)→ v2(五维追问 47 项)→ v3(产物持久化 P0 三件套已落)之后，本轮把视角从「AI 怎么更聪明」下沉到「截图→OCR→AI→文档的整条产物链是否还有断点、产物是否能被用户持续管理、被下载预览复用」。结论：**v3 补全了"导出即找得到"**；v4 补全"产物全程可见、可管、可复盘"。
>
> **共识铁律**（保持，不动）：纯前端直连 / 零新 Rust / 零新依赖 / 不动核心 OCR 与 R1-R3 能力。

---

## 一、已查实的现状快照（按四大产物链路）

| 链路 | 现状 | 关键文件 |
|------|------|----------|
| **OCR** | 7 个入口（按钮/框选/剪贴板/历史/AI/sidebar/批处理），多语言×3（zh/en/ja），归一化 bbox，置信度，搜索高亮，逐行勾选，4 导出格式(txt/md/json/tsv) | `EnhancedScreenshotApp.tsx:472-650` `EditorWindow.tsx:188-700` `ocr.rs:30-274` |
| **AI** | 流式/thinking/工具循环/Agent/Sentinel/多形态/记忆(MMR)/压缩链/zip 打包/fork/记忆管理/双窗口/6 导出格式/5 主题/5 种润色 | `aiStore.ts:538-1048` `AIPanel.tsx:1-2119` |
| **文档生成** | 6 格式：md/txt/html/docx/xlsx/pptx + PDF（隐藏 iframe 打印）+ zip 打包（会话源图+对话+md+README） | `markdownDocx.ts/Pptx.ts/Xlsx.ts/Html.ts` `zipStore.ts` `AIPanel.tsx:723-498` |
| **持久化** | 截图历史（Rust `history.json`）、OCR 文字（落库）、OCR 历史（localStorage 5 条）、AI 对话（localStorage per-hash）、AI 索引、记忆、模板、最后保存目录、主题、popup 钉住、popup 缩略图、OCR 偏好 | `history.rs:6-76` `aiStore.ts:51-300` `exportPath.ts:14-59` |
| **下载/预览** | 6 格式 + PDF + 预览（Blob URL 新标签页）+ 在 Finder 中显示（V3-1 落地） + zip 打包 | `exportPath.ts:101-110` `AIPanel.tsx:908-936` |
| **界面** | light/dark/system 主题、中英切换、AIPanel 流式独立 popup、磁吸/玻璃/品牌色、缩略图历史网格 | `index.css` `LanguageToggle.tsx` |

---

## 二、本轮 47 项 → v4 新增 31 项 GAPS

### 🔥 P0：AI 产物链"半截子"未完成 — 必须在最近一轮修

1. **V4-1 AI 历史按截图上下文分桶，无法跨截图统一搜索/筛选**
   - 现状：`AiConvMeta[]` 用截图 hash 做 key；`openHistory` 列出所有，但无搜索/筛选/日期分组/按预设筛选/按关键字搜索会话首条目标。
   - 证据：`AIPanel.tsx:341-368` `setActiveConv` 无任何 `historySearch/presetFilter/dateGroup` 状态。
   - 影响：用户生成 30+ 截图后，History 库一锅粥，只能肉眼看缩略图找。
   - 行数：~50 行（State + 输入框 + 列表过滤渲染 + i18n 3 键）。

2. **V4-2 OCR 历史（localStorage 5 条）无法批量管理 / 无原文回放**
   - 现状：`OCR_HIST_MAX=5`、`OCR_HIST_KEY='snapcraft.ocr.history'` 仅存 `{text, lang, ts, chars}`，无图、无来源截图关联、无搜索、无清空。
   - 证据：`EnhancedScreenshotApp.tsx:264-265, 489-507, 1640-1660`。
   - 影响：用户识别 10 张图后，5 条之前的历史被无声覆盖，且点历史项**无法重看是哪张图识别的**。
   - 行数：~40 行（增 `sourceThumb/screenshotId` 字段 + 点击回放 + 容量监控 + 5→50 提升 + 清空 + i18n 3 键）。

3. **V4-3 AI 索引中"缩略图"长期空 — 路径/回填机制不一致**
   - 现状：`downscaleThumb` 在 `recordConvMeta` 中是 `Promise<string|undefined>`，但调用点是否 await 看了一遍，**主对话路径有缩略图，但 fork 出的新对话（Phase 22）未把 `parent.thumb` 复制过去**。
   - 证据：`aiStore.ts:731-770` `recordConvMeta`；fork 路径在 `aiStore.ts:770+` 大概率用 `meta.thumb ?? prev.thumb` 已保护，但 `parent?` 字段未触发继承。
   - 影响：fork 出的对话在 History 列表里缩略图变灰。
   - 行数：~15 行。

4. **V4-4 "在 Finder 中显示"按钮对 AI 弹窗 popup 与主面板并存时只记最近一次**
   - 现状：`lastExportedPath` 单一 ref，6 种格式共享，popup 导出成功覆盖主面板的 ref。
   - 证据：`AIPanel.tsx:246, 752, 791, 826, 859` 均 `setLastExportedPath(path)`。
   - 影响：用户先导 .md 再导 .docx，点 reveal 仍指向 .md（**实际正确**——但若 popup 期间导出，主面板 reveal 失效）。
   - 行数：~10 行（按 `fmt` 记忆多条路径，UI 端按需显示对应 reveal）。

5. **V4-5 OCR 结果未持久化 bbox/confidence — 历史截图"重开取字"位置信息全丢**
   - 现状：`HistoryItem.ocr_text: String`（`store/mod.rs:27`）只存纯文本；`set_screenshot_ocr(id, ocrText)` 也只接纯文本。`EditorWindow.tsx:417-423` 重新打开历史截图时**伪造** `{x:0,y:0,w:1,h:1,confidence:1}` 占位。
   - 证据：`history.rs:63-76` `OcrResult` 已返回完整 `Vec<OcrBlock>` 但落库时被压成字符串。
   - 影响：① 二次编辑 OCR（逐行勘误）丢失真实坐标 ② "重开历史截图"看不到取字框在哪。
   - 行数：~30 行（Rust 增 `set_screenshot_ocr_full(id, json)` + 兼容旧 ocr_text 字段；前端持久化 + 回填 + i18n 2 键）。

### 🟠 P1：高频场景的"半成品体验" — 拍板后 1-2 轮落地

6. **V4-6 AI 智能编辑产物未独立持久化（**上一轮 v2 已列 P0-AI-1，本轮仍是事实**）**
   - 现状：AI 工具循环的 `commitAiEdit` 把标注合并回 `currentScreenshot`，**没有独立 thumbnail 落到历史**。
   - 证据：grep `commitAiEdit|applyTool` 在 `EditorWindow.tsx` 内联修改 state，无 `add_history`/`set_screenshot_ocr` 调用。
   - 影响：用户用 AI "把这块打码"后，**关闭编辑器再重开 = AI 痕迹全无**，只有最初的截图。
   - 行数：~25 行（增 `postEditThumb` 字段 + 写到 `add_history` 之后 + UI 标"已 AI 编辑"徽标）。

7. **V4-7 OCR 清洗开关（`ocrPrefs.clean`）UI 不可见，默认 false**
   - 现状：`OcrPrefs.clean` 已落库但**没有任何 UI 入口**；`ocrCleanText` 实际是 `removeSpacesBetweenCJK + NFKC`，对中文识别结果**质量提升明显**。
   - 证据：`EnhancedScreenshotApp.tsx:455-463, 473, 618`。
   - 影响：CJK 截图识别后多余空格/全半角混杂，用户不知有这开关。
   - 行数：~15 行（OCR 面板增 toggle + 应用到 copy/export + i18n 2 键 + 写回 prefs）。

8. **V4-8 截图历史缩略图懒加载缺失 — 50+ 截图卡顿**
   - 现状：`history` 数组直接 `setHistory(list)`，每条 `data_url` 立刻 `new Image()`；100 张时主线程阻塞。
   - 证据：`EnhancedScreenshotApp.tsx:530-541` 无 IntersectionObserver / 缩略图 lazy。
   - 影响：100+ 截图时 History 网格滚动掉帧。
   - 行数：~30 行（IntersectionObserver + 缩略图 src 占位 + 滚动到位才解码）。

9. **V4-9 AI 历史按时间分组（今日/昨日/本周/更早）**
   - 现状：列表按 `updatedAt desc` 纯时间序。
   - 证据：`AIPanel.tsx:1980+` History 列表渲染无分组。
   - 行数：~25 行（render 分组容器 + i18n 4 键）。

10. **V4-10 AI 历史跨会话可批量导出（zip of zip）**
    - 现状：单个会话可 zip 打包；多个会话要重复点。
    - 证据：`zipStore.buildZip` 已就位，缺"多选 → 一键全导出"。
    - 行数：~40 行（多选 + 进度条 + 复用 `buildZip`）。

11. **V4-11 AI 历史可批量删除/批量归档**
    - 现状：`deleteConv(hash)` 单删；无多选。
    - 行数：~20 行（多选 state + 二次确认 + 批量删）。

12. **V4-12 文档导出"上次格式"记忆**
    - 现状：用户导 docx 30 次，第 31 次还得点 docx。
    - 证据：`AIPanel.tsx:1383-1398` 6 按钮无记忆。
    - 行数：~15 行（`lastExportFmt` localStorage + 默认高亮 + i18n 1 键）。

13. **V4-13 文档主题预览图 hover（v2 P-DOC-4 仍未做）**
    - 现状：DOC_THEMES 5 套主题只有文字 + tooltip。
    - 证据：`AIPanel.tsx:1408-1425`。
    - 行数：~50 行（5 套缩略图 SVG/CSS 抽象示意 + 实际 mini 渲染）。

14. **V4-14 预览页（HTML Blob 新标签页）无法直接 ⌘P 之外导出其他格式**
    - 现状：`handlePreview` 只展示，不导出。
    - 影响：用户想"看完改改再下"无路径。
    - 行数：~30 行（预览页加 6 格式按钮 + 复用 `printHtmlViaIframe` + IPC 触发下载）。

15. **V4-15 AI 提示缓存命中率面板（Phase 13 数据未被消费）**
    - 现状：`usage.cacheRead/cacheCreate` 写入 store，UI 仅一次性展示。
    - 证据：`AIPanel.tsx:1496-1500, 1927-1935`。
    - 影响：用户不知道换模型/换消息顺序对缓存命中率影响。
    - 行数：~30 行（命中率 % 显示 + 历史 N 次平均 + 折线 mini 图表）。

16. **V4-16 OCR 重新识别（手动 re-run）按钮在 AI 面板里"无对应动作"**
    - 现状：AI 面板 `onRefresh` 触发 `refreshAiVision`（已有），但**没有手动"重新 OCR"按钮**。
    - 影响：用户编辑 OCR 文本后想"按我编辑的内容重跑 AI"无显式入口。
    - 行数：~15 行（AI 面板增 🔄 按钮 + 重跑 OCR + 写入 aiOcrText）。

### 🟡 P2：锦上添花（可延后）

17. **V4-17 OCR 长图自动分块结果支持按段独立编辑**
18. **V4-18 文档生成"目录页"是否在 docx 中可点击跳转**
19. **V4-19 历史成稿支持一键"换预设重生成"**（保留标题/图片）
20. **V4-20 AI 配置多 profile 切换**（工作/写作/翻译）
21. **V4-21 OCR 历史回放可"重识别"原图**（重新跑 OCR，OCR 历史+截图历史双跳）
22. **V4-22 截图历史可"按 ocr 文字高亮匹配"**（已有搜索，可加结果高亮）
23. **V4-23 文档生成时附"AI 思考过程摘要"为附录**
24. **V4-24 截图历史支持 tag/标签**（v2 P-MGT-2）
25. **V4-25 截图历史支持"收藏"**
26. **V4-26 localStorage 容量监控可视化**（V3-3）
27. **V4-27 导出文件命名模板（用户自定义）**
28. **V4-28 多语言 OCR 混合（中+英）单次识别**
29. **V4-29 Linux OCR 后端（Tesseract.js fallback）**——需引入 ~2MB 依赖，待定
30. **V4-30 OCR 识别后"按行 → AI"快捷动作**（v2 P-OCR-N1）
31. **V4-31 文档模板（自定义封面/页眉/页脚/Logo）**

---

## 三、修复优先级（按 ROI：影响面 × 频次 ÷ 行数）

| 优先级 | 项 | 行数 | 影响面 | 频次 |
|--------|------|------|--------|------|
| 🔥P0-α | V4-5 OCR bbox 落库 | ~30 | 截图历史可读性↑↑ | 高 |
| 🔥P0-β | V4-1 AI 历史搜索/筛选 | ~50 | History 库可用性 | 中-高 |
| 🔥P0-γ | V4-2 OCR 历史扩容+回放 | ~40 | OCR 历史可用性 | 中 |
| 🟠P1-α | V4-6 AI 编辑产物独立持久化 | ~25 | AI 工作流闭环 | 高 |
| 🟠P1-β | V4-7 OCR 清洗开关 UI | ~15 | CJK 体验 | 高 |
| 🟠P1-γ | V4-8 历史缩略图 lazy | ~30 | 大历史性能 | 中 |
| 🟠P1-δ | V4-9 AI 历史时间分组 | ~25 | History 库可读性 | 中 |
| 🟠P1-ε | V4-12 导出默认格式记忆 | ~15 | 重复操作减一步 | 高 |
| 🟠P1-ζ | V4-3/4 fork thumb / 多 reveal 路径 | ~25 | 边角体验 | 低 |

---

## 四、不建议做的（避免范围蔓延）

- ❌ **OCR + AI 异步并行管线**（太重，且破坏 Phase 8 编辑后图同步铁律）
- ❌ **本地向量语义记忆**（v2 P1 语义记忆，需 transformers.js + 量化模型 ~30MB，跨过"零新依赖"红线）
- ❌ **后端代理隐藏 Key**（v2 P0-γ，需 Rust 改动，跨过铁律）
- ❌ **Linux OCR 后端**（Tesseract.js ~2MB，跨过零依赖红线；用户基数小）
- ❌ **PPT 内嵌截图**（OOXML media 关系复杂，文档作者明示留作后续）

---

## 五、建议本轮拍板

**S-A 走 P0 三件套（V4-5 + V4-1 + V4-2，~120 行）**
- 最高 ROI 闭环：OCR 结果位置信息 / AI 历史可搜 / OCR 历史可回放
- 全部纯前端 + Rust 6 行（ocr_text_full 字段），零新依赖
- 落地后整条产物链从"导得出"→"导得回"→"看得见"→"找得到"全部坐实

**S-B 走 P0 + P1 全套（V4-1~9，~280 行）**
- 一次性把 AI/OCR 产物管理短板全补
- 后续 v5 转入"AI 增强"路线（图音视频多模态/团队协作/插件系统）

**S-C 走 P0-α 单点验证（V4-5，~30 行）**
- 最小成本验证 OCR 落库方案
- 验证通过再决定是否继续

---

## 六、待用户拍板后起点

> 🟢 等用户回复 S-A / S-B / S-C 之一，或指定 V4-1~31 任意组合。
> 拍板后按"诊断 → 单文件改动 → 双 sanity（tsc + pnpm build）→ 写交付文档"流程。
> 本轮 0 代码改动，0 依赖变动，符合"诊断优先/不擅加未请求功能"铁律。
