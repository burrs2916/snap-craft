# OCR 增强 · 第十六轮（需求攫取 → 剪贴板取字生产级加固）

> 执行时间：2026-07-14　|　轮次：16（自动化 hourly 触发）　|　聚焦：OCR 需求发现 + 剪贴板取字生产级可用性

---

## 一、需求攫取（OCR 文字识别功能端到端盘点）

前 15 轮已交付的能力（收敛在 OCR 面板 + 隔离 localStorage + 4 个取字入口）：

| 维度 | 已交付 |
|---|---|
| 取字入口 | ① 截图取字 ② 结果条取字 ③ 历史网格取字 ④ **剪贴板取字**（本轮聚焦项） |
| 识别质量 | 语言选择(auto/zh/en)、置信度感知+阈值滑块、智能阅读顺序(多列/竖排)、合并为一行 |
| 编辑/提取 | 逐行内联纠错、逐行勾选(精准提取)、智能实体提取(URL/邮箱/电话)、智能文本清洗 |
| 导出/复制 | 复制全部、带坐标导出(txt/md/JSON/TSV)、自动复制、面板内重识别、结果条/历史一键取字 |
| 体验 | 偏好持久化、耗时+统计、本地历史(最近5)、面板字号、搜索高亮+命中跳转、空间预览联动、实体可点击打开 |

### 剩余候选需求 × 可用性 × 风险矩阵

| 候选需求 | 可用性 | 风险 | 本轮决策 |
|---|---|---|---|
| **剪贴板取字生产级加固**（大图/文件回退/错误分级/防重入） | 高 | 低 | ✅ **本轮落地** |
| 剪贴板 Cmd+V 直接粘贴取字 | 中 | 中（需避免与文本输入框/全局快捷键冲突） | ⏸ 留评估（不碰全局，避免回归） |
| 翻译（OCR 结果多语翻译） | 中 | 高（需离线模型或联网，超出本地工具定位） | ⏸ 持续延后 |
| 多区域批量 OCR | 中 | 中 | ⏸ 延后 |
| PDF / 长图整页 OCR（剪贴板来源扩展） | 低 | 中 | ⏸ 超出定位 |

**结论**：面板内增强已高度收敛，最高价值的增量是**把已有的「剪贴板取字」入口做到生产级**——这是用户明确点名的项，且完全增量、零耦合、不影响其它功能。其余候选或风险偏高、或与本地工具定位不符，本轮不做。

---

## 二、本轮落地：剪贴板取字生产级加固

> 仅改动 `read_clipboard_image` 后端命令 + `startOcrFromClipboard` 前端函数 + 2 个按钮 + 3 个 i18n 键 + 1 处防御性守卫。**未触碰**任何截图/标注/历史/钉图/主题/其它 3 个 OCR 入口逻辑。返回契约（PNG data URL 字符串）不变，前端零改造依赖。

### 2.1 后端 `src-tauri/src/commands/edit.rs`：`read_clipboard_image` 重写

1. **大图保护（核心生产痛点）**：长边超过 `CLIP_MAX_DIM = 3000px` 时等比缩放（Lanczos3）再编码。
   - 动机：4K/Retina 全屏截图复制到剪贴板常达 3000px+，原图直出 PNG data URL 动辄数 MB~数十 MB，经 Tauri IPC 回传会造成主线程卡顿、内存压力，且 OCR 引擎处理巨图变慢。Apple Vision / WinRT 在 3000px 长边下对截图文字精度基本无损。
   - 仅超限才缩放，小图零质量损失。
2. **文件回退（覆盖 Finder/资源管理器复制的图片文件）**：`get_image()` 读不到原始像素时，改走 `clipboard.get().file_list()` 读取首个图片文件，用 `image::open` 解码。初版只调 `get_image()`，遇到「复制图片文件」场景会直接失败。
3. **零尺寸守卫**：解码后宽高任一为 0 直接报错，避免产出损坏 PNG 导致 OCR 静默失败。
4. **错误分级**：区分「无图片」「复制的文件不是图片」「读取失败」，前端据此给精准文案。
5. **诊断日志**：`clog!` 记录原始/输出尺寸、是否缩放、payload 大小，便于线上排查。
6. **零新依赖**：复用既有 `arboard` / `image` / `base64`；`lib.rs` 命令注册行不变。

> 注：arboard 3.6.1 的文件列表 API 是 builder 形式 `clipboard.get().file_list()`（非 `get_files()`），已据此修正首编报错。

### 2.2 前端 `EnhancedScreenshotApp.tsx`：`startOcrFromClipboard` 加固

1. **飞行中锁防重入**：新增 `ocrClipBusyRef` + `ocrClipBusy` 状态；读图阶段连点只算一次，`ocrBusy`（OCR 阶段）也一并拦截。
2. **错误分级文案**：后端「没有图片」/「不是图片」→ `ocr.clipEmpty`；其余（初始化/编码失败）→ `ocr.clipFailed` 附原因。
3. **尺寸守卫**：解码尺寸为 0 时仍跑 OCR（OCR 不依赖 dims），但跳过基于坐标的「贴回标注」防 NaN（新增 `ocr.appliedZero` 提示）。
4. **按钮 busy 态**：主屏 `home-sec-btn` 与编辑器工具栏按钮在读取中显示「读取中…/Reading…」并禁用；编辑器按钮 `disabled={ocrBusy || ocrClipBusy}`。

### 2.3 i18n 新增键
- `ocr.clipBusy`：读取中… / Reading…
- `ocr.appliedZero`：图片尺寸无效，无法贴回标注 / Image has invalid size; cannot paste annotations

---

## 三、验收

| 项 | 命令 | 结果 |
|---|---|---|
| 后端类型/编译 | `cargo check`（src-tauri） | ✅ exit 0（1.74s） |
| 前端构建 | `pnpm build`（tsc && vite build） | ✅ exit 0（125 模块） |
| 产物校验 | dist JS 含 `ocr.clipBusy`/`ocr.appliedZero` 键 | ✅ |
| 契约不变 | `read_clipboard_image` 返回 PNG data URL 字符串 | ✅ 前端零改造 |
| 耦合检查 | 截图/标注/历史/钉图/主题/其它 3 个取字入口逻辑 | ✅ 未改动 |

### 生产级行为对照（修复前 → 修复后）
- 4K 全屏截图复制后取字：原图 ~10–30MB payload、OCR 巨图慢 → **缩放至 ≤3000px、payload 降为 MB 级、识别更快**。
- Finder 复制一张 PNG 文件后取字：初版报「无图片」失败 → **走文件回退成功识别**。
- 剪贴板为空时点按钮：无反馈/可能连点重复 → **精准「剪贴板中没有图片」提示 + 按钮禁用防重入**。
- 大图读取期间：按钮无状态 → **显示「读取中…」并禁用**。

---

## 四、仍留评估（延续，本轮不做）
- 剪贴板 Cmd+V 直接粘贴取字（需谨慎隔离文本输入框，避免全局快捷键回归）
- 翻译、多区域批量 OCR、PDF/长图整页 OCR（风险/定位边界）

OCR 4 个取字入口 + 面板增强现已高度收敛且剪贴板入口达生产级；后续如需突破需先放宽「不影响其它功能」约束或引入离线翻译资源。
