# AI 助手 Phase 30 — 自定义预设支持图文报告模式

> 日期：2026-07-16 ｜ 纯前端 / 零 Rust / 零新依赖 ｜ 增量增强（非重构）

## 根因诊断（先诊断，不闷头改码）

按「零 Rust / 零新依赖」铁律把剩余项盘了一遍后，确认：

- 预设管理 UI **已完整**（增删改 + localStorage 持久化，`addCustomPreset` / `updateCustomPreset` / `deleteCustomPreset` + `loadTemplates` 齐全）——**不是**记忆面板那种只读缺口。
- 但 `makeCustomPreset`（`src/features/ai/aiPresets.ts`）**硬编码 `buildUser: buildDefaultUser`**：无论用户怎么建模板，用户消息构造器永远是「普通 OCR 上下文」模式。
- 后果：用户若新建一个「多图报告」模板，它**不会产生 `<!--SNAP:k-->` 标记**，导致图文报告导出（Phase 19 的 `addAnnotation`/`getMergedImageDataUrl` 内嵌链路）时图片不内嵌——**功能能跑，但会误导用户**，属于真实可用性短板。

这是铁律内、书面路线图之外、且诊断出的真实限制，符合「优化=增量增强」。

## 修复内容（纯前端 / 零 Rust / 零新依赖 / 最小侵入）

| 层 | 改动 | 说明 |
|---|---|---|
| 类型 | `UserPreset` 加可选 `userBuilder?: 'default' \| 'report'` | 仅新增可选字段，向后兼容 |
| 构造器 | `makeCustomPreset` 据此选 `buildReportUser` / `buildDefaultUser` | `report` 走图文报告引导（产出 SNAP 标记），缺省 `default` |
| UI | 模板编辑表单加「消息模式」下拉（默认 / 图文报告） | 复用 `ai-tpl-input` 样式，明暗主题自适应 |
| 状态 | `openNewTemplate` 初值 `userBuilder:'default'`；`saveTemplate` 透传 `userBuilder` | — |
| Store | **零改动** | `addCustomPreset` / `updateCustomPreset` 已是 `{...p}` 透传整个 `UserPreset` |
| i18n | zh-CN / en-US 加 `templateBuilder` / `templateBuilderDefault` / `templateBuilderReport` | — |

**兼容性**：旧 localStorage 里的自定义预设无 `userBuilder` 字段 → `makeCustomPreset` 取 `undefined` → 回落 `default`，与改动前行为完全一致。

## 验证（全绿）

- `npx tsc --noEmit` → 0 错
- `pnpm build` → exit 0（141 模块，2.01s）
- **端到端**（esbuild → node 直跑 `makeCustomPreset`）：
  - `report` 模式走 `buildReportUser`（输出含「按顺序排列的多张截图…图文报告」引导段）✅
  - `default` 模式走 `buildDefaultUser`（不含上述引导段）✅
  - 两模式输出不同 ✅
  - 缺省 `userBuilder` 等同 `default` ✅
- `dist` 含 `templateBuilderDefault`×3 + `templateBuilderReport`×3 + `图文报告（多图`×1

## 用户可感知效果

模板管理 → 编辑/新建模板 → 选「消息模式：图文报告（多图 + SNAP 标记）」→ 保存。之后用该模板做「多截图成稿 / 图文报告」时，AI 输出会带 `<!--SNAP:k-->` 标记，导出 Word/PPT/HTML 时图片正确内嵌到对应章节。

## 路线图状态

至此，AI 助手模块在「零 Rust / 零新依赖」约束下：

- **书面路线图**：Phase 19–29 功能 + B1–B6 bug 全部交付。
- **额外增量**：记忆逐条管理（删除/编辑）、自定义预设图文报告模式——均为诊断出的真实缺口/限制。

**继续推进只能放宽某条铁律**（你拍板）：

- 🔥 **P0-γ 后端代理隐藏 Key** —— 需 Rust
- 🟠 **P1 本地语义记忆** —— 需 transformers.js 新依赖（**除非你特批放宽零新依赖**）

或者转去 EdgeView / biosphere 系列 / privdoc-ai 等项目。方向交给你。
