# AI 助手「生成文档」功能优化（第四轮 · 面板内富文本二次编辑后重导）

> 纯前端 + Tauri 配置级改动，零 Rust、零新依赖，`pnpm build` 绿。

## 背景
前三轮已落地：默认标签错位修复、GFM 表格预览、HTML 导出内嵌截图、复制为富文本、打开文件按钮、docx/pptx 元信息打通。
本轮落地复盘「发现未落地」清单第 ⑤ 项：**面板内富文本二次编辑后重导** —— 用户生成文档后常想微调文字再导出，此前 `output` 是只读展示，没有编辑入口。

## 实现

### 1. store 新增 `setOutput(text)`（`aiStore.ts`）
- 写回 `output`；
- **同步 conversation 末条 `assistant` 消息的 `content`**（若存在，避免误覆盖纯用户轮）；
- 调用 `saveConversation(convKey, conv)` **落盘**。
- 严守项目铁律「**conversation 是单一数据源，output 不另存**」：编辑结果进入会话单一数据源，因此
  「退出重开同一截图」恢复出来的也是**编辑后版本**，不存在 output 与 conversation 双写不一致的经典坑。

### 2. AIPanel 交互（`AIPanel.tsx`）
- 新增 `isEditing` / `editDraft` state，`handleToggleEdit`（进入时载入草稿 → 完成时 `setOutput` 写回）、`handleCancelEdit`（丢弃草稿）。
- 主操作区加「✏️ 编辑文档 / 完成」切换按钮（`t('ai.edit')`）。
- **编辑态禁用** 生成、复制、复制富文本、清空、新对话 —— 防止用旧 `output` 误导出、或误操作清空编辑。
- `ai-post`（导出 / 润色 / 回写区）在编辑态**整体隐藏**，代之以 markdown 源码 `<textarea>` 编辑块（含提示 + 完成 / 取消）。
- 编辑块完成写回后，对话流预览、6 格式导出、复制、应用内预览**全部基于编辑后内容**。

### 3. i18n（`zh-CN.json` / `en-US.json`）
新增键：`edit` / `editDone` / `editTitle` / `editHint` / `cancel`。

### 4. CSS（`index.css`）
新增 `.ai-edit-block` / `.ai-edit-head` / `.ai-edit-hint` / `.ai-edit-btns` / `.ai-edit-area`，复用 `--surface` / `--text` / `--border` 主题变量，浅 / 深主题统一。

## 设计取舍（诚实说明）
- **编辑形态选 markdown 源码 textarea，而非 contenteditable 富文本**：富文本 → markdown 逆向转换需 turndown 类库（违反零新依赖铁律）；textarea 编辑 markdown 对「文档生成器」完全够用，且导出 / 复制 100% 复用现有链路，零风险。
- 未引入所见即所得编辑器 —— 保持「增量增强、不推翻重构」原则。

## 验证
- 两个 locale JSON 均通过 `JSON.parse`；
- `pnpm build`（tsc + vite）**绿，0 error 0 warning**；
- 改动边界：仅前端 store / UI / i18n / CSS，**未触碰任何 Rust 源码**。

## 剩余（待拍板）
- ④ `doc` 预设图文穿插锚点：当前 `doc` 只把截图整块内嵌顶部，不产生 `<!--SNAP:k-->` 锚点，无图文混排；导出链路（docx/html/pptx）已完整支持 SNAP 标记 + sectionImages，核心缺口在**预设 prompt 引导 AI 在文中埋锚点**，属模型行为层面，需评估。
