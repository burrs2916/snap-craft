# SnapCraft · OCR 取字功能增强（第四轮 · 偏好持久化 + 自动复制 + 耗时统计 + 本地历史）

> 实施时间：2026-07-13。状态：前端 `pnpm build` 通过（125 模块，exit 0），未改动后端 Rust，未触碰截图/标注/滚动/历史等其它功能。
> 本轮在已有结构化 OCR（语言选择 / 逐行结果 / 区域框选 / 贴回标注 / 内联纠错 / 导出 / 搜索 / 即时重识 / 置信度 / 合并为一行）基础上，
> 做**四项纯前端、且全部收敛在 OCR 面板内 + 隔离 localStorage** 的增强。

## 一、需求的攫取与发觉（本轮聚焦：留存与复用）

前三轮已解决「能取字、能改、能搜、能导出、能按质量过滤」。从真实使用路径继续向后看，
用户最常被打断的点在于**每次都要重设、识别完还要手动复制、关掉面板就找不回刚识别的内容**：

| # | 痛点 | 为什么影响可用性 | 本轮方案 |
|---|------|----------------|----------|
| 1 | **偏好不留存**：每次重开都要重设「语言 / 合并为一行」 | 高频用户每天重复同一套配置，纯体力浪费 | **偏好持久化**：语言 / 合并 / 自动复制 写入 localStorage，跨会话记住 |
| 2 | **取字后还要手动复制**：截图文字→进面板→点复制→再粘贴 | 截图取字的核心闭环是「截图→粘贴」，多一步就多一次跳出 | **识别后自动复制**：开关（默认关），识别完成即写入剪贴板 |
| 3 | **识别过程不可预期**：不知道快不快、结果多少 | 长图/大图识别时用户无反馈，易以为卡死 | **耗时 + 统计**：面板显示 识别耗时ms · 可见行数 · 字符数 |
| 4 | **识别完即丢**：关面板/切图后找不到刚识别的文字 | 误关或想二次取用只能重截重识 | **本地历史**：最近 5 次识别结果存 localStorage，面板内回看/复制/清空 |

> 上述四项均**不引入跨视图渲染、不改后端命令、不碰截图历史体系**，完全符合「增量增强、不影响其它功能」的硬约束。
> 之前轮次记录的其余候选（翻译、多列顺序校正、跨视图快速取字）仍因成本/超出本地工具定位**本轮不做**。

## 二、本轮实现的需求

选取 **#1 偏好持久化、#2 自动复制、#3 耗时/统计、#4 本地历史**。四项都直接命中 OCR 最高频的
「留存 / 复用 / 可预期」痛点，且改动全部收敛在 OCR 面板 + 两个隔离 localStorage key，零后端改动。

## 三、具体改动

### 前端 `src/features/screenshot/EnhancedScreenshotApp.tsx`
- **模块级隔离存储**（组件外，不影响其它模块）：
  - `loadOcrPrefs / saveOcrPrefs`（`snapcraft.ocr.prefs`）、`loadOcrHist / saveOcrHist`（`snapcraft.ocr.history`）、`OCR_HIST_MAX=5`。
  - `OcrPrefs { lang, merge, autoCopy }` / `OcrHistItem { text, lang, ts, chars }` 接口。
  - `ocrLangTag(lang)`（中/EN/日/A 短标签，与地区无关）、`fmtOcrTime(ts)`（月-日 时:分）。
- **新状态**（紧接既有 OCR 状态）：
  - `ocrAutoCopy`（默认读偏好，false）、`ocrElapsed`（识别耗时 ms）、`ocrHistory`（读本地历史）、`ocrHistoryOpen`（历史面板展开）。
  - 两个 `useEffect` 把 `ocrLang/ocrMerge/ocrAutoCopy` 与 `ocrHistory` 持久化到 localStorage（仅写本地，不碰其它功能）。
  - `ocrAutoCopyRef` / `ocrMergeRef` 镜像，供 `runOcr` 读取最新偏好而不加入其依赖（避免频繁重建）。
- **`runOcr` 增强**（依赖保持 `[ocrBusy, ocrLang, flash, t]` 不变）：
  - `performance.now()` 计 `ocrElapsed`；识别成功写入 `ocrHistory`（规范换行副本，最近 5 条）。
  - 若 `ocrAutoCopyRef.current` 为真，识别完成即用 `navigator.clipboard.writeText`（尊重 `ocrMergeRef` 合并格式）自动复制并 toast `ocr.autoCopied`。
  - 保留逻辑不变：清空 `ocrEdits/ocrSearch/ocrConf`，保留 `ocrMerge/ocrAutoCopy` 偏好。
- **面板 JSX 增强**：
  - 头部新增「历史」按钮（带条数角标，激活态高亮），点击展开历史区。
  - 头部下新增 `.ocr-stats`：识别耗时 · 可见行数 · 字符数（`ocrChars` 受搜索/置信度过滤影响）。
  - 选项行新增「识别后自动复制」勾选（与「合并为一行」并列）。
  - 历史区：列表项显示 语言标签 / 时间 / 字数 + 两行截断预览 + 一键复制；底部「清空」；空态显示「暂无识别历史」。

### 文案 `src/locales/{zh-CN,en-US}.json`（ocr 组新增 11 键）
`elapsed / statLines / statChars / charUnit / autoCopy / autoCopyTitle / history / historyTitle / historyClear / historyClearTitle / historyEmpty / autoCopied`

### 样式 `src/index.css`
`.ocr-panel-hist(.active) / .ocr-hist-count / .ocr-stats / .ocr-stats-sep / .ocr-autocopy`
`.ocr-history / .ocr-history-head / .ocr-history-clear / .ocr-history-empty / .ocr-history-list / .ocr-history-item / .ocr-history-meta / .ocr-history-lang / .ocr-history-time / .ocr-history-chars / .ocr-history-text / .ocr-history-copy`

## 四、如何验收

```bash
pnpm build                       # 前端 tsc+vite，已通过（125 模块，exit 0）；后端无改动
./start.sh app                  # 打开含最新前端的 SnapCraft.app（内嵌 dist）
```

编辑器内点「取字」：
- **统计**：面板头部出现「识别 320ms · 共 12 行 · 86 字」，随搜索/置信度过滤实时变化。
- **自动复制**：勾选「识别后自动复制」后，再次取字完成即自动写入剪贴板并提示「已自动复制识别结果」；默认关闭，不会误覆盖剪贴板。
- **偏好持久化**：设好语言 / 合并为一行 / 自动复制，关闭 .app 重新打开仍保持原设置（存于 localStorage）。
- **历史**：点头部「历史」按钮展开，显示最近 5 次识别（语言/时间/字数 + 预览），可逐条复制或一键「清空」；下次打开 App 历史仍在。

## 五、回归确认

- 截图 / 区域 / 窗口 / 滚动长截图 / 钉图 / 文字标注 / 马赛克：均未改动，行为不变。
- 区域框选 OCR、内联纠错、导出、结果内搜索、切换语言即时重识别、置信度阈值、合并为一行：链路未动，
  新增的 耗时/统计/自动复制/历史 只附加在识别成功之后与面板呈现层。
- 后端 `ocr_image` / `OcrResult` / `OcrBlock` 签名与行为零变化（本轮纯前端）。
- OCR 历史使用**独立** localStorage key（`snapcraft.ocr.history`），与截图历史 store 完全隔离，不触碰历史体系。
- 偏好/历史存储均为 try/catch 包裹的 localStorage 读写，单测/无痕模式下失败静默降级，不影响主流程。
