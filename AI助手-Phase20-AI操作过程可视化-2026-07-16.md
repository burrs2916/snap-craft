# AI 助手 Phase 20 — P0-α AI 操作过程可视化

> **目标**（来自 Phase 19 路线图 P0-α）：AI Agent 调用 `draw_rectangle` / `redact_area` / `highlight_text` 时，在画布上对应区域**先做一次脉冲高亮**，再落定成正式标注。让用户「看见」AI 正在改哪里——补齐 AI 智能编辑的「可解释性」最后一块拼图。

## 实施（3 文件 · ~60 行净增 · 零 Rust · 零新依赖）

### 1. `AnnotationCanvas.tsx`
- **独立 flash Layer**：在 `Stage` 内新增常驻空 `<Layer ref={flashLayerRef} listening={false}>`，与 react-konva 管理的声明式 layer **完全隔离**——flash 节点命令式 add/remove，react 重渲不会触碰它。
- **`flashRegion(rect, color?, kind?)`**：通过 `useImperativeHandle` 暴露给父组件。
  - `rect` 用 natural pixel 坐标（与标注一致），由 `Stage` 的 `scaleX/scaleY` 自动映射到屏幕。
  - 用真正的 `Konva.Group` + `Konva.Rect`（从 `konva` 值导入）命令式创建节点，rAF 驱动动画。
  - **1.4s 脉冲曲线**：`0~0.25` 快速亮起 + 轻微放大（`scale 1→1.04`）；`0.25~1` 缓慢淡出并维持微放大；结束 `group.destroy()` + `layer.batchDraw()`。
  - 纯视觉层：**不写 annotations、不进撤销历史**，对截图导出/复制零副作用。
- **3 种语义配色**：
  - `rect` → 边框 `color || ACCENT(#007aff)`，淡蓝填充 `bgFill(color, 0.10)`
  - `redact` → 边框警示红 `#ff3b30`，红填充 `rgba(255,59,48,0.18)`（强调「正在打码敏感区」）
  - `highlight` → 边框 `color || #FFE600`，黄填充 `bgFill(color, 0.22)`

### 2. `EnhancedScreenshotApp.tsx` & `EditorWindow.tsx`
- 两编辑器的 `aiTools` 三个工具（`drawRectangle` / `redactArea` / `highlightRect`）在 `return` 前各插入一行：
  ```ts
  canvasRef.current?.flashRegion({ x, y, w, h }, color, 'kind');
  ```
- 闭包复用同组件稳定的 `canvasRef` ref（`.current` 运行时读取，不进 useMemo 依赖，无 stale 风险）。
- 坐标直接复用各工具已算好的 `normToPx` 像素结果，零重复计算。

## 技术要点
- **隔离策略**：不同于主 layer（react 声明式协调），flash 走独立 Konva Layer，彻底规避「手动节点被 react 重渲清掉」的坑。
- **60fps 保证**：单 rAF 循环只改 `opacity` + `scaleX/Y` 两个属性，动画结束即销毁节点，无残留、无内存泄漏。
- **零回归**：不影响 `getMergedImageDataUrl`（导出/复制仍是原图+标注合成）、不进撤销栈、`summarizeRegion`（OCR）不触发 flash（无需视觉反馈）。

## 验证
| 项 | 结果 |
|---|---|
| `tsc --noEmit` | **exit 0，0 错** |
| `pnpm build` | **exit 0（1.88s，139 模块）** |
| `test-tool-call-parser.mjs` | **50/50 绿**（Phase 16 多形态解析未回归） |

## 用户可感知效果
AI 智能编辑运行时：画布上 AI 圈定的区域会先亮起一圈带色脉冲（画框=蓝、打码=红、高亮=黄），0.25s 后稳定、1.4s 内淡出，正式标注同时落定。右栏 Agent 步骤时间线与画布脉冲**同步发生**，AI 的每一步操作都「看得见」。

## 变更文件
- `src/features/screenshot/components/AnnotationCanvas.tsx`（+flashLayerRef / +flashRegion / +JSX Layer / +Handle 接口）
- `src/features/screenshot/EnhancedScreenshotApp.tsx`（3 处 flashRegion 触发）
- `src/features/screenshot/components/EditorWindow.tsx`（3 处 flashRegion 触发）

## 路线图状态
- ✅ **P0-α AI 操作过程可视化** — 本轮完成
- 🔥 P0-β 隐私哨兵 Agent（一键批量识别敏感区）
- 🔥 P0-γ 后端代理隐藏 Key（Rust+前端）
- 🟠 P1 本地语义记忆 / 会话 fork / docx 高级排版 / 会话打包 zip

AI 卖点四件套（改图 / 编辑后图即上下文 / 长程不丢上下文 / 全模型通用）经 P0-α 增强为**「可解释、可感知」**的智能编辑。
