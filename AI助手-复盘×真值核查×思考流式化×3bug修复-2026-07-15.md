# AI 助手 · 整体复盘 × 真值核查 × 思考流式化 × 3 项 Bug 修复（Phase 16）

> 自动化轮次：2026-07-15 19:24 · 目标：整体复盘回溯（空/假实现、前后端断联、bug）、易用性优化、参考顶级项目（claw-code / openclaw）发掘 AI 卖点、文档润色、AI 底层增强。
> 方法：直读 `src/features/ai/*` + 两个编辑器接线 + 全仓 `invoke`↔Rust 对照 + `tsc`/`pnpm build` 双关口 + **真读**用户指定的 `claw-code-main` 与 `openclaw-main` 源码。

---

## 一、真值核查结论（系统现状）

- **构建健康**：`tsc --noEmit` 零错误；`pnpm build` exit0（**138 模块**，较上轮 +1 = 新增思考流式链路）。
- **无空/假实现**：Phase 1–15 全部真实可达；AI 历史仅 `localStorage`（架构事实，非 bug）。
- **前后端对齐**：18/18 `invoke` 命令与 Rust `#[tauri::command]` 参数名/形状 100% 对齐；Phase 8 `visionImageUrl`、10 `aiOcrText`、11 历史覆盖层、14 Agent 工具循环均真实挂载。
- **i18n 完整**：AIPanel 全量 `t('ai.*')` 在中英 locale 齐备，本轮新增 `ai.thinkingTitle` 已补（零缺失、零孤儿）。

---

## 二、本轮实修（纯前端 · 零 Rust · 零回归）

### 🐞 Bug 1 — 上下文预算护栏漏算图片 token（可致 API 400/413）
`src/features/ai/aiClient.ts` `trimHistoryToBudget`：总预算统计计入了每张图 `×256`，但逐轮裁剪时的 `keptTokens` 只累加 `estimateTokens(content)`，**忽略图片**。多截图 / 长 OCR 会话即便裁剪后仍可能超模型上下文上限。
→ 修复：内层 `reduce` 一并加上 `(m.images?.length ?? 0) * 256`，裁剪判定与预算统计口径一致。

### 🐞 Bug 2 — 多截图拖拽重排 off-by-one（章节顺序错一位）
`AIPanel.tsx` `onDropAt`：先 `splice(dragIdx,1)` 移除被拖项使后续索引左移，但插入仍用原数组的 `toIdx`；当 `dragIdx < toIdx` 时目标项落到放置点下方一位。
→ 修复：插入点 `dragIdx < toIdx ? toIdx-1 : toIdx`，与「图文混排」章节顺序严格对齐（直接强化差异化卖点）。

### 🐞 Bug 3 — PDF 导出不可靠（图文报告 PDF 空白/截断）
主面板 `handleExportPdf`（:669）与历史阅读器（:321）均用 `width:0;height:0` 隐藏 iframe + 仅 300ms 即 `print()`。内联 base64 截图在 `onload` 时往往尚未解码完成 → 导出 PDF 空白/截断；0×0 尺寸在部分平台也无法正常打印。
→ 修复：抽离共享 `printHtmlViaIframe(html)`，改为 1000px 离屏尺寸，打印前 `Promise.all` 等待全部 `<img>` 解码完成（带 2.5s 兜底超时），两处导出统一复用。

### ✨ 新功能 — 思考过程流式展示（Phase 16，对齐 openclaw thinking 事件流）
顶级项目（openclaw `anthropic-transport-stream.ts` / `openai-transport-stream.ts`、claw-code 无）把模型「推理过程」作为独立事件流展示。此前 SnapCraft 只显示最终正文，用户无法感知 AI 如何拆解截图任务。
→ 实现（纯前端、零后端、零回归）：
- `aiTypes.ts`：`StreamOpts.onThinking?` 回调。
- `aiClient.ts`：`processFrame` 新增 `ThinkState` 跨帧状态，解析 **Anthropic `thinking_delta`** 与 **OpenAI 兼容 `reasoning`/`reasoning_content`**（DeepSeek-R1 / Qwen / o-series 推理模型）；`streamChat` / `streamChatWithTools` 透传。
- `aiStore.ts`：新增 `thinking` 状态，4 处流式入口（`chat`/`refine`/`runAgent` + `generate` 委托）回填并清空。
- `AIPanel.tsx`：流式期间实时渲染可折叠「💭 思考过程」卡片（默认展开、流式时脉冲指示、点击折叠）。
- i18n(zh+en)：`thinkingTitle`（思考过程 / Reasoning）；CSS：`.ai-think-block*` 系列（复用 `--accent/--surface/--border`，明暗自适应）。

> 说明：本实现为**被动展示**——模型/端点原生回传思考内容即显示（中国生态常用的 DeepSeek-R1 / Qwen 推理模型经 OpenAI 兼容端点即生效）；若模型不回传则卡片不出现（优雅降级）。开启 Anthropic 扩展思考（extended thinking）并按多轮回显 thinking 块，列为下一阶段（见路线图④）。

---

## 三、对照顶级项目：尚未落地的「卖点级」差距地图

真读两份源码后，按「对截图工具 AI 价值」排序：

| # | 能力 | 来源(file:line) | 现状 | 价值 |
|---|------|----------------|------|------|
| ① | **会话分叉 Session Fork** | claw-code `session.rs:260 fork()` / openclaw `session-fork.ts` | 未做 | 🔥 对「同一截图换一种标注/文风」试错不污染主对话，**强卖点** |
| ② | **隐私哨兵（自动打码）** | 延伸 Phase 14 工具循环 | 未做 | 🔥 AI 自动识别并 redact 邮箱/手机号/姓名/身份证=截图工具独有杀手锏 |
| ③ | **选区 / 划词 AI** | — | 未做 | 🔥 在编辑器直接框选区域/文字问 AI，最自然的「截图即问答」 |
| ④ | 扩展思考开关(Anthropic) + thinking 回显 | openclaw `anthropic-transport-stream.ts:170-189` | 仅被动展示 | 高：让推理模型（含 Claude）显式思考 |
| ⑤ | **成本/预算软护栏** | openclaw `bootstrap-budget.ts:165-222`(nearLimitRatio) | 仅用量展示 | 中：接近上限前预警，避免静默超支 |
| ⑥ | 提示缓存前缀分裂 | openclaw `anthropic-payload-policy.ts:70-115` | 整段 system 缓存 | 中：稳定前缀缓存、动态后缀不缓存，提升命中率省钱 |
| ⑦ | **PPT 导出** | — | 缺 | 中高：「截图成稿 → 幻灯片」补齐文档形态 |
| ⑧ | 语义记忆(向量 embedding) | openclaw `mmr.ts`/`temporal-decay.ts` | 词法+MMR | 高但重：需本地 embedding(wasm) |
| ⑨ | 缓存断裂检测 | claw-code `prompt_cache.rs:314 detect_cache_break` | 无 | 低：监控缓存命中骤降定位失效根因 |

**已追平顶级项目**：SSE 帧解析 / 退避重试 / 上下文预算护栏 / 记忆多因子+MMR / token 用量与美元成本 / 提示缓存发送+命中回显 / Agent 工具循环+失控检测 / 增量滚动摘要 / **思考流式展示(本轮)**。

---

## 四、文档生成润色剩余项

- docx 高级排版：页眉页脚、分栏、页码（当前仅封面+目录+主题色）。
- PPT 导出（路线⑦）：把 AI 成稿转 `.pptx`（reveal.js 或 pptxgenjs）。
- 导出品牌化：封面副标题/品牌色已接通（Phase 15 死代码解除），可补「自定义 logo」。
- 选区/划词内容直接成为文档章节锚点（结合路线③）。

---

## 五、AI 功能增强路线图（按优先级）

1. **会话分叉 Fork**（①）— 纯前端，复制 convKey 线程 + 新分支标识，UI 加「⑂ 分叉」按钮。最高性价比卖点。
2. **隐私哨兵 Agent**（②）— 扩展 Phase 14 `aiTools.ts` 加 `scan_redact_pii` 预设：模型圈选敏感区→自动 `redact_area`。杀手锏 demo。
3. **选区/划词 AI**（③）— 编辑器框选区域/文字 → 作为 `visionImageUrl`+`ocrText` 注入一轮追问。
4. **扩展思考开关**（④）— Anthropic `thinking:{type:'enabled',budget_tokens}` + 多轮回显 thinking 块。
5. **成本软护栏 + 缓存前缀分裂**（⑤⑥）— 低成本高收益。
6. **PPT 导出**（⑦）/ 语义记忆（⑧）— 择机。

---

## 六、本轮交付与生效

- 修复 3 个真实 bug（图片 token 预算 / 拖拽 off-by-one / PDF 导出可靠性）+ 新增「思考过程」流式展示。
- 构建：`tsc` 零错误、`pnpm build` exit0（138 模块）。
- **重启 App 即见**：`AI 智能编辑`/普通生成/润色时在对话上方出现可折叠「💭 思考过程」卡片；多截图拖拽重排顺序精确；图文报告 PDF 不再空白。
- 交付文件：`AI助手-复盘×真值核查×思考流式化×3bug修复-2026-07-15.md`
