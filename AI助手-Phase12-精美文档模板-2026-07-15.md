# AI 助手 Phase 12 — 精美文档模板（Premium Document Themes）

> 参考 `privdoc-ai` 的「多格式导出」能力，但更进一步：导出的 HTML / PDF 不再是朴素排版，而是多套**精美文档主题**，让成稿像产品推广、广告、插画文档一样美观。

## 需求发掘
复盘 Phase 1–11 的完整链路（截图 → 编辑 → AI 成稿 → 多轮迭代 → 图文报告 → 记忆 → 历史库），导出能力已覆盖 `.md/.txt/.html/.pdf/.docx/.xlsx`，但 **HTML/PDF 仍是朴素黑白文档**（基础衬线 + 简单边框）。
而用户最新诉求明确：生成的文档要「**像产品推广等广告或插画文档一样非常美观，文字和图片布局合理，有标题**」，且要能直接下载、打开预览。
privdoc-ai 的导出是结构化数据驱动、无图、样式基础——本阶段在 Markdown 驱动 + 内嵌截图的基础上，补上「**文档主题系统**」这一 privdoc-ai 没有的独有价值。

## 实现（纯前端，零 Rust 改动，不影响截图/标注/OCR/历史/对话/导出）
- **`markdownHtml.ts` 重构为主题化引擎**：
  - 新增 `DocThemeId` / `DOC_THEMES`（5 套主题元数据，含中英文名称与描述）；`MdToHtmlOpts` 取代原位置参数（兼容 `title/subtitle/sectionImages/theme`）。
  - `mdToHtml(md, opts)` 现在按主题生成**带封面 hero** 的完整 HTML：标题/章节编号徽标、图文卡片框、引用/表格/代码主题化排版。
  - 5 套主题（均印刷优化，`print-color-adjust:exact` 保证打印/PDF 背景不丢）：
    1. **现代简约 modern** — 靛蓝点缀、清爽留白、章节序号徽标。
    2. **雅致衬线 elegant** — 暖纸底、衬线标题、首字下沉、金线分隔，典雅隽永。
    3. **杂志风 magazine** — 大字报头、珊瑚强调、巨型引号 pull-quote，编辑感强。
    4. **产品推广 product** — 紫蓝渐变封面 + AI 徽标 + 卡片章节，广告级美观。
    5. **科技风 tech** — 深色 hero + 青色网格底纹 + 等宽字体，极客质感。
  - 传入 `title` 时**自动抑制正文首个 `#` 标题**，避免封面与正文重复；`<!--SNAP:k-->` 章节内嵌截图改用主题化 `.doc-fig/.doc-img/.doc-cap`。
- **`aiTypes.ts`**：`AiConfig` 新增 `theme?: DocThemeId`（type-only import，零运行时耦合）。
- **`aiStore.ts`**：fallback 配置默认 `theme:'modern'`，经既有 `setConfig` 持久化到本机 `localStorage`。
- **`AIPanel.tsx`**：
  - 导出区新增「**样式**」chips 行（5 主题，按当前语言显示名称/描述 tooltip），点击 `setConfig({theme})`；
  - 「**.html / .pdf**」导出与历史阅读器导出均传入所选 `theme`；
  - 新增「**👁 预览**」按钮：用 Blob 在新标签页直接打开主题化 HTML，**无需先下载即可看效果**（被拦截时提示允许弹窗）。
- **i18n（zh-CN+en-US）**：新增 `style / styleTitle / preview / previewTitle / previewBlocked` 共 5×2 键。
- **`index.css`**：新增 `.ai-style-row / .ai-style-chip / .ai-style-chip.active / .ai-btn-preview`（复用 `--accent` 主题色，明暗自适应）。

## 验证
- `tsc --noEmit` 零错误；`pnpm build` exit0（136 模块，零类型错误，无回归）。
- dist 含全部新 token：CSS `ai-style-chip/ai-btn-preview/doc-cover-*`/`doc-fig`，locale `style/preview/previewBlocked`，主题名中英文。
- **i18n 完整性复查**：脚本扫描 `src/features/ai` 全量 `t('ai.*')` 调用（97 个键），中英 locale **零缺失、零孤儿**；新增键均已覆盖。
- **按钮/UX 复查**：样式 chips 与预览按钮位于滚动容器内（沿用 Phase 11 的 `.ai-panel-scroll`），区块再多也整体滚动不被裁剪；选中态高亮、tooltip 说明、布局不溢出。
- 生成 5 套主题样例 HTML 实测，封面/章节/图文/表格均渲染美观（见交付物预览）。

## 价值
用户生成的每份 AI 成稿，现在可一键选「产品推广 / 杂志风」等精美主题导出 HTML/PDF，下载后直接打开即是**排版精良、带封面与章节、图文混排**的成品文档，真正从「截图工具」升级为「**截图 → 美观文档**」的生产力工具。主题选择持久化，下次导出沿用。

## 后续候选（未做，按需求再排）
- ②后端代理隐藏 Key（需 Rust）；③拖拽重排多截图选择顺序（纯前端，提升批量报告 UX）；
- 衍生：导出时可选「封面副标题/品牌色」自定义；docx 同步套用主题封面；主题再扩展（如「简历」「周报」「白皮书」专用版式）。
