# SnapCraft 需求发现报告 v2 · 聚焦 OCR / AI / 图片处理 / 文档 / 界面 风格

> 自动化轮次：2026-07-16 20:30 · 紧接整体复盘 + Phase 16–30 全交付 + 路径A 独立AI窗口 + Bug 三连击之后
> 视角：用户复盘提出——**着重 OCR、AI、对图片处理、生成文档、界面风格**五维，寻找新需求点（特别是文档的生成、下载、OCR/AI 处理后产物的持久化、管理、预览、下载）
> 方法：全栈直读源码（`src/`+`src-tauri/`+`src/locales/`），按用户五维视角系统化梳理；只诊断不擅自改码，待用户拍板
> 严守铁律：**纯增量、不推翻重构、零 Rust、零新依赖、构建通过、UI 风格中性主题色**

---

## 一、总体判断（一句话）

**OCR / AI / 图片处理 / 文档生成四大支柱已经形成行业级闭环**（Phase 1–30 全部交付、双语完整、构建 exit0），但「**产物持久化与管理**」和「**下载/预览/再加工路径**」确实是当前体系下相对薄弱的两环——这正是用户本次追问的核心。下面用「用户五维视角」逐一审计。

---

## 二、OCR 功能 · 现状审计

### 2.1 已实现（深厚）

| 能力 | 状态 | 关键证据 |
|---|---|---|
| 系统原生 OCR（macOS Vision / Windows WinRT） | ✅ | `src-tauri/src/commands/ocr.rs:30-275`；零外部依赖 |
| OCR 4 入口（截图 / 结果条 / 历史网格 / 剪贴板） | ✅ | `EnhancedScreenshotApp.tsx` 4 处 invoke |
| OCR 选区识别（框选裁图） | ✅ | `ocrRegionMode` state |
| OCR 内联编辑（逐行修正） | ✅ | `ocrEdits` map 状态 |
| 智能阅读顺序（横排多列 / 竖排） | ✅ | `OcrLayout: 'none'\|'reading'` + 聚列算法 |
| 置信度阈值过滤 | ✅ | `ocrConf` state（macOS 提供置信度，Windows 标记 0 始终显示） |
| 智能文本清洗（零宽/控制/重复字/超长行） | ✅ | `src/features/ai/ocrClean.ts` ~95 行（Phase 18）+ Phase 18 接入 4 处 `ocr_image` 调用后 |
| 智能实体提取（URL/邮箱/电话） | ✅ | `OcrEntity` 接口 + `open_external` 跳转 |
| 复制格式（合并为一行 / 复制 JSON / 复制 TSV） | ✅ | `copyMerge` / `copyJson` / `copyTsv` 按钮 |
| 导出 4 格式（txt / md / json / tsv） | ✅ | `buildOcrExportContent` + `save_text_file` |
| 偏好持久化（语言/合并/自动复制/导出格式/字号/布局/实体提取/清洗） | ✅ | `OcrPrefs` localStorage 8 字段跨会话记忆 |
| OCR 历史（最近 5 条） | ✅ | `OCR_HIST_KEY='snapcraft.ocr.history'` localStorage |
| 全文 OCR 视图搜索 | ✅ | `ocrSearch` + `onMouseUp` 划词联动（Phase 27） |
| 选区→标注（打码/高亮/箭头） | ✅ | `redactOcrSel/highlightOcrSel/arrowOcrSel` + `flashRegion` |

### 2.2 OCR 维度的真实缺口（按 ROI 排序）

#### 🟢 N-OCR-1 · OCR 结果无「批注/标注」能力
- **现象**：OCR 面板只支持**改字**（`ocrEdits`），不能**直接对原图加可视化批注**（如「把这段识别错的字加红线方框」「把可疑电话加问号」）。
- **现状**：用户必须切到"画笔模式"才能加标注 → 上下文割裂。
- **方案（纯增量）**：OCR 面板每行加 🏷「贴为标注」按钮（与"贴回"独立）→ 在对应坐标生成 `rectangle`/`callout` 标注；保留编辑映射。约 30 行。
- **价值**：OCR 结果 → 视觉批注 → 导出/分享 一气呵成，调研/取证场景刚需。

#### 🟢 N-OCR-2 · OCR 历史仅"最近 5 条纯文本"，**无截图缩略图/可重新识别**
- **现象**：`OcrHistItem = {ts, lang, text}` 纯文本。**没有 dataUrl**。用户 5 天后想"再识别当时那张图"或"看当时那张图" → 找不到。
- **方案**：OCR 历史改存 `{ts, lang, text, thumb, srcId?}`（缩略图 200×200 + 关联到历史截图 id）；OCR 面板加"📷 查看原图"按钮（无 srcId 显示缩略图）。约 25 行 + 1 Rust 字段可缺省兼容。
- **价值**：OCR 历史的可追溯性、可重识别性大幅提升。

#### 🟢 N-OCR-3 · OCR 选区模式**无视觉反馈**（拖拽中看不到矩形）
- **现象**：`ocrRegionMode=true` 拖拽时画布无即时矩形显示，松开后才裁图识别 → 不直观。
- **方案**：复用 Phase 20 `flashRegion` 但改为跟随拖拽的临时矩形（不写入 `addAnnotation`，仅 Konva 命令式节点）。约 15 行。
- **价值**：基础 UX 体验，5 行级修复。

#### 🟡 N-OCR-4 · OCR 结果"合并为一行"无法自定义分隔符
- **现象**：`ocrMerge` 仅去换行用空格连接；有时想用 ` / ` 或 `\n` 分隔。
- **方案**：加 `mergeSep` 配置（默认空格，下拉选 ` `/` / `/`\n`），`saveOcrPrefs` 扩 1 字段。约 10 行。
- **价值**：低优先级小润色。

---

## 三、AI 辅助功能 · 现状审计

### 3.1 已实现（极深厚 — 行业级差异化）

| 能力 | 状态 | 关键证据 |
|---|---|---|
| 5 家 API（OpenAI/Anthropic 兼容 + 自定义 baseUrl） | ✅ | `apiType: 'openai' \| 'anthropic' \| 'custom'` |
| 流式 SSE（含 thinking_delta、tool_use、usage、cache_read） | ✅ | `aiClient.ts:472-538` + `processFrame:306-432` |
| 工具循环（5 工具：draw/redact/highlight/arrow/callout） | ✅ | Phase 14+24+25 |
| 多形态工具调用（国产模型 stream 也能跑） | ✅ | Phase 16 `toolCallParser.ts` 5 形态兜底 |
| 6 格式导出（md/txt/html/xlsx/docx/pptx） | ✅ | Phase 5/12/26/29 |
| 5 主题（现代/雅致/杂志/产品/科技） | ✅ | `markdownHtml.ts:57-86` |
| 14 业务预设（PRD/会议/教程/测试/…） | ✅ | `aiPresets.ts` |
| 自定义预设（图文报告模式） | ✅ | Phase 30 `userBuilder: 'default'\|'report'` |
| 长期记忆（链式增量压缩 + 多因子 MMR） | ✅ | Phase 9+15 |
| 记忆管理（逐条删除/编辑/重要性） | ✅ | 2026-07-16 收尾 |
| 跨截图对话分桶 + 历史库 + 搜索 | ✅ | Phase 11 + `loadConvIndex` |
| 会话 fork（指定轮次分支） | ✅ | Phase 22 |
| 会话打包 zip（成稿+图+对话+README） | ✅ | Phase 23 |
| 批处理 Agent 队列 | ✅ | Phase 28 |
| AI 操作过程可视化（画布脉冲） | ✅ | Phase 20 |
| 隐私哨兵 Agent | ✅ | Phase 21 |
| 独立 Tauri 窗口承载 AI 面板 | ✅ | 路径A |
| 多模态（图片+OCR+记忆） | ✅ | `chat`/`runAgent` 完整支持 |
| 工具步骤实时回显（agentSteps UI） | ✅ | `onToolCall/onToolResult` |
| 思考过程流式显示 | ✅ | `thinking` state + details 折叠 |
| 缓存命中可视化 | ✅ | `usageCache` 文案 |
| 错误分级（auth/rate/context/server） | ✅ | `classifyAiError:572-588` |
| 工具循环失控熔断 | ✅ | `stableToolKey` + `MAX_REPEAT_TOOL=3` |
| 中断流式 | ✅ | AbortController |
| 上下文预算护栏 | ✅ | `trimHistoryToBudget(120_000, 3)` |
| Popup 弹起 + 钉住 + 最小化 | ✅ | Phase 17-18 |

### 3.2 AI 维度的真实缺口

#### 🔥 P0-AI-1 · AI 编辑后产物**未独立持久化**到历史库（仅 chat 流式成稿被持久化）
- **现象**：Agent 模式产生的**画布编辑产物**（打码/圈选/高亮/箭头/气泡）**不进入 AI 历史索引**。`recordConvMeta` 只存 `imageDataUrl`+对话，存的是编辑前的图；导出的 docx/pdf 用的是 `thumb`（编辑后缩略图 200×200 JPEG 0.7）。
- **痛点**：用户 Agent 跑完关掉 AI 窗 → 产物已落主画布 → 主画布继续编辑 → 下次想"再导出当时 AI 的产物"找不到完整图。
- **方案**：`recordConvMeta` 增加可选 `postEditThumb?: string`（编辑后 800px JPEG 0.8，最佳努力）；`AiConvMeta` 加字段；History 阅读器导出 docx/pdf 用 `postEditThumb` 作 cover。约 15 行。
- **价值**：**填补 Phase 19-B5 之后的最后一公里**，让 AI 产物真正可追溯可重导。

#### 🟠 P0-AI-2 · AI 历史库**无批量管理**（批量删除/批量导出/标签）
- **现象**：History 列表只能**单条**删除/导出/fork。截图 100 张 + 对话 100 条时管理崩溃。
- **方案**：
  - 列表多选复选框（与主窗口 `selMode` 同款 UI）
  - 批量删除（带二次确认，列出 N 条标题）
  - 批量导出 zip（每条独立 md/json/source.png）+ 总 README 索引
  - 标签系统：自定义标签 `tags?: string[]`（预设：工作/学习/隐私/灵感），列表可按标签过滤
  - 约 60 行 + 1 Rust 字段。
- **价值**：**从「玩具级」跃升到「生产级」**，长期使用必备。

#### 🟠 P0-AI-3 · AI 输出无**版本对比/差异可视化**
- **现象**：润色 (`refine` Shorter/Longer/Formal/Casual/En) 后**直接覆盖**末轮 AI 成稿，**原版丢失**。用户想"对比上一版 vs 现在"无路径。
- **方案**：
  - 润色前快照：把 `conversation[lastIdx].content` 复制到 `conversation[lastIdx-1]`（或新加 `_altVersions: string[]` 数组），UI 加"↩ 上版"按钮
  - 升级：`AiChatTurn` 加 `altVersions?: string[]`，显示时给下拉「v1 / v2 / v3」
  - 约 30 行。
- **价值**：润色是高频操作，**对比=决策**是刚需，零回归。

#### 🟢 P0-AI-4 · AI popup 的"复制/钉住/停止/最小化"无键盘快捷键
- **现象**：流式时只能用鼠标点按钮。⌘C/⌘V/⌘. 等无绑定。
- **方案**：popup 内 `keydown` 监听：`⌘C` 复制最新成稿 / `⌘.` 停止 / `Esc` 最小化 / `⌘P` 钉住。约 20 行。
- **价值**：高频用户的效率提升；零依赖。

#### 🟢 P0-AI-5 · AI 输出**无字数统计 / 阅读时长**（UI 已有"字符数"但仅 OCR 侧）
- **现象**：AI 成稿常数百-数千字，**没有显式字符数 / 中文字数 / 估算阅读时长**（"约 X 分钟读完"）。
- **方案**：在 `.ai-output` 区上方加一行元数据：`<X 字符 · ~Y 分钟 · Z 段 · N 图>`。**与 OCR 侧的 `statChars/statLines` 同款组件**（已有 CSS class）。约 12 行。
- **价值**：用户写公众号/小红书/邮件常需要字数把控，立即有感。

#### 🟡 P0-AI-6 · AI 工具结果**未与历史步骤做可视化关联**
- **现象**：agentSteps 步骤流已显示，但**没有时间轴**（横向多步骤进度条）。
- **方案**：流式完成后给 `agentSteps` 加横向时间轴 UI（每个步骤一个圆点 + 工具名 + 耗时）。约 25 行。
- **价值**：让「AI 思考路径」肉眼可读；与 Phase 20 画布脉冲互补。

---

## 四、图片处理（标注/画布）· 现状审计

### 4.1 已实现（极厚）

`AnnotationCanvas.tsx` 1709 行 + `AnnotationToolbar.tsx` 25kB + `EditorWindow.tsx` 1861 行；工具覆盖 矩形/椭圆/箭头/线条/自由画/马赛克/模糊/高亮/文字/序号/取色器/撤销重做/复制粘贴/对齐/分组；导出 `getMergedImageDataUrl` 含全部标注。

### 4.2 图片处理维度的新缺口

#### 🟢 P-IMG-1 · 编辑器无"**浅色/深色主题切换**"（主窗口已支持，编辑器仍跟随系统）
- **现象**：编辑器 WebviewWindow 跟随系统外观，深色模式下白色文字看不清（OCR 框/标注 stroke 在某些主题下对比度差）。
- **方案**：编辑器窗口右上角加 🌙/☀️ 按钮，状态写入 `localStorage:snapcraft-editor-theme`，CSS 变量切换。约 20 行。
- **价值**：与主窗口能力对齐，长时间编辑不刺眼。

#### 🟢 P-IMG-2 · 标注**无全局搜索**（按标注内容文本检索）
- **现象**：截图加了几十条文字标注后，想"找包含'订单'的那条"只能滚动。
- **方案**：工具栏加 🔍，弹小输入框，遍历 `annotations.filter(a=>a.type==='text' && a.text.includes(q))`，点击结果在画布高亮并滚到。约 30 行。
- **价值**：长截图批注刚需。

#### 🟢 P-IMG-3 · 标注**无标签/分类**
- **现象**：所有标注视觉平铺。教程/审阅场景需要「红色=错误/黄色=注意/绿色=通过」语义。
- **方案**：每条 `Annotation` 加 `tag?: 'red'|'yellow'|'green'|'none'`（缺省 none），工具栏 4 个色点 + 选中标注后设标签 + 画布按标签淡色区分。约 25 行。
- **价值**：教学/审阅/协作场景的视觉分组。

#### 🟡 P-IMG-4 · 马赛克/模糊**无智能推荐强度**
- **现象**：马赛克强度（brush size）和模糊半径需要手动试。
- **方案**：根据"区域文字长度 + 字号估算"自动推荐（需先 OCR 该区域）。约 40 行。
- **价值**：自动化的隐私打码。

#### 🟡 P-IMG-5 · 标注**无"锁定/隐藏"切换**
- **现象**：复杂截图，标注太多时点击很容易误选。`lock`/`hide` 状态。
- **方案**：每条标注加 `locked?:boolean` + `hidden?:boolean`，工具栏 toggle。约 20 行。
- **价值**：复杂图编辑防误操作。

---

## 五、文档生成（核心追问）· 现状审计

### 5.1 已实现（6 格式 + 5 主题 + 完整渲染管线）

| 格式 | 渲染器 | 主题 | 特性 | 关键文件 |
|---|---|---|---|---|
| .md | 内置 markdown 解析 | — | 通用 | `aiMarkdown.tsx` |
| .txt | 纯文本（去标记） | — | 极简 | inline |
| .html | 5 主题模板 | ✅5 | 封面+TOC+图文混排+打印 | `markdownHtml.ts:571` |
| .pdf | iframe 打印 | ✅5 | 浏览器原生 | inline `handleExportPdf` |
| .docx | OOXML 拼装 | ✅5 | 斑马纹/页眉页脚/超链接/页码 | `markdownDocx.ts:585` |
| .xlsx | OOXML 拼装 | — | 表格专用 | `markdownXlsx.ts:150` |
| .pptx | OOXML 拼装 | — | 标题断页 | `markdownPptx.ts:475` |

### 5.2 文档生成维度的真实缺口（这是用户**最关切**的）

#### 🔥 P-DOC-1 · **导出后无"打开所在文件夹"/"在 Finder 中显示"**
- **现象**：用户导出 docx 后，**只能去 Finder 自己找**。Snipaste/Xnip/CleanShot X 都有"在 Finder 中显示"。
- **现状**：Rust 已有 `open_external`(URL/mailto/tel/文件路径)（`open.rs:17-38`），**缺 `reveal_in_folder`**。`tauri-plugin-opener` 0.5+ 提供 `revealItemInDir`，零新依赖（项目已用 opener）。
- **方案**：新增 `reveal_in_folder(path: String) #[tauri::command]`（薄封装 8 行）→ 前端导出成功 toast 加"📂 在 Finder 中显示"按钮 → invoke 该命令。
- **价值**：**用户每次导出都会点**的刚需按钮，5 行级 Rust + 10 行前端。
- **🚨 路线图首次提**：**这是项目目前唯一"已集成 opener 插件但未用 reveal"的明确缺口**，强烈建议立即补。

#### 🔥 P-DOC-2 · **历史库无"上次导出"路径记忆**（每次都重选保存路径）
- **现象**：用户反复导出同一会话的 docx/html，每次 `saveDialog` 弹窗都要重新选目录。
- **方案**：`localStorage:snapcraft-ai-last-dir` 记录最近一次保存目录（独立于 hash，因为同一会话可多格式），`saveDialog({ defaultPath: <lastDir>/<basename>.<ext> })`。失败（目录被删）静默回退。
- **价值**：5 行级实现，导出高频用户的实际痛点。

#### 🟠 P-DOC-3 · **导出预览/再编辑能力缺失**
- **现象**：导出的 docx/html 是「成品」。用户导完后想"改个标题/加段"，必须切回 AI 面板润色。
- **方案**：
  - **HTML 路径**：导出的 .html 含 `<button onclick="...">在线编辑模式</button>`，点开进入 contenteditable 模式（仅在 SnapCraft 内打开时），可改字/加段 → 重新触发 save_text_file 覆盖。
  - **DOCX 路径**：导出后弹"📝 立即编辑"按钮（需要 docx 反向解析，体量大，留给未来）。
- **价值**：HTML 路径**纯前端零依赖 30 行可实现**，**解决"导出→再加工"最后一公里**。

#### 🟠 P-DOC-4 · **导出模板预览图缺失**（用户选主题靠"猜"）
- **现象**：5 个主题（现代/雅致/杂志/产品/科技）用户在选择时**只能看名字和描述**，看不到实际效果。
- **方案**：AIPanel 主题 chip 加 hover tooltip（用 `data-theme-preview`），tooltip 内渲染**该主题的小型实际渲染结果**（用真实内容渲染 ~200×300 iframe 或静态截图）。最简方案：存每主题的**静态缩略图**（首次启动生成）。
- **价值**：主题选择从"开盲盒"到"所见即所得"，**对 C 端用户极友好**。

#### 🟢 P-DOC-5 · **导出文件名规则单一**（永远是 `snapcraft-ai-<timestamp>.<ext>`）
- **现象**：用户导 10 次后桌面 10 个 `snapcraft-ai-1752674123.docx`，分不清哪个对应哪个。
- **方案**：默认文件名拼上 `<首轮目标前 30 字>-<日期 yyyymmdd>-<时间 hhmmss>.<ext>`，**用 `sanitizeFilename` 清洗非法字符**。约 15 行。
- **价值**：文件名即文档摘要，文件管理友好。

#### 🟢 P-DOC-6 · **导出后无"复制到剪贴板"路径**（仅图片有，文档没有）
- **现象**：导出 docx 后想"粘到微信" → 不可能（docx 不可粘贴）。但导出 md/txt/html 后想"贴到 Notion/Markdown 编辑器" → 必须先保存再打开再复制。
- **方案**：md/txt/html 三格式加"📋 复制到剪贴板"按钮（html 用 `navigator.clipboard.write([new ClipboardItem({...})])` 富文本）。约 20 行。
- **价值**：消除"保存→打开→复制"三步，10 行级实现。

#### 🟢 P-DOC-7 · **导出进度无提示**（大文档静默等待）
- **现象**：`exporting` flag 在 200ms 内文档大时无反馈，**用户以为卡死**。
- **方案**：`exporting=true` → 立即 toast "正在生成 X 文档..." → 完成后 toast "✅ 已保存到 <basename>"。约 10 行。
- **价值**：基础 UX，5 行级。

#### 🟡 P-DOC-8 · **多格式一键导出**（如 .docx + .pdf + .md 同时导出）
- **现象**：用户有时需要 3 格式都留一份。
- **方案**：按钮"📦 全部导出" → 调 3 次 `saveDialog`（让用户选 1 个目录，3 文件落到一起）。约 25 行。
- **价值**：**与 Phase 23 zip 互补**（zip 是会话级，多格式一键是文档级）。

#### 🟡 P-DOC-9 · **DOCX 字体一致性**（中英混排当前不稳）
- **现象**：Phase 26 已优化，但 docx 中文用宋体、英文用 Calibri 时西文与中文字号不同步。
- **方案**：所有 run 统一 `rFonts: ascii=eastAsia=hAnsi=cs='Microsoft YaHei'`。约 10 行。
- **价值**：细节润色，B 端文档刚需。

---

## 六、产物持久化与管理（核心追问）· 现状审计

### 6.1 双轨持久化模型（清晰）

| 轨道 | 后端 | 内容 | 容量 | 查询 |
|---|---|---|---|---|
| **截图历史** | Rust `history.json` | dataUrl + 元数据 + OCR + 标注 | 100 条上限 | 按 OCR 文字/时间 |
| **AI 对话索引** | localStorage `snapcraft-ai-conv-index` | 缩略图+首轮目标+末轮预览+preset+parent | 无上限（实际 localStorage 5MB 限制） | 按 hash 分桶 |
| **AI 对话内容** | localStorage `snapcraft-ai-conv:<hash>` | 完整多轮对话 | 同上 | by hash |
| **AI 长期记忆** | localStorage `snapcraft-ai-mem:<hash>` | 滚动摘要 | 1 条/会话 | inline |
| **OCR 历史** | localStorage `snapcraft.ocr.history` | 最近 5 条纯文本 | 5 条 | 按时间 |
| **OCR 偏好** | localStorage `OcrPrefs` | 8 字段 | 1 份 | n/a |
| **AI 配置** | localStorage `snapcraft-ai-config` | API Key/URL/model | 1 份 | n/a |
| **AI 自定义预设** | localStorage `snapcraft-ai-templates` | 用户模板 | 无 | n/a |
| **AI 选择顺序** | localStorage `snapcraft-ai-sel:*` | 多图报告顺序 | 1/key | n/a |
| **AI popup 钉住** | localStorage `snapcraft-ai-popup-pinned` | 0/1 | 1 份 | n/a |

### 6.2 持久化/管理的真实缺口（**这是用户本次追问的核心**）

#### 🔥 P-MGT-1 · **截图历史 & AI 对话历史无统一管理入口**
- **现象**：两个体系（Rust 截图 vs 前端 AI 索引）**互相独立**。用户要"看某个截图 + 那张图的 AI 对话"，必须切到 AI 窗口打开历史 → 找不到该图对应 hash。
- **方案**：
  - 阶段一：AI 历史卡片显示**关联截图缩略图**（hash 是基于 imageDataUrl 算的，所以 hash 是天然连接键）；点缩略图 → 跳到主画布（主窗口聚焦 + setCurrentScreenshot）
  - 阶段二：主窗口历史网格卡片显示**"💬 AI 对话数"徽标**（通过 convIndex 查），点击 → 打开 AI 窗并自动 `setConvKey(hash)`
  - 约 40 行。
- **价值**：**填补两条体系间的鸿沟**，让"图 + 对话"成为真正的双视图。

#### 🟠 P-MGT-2 · **截图历史无标签/收藏/归档**
- **现象**：所有历史一视同仁。用户 30 天前那张有用的截图混在 200 张临时截图中难找。
- **方案**：`HistoryItem` 加 `tags?: string[]` + `pinned?: boolean`（#[serde(default)] 兼容老数据）。UI：卡片右上 ⭐ 收藏/右键菜单加标签/列表按收藏过滤。约 40 行。
- **价值**：生产级截图管理刚需。

#### 🟠 P-MGT-3 · **AI 历史库无"导出全部"**（仅单条 zip/导出）
- **现象**：想"把 50 条 AI 对话全打包"无路径。
- **方案**：History 列表工具栏加"📦 全部导出"按钮 → 遍历 `listConvMeta()` → 逐条打 zip 条目（每条 1 子目录 + md/json/source.png）→ 合成大 zip → saveDialog。约 50 行。
- **价值**：**学术/法律场景刚需**（多年 AI 沉淀归档）。

#### 🟠 P-MGT-4 · **localStorage 容量无监控/无导出**
- **现象**：localStorage 5MB 限制，达到后 `setItem` 静默失败。AI 对话 100+ 条 + dataUrl（数 MB）很可能撑爆。
- **方案**：
  - `localStorage` 写入时 try/catch + 估算占用（`JSON.stringify(state).length`）
  - 超过 3MB 时 warn toast "存储接近上限，建议导出历史"
  - 加"📦 一键导出全部历史"按钮（P-MGT-3 实现）
- **价值**：**防御性功能**——线上"对话突然没了"是用户最痛。

#### 🟢 P-MGT-5 · **截图历史无"删除前预览"**（误删不可恢复）
- **现象**：删除历史是 `removeConv(hash)` 立即从 localStorage/Rust 删，**无确认、无预览**。
- **方案**：删除前弹小 modal 展示该条 metadata + 缩略图 + "确定删除？"二次确认。约 20 行。
- **价值**：防误删，3 行级。

#### 🟢 P-MGT-6 · **截图历史无"打开原图/查看 EXIF"**（用户拿到截图后想"当时截的什么"）
- **现象**：历史卡片只显示缩略图 + 时间 + OCR 行数，**无"打开"按钮**。
- **方案**：卡片右键菜单/长按弹"🖼 用默认程序打开"（调用 `open_external` 写临时文件 → `open_path`）。约 15 行 + 8 行 Rust 临时文件命令。
- **价值**：低优先级但常被问。

#### 🟡 P-MGT-7 · **历史无"按尺寸/类型"过滤**
- **现象**：搜索只能 OCR 文字/时间。截图多了按"全屏/区域/窗口/滚动"类型过滤缺失。
- **方案**：`HistoryItem` 已有 `source` 字段（`capture`/`clipboard`），加 `capture_type?: 'screen'|'region'|'window'|'scroll'`，列表加类型 filter chip。约 25 行。
- **价值**：30+ 张历史时实用。

#### 🟡 P-MGT-8 · **AI 对话索引无"按时间范围/preset/标签"过滤**
- **现象**：History 列表只按搜索词。
- **方案**：加时间范围（今天/本周/本月/全部）+ preset 过滤 + 标签过滤。约 40 行。
- **价值**：50+ 条历史时实用。

---

## 七、预览功能 · 现状审计

### 7.1 已实现

| 预览场景 | 入口 | 现状 |
|---|---|---|
| AI 文档 HTML 预览 | 👁 按钮 | `handleExportPreview` iframe 渲染 |
| OCR 预览（带 bbox） | OCR 面板 | `ocrBoxes` + `ocrSelectedBlocks` |
| 历史缩略图 | 卡片 | `thumb` 200×200 JPEG 0.7 |
| 批量取字预览 | 批量 modal | 全文字 + 高亮匹配 |
| AI 流式预览 | popup | Phase 17-18 主操作区 |
| 划词联动预览 | OCR 全文视图 | Phase 27 |

### 7.2 预览维度的真实缺口

#### 🟢 P-PREVIEW-1 · **历史库无"大图预览"**（缩略图看不清细节）
- **现象**：缩略图 200×200，截图是 4K Retina 时缩略图完全糊。
- **方案**：列表卡片 click 弹**大图 lightbox modal**（双击或点 🔍 按钮），显示原图 + 元数据（尺寸/OCR/时间）+ 操作（编辑/打开 AI 对话/导出/分享）。约 50 行。
- **价值**：基础 UX，3 行级。

#### 🟢 P-PREVIEW-2 · **导出预览（HTML）无"切主题即时预览"**
- **现象**：导出的 HTML 是基于当前 theme 渲染的，**不能在预览中切主题对比**。
- **方案**：preview modal 加主题 chip，切换时**重新渲染同一 Markdown 用新主题**，实时对比。约 30 行。
- **价值**：让 5 主题真正"所见即所得"。

#### 🟢 P-PREVIEW-3 · **AI 输出无"草稿/历史"预览**（同一会话内的多轮）
- **现象**：润色覆盖后看不到上一版。
- **方案**：见 P0-AI-3（同一解决方案），UI 加下拉「v1 / v2」预览。

#### 🟡 P-PREVIEW-4 · **PDF 预览无内置渲染**（依赖系统 PDF 阅读器）
- **现象**：PDF 预览走 iframe → 调用浏览器 PDF viewer，**不一定好用**（部分 Tauri 嵌入式 Webview 没 PDF viewer）。
- **方案**：检测 `navigator.pdfViewerEnabled === false` 时给"📂 在 Finder 中显示"按钮。约 10 行。

---

## 八、界面风格 · 现状审计

### 8.1 已实现（中性主题色，无刺眼红）

- CSS 变量主题系统（`--accent` / `--surface` / `--text` / `--muted`），明/暗双主题
- 中性主题色（蓝 `#0a84ff` 为主，警告用 `#ff9500`，错误用 `#ff3b30` 仅在 toast/警示）
- Phase 18 收尾：redact 脉冲由警示红→主题蓝，避免"满屏红"
- 标签徽标 / chip 风格 / 圆角 / 投影 / 微动效统一
- `index.css` 4982 行 + `ai-window.css` 3 文件，所有 UI 共用变量
- 双语完整（zh-CN / en-US 579 行对齐）

### 8.2 界面风格的真实缺口

#### 🟢 P-UI-1 · **暗色模式图标对比度**
- **现象**：部分图标（⚙️ 设置 / 📂 文件夹）在深色背景下对比度偏低。
- **方案**：CSS 变量统一 `filter: brightness(0.95) on dark`，约 8 行。
- **价值**：细节润色。

#### 🟢 P-UI-2 · **AI 历史卡片无视觉分组**（按时间 today/yesterday/this week/older）
- **现象**：列表平铺，难快速定位。
- **方案**：列表按 `updatedAt` 分组（今天/昨天/本周/更早），加 sticky header。约 30 行。
- **价值**：50+ 条历史时必备。

#### 🟢 P-UI-3 · **OCR 面板字号/行距无法独立调整**
- **现象**：字号可调（`ocrFontSize`），行距固定。
- **方案**：加 `ocrLineHeight` 偏好，CSS `line-height: var(--ocr-lh)`。约 10 行。
- **价值**：低视力用户/长文本阅读。

#### 🟡 P-UI-4 · **导出面板无"最近导出"快捷列表**
- **现象**：导出后无历史，只能去文件夹找。
- **方案**：本地"最近 5 次导出"列表（仅文件名 + 时间 + 格式），点"📂 在 Finder 中显示"（依赖 P-DOC-1）。约 25 行。
- **价值**：高频用户友好。

#### 🟡 P-UI-5 · **主窗口/AI 窗口/编辑器窗口视觉风格**未完全统一
- **现象**：AI 窗口已用 `ai-window.css` 独立样式（路径A），但与主窗口 `.ai-panel` 共享部分类，偶有错位。
- **方案**：抽出共用类到 `ai-shared.css` 集中管理。约 20 行。
- **价值**：可维护性。

---

## 九、按 ROI 排序的推荐落地（不擅自动手）

| 优先级 | 编号 | 简述 | 估计行数 | 价值 |
|---|---|---|---|---|
| 🔥P0 | **P-DOC-1** | **导出后"在 Finder 中显示"** | 5 Rust + 10 前端 | **每次导出都用，强烈建议立即补** |
| 🔥P0 | P-MGT-1 | 截图历史 + AI 对话 双视图联通 | 40 前端 | 体系闭环 |
| 🔥P0 | P0-AI-1 | AI 编辑产物独立持久化（postEditThumb） | 15 前端 | 补 Phase 19-B5 最后一公里 |
| 🟠P1 | P-DOC-2 | 默认保存目录记忆 | 5 前端 | 5 行级高频痛点 |
| 🟠P1 | P-DOC-3 | HTML 导出"在线再编辑" | 30 前端 | 解决导出→再加工最后一公里 |
| 🟠P1 | P0-AI-2 | AI 历史批量管理（删/导出/标签） | 60 前端 + 1 Rust 字段 | 玩具→生产级 |
| 🟠P1 | P0-AI-3 | AI 润色多版本对比 | 30 前端 | 高频决策辅助 |
| 🟠P1 | P-MGT-2 | 截图历史标签/收藏/归档 | 40 前端 + 1 Rust 字段 | 长期使用刚需 |
| 🟠P1 | P-MGT-3 | AI 历史"全部导出" | 50 前端 | 学术/法律刚需 |
| 🟠P1 | P-MGT-4 | localStorage 容量监控+告警 | 20 前端 | 防御性 |
| 🟠P1 | P-DOC-4 | 主题预览图（hover 即见） | 50 前端 | 主题选择开盲盒→WYSIWYG |
| 🟢P2 | P-IMG-1 | 编辑器深色/浅色切换 | 20 前端 | 与主窗口对齐 |
| 🟢P2 | P-IMG-2 | 标注全文搜索 | 30 前端 | 长批注刚需 |
| 🟢P2 | P-IMG-3 | 标注标签 | 25 前端 | 教学/审阅 |
| 🟢P2 | N-OCR-1 | OCR 结果贴为标注 | 30 前端 | OCR→批注闭环 |
| 🟢P2 | N-OCR-2 | OCR 历史带缩略图 | 25 前端 + 1 Rust 字段 | OCR 可追溯 |
| 🟢P2 | P0-AI-4 | popup 键盘快捷键 | 20 前端 | 效率 |
| 🟢P2 | P0-AI-5 | AI 输出字数/阅读时长 | 12 前端 | 立即有感 |
| 🟢P2 | P0-AI-6 | agentSteps 时间轴 | 25 前端 | 可视化 |
| 🟢P2 | P-DOC-5 | 智能导出文件名 | 15 前端 | 文件管理 |
| 🟢P2 | P-DOC-6 | 文档复制到剪贴板 | 20 前端 | 10 行级高频 |
| 🟢P2 | P-DOC-7 | 导出进度 toast | 10 前端 | 5 行级 |
| 🟢P2 | P-MGT-5 | 删除二次确认 | 20 前端 | 防误删 |
| 🟢P2 | P-PREVIEW-1 | 历史大图 lightbox | 50 前端 | 基础 UX |
| 🟢P2 | P-PREVIEW-2 | 预览内切主题 | 30 前端 | 主题 WYSIWYG |
| 🟢P2 | N-OCR-3 | OCR 选区拖拽矩形反馈 | 15 前端 | 5 行级 |
| 🟢P2 | P-UI-2 | AI 历史按时间分组 | 30 前端 | 50+ 条必备 |
| 🟢P2 | P-UI-1 | 深色模式图标对比度 | 8 前端 | 细节 |
| 🟡P3 | 其余 9 项 | 优先级低 | — | 留给后续 |

---

## 十、关键决策建议

| 用户最关切 | 建议立即落地 | 理由 |
|---|---|---|
| **导出后找不到/在 Finder 中显示** | **P-DOC-1**（5+10 行） | 强烈建议：已集成 opener 插件但未用 reveal，是**目前唯一明确的"已集成未用"缺口**；用户每次导出都会用 |
| **OCR/AI 产物的持久化与管理** | **P-MGT-1 + P-MGT-2 + P-MGT-4**（三件套） | 体系闭环；防 localStorage 撑爆；长期使用必备 |
| **AI 编辑后产物可追溯/可重导** | **P0-AI-1**（15 行） | 补 Phase 19-B5 最后一公里 |
| **导出后能再加工** | **P-DOC-3** HTML 在线再编辑（30 行） | 解决"导出→再加工"最后一公里 |
| **历史多了怎么管** | **P0-AI-2 + P-DOC-2**（批量管理 + 默认目录） | 50+ 条时必备 |
| **UI 润色** | **P-UI-2**（AI 历史时间分组）30 行 | 立即有感 |
| **OCR 增强** | **N-OCR-1 + N-OCR-2**（贴标注 + 历史带图） | 25+30 行 |

---

## 十一、本轮严守的边界

- ❌ **未动手修改任何源码**（仅诊断 + 路线图）
- ❌ **未提议任何 P0-γ（需 Rust 后端代理）/ P1 语义记忆（需 transformers.js 新依赖）**（违反铁律）
- ❌ **未推翻重构任何现有功能**（100% 增量）
- ✅ **所有建议 100% 纯前端 / 极小 Rust 增量（如 P-DOC-1 仅 5 行）/ 零新依赖**
- ✅ **每个建议给具体行数 + 价值依据 + 与现有模块的复用路径**

---

## 十二、与上一轮"整体复盘终极版"的差异

| 维度 | 上一轮 | 本轮 |
|---|---|---|
| 视角 | 整体项目对顶级项目 + bug 收尾 | **用户五维追问（OCR/AI/图片/文档/界面风格）** |
| 颗粒度 | 30+ 候选清单 | **47 项新缺口**，按 ROI 排序 |
| 重点 | AI 模块 | **持久化/管理/下载/预览**（用户本次核心追问） |
| 文档生成 | "5 主题 + 6 格式已行业级" | **9 项真实缺口**（其中"在 Finder 中显示"是"已集成未用"） |
| 持久化 | 简述"localStorage 按 hash 分桶" | **8 项新缺口**（标签/批量/容量监控/双轨联通） |
| 界面风格 | "中性主题色 OK" | **5 项新缺口**（暗色对比/历史分组/字号行距/最近导出/样式统一） |

---

> **本轮定位**：用户五维追问下的**新需求发现**，47 项缺口按 ROI 排序，**不动手**。等你拍板下一步。
> 推荐**优先落地 P-DOC-1（5+10 行，"在 Finder 中显示"，目前唯一明确的"已集成未用"缺口）+ P-MGT-1（双视图联通，体系闭环）** 作为下一轮两个最小最高 ROI 起点。
