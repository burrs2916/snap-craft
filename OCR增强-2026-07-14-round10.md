# OCR 增强 · 第 10 轮（需求攫取 → 历史网格一键取字）

> 实施时间：2026-07-14。状态：代码完成，`pnpm build`（tsc + vite）exit 0（125 模块），dist 已含新 locale 键。
> 约束：纯前端增量，复用既有 `startOcrFromShot` / `openEditor` / `runOcr`，**零后端改动**，**未触碰截图/滚动/标注/裁剪/编辑/结果条等其它功能**。

## 一、需求攫取与可用性评估

对已完成 9 轮增强的 OCR 能力做「还差什么」的需求回顾。当前 OCR 能力已覆盖：结构化识别（坐标+置信度）、语言选择、区域框选、逐行内联纠错、导出 txt/md/json/tsv、搜索+命中高亮跳转、置信度阈值、合并为一行、偏好持久化、自动复制、耗时/行数/字符统计、本地历史、智能阅读顺序、面板重识别、面板快捷键、字号 A−/A+、逐行勾选（精准提取）、面板内空间缩略图+边界框双向联动、结果条一键取字。

**仍欠服务的用户需求（按可用性排序）：**

| # | 需求 | 可用性 | 落地风险评估 | 本轮处理 |
|---|------|--------|--------------|----------|
| 1 | **历史截图直接取字**：历史网格当前只能 复制/保存/钉图/删除，要 OCR 旧截图必须先点缩略图进编辑器、再点「取字」，两步且易误触编辑 | 高 | 低（仅加 1 个按钮，复用已在结果条验证过的 `startOcrFromShot`，隔离在 `stopPropagation` 内） | ✅ 实现 |
| 2 | 翻译 / 离线词典 | 中 | 高（需网络或离线资源，超出本地工具定位） | 暂缓 |
| 3 | 去重/去空行、表格化等文本清洗 | 低 | 低（但面板已足够丰富，继续膨胀收益低） | 暂缓 |

结论：本轮落地 **需求#1**——「高频刚需（常需回看并提取旧截图文字）+ 零风险（纯历史网格按钮，复用已验证路径）」，最符合「提升可用性且不影响其它功能」的约束。需求#2/#3 延续前几轮的高风险/低收益判断暂缓。

## 二、实现项：历史网格一键取字

- **新增按钮**：历史网格每项的悬浮操作行（`history-item-actions`）在「复制 📋」之后插入「取字 🔍」按钮，顺序与结果条（复制 → 取字）保持一致，视觉协调。
- **复用既有路径**：`onClick={(e) => { e.stopPropagation(); startOcrFromShot(h); }}`。
  - `startOcrFromShot(h)` 内部先 `openEditor(h)`（设 `current` + 切编辑视图），再 `runOcr(h.dataUrl)`（接收 `imageData` 入参，不依赖 `current` 闭包）。
  - `HistoryEntry`（含 `id/dataUrl/width/height`）与 `startOcrFromShot` 的形参结构 `{id,dataUrl,width,height}` 完全兼容，类型检查通过。
- **隔离性**：`stopPropagation()` 确保点击取字不会冒泡触发卡片本身的 `openHistory(h)`（进编辑器但不识别）；与既有的 复制/保存/钉图/删除 四个按钮行为互不干扰。
- **文案**：新增 `history.ocrTitle`（`对该截图直接识别文字（进入编辑器并取字）`）与 `history.ocrAria`（`识别该截图中的文字`），中英文 locale 同步。

## 三、关键设计 / 安全边界

- **零回归**：历史网格原有 复制/保存/钉图/删除 四按钮行为完全不变；新增按钮仅在 hover/focus 时浮现（沿用 `.history-item-actions` 的 `opacity` 切换），平时不可见，不改变既有布局与交互。
- **零后端改动**：未改 `ocr.rs` / `commands/mod.rs` / `lib.rs`；新增逻辑 100% 在前端 `EnhancedScreenshotApp.tsx`（历史网格按钮）+ 两个 locale 文件。
- **不影响其它功能**：改动收敛在历史网格悬浮操作行；截图/滚动长截/区域/窗口/标注/裁剪/编辑/结果条/OCR 面板/权限/主题/多语言均未被触碰。
- **主题兼容**：按钮沿用 `.history-act-btn`（`--accent` 着色），`prefers-color-scheme` 切换无需额外处理。

## 四、验证

- `pnpm build` → `tsc && vite build` exit 0，125 模块，dist JS 已含 `history.ocrAria` / `history.ocrTitle` 键（确认按钮已接线）。
- 类型检查通过（tsc 无报错），`startOcrFromShot(h)` 调用无未定义引用。
- 因无后端改动，`cargo check` 非必需，未运行以避免无关噪音。

## 五、后续可评估（延续前几轮，本轮不做）

- 翻译 / 离线词典（需求#2，网络/资源边界）
- 文本清洗（去重/去空/表格化，需求#3，面板膨胀风险）
- OCR 面板内增强已高度收敛；后续如需突破「不影响其它功能」边界，可考虑跨视图/离线翻译资源。

## 六、验收方式

```bash
./start.sh app        # 打开已含新前端的 SnapCraft.app（改完前端 pnpm build 后重跑）
```

1. 主页历史区：鼠标悬停任意历史截图 → 右上角操作行出现 `📋 🔍 💾 📌 🗑`。
2. 点 `🔍` → 直接进编辑器并弹出 OCR 面板（无需先点缩略图再点「取字」），语言沿用偏好。
3. 复制/保存/钉图/删除 四个原按钮行为不变；点缩略图本体仍走「进编辑器」旧路径（不识别）。
