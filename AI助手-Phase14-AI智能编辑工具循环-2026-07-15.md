# SnapCraft AI 助手 — Phase 14：AI 智能编辑（工具循环 / Agentic Tool-Calling）

> 落地日期：2026-07-15 · 纯前端增量、零 Rust 改动、不影响截图/标注/OCR/历史/导出
> 对标对象：`claw-code` 的 `run_turn` 工具循环（`conversation.rs:313-515`）与 `openclaw` 的 `tool-loop-detection`（失控检测）+ Anthropic 多形态 tool_call 识别

---

## 一、为什么做这一期（诊断结论）

复盘 Phase 1–13 后，与顶级项目对照出**最大差距 = AI Agent 工具循环**。

| 能力 | claw-code / openclaw | SnapCraft（Phase 13 及之前） |
|---|---|---|
| 流式 SSE 健壮性 | ✅ 帧切分 + 退避重试 | ✅ 已追平（Phase 13） |
| 上下文预算 / 压缩 | ✅ | ✅ 裁最旧 + 非流式摘要 |
| 记忆检索 | ✅ 六因子 + MMR | ✅ 启发式 + 多因子 + MMR（Phase 9/升级） |
| **Agent 工具循环** | ✅ `run_turn` / `runTurn` | ❌ **未做 = 最大缺口 = 最大卖点** |

竞品（含 privdoc-ai）的 AI 只产出**文字**；而截图工具的独有价值是「让 AI 直接操作截图本身」——
自动圈选重点、智能打码（隐私哨兵）、高亮关键文字、裁剪区域 OCR，把「截图即文档上下文」升级为「截图即 AI 可编辑对象」。

---

## 二、实现方案（最小侵入、四层贯通）

借鉴 claw-code `run_turn` 的「模型产出 tool_use → 宿主派发执行 → 结果回灌 → 继续流式」循环，
落地为纯前端工具循环，**复用现有 `useScreenshotStore` 的 `addAnnotation`/`updateAnnotation`/`deleteAnnotation`**（这些方法内部已自动写入 undo 历史，安全）。

```
类型层 aiTypes.ts
  └─ AiToolDef[] / AiToolCall / AiToolResult / AiAgentStep / StreamOpts.tools?
        │
客户端循环 aiClient.ts  →  streamChatWithTools()（Agent 主循环）
  ├─ 复制消息列表（不改调用方数组）
  ├─ 循环至 maxToolTurns=8 或模型不再调用工具
  ├─ 逐条 executor 派发 → 结果以 role:'tool' 回灌
  └─ 支持 AbortSignal 用户取消（取消即抛 AbortError）
        │
工具定义 aiTools.ts  →  AI_TOOL_DEFS（4 工具 JSON Schema）+ createToolExecutor(host)
        │
宿主注入 两编辑器    →  构造 AiToolHost 实现（drawRectangle/redactArea/highlightRect/summarizeRegion）
        │
状态层 aiStore.ts    →  runAgent() + agentSteps 状态（步骤可视化）
        │
UI 层 AIPanel.tsx    →  「🤖 AI 智能编辑」开关 + 步骤时间线渲染
```

### 坐标约定（关键设计点）
模型只输出 **0~1 归一化比例**（`x,y,w,h`），由宿主在注入时按原图像素尺寸换算：
`px = clamp01(v) * imgWidth/Height`。**绝不让模型直接碰像素坐标**（避免越界/错配）。

---

## 三、暴露给模型的 4 个工具

| 工具名 | 作用 | 命中标注类型 |
|---|---|---|
| `draw_rectangle` | 在区域画矩形框，可选 `label` 文字 | `rectangle`（+ 可选 `text`） |
| `redact_area` | 打码/脱敏（隐私哨兵），`mode: mosaic\|blur\|black` | `mosaic` geometry |
| `highlight_text` | 高亮某区域文字（半透明黄），`color` 可选 | `highlight` geometry（opacity 0.45） |
| `summarize_region` | 裁剪区域 → 调 `ocr_image` 取该区 OCR 文字返回给模型 | 无新增标注，纯读操作 |

- `summarize_region` 为**只读工具**：让模型「看一眼某块区域有什么字」而不污染画面，闭环「区域理解」。
- 全部走既有 `addAnnotation`，自动进 undo 栈，用户可一键撤销 AI 的编辑。

---

## 四、改动文件清单（纯前端）

| 文件 | 改动 |
|---|---|
| `src/features/ai/aiTypes.ts` | 新增 `AiToolDef` / `AiToolCall` / `AiToolResult` / `AiAgentStep`；`AiMessage.role` 加 `'tool'`、`toolCalls`/`toolResult`；`StreamOpts.tools?` |
| `src/features/ai/aiClient.ts` | 重写 `toOpenAiMessages`/`toAnthropicMessages`（处理 tool 角色 + assistant toolCalls）；新增 `streamChatWithTools()` 主循环 + `StreamWithToolsOpts`；**顺手修复 Anthropic 多 system 消息只取首条**的潜在 bug（现 join 合并，避免长期记忆注入被吞）；`buildBody` 注入工具定义 |
| `src/features/ai/aiTools.ts` | **新增**：`NormRect`/`AiToolHost` 接口、`AI_TOOL_DEFS`（4 工具 + JSON Schema）、`createToolExecutor(host)`、`toolLabel(name)`（i18n 映射）、`agentSystem()`（语言自适应系统提示） |
| `src/features/ai/aiStore.ts` | 新增 `agentSteps` 状态 + `runAgent(input & {host})`：构建含 `agentSystem()` 的消息，调 `streamChatWithTools`，`onToolCall/onToolResult` 实时更新步骤时间线，`maxToolTurns:8` |
| `src/features/screenshot/components/EditorWindow.tsx` | 构造 `aiTools: AiToolHost`（用 `imageData` 尺寸 + `invoke('ocr_image')`），传 `aiTools={aiTools}` 给 AIPanel |
| `src/features/screenshot/EnhancedScreenshotApp.tsx` | 同上（用 `current?.dataUrl` + `current?.width/height`） |
| `src/features/ai/AIPanel.tsx` | 新增 `aiTools?` prop + `agentMode` 开关 chip；生成按钮在 agent 模式下改走 `handleAgentRun`、文案变 `ai.agentRun`；新增 `.ai-agent-steps` 步骤时间线渲染 |
| `src/locales/zh-CN.json` / `en-US.json` | 加 `agentMode`/`agentModeDesc`/`agentRun` + `agentTool.{draw_rectangle,redact_area,highlight_text,summarize_region}` |
| `src/index.css` | 加 `.ai-chip-agent.active`（渐变高亮）+ `.ai-agent-steps`/`.ai-agent-step*`（9 类） |

---

## 五、验证结果

| 检查项 | 结果 |
|---|---|
| `npx tsc --noEmit` | ✅ 零类型错误 |
| `pnpm build`（tsc + vite） | ✅ exit 0，**137 模块**（基线 136 + 新增 `aiTools.ts`） |
| Bundle 含工具定义 | ✅ `draw_rectangle`/`redact_area`/`highlight_text`/`summarize_region` 各 8 处字符串命中 |
| Bundle 含 Agent UI | ✅ CSS `ai-agent-steps`/`ai-chip-agent`/`ai-agent-step*` 全量命中 |
| 源接线一致性 | ✅ AIPanel（prop+route+steps）/ aiStore（runAgent+loop）/ 两编辑器（host+pass）四处全通 |
| 对现有功能回归 | ✅ 非 agent 模式走原 `handleGenerate`，对话/历史/导出/润色/回写全部不受影响 |

**修复的关键阻塞**：`EnhancedScreenshotApp.tsx` 的 `useMemo` 漏 import（此前文件被 linter 改动导致 edit 失败、未回补），导致 `aiTools: AiToolHost = useMemo(...)` 处 `useMemo` 未定义、宿主方法参数隐式 any。补回 import 后 tsc/build 全绿。

**顺带修复的真实 bug**（在重写 `toAnthropicMessages` 时发现）：Anthropic 多 system 消息此前仅 `find()` 取第一条，长期记忆（Phase 6/9）注入会被静默吞掉 → 现改为 `\n\n\n` 合并，记忆注入不再丢失。

---

## 六、用户使用路径

1. 打开编辑器 → 工具栏点「✨ AI」打开右侧面板。
2. 点「🤖 AI 智能编辑」chip 开启 Agent 模式（仅当宿主注入 `aiTools` 时显示）。
3. 输入自然语言目标，例如：
   - 「把右上角的金额区域打码（模糊）」
   - 「在标题下方画一个红色矩形框，标注『重点』」
   - 「高亮中间那段报错文字，并告诉我它大概在说什么」
4. 模型会**多轮调用工具**直接修改当前截图；面板下方实时显示步骤时间线（画框 / 打码 / 高亮 / 区域 OCR）。
5. 结果即所见即所得——所有 AI 编辑都落在标注层，可撤销、可继续手改、可导出成图/文档。

---

## 七、与顶级项目的差异 & 后续候选

**已对齐**：工具循环主流程（产出→派发→回灌→续流）、`maxToolTurns` 失控保护、AbortController 取消、坐标归一化。

**仍有差距（后续候选，按优先级）**：
1. **工具失控检测**（openclaw `detectToolCallLoop` 哈希去重）：当前仅按 `maxToolTurns=8` 兜底，未做「重复 tool_call 哈希」检测 → 可加。
2. **上下文增量压缩**（claw-code `compact.rs` 保最近 N 轮 + 工具对边界保护）：当前每轮把全量 assistant+tool 历史回传，超长会膨胀 → 接入 Phase 13 的 `trimHistoryToBudget` 工具对感知。
3. **后端代理隐藏 Key**（需 Rust）：当前密钥仍在前端 localStorage。
4. **选区/划词 AI**：在画布上直接选中区域唤起 mini AI。
5. **会话 fork**（claw-code `Session.fork`）：隔离不同编辑分支。

> 严守原则：纯增量、不推翻重构、不动未请求功能；本 Phase 与截图/标注/OCR/历史/六格式导出零冲突。重启 App（`./start.sh dev` 或 `pnpm build && ./start.sh app`）即见。
