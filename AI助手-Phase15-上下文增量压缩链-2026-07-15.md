# AI 助手 Phase 15 — 上下文增量压缩链（对齐顶级项目「滚动摘要」）

> 本轮为「继续下一步」的落地项：Phase 14 后续候选清单的第 ② 项（上下文增量压缩），也是用户诉求「看看人家如何处理**上下文/记忆**」的直接回应。
> 纯前端、零 Rust、不影响截图/标注/OCR/历史/导出/生成；`tsc --noEmit` 与 `pnpm build` 全绿（137 模块）。

## 一、诊断：当前压缩的真实短板

`aiStore.ts` 的 `compactMemory`（Phase 6 落地）每次把最早 `COMPACT_ENTRIES=4` 条（=2 轮）对话**独立摘要成一条新记忆，追加进 `memories` 数组**。

这带来两个隐性缺陷，恰好是顶级项目已解决、而我们此前标注为「待做」的差距：

1. **无累积链（dilution risk）**：每条记忆都是「窗口内孤立摘要」。当一段事实在窗口 A 引入、在窗口 B 才显重要时，两次独立摘要之间会**断裂**，关键信息可能被拆散。
2. **列表无界增长 + 注入上限稀释**：长会话里 `memories` 不断变长；而 `selectMemories`（Phase 9）单轮最多只注入 `MAX_RELEVANT=5` 条。当记忆 >5 条时，靠「相关性/重要性」筛选——早期但当下「不相关」的关键事实，**会被挤出上下文**。

对照顶级项目：
- **openclaw** `compaction.ts`（增量摘要）：把 `previousSummary` + 新消息 → 产出**更新后的完整摘要**，链式累积，单一演进摘要永不丢旧事实。
- **claw-code** `compact.rs`：`summary` 滚动重压，保留最近 N 轮 + 一段持续演进的摘要。

我们此前的实现二者都没追平。本轮补齐。

## 二、实现（4 个文件，增量改动）

### 1. `aiStore.ts` — 核心改造
- **`buildCompactSystem` 重写为「增量滚动摘要」契约**：明确告知模型——会收到【已有长期摘要】（可能为空）+【新增对话片段】，必须输出**更新后的完整摘要**，融合二者、保留全部既有事实、只丢寒暄/重复/草稿（中英双语）。
- **`compactMemory` 改为链式折叠**：
  - `existing = memories.map(m => m.summary).join('\n\n')` —— 取出累积知识作为 `previousSummary`；
  - 用户消息改为 `【已有长期摘要】\n{existing}\n\n【新增对话片段】\n{transcript}`（首次为空则退化原行为）；
  - 结果**用「一条滚动摘要」替代全部旧记忆**（`memories = [rolled]`），而非 `[...memories, mem]`：
    - `turnsCovered` 累加（覆盖轮数持续增长）；
    - `importance = max(历史最高, 本论解析值, 3)`（关键事实永不降级）；
    - `merged` 计数 +1（记录链累加次数，供 UI 透出）；
    - 长度恒为 1 → `selectMemories` 永远全量注入 → **零稀释**。
  - 失败路径不变（空摘要/异常 → 直接 return，不裁对话、不丢记忆）。

### 2. `aiTypes.ts` — `AiMemory` 新增 `merged?: number`
   注释明确其为「滚动摘要由多次压缩链累加」的标志，语义对齐 openclaw/claw-code。

### 3. `AIPanel.tsx` — 记忆区透出增量信号
- 记忆项在 `覆盖 N 轮` 旁，当 `merged>1` 时追加 `· 增量合并 N 次`（中文）/ `· merged N× incrementally`（英文）。
- 记忆栏标题徽标由「· 条数」改为「· 累计覆盖轮数（轮已沉淀）」——更准确反映「一条滚动摘要沉淀了整段对话」的事实。

### 4. 国际化 + CSS
- `zh-CN.json` / `en-US.json`：新增 `memMerged` / `memRounds`。
- `index.css`：新增 `.ai-mem-merged`（复用 `--accent`，明暗自适应）。

## 三、验证
- `tsc --noEmit`：exit 0，零类型错误。
- `pnpm build`：exit 0，137 模块，无回归。
- 产物核对：`dist` 含 `已有长期摘要`/`EXISTING SUMMARY`（链式输入）、`滚动长期摘要`/`ROLLING long-term summary`（增量契约）、`memMerged`/`memRounds`/`增量合并`/`ai-mem-merged`（UI 透出）。
- 行为核对：
  - 首次压缩：`memories=[]` → `existing=''` → 退化为摘要首窗，结果 `memories=[1条, merged:1]`；
  - 第二次压缩：`existing=上一条摘要` → 模型在其上融合新窗 → `memories` 仍为 1 条、`merged:2`、`turnsCovered` 累加；
  - 长会话：`memories` 恒为 1 条 → 注入永不满 5 上限 → 早期事实零稀释；
  - 关闭/重开面板：`saveMemories` 落盘 `[rolled]`，`loadMemories` 恢复 → 累积跨会话保留。

## 四、与顶级项目的差距地图（更新）

| 能力 | 顶级项目 | 我们（本轮后） | 状态 |
|---|---|---|---|
| SSE 健壮解析 / 退避重试 | openclaw / claw-code | Phase 13 | ✅ 追平 |
| 上下文预算护栏 | openclaw compaction | Phase 13 `trimHistoryToBudget` | ✅ 追平 |
| **增量滚动摘要（上下文/记忆）** | openclaw / claw-code | **本轮** | ✅ **新追平** |
| 记忆多因子 + MMR | openclaw memory-core | Phase 9 | ✅ 追平 |
| token 成本 / 错误分类 / 提示缓存 | claw-code | Phase 16:55 轮 | ✅ 追平 |
| Agent 工具循环 | claw-code / openclaw | Phase 14 | ✅ 追平 |
| 会话分桶隔离 | （我们 localStorage 按截图哈希） | Phase 4 | ✅ 足够 |

**剩余待做（按优先级）**：
1. 🟢 **语义记忆（本地 embedding 替代词法启发式）** —— 当前 Phase 9 仍为词法重叠，无向量/概念层；
2. 🟢 **选区 / 划词 AI** —— 在截图上圈选区域或划词，直接就该区域问 AI（强卖点，纯前端可行）；
3. 🟡 **会话 fork**（claw-code `Session.fork`）—— 克隆线程试不同分支；
4. 🟡 **隐私哨兵 Agent** —— 基于 Phase 14 的 `redact_area` 做自动敏感信息打码；
5. 🔴 **后端代理隐藏 Key**（需 Rust）；
6. 🟢 **PPT 导出 / docx 高级排版**。

## 五、卖点收敛
AI 助手的「真卖点」三件套现已全部坐实且经过真值核查：
- **AI 直接改图**（Phase 14 工具循环：圈选/打码/高亮/区域 OCR）—— 竞品只产文字；
- **编辑后图即上下文**（Phase 8/10）—— 打码/模糊已合入，隐私不外泄；
- **长程不丢上下文**（Phase 6 + 本轮增量链）—— 多轮迭代成稿时早期事实恒定在域。

下一步建议从 **①语义记忆** 或 **②选区/划词 AI** 切入（均为纯前端、高收益、零 Rust）。
