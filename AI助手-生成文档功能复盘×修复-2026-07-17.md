# AI 助手·第一个功能标签「生成文档」复盘 × 修复（2026-07-17）

> 范围：仅聚焦第一个功能标签 `doc`（生成文档）及其连带的「生成 → 预览 → 导出」前端链路。
> 方法：直读源码（file:line 实证），区分「真 bug / 空实现 / 假实现 / 达不到预期」与「需求点」。
> 铁律保持：纯前端直连、0 Rust 改动、0 新依赖、0 破坏现有功能。

## 一、审计结论：4 项真实缺陷（已全部修复）

### 🔴 缺陷 1（P0）：默认激活的预设是数组末项 `table`，不是第一个 `doc`
- **位置**：`src/features/ai/aiStore.ts:482`
- **根因**：`let activePresetId = AI_PRESETS[AI_PRESETS.length - 1]?.id`（数组末项 = `table` 提取表格）。`AIPanel` 挂载时无任何 `setActivePreset('doc')`，模块级变量每次加载都重置为末项。
- **现象**：打开 AI 面板，高亮/默认选中的是**最后一个**标签（提取表格），而非第一个卖点「生成文档」——与"第一个功能标签=生成文档"的产品预期不符，属典型"末项默认"笔误。
- **修复**：默认改为 `AI_PRESETS[0]?.id`（即 `doc`）。一行，零风险。

### 🔴 缺陷 2（P0）：预览渲染器 `AiMarkdown` 不支持 GFM 表格
- **位置**：`src/features/ai/aiMarkdown.tsx`
- **根因**：内联渲染器只覆盖 标题/段落/有序·无序列表/引用/代码块/分割线，**完全没有表格解析**。而导出器 `markdownHtml.ts:474-516` 是支持表格的。
- **现象**：`doc`/`bug`/`prd`/`compete`/`meeting`/`insight`/`table` 等预设产物里大量出现 GFM 表格，但**聊天流与历史阅读器里显示成原始 `| 列1 | 列2 |` 管道文本**，而导出的 docx/html/pptx 却是正常表格 → 经典"所见≠所得"。（v6 曾列为 Top5 必做项，但此前只诊断未落地。）
- **修复**：在 `AiMarkdown` 主循环里、段落兜底之前插入表格解析块，规则**逐字对齐** `markdownHtml.splitRow`/分隔行对齐（`left/center/right`）——保证预览与导出 100% 一致。4 个输出面（行 1760/1788/1904/2091）一次性受益。

### 🟠 缺陷 3（P1）：HTML 导出丢失来源截图（与 docx/pptx 不一致）
- **位置**：`src/features/ai/AIPanel.tsx` `handleExport`（md/txt/html 分支）
- **根因**：HTML 分支仅 `sectionImages: hasSec ? orderedImages() : undefined`。纯文档（无 `<!--SNAP:k-->` 标记）时 `undefined`，而 `handleExportDocx/Pptx` 走 `images: useSections ? [] : imgs` 会把来源截图整块前置内嵌。
- **现象**：同一份「生成文档」导出 docx 顶部有截图、导出 HTML 却没有 → 导出格式之间视觉不一致。
- **修复**：HTML 分支对非章节文档注入与 PDF 分支相同的 `<figure class="doc-fig">…</figure>` 前置块，与 docx/pptx 对齐。

### 🟠 缺陷 4（P1）：应用内预览同样丢失来源截图
- **位置**：`src/features/ai/AIPanel.tsx` `handlePreview`
- **根因**：同上，`sectionImages: useSections ? orderedImages() : undefined`。
- **修复**：与 HTML 导出同源的 `<figure>` 前置注入，使「预览」与「导出 docx」视觉一致。

## 二、验证
- `pnpm build`（tsc && vite build）exit 0，0 错误 0 警告，0 新依赖，0 Rust 改动。
- 改动文件 4 个：`aiStore.ts`(1 行) / `aiMarkdown.tsx`(表格解析+helper) / `index.css`(.md-table 主题化样式) / `AIPanel.tsx`(2 处导出/预览内嵌)。

## 三、复盘发现但不本次落地（新增需求点，待拍板）
1. **「复制」只复制 Markdown 源码**：`handleCopy` 直接 `navigator.clipboard.writeText(output)`（原始 `**粗体**` 标记）。用户生成文档后想贴进 Word/Notion，拿到的是带星号的源码而非富文本。**建议新增「复制为富文本」（写 HTML 到 clipboard）**。
2. **导出后「用 Word/PPT 打开」按钮缺失**：v13 已列 P0，本次确认仍未落地——导出 docx/pptx 后只能去 Finder 翻找（`revealInFolder` 已就绪，仅差一个按钮）。
3. **docx 元信息硬编码**：`core.xml` 作者/标题为空或写死（v10 提过），导出的 Word 里看不到"标题/时间/作者"。
4. **纯文档截图只能"顶部整块"内嵌**：`doc` 预设不产生 `<!--SNAP:k-->` 锚点，故截图永远堆在文首；真正"图文穿插"需切到 `report` 预设。可考虑让 `doc` 也支持按章节锚点。
5. **无「所见即所得」编辑**：预览/聊天流里表格/图片只读，不能直接在面板内二次编辑后重导（v10/v13 闭环断点）。

## 四、状态
- 0 代码破坏 ✓ 0 Rust ✓ 0 新依赖 ✓ 严守"诊断优先、增量修复" ✓
