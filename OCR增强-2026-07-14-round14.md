# OCR 功能增强 · 第十四轮（需求攫取 → 智能文本清洗 + 实体整块复制）

> 时间：2026-07-14 09:22。状态：代码完成，`pnpm build`（前端 tsc + vite）通过（125 模块，exit 0），**后端零改动**，未触碰截图/标注/历史/钉图/主题等任何其它功能。

## 一、需求攫取与发觉（本轮起点）

对前 13 轮已落地的 OCR 能力做盘点（结构化识别/语言/区域/内联纠错/导出/搜索高亮/置信度/合并/持久化/自动复制/统计/历史/阅读顺序/重识别/快捷键/字号/勾选/空间预览联动/4 个取字入口/智能实体提取/预览框框选范围/结构化复制），按「可用性 × 风险」矩阵重新筛选剩余需求：

| 候选需求 | 可用性 | 风险 | 结论 |
|---|---|---|---|
| **智能文本清洗（全半角归一 / 去 CJK 间空格 / 折叠空白）** | 高 | 低 | ✅ 落地 |
| 实体区块「复制全部」按钮（一次复制全部网址/邮箱/电话） | 中 | 低 | ✅ 落地 |
| 实体项可点击打开（URL/email/tel） | 中 | 中 | ❌ 跳过（Tauri 拦截 `window.open`，需 shell 插件，跨组件风险） |
| 多区域批量 OCR | 中 | 中 | ❌ 跳过（延续：需改造框选流程，改动面大） |
| 翻译 | 中 | 高 | ❌ 跳过（延续：网络/离线边界） |
| OCR 历史搜索 | 低 | 低 | ❌ 跳过（历史仅 5 条，价值低） |

> 选中两项均 **纯前端、零后端改动、收敛在 OCR 面板内、opt-in 默认关闭（零回归）**，完美满足「集成/改善/优化且不影响其它功能」约束。
> 注：历史轮次曾把「文本清洗」整体判为低可用/易误导——那是指「1→l / O→0 字符猜测」。**本轮明确剔除字符猜测**，只做可逆的格式规范化（全半角/空格/空白），对中文截图取字直贴文档是首要痛点，故重新评估为高可用。

## 二、实现（增量、零回归）

### 1. 智能文本清洗 `ocrCleanText(text)`（模块级纯函数，无副作用）
- 仅做安全、可逆的格式整理，**绝不猜测字符**：
  1. `normalize('NFKC')` 全半角归一（全角字母/数字/括号/全角空格 → 半角）；CJK 标点（，。、）无兼容映射故保留；
  2. 去除 **CJK/Kana 之间** 的多余空格（OCR 常见逐字空格「你 好 世 界」→「你好世界」），英文单词内空格保留（用 `removeSpacesBetweenCJK` 逐字符扫描，避免正则 lookbehind 兼容隐患）；
  3. 折叠连续空白为单空格；
  4. 逐行去尾随空白；
  5. 折叠 ≥3 连续换行为单空行。
- 位置：紧随 `ocrExtractEntities` 之后，模块级纯函数，无平台/网络依赖。

### 2. 偏好与开关（收敛在既有 OCR 面板 + 隔离 localStorage `snapcraft.ocr.prefs`）
- `OcrPrefs` 新增 `clean: boolean`（默认 `false` = 历史观感零回归），`loadOcrPrefs` 默认同步；
- 新增 `ocrClean` 状态（从偏好初始化）；持久化 `useEffect` 写入 `snapcraft.ocr.prefs`，跨会话记住；
- 选项行新增 checkbox **「智能清洗」**（`.ocr-clean`，与其它开关同风格）。

### 3. 作用范围（WYSIWYG，精准收敛）
- 作用于 **`ocrDisplayText`**（全文框显示 + 复制全部 + 搜索高亮/命中计数 的数据源）→ 所见即所得；
- 作用于 **txt / md 导出**（`buildOcrExportContent` 的纯文本分支做清洗）；
- **不影响**：JSON/TSV 导出（带坐标，供下游精确解析，保持原始逐块文字）、逐行内联编辑框（显示原始识别文字，供人工校正）、「贴回标注」（用 `ocrTextAt` 原始块文字，按坐标定位）、智能实体提取（已基于清洗后的 `ocrDisplayText`，与全文框一致）；
- `runOcr` 不重置该偏好（与合并/自动复制同属格式偏好，跨识别持续有效）。

### 4. 实体「复制全部」按钮（隔离增量）
- 实体区块头部（已有总数 badge 旁）新增 **复制全部** 按钮（`margin-left:auto` 推到右侧），一次复制全部 网址+邮箱+电话；
- 复用既有 `copyEntItems`（整块数组 join `\n` 写剪贴板 + toast）；仅 `entTotal>0` 时渲染。

### 5. i18n（zh-CN / en-US 同步）
新增 `ocr.clean` / `ocr.cleanTitle` / `ocr.entCopyAllAll`；实体逐条复制复用既有 `ocr.copy`、组复制复用 `ocr.entCopyAll`。

### 6. CSS
新增 `.ocr-clean`（对齐 `.ocr-extract` 的 `inline-flex / accent-color` 风格）；实体头部按钮复用 `.ocr-entity-copyall`。

## 三、验证
- `pnpm build` → exit 0（tsc 通过，125 模块，vite 产物正常）。
- `dist/assets/*.js` 含 `智能清洗`/`Smart clean`/`复制全部网址`/`Copy all URLs`/`ocr-clean`/`entCopyAllAll`；
- `dist/assets/*.css` 含 `.ocr-clean`；
- **后端零改动**：`src-tauri/src/**` 本轮未触碰（`ocr.rs`/`capture.rs` 等 M/?? 均为前 13 轮遗留未提交改动，非本轮产生）。

## 四、零回归 / 不影响其它功能
- **仅改 OCR 面板内**：类型/默认/状态/持久化/纯函数/JSX 选项行与实体头/两个 locale/CSS，全部收敛在取字面板；未改任何命令签名、截图/标注/历史/钉图/主题/权限流程。
- **默认行为完全一致**：`ocrClean` 默认 `false` → `ocrCleanText` 不被调用，全文框/复制/导出/搜索/实体提取与历史版本完全相同；新增开关与按钮为纯增量。
- 所有复制/导出/贴回/历史数据链未被触碰；清洗只是「另一种方式去格式化既有显示文字」。

## 五、仍留后续评估（延续前几轮，本轮不做）
- 实体项可点击打开（Tauri `window.open` 拦截，需引入 shell 插件，跨组件风险）；
- 翻译、多区域批量 OCR（成本/跨组件/网络边界偏高，或超出本地工具定位）。
- OCR 面板纯前端增强已高度收敛；后续如要再突破需先放宽「不影响其它功能」约束或引入离线资源。
