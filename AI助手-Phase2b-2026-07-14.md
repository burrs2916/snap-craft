# AI 助手集成 · Phase 2b 报告（2026-07-14）

> 参考项目：`/Users/liwenchao/GithubProSpace/privdoc-ai`
> 目标：把 SnapCraft 从「截图 + 截图编辑」升级为「编辑截图 → AI 生成完整文档 / 文案」，且**完全不影响现有截图 / 标注 / OCR / 历史功能**。

## 需求发掘（对照 privdoc-ai）

privdoc-ai 的 AI 能力核心有三点，正好映射到 SnapCraft 的短板：

| privdoc-ai 能力 | SnapCraft 现状 | Phase 2b 落地 |
|---|---|---|
| 多文档综合分析（多源上下文） | 仅支持单张当前截图 | ✅ 多截图成稿：从历史多选，一并分析 |
| 可复用「模板 / 指令」 | 仅 7 个写死预设 | ✅ 自定义业务模板（localStorage 持久化） |
| 富文本导出（HTML/PDF/打印） | 仅 .md / .txt | ✅ 导出 .html（可 ⌘P 另存为 PDF） |

## 本轮实现（纯前端，零 Rust 改动）

### 1. 多截图成稿
- `aiTypes.ts`：`AiMessage` 新增 `images?: string[]`，单条 user 消息可携带多张图。
- `aiClient.ts`：`toOpenAiMessages` / `toAnthropicMessages` 在单条消息内拼多张 `image_url` / `image` block（去重、向后兼容 `imageDataUrl`）。
- `AIPanel.tsx`：新增「📎 附加更多截图」折叠区，`invoke('get_history')` 拉取本机历史缩略图（自动排除当前截图避免重复），勾选即作为附加上下文。
- `aiStore.ts` / `aiPresets.ts`：`generate` 接收 `images` + `ocrTexts[]`，与当前图合并为一条多模态消息；`buildUser` 支持「当前图 OCR + 多张附加图 OCR」分块注入。

### 2. 自定义业务模板
- `aiStore.ts`：新增 `customPresets`（`localStorage: snapcraft-ai-templates`），及 `addCustomPreset / updateCustomPreset / deleteCustomPreset`；`allPresets()` 合并内置与自定义，`resolvePreset()` 按 id 解析。
- `aiPresets.ts`：抽出共享 `buildDefaultUser()`；`AiPreset` 增加 `name/desc/custom` 字段；新增 `makeCustomPreset()` 工厂。
- `AIPanel.tsx`：预设芯片区末尾加「✎ 管理模板」，内联增 / 删 / 改自定义模板（名称 + 说明 + 系统指令 + 是否视觉）。自定义预设在芯片上以 ★ 标记。

### 3. 富文本导出（HTML / PDF）
- 新增 `markdownHtml.ts`：零依赖 `mdToHtml()`，覆盖标题 / 列表 / 代码块 / 表格 / 引用 / 链接 / 粗斜体 / 删除线 / 分割线 / 段落，输出自带 CJK 字体栈与打印样式。
- `AIPanel.tsx`：导出区新增 `.html` 按钮，复用既有 `save_text_file` Rust 命令写出独立 HTML（打开即可 ⌘P 另存为 PDF），无新后端。

### i18n / CSS
- `zh-CN.json` / `en-US.json` 新增 `attachMore*` / `template*` 键（共 14 个），与既有 `ai.*` 命名一致。
- `index.css` 新增 `.ai-attach*`、`.ai-tpl*`、`.ai-chip-manage` 样式，沿用 `--accent / --border / --surface` 变量，深色 / 浅色主题自适应。

## 验证
- `pnpm build`（tsc + vite）通过：**132 模块**，零类型错误（较 Phase 2a 的 131 多 1 个 `markdownHtml.ts`）。
- `dist` 含全部新 token：`.ai-attach-thumb`、`.ai-tpl-form`、`.ai-chip-manage`、`snapcraft-ai-templates`、`get_history`、`attachMoreCount`、`templateVision` 等。

## 生效方式
纯前端改动，重启 App 即可：`./start.sh dev`（连 vite）或 `pnpm build && ./start.sh app`（内嵌 dist）。

## 后续可继续发掘（待确认，未做以免影响现有功能）
- **AI 结果回写为标注**：把生成文案直接贴回截图编辑器。
- **对话历史持久化**（SQLite / Rust）。
- **后端代理**：密钥不下发渲染层（Phase 2 规划项）。
- **PDF 直出**：当前走「HTML → 系统打印另存为 PDF」；如需一键静默导出 PDF，可后续引入无头打印。
- **批量截图一键成报告**：多选历史 → 模板化生成周报 / 复盘。
