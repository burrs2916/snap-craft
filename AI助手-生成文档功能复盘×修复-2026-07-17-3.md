# AI「生成文档」第一标签 · 代码级复盘 × 修复（第三轮 · 2026-07-17 21:18）

> 自动化任务：对「生成文档」第一个功能标签（doc 预设）做前端代码级回溯，找空/假实现、达不到预期的缺陷并直接修复，发掘新卖点。
> 方法：直读 `AIPanel.tsx` / `markdownHtml.ts` / `markdownDocx.ts` / `aiStore.ts` / `aiPresets.ts` 取得 file:line 实证，并以 esbuild→Node 跑真实产物验证（非仅编译）。

## 一、先确认「没有」的问题（避免误改，严守铁律）

逐项核对用户点名的功能，确认真实可用、非假实现：

| 功能 | 结论 | 证据 |
|---|---|---|
| 附带截图（复选框） | ✅ 真控模型输入 | `aiStore.ts:716` `...(attachImage && ctx.imageDataUrl ? [ctx.imageDataUrl] : [])` |
| 附带识别文字（复选框） | ✅ 真控 OCR 注入 | `aiStore.ts:710-711` `ocrText: attachOcr ? ctx.ocrText : undefined` |
| 导出 6 格式（md/txt/html/docx/xlsx/pptx/pdf） | ✅ 全可用 | 前序轮已修 docx `toBuffer`/PPTX 表格/任务清单/空实现 |
| 下面的样式（5 套主题） | ✅ 贯通全部导出 | docx/pptx 改强调色 `THEME_ACCENT`；html/pdf 全 CSS；富文本复制带 theme |
| 润色（更简洁/详细/正式/口语/译英文） | ✅ 真闭环 | `aiStore.ts:962 refine()` 以 `refineSystem()+output` 重生成并回写对话末条 |
| 回写截图 / 复制富文本 / 二次编辑 | ✅ 落地 | `handleApplyToScreenshot` / `handleCopyRich` / `setOutput` 同步落盘 |

## 二、本轮修复的 2 个真实缺陷

### 缺陷 1（P1）· 导出「附带截图」位置不一致 —— 达不到预期效果

- **现象**：同一篇文档、同样勾选「附带截图」，6 种产物里截图的位置竟然不同：
  - DOCX / PPTX / 复制为富文本 → 截图整块在**正文顶部**（作为可视化证据锚点）
  - HTML 文件 / PDF 打印 / 应用内预览 → 截图被 `html.replace('</body>', …)` 丢在**正文底部（footer 之后）**
- **根因**：`AIPanel.tsx` 的 `handleExport('html')` / `handleExportPdf` / `handlePreview` 三处用字符串替换把整块图塞进 `</body>` 之前（即 footer 之后），与 DOCX/PPTX 的「前置整块」（`markdownDocx.ts:354` `// 1) 前置整块内嵌截图 … 放到正文前`）不一致。
- **修复**：
  - 新增共享辅助 `frontImageBlockHtml(imgs)`（`AIPanel.tsx`）。
  - 三处统一改为 `html.replace('<main class="doc-main">', block + '<main class="doc-main">')`，把截图整块插入到 `<main>` 之前（顶部），与 DOCX/PPTX/富文本复制完全对齐。
- **收益**：6 种产物视觉一致，截图始终作为文档「源证据」出现在顶部，符合用户对「生成文档」的预期。

### 缺陷 2（P1）· AI 真实标题被封面吞掉 + HTML/DOCX 双标

- **现象**：用 doc 预设生成「# 第三季度产品复盘」，导出的 HTML/PDF/预览里**封面上写的是通用名"文档"、正文里 AI 写的标题整个消失了**；而 DOCX 里标题却保留——同一个功能两种表现。
- **根因**：
  - `markdownHtml.ts` 用 `const suppressFirstH1 = !!opts.title;` **无条件**吞掉正文首个 H1；
  - 而各导出器传入的 `title` 是通用预设名 `presetLabel(activePreset)`（如"文档"），永远 ≠ AI 的 H1 → HTML 把真实标题静默丢弃；
  - `markdownDocx.ts:488` 则只当 `text === title` 才跳 → DOCX 保留 H1。两条路径逻辑双标。
- **修复**：
  1. `markdownHtml.ts` 抑制逻辑对齐 DOCX：仅当正文首个 H1 与封面标题**同名**才跳过（`lvl === 1 && opts?.title && !firstH1Skipped && txt.trim() === String(opts.title).trim()`）。
  2. 新增 `firstHeading(md)`（跳过 `<!--SNAP:k-->` 锚点行）提取 AI 首个一级标题；`AIPanel.tsx` 的 HTML/DOCX/PDF/预览 4 个实时导出 + 历史库 docx/html/pdf 3 个导出，封面统一改用 `firstHeading(...) || 预设名`。
- **收益**：封面显示 **AI 自己写的真实标题**，正文不再重复；即便回退到通用名，AI 的 H1 也**完整保留在正文**（不再被吞）。HTML 与 DOCX 行为终于一致。

## 三、验证（真实产物，非仅编译）

esbuild 转译 `markdownHtml.ts` → Node 运行 `mdToHtml`：

| 检查 | 结果 |
|---|---|
| 封面显示 AI 真实标题 | ✅ `<h1 class="doc-title">第三季度产品复盘</h1>` |
| 正文无重复 H1（dup） | ✅ 0 |
| 通用名回退时 AI 的 H1 仍保留在正文 | ✅ `<h1 id="…">第三季度产品复盘</h1>` |
| 截图整块位于 `<main>` 之前（顶部） | ✅ `FIG_BEFORE_MAIN=true` |
| 截图不在 footer 之后 | ✅ `FIG_AFTER_FOOTER=false` |

`pnpm build` exit 0（1.96s），双入口（index / ai-panel）编译绿。

## 四、铁律自检

- 0 代码破坏 ✓
- 0 Rust 改动 ✓（仅 `AIPanel.tsx` + `markdownHtml.ts`）
- 0 新依赖 ✓
- 纯前端增量、双入口编译绿 ✓

## 五、新需求发掘（仅记录，未落地，待拍板）

1. **doc/prd 单图图文穿插锚点**：单图预设也可让 AI 埋 `<!--SNAP:1-->` 实现「文字—截图」逐段穿插（多图 report 已支持，单图预设尚未引导）。
2. **历史库导出区复用 reveal / 打开文件 双按钮**：实时导出已有「在 Finder 中显示 / 打开文件」，历史库导出目前只有成功文案，可对齐。
3. **历史库导出支持 PPTX**：当前历史导出支持 md/txt/html/xlsx/docx/pdf，缺 pptx。
4. **导出 md/txt/html/pdf 也加 `exporting` 态与防重复点击**：xlsx/docx/pptx 已有旋转 spinner，其余 4 个按钮暂无统一禁用/loading 态（细节一致性卖点）。

> 本轮未擅加未请求功能，仅修复上述 2 个真实缺陷并统一一致性。
