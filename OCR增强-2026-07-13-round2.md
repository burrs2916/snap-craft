# SnapCraft · OCR 取字功能增强（第二轮）

> 实施时间：2026-07-13。状态：前端 `pnpm build` + 后端 `cargo check` 均通过（exit 0）。
> 本轮在已有结构化 OCR（语言选择 / 逐行结果 / 区域框选 / 贴回文字标注）基础上做**增量增强**，
> 所有改动均为**新增/附加**，未触碰截图、标注、滚动、历史等其它功能。

## 一、需求攫取与发觉（OCR 取字潜在的可用性问题）

对一个「截图 + OCR 取字」工具，从用户真实使用路径出发，梳理出以下潜在需求：

| # | 需求 | 提升可用性的点 | 风险/成本 |
|---|------|----------------|-----------|
| 1 | **内联纠错**：识别出的文字可直接改 | OCR 必然有漏识/误识，改一字比重新打字快得多 | 低（纯前端状态） |
| 2 | **导出文本**：识别结果存成 .txt/.md | 用户常要把提取文字落盘，而非只进剪贴板 | 低（新增 1 个后端命令） |
| 3 | **结果内搜索**：在长文本里过滤/定位 | 识别整页文档时快速找目标行 | 低（纯前端过滤） |
| 4 | **切换语言即时重识别** | 选了语言就该立刻重跑，而非再点一次取字 | 低（复用 runOcr） |
| 5 | 置信度阈值过滤 | 只看高置信行 / 标红低置信 | 中（与搜索功能重叠） |
| 6 | **跨视图快速取字**（结果条/历史直接取字） | 不想进编辑器也能拿文字 | 高（需跨视图渲染 OCR 面板） |
| 7 | OCR 历史持久化 | 跨会话回看识别结果 | 中（需新增存储 + 命令） |
| 8 | 复制格式切换（保留/去除换行） | 贴进不同场景 | 低（但收益小） |
| 9 | 翻译识别结果 | 需要联网/外部 API | 高（超出本地工具定位） |
| 10 | 多列/竖排阅读顺序校正 | 复杂版面重排 | 高（Vision 已较准，边际收益低） |

## 二、本轮选择实现的需求（按"可用性↑ / 风险↓"筛选）

选取 **#1 内联纠错、#2 导出文本、#3 结果内搜索、#4 切换语言即时重识别**。
理由：四项都直接命中 OCR 最高频痛点，且**全部收敛在现有 OCR 面板内 + 1 个孤立后端命令**，
不引入跨视图渲染、不改存储格式、不碰截图/标注链路，符合「增量增强、不影响其它功能」的硬约束。

`#5` 与搜索重叠、`#6/#7` 跨视图/跨会话成本偏高、`#9/#10` 超出本地工具定位 → 本轮**不做**，留作后续评估。

## 三、具体改动

### 后端 `src-tauri/src/commands/edit.rs`（新增，孤立）
- `save_text_file { content, file_path }`：把 UTF-8 文本写入用户选定路径（与 `save_screenshot` 平级、互不干扰）。
- 仅在 `lib.rs` 的 `invoke_handler` 注册，不改动任何既有命令。

### 前端 `src/features/screenshot/EnhancedScreenshotApp.tsx`
- 新增状态 `ocrEdits: Record<number,string>`（块下标→修正文字）、`ocrSearch: string`。
- `runOcr(imageData, langOverride?)`：支持显式语言参数；**每次新识别清空 edits/search**。
- `ocrTextAt(i,b)`：取「修正后优先、否则原始」的文字——复制/贴回/导出/全文预览全部走它，保证编辑贯穿全链路。
- `handleLangChange`：非 macOS（语言强制生效）下切换即重识别；macOS 走系统自动、忽略语言，故不重跑（避免无谓等待）。
- `handleExportOcr`：组装（含修正）文字 → 系统对话框选 .txt/.md → `save_text_file` 落盘。
- OCR 面板升级：
  - 逐行 `<input class="ocr-block-edit">` **可内联编辑**；有修改的行高亮 `.edited`。
  - 新增**搜索行**（输入框 + 命中计数 `{shown}/{total}` + 清除），按子串忽略大小写过滤逐行列表。
  - 动作栏新增 **导出文本** 按钮；有修正时显示 **还原识别** 按钮。

### 文案 `src/locales/{zh-CN,en-US}.json`
- `ocr.search / searchClear / matchCount / export / exportTitle / exported / exportFailed / exportEmpty / resetEdits / resetEditsTitle`。

### 样式 `src/index.css`
- `.ocr-search-row / .ocr-search / .ocr-search-clear / .ocr-search-count / .ocr-block-edit / .ocr-block.edited`。

## 四、如何验收

```bash
pnpm build          # 前端 tsc+vite，已通过（125 模块，exit 0）
cd src-tauri && cargo check   # 后端类型检查，已通过（exit 0）
./start.sh app      # 打开含新后端的 SnapCraft.app
```
编辑器内点「取字」：
- 逐行直接改字 → 点「复制」/「复制全部」/「作为文字标注贴回」/「导出文本」均用改后内容。
- 顶部搜索框输入 → 逐行列表实时过滤，显示 `匹配 m/n`；清除恢复。
- 切换语言（Windows 侧）即自动重识别；macOS 自动语言下不重跑，手动点「取字」即可。
- 点「导出文本」→ 选路径存为 .txt / .md。

## 五、回归确认
- 截图 / 区域 / 窗口 / 滚动长截图 / 钉图 / 历史 / 文字标注 / 马赛克：均未改动，行为不变。
- 区域框选 OCR（`ocrRegionMode`）链路未动，仍走 `onRegionOcr → runOcr(dataUrl)`。
- 新增后端命令为纯附加，既有命令签名/行为零变化。
