# AI 助手 Phase 27 — P2 ① 选区 / 划词 AI（OCR 文字 → 标注 扩充）

> 日期：2026-07-16 ｜ 栈：React 18 + TS + Vite + Tauri 2 + Konva ｜ 约束：零 Rust、零新依赖、增量优先、诊断优先

## 1. 背景与诊断
路线图 P2 ① 是「选区 / 划词 AI（OCR 文字 → 标注）」。先做了全仓探查（诊断优先，不动码）：
- OCR 已完整：`OcrBlock` 归一化坐标 `(x,y,w,h)` 就绪；`EnhancedScreenshotApp` 已有 `ocrSel` 多选（复选框 + 框选拖拽 `ocrRegionPick`）+ 预览逐块框 `ocr-preview-boxes`。
- `applyOcrAsAnnotations` 已实现「选区 → 文字标注」——但**只有贴回文字**这一种形态，且全文视图的划词不会回灌到选区。
- 闭环先例存在：`addAnnotation` + `flashRegion`（Phase 20/24/25 已建）可直接复用，导出 `getMergedImageDataUrl` 自动捕获。

**结论**：P2 ① 基础已具备，本阶段把它升级为「多形态标注 + 划词联动」，纯前端、零 Rust、零新依赖。

## 2. 改动清单
### 2.1 `EnhancedScreenshotApp.tsx`（核心）
- 新增 `ocrSelectedBlocks()`（useCallback）：严格子集——仅 `ocrSel` 勾选/框选的**可见**块；无选择返回空。区别于 `ocrIncludedLines`（无选择退回全部可见），专供「选区 → 标注」防误伤整图。
- 新增 3 个 handler，全部复用 `addAnnotation` + `flashRegion`：
  - `redactOcrSel`：逐选中块加 `mosaic`（black / brush / solid）打码；
  - `highlightOcrSel`：逐选中块加 `highlight` 矩形（黄 #FFE600，opacity .45）；
  - `arrowOcrSel`：从左侧外缘引 `arrow` 指向选中块合并外接框中心（#0a84ff）。
  - 三者**强制要求选区**：无选区时 `flash(t('ocr.selNeeded'),'error')` 提示，按钮不禁用（保留可发现性）。
- `.ocr-panel-actions` 在「作为文字标注贴回」后补 3 按钮：打码选中 / 高亮选中 / 箭头指向（title 提示需先选择）。
- OCR 预览框加 `if (ocrSel[i]) bc.push('selected')` —— 选中块橙色描边，形成选区可见反馈。
- **划词联动**：全文 OCR 视图加 `onMouseUp`——读 `window.getSelection()`，子串匹配（块含划词 或 划词含块）→ 映射 `ocrSel` → 预览高亮 + 立即可操作；带「全选」守卫（划词长度 ≥ 全文长度则忽略），只响应真实划词子集。

### 2.2 `src/index.css`
- `.ocr-box.selected`：橙 #ff9500 描边 + 浅底 + 外发光（z-index:4），与悬停 accent 区分。

### 2.3 `zh-CN.json` / `en-US.json`
新增 10 键：`redactSel` / `highlightSel` / `arrowSel`（+ title）、`selNeeded`、`redacted`、`highlighted`、`arrowed`、`selByText`。

## 3. 关键设计决策
- **复用同源管线**：选区 → 标注与 AI 智能编辑（Phase 14/20/24/25）共用 `addAnnotation` + `flashRegion`，几何一致、导出自动捕获，零逻辑复制。
- **防误伤**：打码/高亮/箭头强制选区（点击无选区给提示而非禁用），契合「选区/划词」语义；与隐私哨兵（Phase 21 扫全图）形成「精准局部 vs 主动全扫」互补。
- **划词映射走 `ocrVisibleLines()`**：受搜索/置信度过滤，隐藏块不参与，零回归。
- **极小侵入**：仅 1 个 useCallback + 3 函数 + 1 个 onMouseUp + CSS/键，不新建状态或组件；未动 `applyOcrAsAnnotations` / `ocrIncludedLines` 既有行为。

## 4. 验证（全绿）
- `npx tsc --noEmit` → 0 错
- `pnpm build` → **exit 0，140 模块（2.08s）**
- `dist` 含新 token：`打码选中`·`高亮选中`·`箭头指向`·`按划词选中`·`请先在预览图` + `.ocr-box.selected`

## 5. 用户可感知流程
1. 取字 → 预览图逐块框 / 列表复选 / 框选拖拽 **选文字块**（橙色高亮）。
2. 在全文视图**划词**选中一段 → 自动映射并高亮对应块。
3. 点「打码选中 / 高亮选中 / 箭头指向 / 作为文字标注贴回」→ 对应标注落图 + 脉冲反馈。
4. 导出时 `getMergedImageDataUrl` 自动把标注合入底图。

## 6. 路线图剩余
P0-γ 后端代理隐藏 Key（需 Rust，零 Rust 约束外）｜ P1 本地语义记忆（transformers.js+MiniLM）｜ P2 批量 Agent 队列 / crop / PPT / 缩略图回传 History。
