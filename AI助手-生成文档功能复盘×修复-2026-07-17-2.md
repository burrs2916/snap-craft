# AI「生成文档」(第一个功能标签 · doc 预设) 复盘 × 修复 · 第二轮

> 时间：2026-07-17 20:15 ｜ 类型：代码级复盘回溯（直读源码 file:line 实证）+ 缺陷修复
> 范围：AI 助手**第一个功能标签「生成文档」** 全链路（生成 → 预览 → 复制/富文本复制 → 编辑 → 导出 6 格式 → 历史阅读器导出/打包）
> 方法：聚焦「前端页面已有功能」，逐项核对实现是否真生效、是否空/假实现、是否达不到预期；另派只读子代理做交叉审计。

## 本轮结论

- **发现并修复 3 个真实缺陷**（均纯前端增量、0 Rust、0 新依赖、`pnpm build` exit 0）。
- 子代理交叉审计确认：此前多轮已修的默认预设/表格/任务清单/HTML 内嵌/PDF 诚实提示/复制富文本/元信息 等均**仍然有效，无回退**。
- 同时复核了多个「疑似缺陷」点，确认实际无问题（见文末「已核无问题」），避免误改。

## 3 项已修缺陷

### 🟠 P1-① 富文本复制对图文报告丢图（`AIPanel.tsx` handleCopyRich）
- **根因**：先 `stripSnapMarkers(output)` 剥掉 `<!--SNAP:k-->` 标记，再把原文交给 `mdToHtml`。而 `mdToHtml` 仅在**遇到标记**时才消费 `sectionImages`（markdownHtml.ts:407）——标记已被剥掉，`sectionImages` 永不触发 → 图文报告的富文本复制**完全不含截图**。
- **对照**：`handleExport` 的 HTML 分支传的是未剥标记的 `output`（AIPanel:810），所以只有「复制富文本」这一路坏掉，用户会困惑"为什么我复制去 Word 没有图"。
- **修复**：图文报告传**未剥标记的 `output`** 给渲染器；纯文档（无标记）则手动把来源截图整块前置内嵌（与导出/预览一致）。`text/plain` 兜底仍用干净文本。

### 🟠 P1-② 历史导出 docx/pdf/html 永远丢来源截图（`AIPanel.tsx` handleHistoryExport）
- **根因**：Phase 19-B5 意图"docx/pdf/html 附带来源截图缩略图作为封面证据"，但代码把缩略图当作 `sectionImages: coverImages` 传入。而历史成稿已 `stripSnapMarkers` 无 `<!--SNAP:k-->` 标记——`markdownToDocx` 仅在有标记时用 `sectionImages`，且无标记时前置整块嵌入要求 `images.length && !sectionImages`（markdownDocx.ts:356/405，此处 `images` 未传=`[]`）→ 两路都不嵌，缩略图**静默丢弃**。
- **对照**：实时导出的 `handleExport` 有手动 `html.replace('</body>', ...)` 兜底（AIPanel:819），**历史路径没有这层兜底** → 历史 docx/pdf/html 完全无图。
- **修复**：
  - docx：`sectionImages` → `images`（前置整块内嵌，缩略图即封面证据）。
  - pdf / html：补上与实时导出一致的前置内嵌 `</body>` 注入兜底。

### 🟡 P2-③ Agent/哨兵模式「附上原图」勾选与实际导出不一致（`AIPanel.tsx` orderedImages）
- **根因**：操作区复选框 `checked={agentMode||sentinelMode ? true : attachImage}` 在 agent/sentinel 模式**恒显示已勾选**，但从未 `setAttachImage(true)`。所有导出 handler 用的 `orderedImages()` 读的是真实 `attachImage` 值——若用户此前手动取消过"附上原图"，`attachImage=false`，导出会漏掉当前截图，尽管 UI 勾选框显示已勾选 → 视觉与结果矛盾。
- **修复**：`orderedImages()` 在 agent/sentinel 模式下强制包含当前截图，使勾选态与导出结果对齐（不改 `attachImage` 持久状态，退出模式后恢复原用户偏好）。

## 已核无问题（避免误改）

- `printHtmlViaIframe` / `dataUrlToBytes` / `orderedImages` / `estimateCost` / `revealInFolder` / `pickExportPath` 均已定义并正确导入，无未定义调用（P0 排雷通过）。
- 生成按钮禁用逻辑对 `doc` 预设仅判 `!apiKey || !goal`，无误禁。
- SNAP 索引 `sectionImages[parseInt(k)-1]` 与 `orderedImages()` 的 `[当前, ...附加]` 顺序一致，无 off-by-one。
- i18n 键 `ai.exportReveal` / `ai.openApp` / `ai.copyRich` / `ai.embedCurrent` / `ai.embedExtra` / `ai.toc` / `ai.edit` / `ai.preview` 等在 zh-CN/en-US 均存在，无露 key。
- 编辑态 `isEditing` 在生成/清空/新对话上均被禁用，`editDraft` 每次进入编辑从 `output` 重载，无 stale。
- 实时导出 docx/html/pptx 前置内嵌、PDF 诚实提示（不写 lastExportedPath）、`mdToPlainText` 本地定义——均正常。

## 新需求点（仅发掘、未落地，待拍板）

复盘中发现、但**超出「修复已有功能」范围、按铁律未擅加**的潜在增强：
1. **doc/prd 单图预设图文穿插锚点**（导出链路已支持 SNAP 内嵌，缺在预设 prompt 引导 AI 埋 `<!--SNAP:1-->`）——目前单图预设走"整块前置"，穿插效果弱于 report。
2. **富文本复制的纯文档也该内嵌截图**（本轮已修对齐，但仅图片；可进一步在内嵌图前加"来源截图"小标题）。
3. **导出后"在 Finder 显示 / 打开文件"按钮**已落地，但历史阅读器导出成功区未复用该双按钮（仅实时导出区有）——可统一。
4. 历史导出支持 PPTX（当前仅 md/txt/html/xlsx/docx/pdf）。

## 状态

- 0 代码破坏 ✓
- 0 Rust 改动 ✓
- 0 新依赖 ✓
- `pnpm build` exit 0（tsc && vite build，2.03s）✓
- 严守"诊断优先、增量修复、不擅加未请求功能" ✓

## 交付物

- 本报告
- 代码改动：仅 `src/features/ai/AIPanel.tsx`（handleCopyRich / handleHistoryExport ×3 分支 / orderedImages）
