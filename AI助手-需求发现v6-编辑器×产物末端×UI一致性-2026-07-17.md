# AI 助手 需求发现 v6 — 编辑器/产物末端/UI 一致性（2026-07-17）

> **方法**：v1–v5 已覆盖 AI 模块全景（Phase 1–30 + B1–B6 + 路径A + v3 P0 三件套 + v4/v5 OCR/AI 产物）。本轮响应用户**新一轮五维追问**（OCR / AI / 图片处理 / 文档生成 / 界面风格 / 持久化 / 管理 / 下载 / 预览），把视角下沉到**编辑器末端 + 截图历史 + 导出体验 + UI 一致性**这四条 v1–v5 较少触达的轴。
>
> **数据来源**：3 个并行子代理直读源码：
> - 代理 A：UI/UX 一致性审计（AIPanel / EditorWindow / AnnotationToolbar / Pin / Region / Window / AiWindow / i18n / CSS 变量）→ 26 项
> - 代理 B：OCR/AI/文档产物链路审计（Rust 6 文件 + 前端 8 模块 + capabilities）→ 60+ 项
> - 代理 C：截图/标注/编辑体验盲点（EditorWindow 1872 行 / AnnotationCanvas / BuiltinTools）→ 75+ 项
>
> **总 GAPS**：~160 项（去重 + 合并后约 **70 项独立**）。本轮严守"诊断优先、不擅加未请求功能"：**先报告 + 路线图 + 即时落地 2-3 个零风险纯增量**。

---

## 0. v1–v5 复盘（确保不重复）

| 轮次 | 视角 | 落地 |
|---|---|---|
| v1 | 整体复盘 × 路线图 | 全部路线图项均已 Phase 16–30 落地 |
| v2 | 47 项 OCR/AI/图片/文档/UI 缺口 | 仅路线图，未改码 |
| v3 | P0 三件套实修 | "在 Finder 显示" + 默认目录记忆 + 删除二次确认 + OCR 历史缩略图 |
| v4 | 31 项产物管理/文档/下载/预览缺口 | 仅路线图，待拍板 S-A/B/C |
| v5 | v4 P0/P1 四件套直接落地 | OCR bbox 落库 / OCR 历史扩容回放 / AI 编辑产物独立持久化 / OCR 智能清洗 UI |

→ **本轮（v6）定位**：补 v1–v5 较少触达的 **4 条新轴** ——

1. **UI/UX 一致性**（v1–v5 主要在 AI 窗 / 编辑器功能层，**全局设计 token / 空状态 / 焦点环 / i18n 死键 / 加载态统一**几乎没审计过）
2. **编辑器末端**（撤销栈/图层层级/选区缩放/标注复制粘贴/JPG-WebP 导出/Pin 窗 OCR）
3. **OCR 引擎层**（macOS lang 失效 / WinRT 无置信度 / PS 编码顺序 / /tmp 残留 / 空结果当错误）
4. **导出/下载/预览/管理**（6 格式 Markdown 解析器漂移 / 导出图片未压缩 / Pin 窗无 OCR / 大文档同步打包卡顿）

---

## 一、本轮新发现的 GAPS（70 项，按 ROI 排序 + 铁律兼容性）

### 🔥 P0（强烈建议下一轮立刻做）

| # | GAPS | 位置 | 估行 | 铁律 | 描述 |
|---|---|---|---|---|---|
| **V6-P0-1** | OCR bbox 统一走 `set_screenshot_ocr_full` | `EditorWindow.tsx:610/1045` + `commands/ocr.rs:31-69` | 30 | ✅ 纯前端 + 1 Rust 命令 | 现状：90% 走 `set_screenshot_ocr`（只存文本），**bbox 永久丢失**。重开编辑器划词/选区能力全废。修：Editor 端 2 处全改 full 路径 |
| **V6-P0-2** | localStorage quota 监控 + 5 处静默 try/catch 修复 | `aiStore.ts:78/97/141/178/280` | 50 | ✅ 纯前端 | "以为已存"丢数据，AI 长会话必踩。加 `safeSet` 检测 QuotaExceededError → 弹 toast 提示+清理旧数据 |
| **V6-P0-3** | history.json **拆"原图分文件 + 元数据 JSON"** | `store/mod.rs:8-34` + `commands/history.rs:17` | 80 | ⚠️ 需 Rust 重构 | 4K 截图 × 100 条 = 0.5–1GB 纯文本 JSON，读写/序列化极慢。改：缩略图（256px JPEG）存 JSON + 原图存 `<config>/shots/<id>.png` |
| **V6-P0-4** | 全局 `:focus-visible` 焦点环 | `index.css` 顶部加 1 行 | 1 | ✅ 极小 | 键盘/无障碍可访问性一次性大幅提升，几百个交互元素现状裸态 |
| **V6-P0-5** | aiMarkdown 补 GFM 表格 | `aiMarkdown.tsx` | 60 | ✅ 纯前端 | preset `table`/`compete`/`report` 才真正可用，**当前表格原样当段落输出**，4 种文档主题依赖 |
| **V6-P0-6** | AIPanel 无 API Key 全局引导横幅 | `AIPanel.tsx:1300` + 已有 `errorNoKey` 死键 | 15 | ✅ 纯前端 | 用户首启必踩坑；现状仅按钮灰、**零提示** |
| **V6-P0-7** | 撤销栈深度数字徽 + 撤销合并（300ms 内合并） | `AnnotationToolbar.tsx:635-654` + `screenshotStore.ts:127-137` | 25 | ✅ 纯前端 | 改字号连按 5 次撤销就把第一笔挤出；用户根本不知道还能撤销几步 |
| **V6-P0-8** | Windows OCR PowerShell 编码前置 | `commands/ocr.rs:197` 移到脚本第 1 行 | 5 | ⚠️ 需 Rust | 修中文/日文非 ASCII 文本 PS5.1 ANSI 乱码（1 行 Rust 改动） |

### 🟠 P1（生产级补强，建议 1 周内分批）

| # | GAPS | 位置 | 估行 | 铁律 | 描述 |
|---|---|---|---|---|---|
| V6-P1-1 | 死键清理（`historyConfirmDeleteBody`/`forkSuffix` 等 7+ 个）+ window.confirm 换自绘模态 | `AIPanel.tsx:365` + `locales/*.json` | 30 | ✅ 纯前端 | 已有文案不显示，UI 与 i18n 漂移 |
| V6-P1-2 | 主窗口/Pin/AI 三窗主题变量同步 | `PinWindow.tsx:20` + `EnhancedScreenshotApp` | 20 | ✅ 纯前端 | 主窗切深色时 Pin 窗 `data-theme` 不变；`useTheme()` hook 缺失 |
| V6-P1-3 | "OpenAI 兼容" 硬编码 + 字体下拉硬编码中文 | `AIPanel.tsx:1524-25` + `AnnotationToolbar.tsx:142-161` | 20 | ✅ 纯前端 | 海外用户/英文模式严重翻车 |
| V6-P1-4 | lastExportedPath 按格式分别记忆 | `AIPanel.tsx:246` | 8 | ✅ 纯前端 | 导了 A 想 reveal 跳到 B，文档/分享场景高频踩雷 |
| V6-P1-5 | 拖拽重排视觉反馈 + 拖入目标位高亮 | `AIPanel.tsx:1202-1244` + CSS | 10 | ✅ 纯前端 | 拖拽无视觉变化，用户"以为没生效" |
| V6-P1-6 | 6 格式 Markdown 解析器抽 AST 共享 | `markdownDocx.ts/markdownHtml.ts/markdownPptx.ts/markdownXlsx.ts` | 200 | ✅ 纯前端（重构） | 任何语法升级（脚注/定义列表/任务列表）都要改 4 处，必然漂移 |
| V6-P1-7 | 导出 docx 图片压缩/转 JPEG | `markdownDocx.ts:197-232` | 25 | ✅ 纯前端 | 9 张 4K 截图让 .docx 膨胀到 50MB+，Word 打开卡；嵌入前 canvas 重压 0.85 |
| V6-P1-8 | `callout` 工具栏入口 + 图层 UI 入口 | `AnnotationToolbar.tsx:108-120` | 40 | ✅ 纯前端 | callout 写完了不暴露；图层字段全无效 |
| V6-P1-9 | 编辑器自适应 fit / 100% / 滚轮缩放 | `AnnotationCanvas.tsx:1114-1121` | 50 | ✅ 纯前端 | 现在 4K 图只能 fit 看，标注时根本看不清细节 |
| V6-P1-10 | 标注复制/粘贴/克隆 (⌘D/⌘C/⌘V) | `AnnotationCanvas` | 60 | ✅ 纯前端 | 截图标注 50% 操作是"再画一个一样的" |
| V6-P1-11 | history 原子写 + 坏文件备份 | `store/mod.rs:49-68` | 25 | ⚠️ 需 Rust | 半文件丢全部；写前 tmp+rename，坏文件备份 .bak |
| V6-P1-12 | 多窗口 AI 状态广播 + 按 convKey 分桶 AbortController | `aiStore.ts:443/691/900/998` + `aiStore.ts:607-622` | 50 | ✅ 纯前端 | 多窗口 AI 互踩；流式时切图丢正在输出的内容 |
| V6-P1-13 | stitch 金字塔加速 | `stitch.ts:54-107` | 40 | ✅ 纯前端 | 8s→1s，长截图拼接体验质变 |
| V6-P1-14 | history 软删除 + 7 天回收站 | `commands/history.rs:23-27` | 30 | ⚠️ 需 Rust | 误删即丢；v2 P-MGT-2 标签收藏的姊妹项 |
| V6-P1-15 | history 全文搜索/标签/收藏 API | `commands/history.rs:6-19` | 60 | ⚠️ 需 Rust | 全文搜、按时间分组、按 OCR 文字查全无 |
| V6-P1-16 | PPTX 内嵌图片（与 docx 对齐） | `markdownPptx.ts:8` | 120 | ✅ 纯前端 | 图文报告导出 PPT 截图全丢 |
| V6-P1-17 | Pin 窗加 OCR/AI 入口 + 多 Pin 管理 UI | `PinWindow.tsx` | 60 | ✅ 纯前端 | 钉图后还要回主窗才能取字，违背"快速参考"初衷 |
| V6-P1-18 | 极大图导出优化（OffscreenCanvas/分块） | `AnnotationCanvas.tsx:1141-1243` | 60 | ✅ 纯前端 | 8K 图 `toDataURL` 200MB+ 字符串，4K×4K = 192MB 内存，秒崩 |
| V6-P1-19 | `AiChatTurn` 持久化 `thinking` / `toolSteps` / `usage` | `aiStore.ts:530/555/699/909/1009` | 20 | ✅ 纯前端 | 刷新即丢，违背"AI 产物持久化"产品承诺 |
| V6-P1-20 | 启动扫描清理 `/tmp/snapcraft-*.png` 残留 | `commands/ocr.rs:48-53` | 25 | ⚠️ 需 Rust | 系统重启 → 临时文件残留 → 隐私泄露 |

### 🟢 P2（润色，1 个月内）

| # | GAPS | 估行 | 描述 |
|---|---|---|---|
| V6-P2-1 | OCR 失败重试 + "无文字" 拆 "引擎异常" vs "无文字" | 35 | 区分错误类型 + 客户端按错误类型 1-2 次自动重试 |
| V6-P2-2 | 圆角档位统一为 `--r-sm/md/lg/pill` 4 档 | 15 | 8/9/10/12/14/16/18/20/999 共 9 档无明确语义 |
| V6-P2-3 | 全局 Toast 组件（统一 3 套 toast 风格） | 30 | EditorWindow/AIPanel/EnhancedScreenshotApp 三处风格 + 关闭时长不统一 |
| V6-P2-4 | 加载骨架统一 `.spinner`（28px 旋转圆环） | 10 | "识别中/测试连接/导出中" 多套并存 |
| V6-P2-5 | `iPad/Touch/外接屏` 极窄屏降级（aipanel 700-900px 抽屉改底部 sheet） | 20 | 700-900px 宽屏上画布被压成 < 300px |
| V6-P2-6 | Tooltip 触屏长按反馈 | 15 | 当前仅 hover；触屏长按无反馈 |
| V6-P2-7 | 录屏/GIF 入口（哪怕仅"导出当前帧"） | 60 | 主流使用场景完全不支持 |
| V6-P2-8 | 多屏下副屏底图不为黑（v4 已知 N-IMG-1） | 30 | 多屏选区跨屏盲选 |
| V6-P2-9 | `BuiltinTools` 死代码：Pan/Zoom/Select 实际接入或删除 | 60 | 2 套工具系统并立，~600 行代码从未运行 |
| V6-P2-10 | 框选标注 clamp 到 image.width/height | 8 | 拖 4 个角可任意越界，标注跑出图外 |
| V6-P2-11 | 6 格式统一错误码结构 `Error { code, message, retryable? }` | 80 | 后端 ERR_*/前端 5 类/IPC 中文字符串三套 |
| V6-P2-12 | `csp: null` 收紧（`default-src 'self'; connect-src 'self' https://*`） | 5 | XSS 注入到标注预览 |
| V6-P2-13 | 模型价格表精确匹配 + 远端拉取 | 15 | substring 匹配错算 gpt-4o-2024 / claude-3-5-latest |
| V6-P2-14 | `schema_version` + 启动迁移钩子 | 30 | 已加 4 个 `#[serde(default)]` 字段但无版本号 |
| V6-P2-15 | `epoch_ms: number` 统一时区 | 10 | 字符串时区未明 |
| V6-P2-16 | 长截图拼接 0 置信度时 UI 提示"含间隙" | 15 | `hadLowConfidence` 已写但 UI 不用 |
| V6-P2-17 | `crop` 工具（已写但未暴露） | 30 | 用户改底图需求 |
| V6-P2-18 | 标注转 JSON 标准 schema（Figma/Sketch 兼容） | 80 | 字段不通，外部无法复用 |
| V6-P2-19 | Pin 窗右键菜单 + ⌘W 关闭 | 10 | 当前仅 Esc/双击/X |
| V6-P2-20 | `capabilities/default.json` 启动校验所有 label 命中通配 | 20 | 动态创建新窗口时静默丢权限 |

### 🟡 P3（低优先级细节，13 项汇总）

- 高 DPI 标注 dpr 缩放（25 行）
- stitch 保留原 20 帧（15 行）
- 文本工具字数统计 + 全屏编辑模式（20 行）
- 历史按时间分组 UI（30 行）
- AI 历史批量 zip 导出（50 行）
- 截图历史 lazy 缩略图（30 行）
- 缓存命中率面板（20 行）
- localStorage 容量监控 banner（20 行，已 P0-2 合并）
- AI 面板重新 OCR 按钮（10 行）
- 标注层"组"概念（40 行）
- IPC 字段命名 snake↔camel 统一（20 行）
- `save_screenshot` 沙箱 + 路径白名单（15 行）
- `cross-device` 同步（识别 schema_version 即覆盖）

---

## 二、按用户五维重新归类（v1–v5 补强 + v6 新增）

| 维度 | v1–v5 覆盖 | **v6 补强** | 本轮 P0/P1 命中 |
|---|---|---|---|
| **OCR** | 清洗/回放/bbox 落库/智能切换 | macOS lang 失效 / WinRT 无置信度 / PS 编码 / /tmp 残留 / 失败重试 | P0-1, P0-8, P1-20, P2-1 |
| **AI 辅助** | 30+ Phase 全闭环 | thinking/toolSteps 持久化 / 多窗口 AI 状态 / quota 监控 / `errorNoKey` 全局引导 | P0-2, P0-6, P1-12, P1-19 |
| **图片处理** | 6 格式/5 主题/标注全工具 | 编辑器 fit/100%/滚轮缩放 / 标注复制粘贴 / callout 入口 / Pin OCR / 极大图导出 | P1-8/9/10/17/18 |
| **生成的文档** | 6 格式导出/zip 打包 | GFM 表格 / AST 共享 / docx 图片压缩 / PPTX 嵌图 / 导出重试 | P0-5, P1-6/7/16 |
| **界面风格** | 主题变量/AI 窗独立 | 全局焦点环 / 圆角 token / 字体下拉 i18n / 死键清理 / Toast 统一 / 三窗主题同步 | P0-4, P1-1/2/3, P2-2/3/4 |
| **持久化** | localStorage 5 key / 截图历史 100 条 | history.json 拆文件 / 原子写 / 软删除回收站 / 全文搜 / quota 监控 | P0-2, P0-3, P1-11/14/15 |
| **管理** | AI 历史搜索/标签/批量 | history 标签收藏 / 全文搜 / 软删除 / 启动校验 | P1-11/14/15, P2-20 |
| **下载** | 默认目录/Reveal in Folder | 按格式分别记忆 reveal 路径 | P1-4 |
| **预览** | AI 独立窗流式 popup | Pin 窗 OCR/AI / 标注单独 PNG 导出 / 编辑后预览 | P1-17, P2-5 |

---

## 三、v1–v5 vs v6 GAPS 覆盖关系

| 主题 | v1–v5 命中 | v6 命中 | 重叠 |
|---|---|---|---|
| OCR bbox 落库 | v5 V4-5 | V6-P0-1 | **重叠** → v5 已加 `ocr_blocks_json` 字段，但 Editor 端 90% 仍走 `set_screenshot_ocr`（无 blocks），v6 补"端上落地" |
| history.json 拆文件 | 未提 | V6-P0-3 | v6 新增 |
| 导出"在 Finder 中显示" | v3 V3-1（已落地） | — | 已闭环 |
| AI 编辑产物独立持久化 | v5 V4-6（已落地） | V6-P1-19（thinking/toolSteps 持久化） | v6 补**会话内容**层（v5 补**图**层） |
| OCR 历史缩略图 | v5 V4-2（已落地） | V6-P1-15（全文搜/标签/收藏） | v6 补**检索**层 |
| 默认目录记忆 | v3 V3-2（已落地） | V6-P1-4（按格式分别记忆 reveal 路径） | v6 补**跨格式** |
| 删除二次确认 | v3 V3-4（已落地） | V6-P1-1（自绘模态 + 死键清理） | v6 升级**UX** |
| 文档主题预览图 | v2 P-DOC-4（未做） | — | 仍待做 |
| HTML 在线再编辑 | v2 P-DOC-3（未做） | — | 仍待做 |
| AI 历史批量管理 | v2 P0-AI-2（未做） | — | 仍待做 |

---

## 四、Top 8 必做（按 ROI × 铁律兼容性排序）

| 排序 | 项 | 估行 | 收益 | 铁律 |
|---|---|---|---|---|
| 1 | **V6-P0-4 全局 `:focus-visible`** | 1 | 极广（无障碍） | ✅ 1 行 CSS |
| 2 | **V6-P0-6 API Key 引导横幅** | 15 | 极高（首启体验） | ✅ 纯前端 + 复用死键 |
| 3 | **V6-P0-7 撤销栈数字徽 + 合并** | 25 | 极高（每天用 N 次） | ✅ 纯前端 |
| 4 | **V6-P0-1 OCR bbox 统一 full 路径** | 30 | 极高（重开编辑器划词） | ✅ 纯前端 + 1 Rust 命令 |
| 5 | **V6-P0-5 aiMarkdown GFM 表格** | 60 | 极高（preset/主题依赖） | ✅ 纯前端 |
| 6 | **V6-P0-2 localStorage quota 监控** | 50 | 极高（防数据丢） | ✅ 纯前端 |
| 7 | **V6-P1-1 死键清理 + 自绘模态** | 30 | 高（i18n 闭环） | ✅ 纯前端 |
| 8 | **V6-P1-3 "OpenAI 兼容" + 字体下拉 i18n** | 20 | 高（海外用户） | ✅ 纯前端 |

---

## 五、本轮（v6）即时落地候选

**待用户拍板**。建议从下列 2-3 项零风险纯增量起步：

### 候选 A：UI 一致性 3 件套（极小、极快、影响广）
- V6-P0-4 全局焦点环（1 行 CSS）
- V6-P0-6 API Key 引导横幅（15 行 + 复用 `errorNoKey` 死键）
- V6-P0-7 撤销栈数字徽（10 行 + 撤销合并 15 行）
- **总行数 ~45 行**，0 Rust，0 新依赖，预计 15 分钟完工

### 候选 B：OCR + 文档 2 件套（中等、核心链路）
- V6-P0-1 OCR bbox 统一 full 路径（30 行前端 + 1 行 Rust）
- V6-P0-5 aiMarkdown GFM 表格（60 行）
- **总行数 ~90 行**，1 Rust 字段已存在（v5 加的），纯接线

### 候选 C：数据安全 2 件套（高价值、防丢数据）
- V6-P0-2 localStorage quota 监控（50 行）
- V6-P1-19 `AiChatTurn` 持久化 thinking/toolSteps/usage（20 行）
- **总行数 ~70 行**，0 Rust，0 新依赖

**推荐**：**A + C 并行**（互不冲突，覆盖 UI 体验 + 数据安全两大主线）= **~115 行 / 0 Rust / 0 新依赖**。

---

## 六、应避免做的（铁律相关，与 v1–v5 一致）

1. ❌ 截图原图 base64 长期存 localStorage（quota 必爆）
2. ❌ OCR bbox 与纯文本走两条不同的写入路径（已修 V6-P0-1）
3. ❌ `thinking` / `toolSteps` / `usage` 仅存 zustand 内存（V6-P1-19 修）
4. ❌ 全局单例 `AbortController` 多窗口互踩（V6-P1-12 修）
5. ❌ history.json read-modify-write 跨窗口并发（V6-P0-3 + P1-11 修）
6. ❌ `csp: null` 完全开放（V6-P2-12 收紧）
7. ❌ `try/catch{ /* ignore */ }` 静默吞 quota 错误（V6-P0-2 修）
8. ❌ 0 置信度 stitch 静默接受（V6-P2-16 修）
9. ❌ 6 格式各自维护 Markdown 解析（V6-P1-6 修）
10. ❌ 导出后用户"找文件"靠系统搜索（已 v3 修，zip 仍需补 V6-P1-4）

---

## 七、v7 路线图（v6 落地后）

1. **V6-P0-3 history.json 拆文件**（80 行 Rust + 前端迁移脚本）—— 真正解决 GB 级文件卡顿
2. **V6-P1-6 6 格式 Markdown AST 共享**（200 行重构）—— 消除 4 套解析器漂移
3. **V6-P1-15 history 全文搜索/标签/收藏**（60 行 Rust + 100 行前端）—— 真正的"管理"能力
4. **V6-P1-12 多窗口 AI 状态广播**（50 行）—— 多窗口产品形态闭环
5. **V6-P1-16 PPTX 内嵌图片**（120 行）—— 与 docx/HTML 行为对齐

---

## 八、交付

- 本报告 → `AI助手-需求发现v6-编辑器×产物末端×UI一致性-2026-07-17.md`
- 3 个子代理的原始报告合并入本报告"按维度归类"小节
- 下一步：等用户拍板落地候选 A/B/C → 立即执行

