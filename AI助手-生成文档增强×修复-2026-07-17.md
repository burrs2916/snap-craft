# AI 助手「生成文档」功能增强 × 修复（2026-07-17 第二轮）

> 上一轮复盘（AI助手-生成文档功能复盘×修复-2026-07-17.md）定位了 4 个真实缺陷并已修复；
> 本轮落地当时"发现但未落地"的两个最高 ROI 增强项，并**纠正了上一轮的一处误判**。

## 本轮落地内容

### ① 「复制为富文本」按钮（纯前端，零 Rust）
- **问题**：原「复制」按钮只 `navigator.clipboard.writeText(output)` 复制 **Markdown 源码**（带 `**`、`|`、`#` 标记），粘到 Word/邮件里是裸文本，与导出的精美文档割裂。
- **修复**：
  - `markdownHtml.ts` 的 `mdToHtml` 新增 `fragment?: boolean` 选项 —— 只返回正文 HTML 片段（`out.join`），不包裹 `<!DOCTYPE>/<style>/<nav>`。
  - `AIPanel.tsx` 新增 `handleCopyRich`：把 Markdown 渲染成 HTML 后，以 `ClipboardItem({ 'text/html': htmlBlob, 'text/plain': mdBlob })` 写入剪贴板，`text/plain` 兜底源码。粘贴到 Word/邮件/富文本编辑器时**保留标题/列表/表格/引用/代码结构**。
  - 操作区新增「复制为富文本」按钮（与「复制」并列），中英文案 + title 提示均已国际化。

### ② 「用默认应用打开」按钮（导出成功区）
- **纠正误判**：上一轮我写"open 命令已就绪，只差一个按钮" —— **这是错的**。`opener:default` 能力集仅含 `allow-open-url`(限 http/https/mailto/tel)、`allow-reveal-item-in-dir`、`allow-default-urls`，**不含 `open_path`**。若只加按钮不补能力，点击会静默失败（Tauri 收 "open_path not allowed"），正是用户最反感的"假实现/空实现"。
- **修复（三处联动）**：
  1. `src-tauri/capabilities/default.json` 补 `"opener:allow-open-path"`（默认 scope 放行任意路径，无需额外 allowlist，零 Rust 改动）。
  2. `AIPanel.tsx` 新增 `openExported(path)` → `invoke('open_path', { path })`，错误回写 exportMsg。
  3. 主输出区 + 历史阅读器两处导出成功区，在「📂 在 Finder 中显示」后新增并列「📂 打开文件」按钮（Word 开 .docx、Keynote 开 .pptx 等）。
- **⚠️ 生效前提**：能力改动需 `cargo build` 重新编译进二进制（`./start.sh dev` 会自动重编）。纯前端 `pnpm build` 不编译 Rust，本机跑 `./start.sh dev` 后即可见。

## 改动文件清单
| 文件 | 改动 |
|---|---|
| `src/features/ai/markdownHtml.ts` | `MdToHtmlOpts` 增 `fragment`；`mdToHtml` 末尾 `fragment` 早返正文片段 |
| `src/features/ai/AIPanel.tsx` | 增 `copiedRich` 状态、`handleCopyRich`、`openExported`；操作区加「复制为富文本」按钮；两处导出成功区加「打开文件」按钮 |
| `src-tauri/capabilities/default.json` | 增 `"opener:allow-open-path"` |
| `src/locales/zh-CN.json` / `en-US.json` | 增 `copyRich`/`copyRichTitle`/`openApp`/`openAppTitle` |
| `src/index.css` | 增 `.ai-btn-open` 样式（对齐 `.ai-btn-reveal`） |

## 验证
- JSON 合法性：`zh-CN.json` / `en-US.json` / `default.json` 均通过 `JSON.parse`。
- 前端编译：`pnpm build`（tsc + vite）**绿**，0 error 0 warning。
- 类型陷阱已排除：初版 `handleCopyRich` 用 `'write' in navigator.clipboard` 守卫把 else 分支收窄成 `never` 导致 `writeText` 编译报错，已改为直接 `clipboard.write(...)` + try/catch 兜底。

## 仍待你拍板（未做，避免越界）
- docx `core.xml` 元信息硬编码（作者/标题/时间），Word 里看不到 → 低优。
- 纯文档截图只能顶部整块内嵌，无 `<!--SNAP:k-->` 图文穿插锚点。
- 面板内富文本二次编辑后"重导"能力缺失。
- "复制为富文本"目前粘贴到无样式富文本编辑器（如浏览器 contenteditable）时仅保留标签结构、无主题样式；如需"所见即所得"可后续给片段内联 style（约 +20 行）。
