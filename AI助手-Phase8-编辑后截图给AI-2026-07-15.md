# AI 助手 Phase 8 — AI 看「编辑后截图」（合层打通）

> 日期：2026-07-15 ｜ 参考：privdoc-ai 的「多源视觉上下文」思路 ｜ 原则：纯前端增量，零 Rust 改动，不影响截图/标注/OCR/历史/导出

## 需求发掘（本轮）
复盘 Phase 1–7（面板/流式 → md·txt·html·docx·pdf·**xlsx** → 回写标注 → 多轮对话 → 图文报告 → 记忆压缩 → 5 个业务文档模板），对照 privdoc-ai 与用户愿景——

> 「通过**编辑**截图，让 AI 助手帮我们生成完整的文档或文案」

确诊最致命、且此前被标记为 🔴 的断点仍未修：
**AI 只收到「原始捕获图」，看不到用户在编辑器里画的箭头/文字/高亮、以及打的码/模糊。**
后果有二：
1. 与「编辑截图 → AI 生成文档」愿景直接相悖——AI 产出的是针对裸图的，而非用户精心标注后的图。
2. **隐私隐患**：用户打了马赛克，AI 仍可能基于未打码原图产出含敏感内容的文档。

而项目里 `AnnotationCanvas.getMergedImageDataUrl()`（L1066/L1158）本就是把「底图 + 全部标注（含 mosaic/blur 二次合成）」导出为一张图的成熟链路（保存/复制都在用）。**此前没把它接给 AI**——这是纯漏接，不是能力缺失。

## 实现（纯前端，零 Rust 改动）
### 设计要点：身份 / 视觉 分离
- `imageDataUrl`（原始截图）继续充当「截图身份」：对话线程键 `convHash`、历史「附加更多截图」排除当前图、上下文勾选启用判断——**保持不变，对话连续性零回归**。
- 新增 `visionImageUrl` 仅决定「发给模型的视觉内容」= `getMergedImageDataUrl()` 合成的编辑后截图。两者解耦，是最稳的增量方式。

### 改动清单
- `src/features/ai/AIPanel.tsx`
  - `AIPanelProps` 新增 `visionImageUrl?: string` 与 `onRefreshImage?: () => void`。
  - 组件内 `const visionImg = visionImageUrl || imageDataUrl;`，以下原本用 `imageDataUrl` 喂模型的四处全部改为 `visionImg`：
    - `handleGenerate` → `generate({ imageDataUrl: visionImg, ... })`
    - `orderedImages()` 「当前截图」内嵌（docx/html 导出图）→ `visionImg`
    - `handleMakeReport` → `generate({ imageDataUrl: visionImg, ... })`
    - `handleFollow` 后续轮追问 → `chat({ imageDataUrl: visionImg, ... })`
  - 上下文区新增「🔄 同步最新编辑」按钮（`onRefreshImage?.()`，宿主提供时显示），让用户边开面板边继续编辑后即时同步。
- `src/features/screenshot/components/EditorWindow.tsx` 与 `src/features/screenshot/EnhancedScreenshotApp.tsx`
  - 新增 `aiVisionUrl` 状态（默认=原图）。
  - 新增 `refreshAiVision()`：`await canvasRef.current.getMergedImageDataUrl()` 合成编辑后图并 `setAiVisionUrl`；**失败回退原图**，保证 AI 始终有图、且不影响其它功能。
  - 两个 `useEffect`：**切换截图**时重置为原图；**面板打开**时立即 `refreshAiVision()` 同步最新编辑。
  - `AIPanel` 挂载处加 `visionImageUrl={aiVisionUrl}` 与 `onRefreshImage={refreshAiVision}`。
- `src/locales/zh-CN.json` / `en-US.json`：新增 `ai.refreshImage` / `ai.visionImageTitle`。
- `src/index.css`：新增 `.ai-btn-refresh`（主题色描边+淡底，复用 `.ai-btn-sm` 尺寸，深浅色自适应）。

## 验证
- `pnpm build`：`tsc && vite build` exit 0，**136 模块，零类型错误**（与 Phase 7 持平，无回归）。
- `dist` 含新 token：`refreshImage` / `visionImageTitle` / `ai-btn-refresh`；`getMergedImageDataUrl` 链路本就在。
- 行为正确性：AI 收到的图 = 保存/复制同一条合成链路 → **与用户导出的成图完全一致**，且打码/模糊区已合入，敏感信息不会泄露给模型。

## 与 privdoc-ai 差异
其 `agentStore` 的视觉上下文是「文档/图片附件」；本方案更进一步——**截图工具的标注即视觉上下文**，AI 看到的是用户「加工后的图」而非裸图，且天然带隐私保护（打码生效）。

## 生效方式
`./start.sh dev`（或 `pnpm build && ./start.sh app`）。若 dev 窗口加载的是旧快照，kill 旧 dev 主 PID 后重开 `SnapCraft-dev.app`。

## 后续候选（未做，按优先级）
1. **记忆相关性检索**：压缩记忆后，按当前追问语义筛选注入哪些记忆（而非全量注入）。
2. **跨截图会话列表**：在侧栏看到每个截图的历史对话线程，可切换/复盘。
3. **后端代理隐藏 Key**：Rust 侧代理转发，密钥不下发渲染层（更强安全，但需动 Rust）。
4. **拖拽重排多截图选择顺序**：让报告章节顺序可手动拖拽调整。
