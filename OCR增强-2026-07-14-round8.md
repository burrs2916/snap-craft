# SnapCraft · OCR 取字功能增强（第八轮 · 搜索命中高亮 & 命中跳转导航）

> 实施时间：2026-07-14。状态：前端 `pnpm build` 通过（125 模块，exit 0），未改动后端 Rust，未触碰截图 / 标注 / 滚动 / 钉图 / 历史等其它功能。
> 本轮在已有 OCR（语言选择 / 逐行结果 / 区域框选 / 贴回标注 / 内联纠错 / 导出 / 搜索过滤 / 即时重识 / 置信度阈值 / 合并为一行 / 偏好持久化 / 自动复制 / 耗时统计 / 本地历史 / 阅读顺序校正 / 面板快捷键 / 重新识别 / 带坐标导出 / 字号 A−A+ / 逐行勾选 / 导出格式持久化）基础上，
> 做**两项纯前端、且全部收敛在 OCR 面板内 + 复用既有 `ocrSearch` / `ocrVisibleLines` / `ocrDisplayText`** 的增强：**搜索命中高亮** + **命中跳转导航（上一处/下一处 + 当前命中强调 + 滚动定位）**。
> 这是前 7 轮「仍留评估」清单里唯一同时满足「高可用价值 + 纯前端 + 不跨视图 + 零后端改动 + 不影响其它功能」的头部需求。

## 一、需求的攫取与发觉（本轮聚焦：搜索结果可读、可定位）

前 7 轮已把「能取字、能改、能搜、能导出、能按质量/版式过滤、能留存复用、能重跑、能调字号、能精准勾选」做齐。但「结果内搜索」一直停留在 **round2 的形态：只做行级过滤 + 显示「匹配 N/M」计数**——即把不含关键词的行藏掉、告诉用户有几行命中，**但没有把命中的关键词本身高亮出来**，也没有在多处命中之间快速跳转。

从真实使用路径看，这留下两个明确的可用性断点：

| # | 痛点 | 为什么影响可用性 | 本轮方案 | 状态 |
|---|------|----------------|----------|------|
| 1 | **搜索命中不高亮**：输入关键词后，命中的行被保留，但关键词淹没在行文字里，肉眼要逐字找 | 长文档 / 表格 / 代码截图里找一个词极费眼；round6/round7 两度标记为「仍留评估」的头部项，但被「textarea 内高亮成本高」挡住 | **命中高亮**：全文框改为只读高亮 `div`，用 `<mark>` 包裹命中词（大小写不敏感，纯 `indexOf` 非正则、无注入风险）；无搜索时渲染与历史完全一致（零回归） | ✅ 已实现 |
| 2 | **多处命中无跳转**：一个词在结果里出现 5~10 次，只能手动滚全文框逐个找 | 长文本里定位特定一处很慢 | **命中跳转导航**：搜索框右侧加 `‹ 当前/总数 ›`（`上一处/下一处`），当前命中用更强高亮 `.ocr-mark.active` 强调，并 `scrollIntoView` 滚动到全文框可视区 | ✅ 已实现 |
| 3 | 文字↔图块跨视图联动高亮 | 需跨 AnnotationCanvas，超出「不影响其它功能」边界 | 留评估（延续前几轮结论，本轮不做） |
| 4 | 翻译 / 跨视图快速取字 | 成本偏高或超出本地工具定位 | 留评估（延续前几轮结论，本轮不做） |

> 两项改动**不引入跨视图渲染、不改后端命令、不碰截图历史体系、不碰标注/画布/编辑输入**，完全符合「增量增强、不影响其它功能」的硬约束。

## 二、本轮实现的需求

选取 **#1 搜索命中高亮 + #2 命中跳转导航**。直接命中 OCR「结果里找一个词」这一最高频路径的体验断点，且改动全部收敛在 OCR 面板 + 复用既有搜索状态，零后端改动。

## 三、具体改动

### 模块级纯函数 `ocrHighlightParts(text, query)`（`EnhancedScreenshotApp.tsx`，组件外，无依赖、易测）
- 把文字按搜索词切成 `{text, hit}[]` 片段；命中片段 `hit=true` 供渲染 `<mark>`。
- **大小写不敏感**；**不使用正则**（纯 `toLowerCase()` + `indexOf`），彻底规避搜特殊字符（`.` `*` `(` 等）导致的注入/报错。
- 空查询返回单段 `{hit:false}` → 渲染与历史（纯文本 `textarea`）观感完全一致，零回归。

### 前端 `src/features/screenshot/EnhancedScreenshotApp.tsx`
- **状态 / ref**：新增 `ocrMatchIdx`（当前命中序号，0 起）；`ocrTextRef`（全文框 `div` ref，替代只读 `textarea`）；`ocrActiveMarkRef`（当前命中 `<mark>` ref，供滚动定位）。
- **`useEffect`**：`[ocrSearch]` 变化时 `setOcrMatchIdx(0)`（归零防越界、回到第一处）；`[ocrMatchIdx, ocrSearch]` 变化时把 `ocrActiveMarkRef` `scrollIntoView({block:'nearest'})` 滚进全文框可视区。
- **`selectOcrText()`**：用 `Range` 选中全文框全部内容，替代旧 `textarea` 的 `onFocus` 全选，保留「聚焦即全选、便于手动复制」的既有便利。
- **全文框**：只读 `textarea` → 只读可选择 `div.ocr-panel-text.ocr-hl`；渲染 `ocrHighlightParts(ocrDisplayText, ocrSearch)`，命中片段用 `<mark class="ocr-mark">`、当前命中用 `<mark class="ocr-mark active">`（带 `ref`）。`tabIndex=0` + `onFocus` 全选 + `onClick` 仅在无选区时全选（与旧 `textarea` 行为一致）。
- **搜索行**：在既有「匹配 N/M」（命中行数）右侧，当 `ocrMatchTotal>0` 时新增 `‹ 当前/总数 ›` 跳转导航（`ocr-match-nav`），`上一处/下一处` 用取模实现环形跳转，`ocrMatchTotal` 在编辑视图本地由 `ocrDisplayText` 统计（大小写不敏感出现次数）。
- **消费者零改动**：复制全部（`ocrDisplayText` state）、导出、贴回、逐行列表、全文框 全部仍走既有 `ocrVisibleLines` / `ocrIncludedLines` / `ocrDisplayText`，高亮只在「全文框渲染」这一层叠加，不进入数据链。

### 文案 `src/locales/{zh-CN,en-US}.json`（ocr 组新增 2 键）
`matchPrev`（上一处匹配 / Previous match）、`matchNext`（下一处匹配 / Next match）。

### 样式 `src/index.css`
- `.ocr-panel-text` 补 `overflow-y: auto`（长文本可滚动，对齐原 `textarea` 行为）+ `cursor: text`。
- 新增 `.ocr-mark`（半透明琥珀底，深/浅色主题均可见）、`.ocr-mark.active`（更强琥珀底 + 深色字 + 加粗，当前命中强调）、`.ocr-match-nav`（内联 flex）、`.ocr-match-btn`（‹ › 小方块按钮）、`.ocr-match-pos`（「当前/总数」计数）。

## 四、如何验收

```bash
pnpm build                       # 前端 tsc+vite，已通过（125 模块，exit 0）；后端无改动
./start.sh app                  # 打开含最新前端的 SnapCraft.app（内嵌 dist）
```

编辑器内点「取字」对一张含重复词的图（如多次出现「SnapCraft」）：
- **命中高亮**：在搜索框输入关键词 → 全文框里所有该词被琥珀色高亮；当前命中（第一处）用更深琥珀 + 加粗强调；逐行列表仍按搜索过滤正常显示。
- **跳转导航**：点 `›`/`‹` 切换当前命中，全文框自动滚动到对应位置，计数 `当前/总数` 同步更新；到末尾再 `›` 回到第一处（环形）。
- **无搜索时零变化**：未输入关键词时全文框 = 纯文本（与历史 `textarea` 观感一致）；`A−`/`A+` 字号、复制全部、导出、贴回、逐行编辑、勾选、置信度、阅读顺序等全部链路未动。
- **特殊字符安全**：搜 `.` `*` `(` `+` 等不会报错或异常高亮（纯 `indexOf`，无正则）。

## 五、回归确认

- 截图 / 区域 / 窗口 / 滚动长截图 / 钉图 / 文字标注 / 马赛克：均未改动，行为不变。
- 区域框选 OCR、内联纠错、结果内搜索过滤、切换语言即时重识别、置信度阈值、合并为一行、自动复制、耗时统计、本地历史、阅读顺序校正、面板快捷键、重新识别、带坐标导出、字号 A−A+、逐行勾选、导出格式持久化：链路未动；
  本轮只在「全文框渲染」层叠加高亮 + 在搜索行加跳转控件，**不进入复制/导出/贴回数据链**（这些仍读 `ocrDisplayText` state）。
- 后端 `ocr_image` / `OcrResult` / `OcrBlock` / `save_text_file` 签名与行为零变化（本轮纯前端）。
- 旧 `textarea` 的「聚焦全选便于复制」行为由 `div` + `selectOcrText` 完整保留；`复制全部文字` 按钮仍走 state，不受元素替换影响。
- 默认行为零回归：无搜索时全文框渲染为单段纯文本 = 历史观感；`ocrMatchIdx` 默认 0，搜索词变化即归零。
- 边界安全：搜索词含正则元字符不报错；空结果不渲染跳转控件；`scrollIntoView` 在 `ref` 为空时 `?.` 短路无副作用。
