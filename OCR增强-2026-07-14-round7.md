# SnapCraft · OCR 取字功能增强（第七轮 · 面板可读性 & 精准提取）

> 实施时间：2026-07-14。状态：前端 `pnpm build` 通过（125 模块，exit 0），未改动后端 Rust，未触碰截图 / 标注 / 滚动 / 钉图 / 历史等其它功能。
> 本轮在已有 OCR（语言选择 / 逐行结果 / 区域框选 / 贴回标注 / 内联纠错 / 导出 / 搜索 / 即时重识 / 置信度 / 合并为一行 / 偏好持久化 / 自动复制 / 耗时统计 / 本地历史 / 阅读顺序校正 / 面板快捷键 / 重新识别 / 带坐标导出）基础上，
> 做**三项纯前端、且全部收敛在 OCR 面板内 + 复用既有命令**的增强：**面板字号可调（A−/A+）**、**逐行勾选（选择性提取）**、**导出格式持久化**。

## 一、需求的攫取与发觉（本轮聚焦：可读性 & 精准提取）

前 6 轮已把「能取字、能改、能搜、能导出、能按质量/版式过滤、能留存复用、能重跑」做齐。从「结果真正好读、好用」的路径继续看仍存在的可用性断点：

| # | 痛点 | 为什么影响可用性 | 本轮方案 | 状态 |
|---|------|----------------|----------|------|
| 1 | **面板文字大小固定**：小字截图识别后，结果文字也细，凑近才看清 | 长文/密集文档阅读体验差，老人或高 DPI 屏更明显 | **面板字号 A−/A+**：经 `--ocr-fs` CSS 变量作用于结果文字，跨会话记住 | ✅ 已实现（解决第 6 轮留评的 #4） |
| 2 | **复制/导出/贴回只能「全要」**：表单/表格/部分内容想只取几行，只能手动在文本框里选 | 精准提取高频刚需，目前无入口 | **逐行勾选**：每行加复选框；有勾选时复制/导出/贴回仅取勾选的可见行，无勾选时取全部（零回归）；选项行「全选/取消」+ 已选计数 | ✅ 已实现 |
| 3 | **导出格式每次重选**：常导 JSON/TSV 的人每次都要重选 | 重复操作，打断心流 | **导出格式持久化**：写入 OcrPrefs 跨会话记住（默认 txt） | ✅ 已实现 |
| 4 | 搜索命中不高亮 | textarea 内高亮实现成本高、易引入回归 | 留评估（延续第 6 轮结论，本轮不做） |
| 5 | 文字↔图块跨视图联动高亮 | 需跨 AnnotationCanvas，超出「不影响其它功能」边界 | 留评估（延续第 6 轮结论，本轮不做） |
| 6 | 翻译 / 跨视图快速取字 | 成本偏高或超出本地工具定位 | 留评估（延续前几轮结论，本轮不做） |

> 上述 3 项改动**不引入跨视图渲染、不改后端命令、不碰截图历史体系、不碰标注/画布**，完全符合「增量增强、不影响其它功能」的硬约束。

## 二、本轮实现的需求

选取 **#1 字号可调 + #2 逐行勾选 + #3 导出格式持久化**。三者都命中「结果更好读、更好取」的最高频痛点，且改动全部收敛在 OCR 面板 + 复用既有 `ocr_image` / `save_text_file` 命令，零后端改动。

## 三、具体改动

### 前端 `src/features/screenshot/EnhancedScreenshotApp.tsx`
- **类型 / 偏好**：`OcrPrefs` 新增 `fontSize: number` 与 `exportFmt: OcrExportFmt`（默认 `fontSize:14`、`exportFmt:'txt'`）；`loadOcrPrefs` / `saveOcrPrefs` 同步读写；持久化 `useEffect` 依赖追加 `ocrFontSize` / `ocrExportFmt`。
- **状态**：
  - `ocrFontSize`（默认 `ocrPrefs0.fontSize`，即 14，= 历史观感）；
  - `ocrExportFmt`（默认 `ocrPrefs0.exportFmt`，即 `'txt'`，零回归）；
  - `ocrSel: Record<number, boolean>`（逐行勾选集合，原始块下标）。
- **`runOcr`**：新识别时 `setOcrSel({})`，选择只针对本次结果（与 `ocrEdits` 同步清空）。
- **`ocrIncludedLines()`（组件内纯函数，useCallback）**：在 `ocrVisibleLines()`（搜索+置信度过滤）之上叠加逐行勾选——
  - 未勾选任何行 → 取全部可见行（历史行为）；
  - 有可见行被勾选 → 仅取勾选的可见行（精准提取）。
  - 隐藏行（被搜索/置信度过滤掉）即使曾勾选也不计入，杜绝带上不可见数据。
- **消费者统一走「纳入集合」**（WYSIWYG）：
  - `buildOcrExportContent`：`vis` 改 `ocrIncludedLines()`；
  - `applyOcrAsAnnotations`：`ocrVisibleLines()` → `ocrIncludedLines()`；
  - `handleExportOcr`：空检查用 `ocrIncludedLines()`；
  - 编辑视图 `ocrDisplayText` / `ocrChars`：改用 `ocrIncludedLines()`（复制全部按钮、全文框随之同步）；逐行列表仍遍历 `ocrVisibleLines()`（展示全部过滤后行 + 各自勾选框）。
- **面板头部**：在「历史」与「关闭」之间加 `.ocr-fs` 字号控制（`A−`/`A+`，范围 11–22，边界禁用），写入 `--ocr-fs` 注入 `.ocr-panel` 根节点。
- **逐行列表**：每行 `<input type=checkbox class="ocr-block-sel">`，`checked={!!ocrSel[i]}`，变更增量更新 `ocrSel`。
- **选项行 `.ocr-opts-row`**：在「导出格式」后新增 `.ocr-sel-btn`「全选/取消」（有勾选显示「取消选择」、否则「全选」，对当前可见行整体切换）+ 已选计数 `.ocr-sel-info`（「已选 N 行」），仅在 `ocrSelActive` 时显示。

### 文案 `src/locales/{zh-CN,en-US}.json`（ocr 组新增 7 键）
`fsMinus` / `fsPlus` / `selLineTitle` / `selAll` / `selClear` / `selAllTitle` / `selInfo`。

### 样式 `src/index.css`
- `.ocr-panel-text` / `.ocr-block-edit` / `.ocr-history-text` 的 `font-size` 改为 `var(--ocr-fs, 14px)`（默认 14 = 历史观感，零回归）。
- 新增 `.ocr-fs` / `.ocr-fs-btn`（A−/A+ 按钮，边界禁用态）/ `.ocr-block-sel`（行前复选框）/ `.ocr-sel-btn`（全选/取消）/ `.ocr-sel-info`（已选计数）。

## 四、如何验收

```bash
pnpm build                       # 前端 tsc+vite，已通过（125 模块，exit 0）；后端无改动
./start.sh app                  # 打开含最新前端的 SnapCraft.app（内嵌 dist）
```

编辑器内点「取字」对一张图：
- **面板字号**：点头部 `A+`/`A−`，结果文字（全文框 + 逐行 + 历史）实时缩放；关闭重开后面板字号保持（跨会话记住）；`A−` 到 11、`A+` 到 22 时对应按钮禁用。
- **逐行勾选**：勾选若干行 → 点「复制全部文字」/「导出文本」/「作为文字标注贴回」只含勾选行；勾选部分后选项行显示「已选 N 行」与「取消选择」；「全选」一键勾选全部可见行；清空搜索/改阈值后隐藏行的勾选自动不计入。
- **导出格式持久化**：选「JSON（含坐标）」后关闭面板、再次取字（或重开应用），导出格式下拉仍停在 JSON。
- **默认零变化**：未调字号时全文框=14px（=历史）；未勾选任何行时复制/导出/贴回=全部可见行（=历史）；未改导出格式时默认 `.txt`（=历史）。

## 五、回归确认

- 截图 / 区域 / 窗口 / 滚动长截图 / 钉图 / 文字标注 / 马赛克：均未改动，行为不变。
- 区域框选 OCR、内联纠错、结果内搜索、切换语言即时重识别、置信度阈值、合并为一行、自动复制、耗时统计、本地历史、阅读顺序校正、面板快捷键、重新识别、带坐标导出：链路未动；
  新增三项只增不替——字号经 CSS 变量注入、勾选经 `ocrIncludedLines` 叠加在既有 `ocrVisibleLines` 之上、导出格式仅在既有 `save_text_file` 上复用偏好。
- 后端 `ocr_image` / `OcrResult` / `OcrBlock` / `save_text_file` 签名与行为零变化（本轮纯前端）。
- **默认行为零回归**：`ocrFontSize` 默认 14（=历史全文框字号）；`ocrExportFmt` 默认 `'txt'`；`ocrSel` 默认空 → 复制/导出/贴回取全部可见行（=历史）；新识别清空 `ocrSel` 与 `ocrEdits`。
- OCR 偏好仍用**既有**隔离 localStorage key（`snapcraft.ocr.prefs`），与截图历史 store 完全隔离；本轮仅扩展该 key 的字段（fontSize / exportFmt），旧 key 读取时向后兼容（`{...def, ...parsed}`）。
