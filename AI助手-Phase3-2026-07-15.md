# AI 助手集成 · Phase 3 报告（2026-07-15）

> 参考项目：`/Users/liwenchao/GithubProSpace/privdoc-ai`（本地 AI 文档助手）
> 本期目标：在不影响现有截图/标注/OCR/历史功能的前提下，持续发掘需求，把「AI 助手」从「生成文档/文案」再往前推一步——**让 AI 结果落回截图本身**。

## 1. 需求发掘（对照 privdoc-ai）

privdoc-ai 的强项在「多源文档 → 结构化产出 → docx/xlsx/pdf 导出」，但它本质是**纯文档工具**，产出的文字只能落在文档里。

回顾我们 SnapCraft AI 已走过的路：

| 阶段 | 能力 | 状态 |
|---|---|---|
| Phase 1 | AI 面板 + 单图 + OCR 上下文 + 7 预设 | ✅ |
| Phase 2a | 导出 .md/.txt + 润色迭代 | ✅ |
| Phase 2b | 多截图成稿 + 自定义模板 + .html | ✅ |
| Phase 2c | 真实 .docx（内嵌来源截图）+ .pdf | ✅ |

**确诊的关键断点**：AI 结果始终**停在面板里**（展示 / 复制到剪贴板 / 导出成独立文档），却**回不到截图画面**。而「截图工具」的独有价值，恰恰是让 AI 产出的文案成为**图上可编辑、可二次加工、可带出成图的证据**。privdoc-ai 做不到这一点——这正是 SnapCraft 的差异点。

→ 本期落地 **「AI 文案 → 截图标注回写」**，闭合整个闭环：

```
截图 → 编辑/标注 → AI 生成文案 → 一键回写截图 → 再移动/改样式/组合 → 导出带 AI 文案的成图
```

## 2. 实现（纯前端，零 Rust 改动，不影响现有功能）

### 2.1 `src/features/ai/AIPanel.tsx`
- 新增可选 prop `onApplyToScreenshot?: (text: string) => void`。
- 结果区「导出/润色」下方新增一行按钮 **「📝 回写截图」**：
  - 点击将 AI 输出经 `mdToPlainText()` 转纯文本（去掉 Markdown 标记，保留可读结构）后回调宿主；
  - 成功后给轻量提示（1.8s 自动消失）；
  - 无输出 / 流式中 / 宿主未接入时自动禁用。

### 2.2 两个编辑器接入处理器
`EnhancedScreenshotApp.tsx` 与 `EditorWindow.tsx` 均新增 `applyAiToScreenshot(text)`：
- 把纯文本构造成一个 `text` 标注（`AnnotationObject`），**完全复用既有标注体系**（与 OCR 贴回、手动加文字走同一渲染/撤销/导出链路）：
  - 默认位置：左上角 `(16, 16)` 自然像素；
  - 半透明深底衬 `bg:true / bgColor:#1d1d1f / bgOpacity:0.72` —— 保证在任意截图背景上都清晰可读；
  - 字号按图宽自适应 `14~24px`；
  - 字体/颜色继承当前工具栏默认。
- 调 `addAnnotation` 写入，并 `flash` 成功 toast；
- **尺寸守卫**：宽高无效时跳过并提示，避免 NaN 坐标污染截图。

### 2.3 i18n（zh-CN + en-US）
新增 `ai.applyToScreenshot` / `ai.applyTitle` / `ai.applied` / `ai.applyZero` 共 4 键。

### 2.4 CSS
`.ai-btn-apply`：主题色描边 + 淡底，与现有 `.ai-btn-sm` 共用尺寸，无新增布局风险。

## 3. 验证
- `pnpm build`：**exit 0，134 模块，零类型错误**（tsc + vite）。
- `dist` 已含全部新 token：`ai-btn-apply`、locale 键 `applyToScreenshot`/`applied`/`applyZero`、`onApplyToScreenshot`。
- 改动范围严格限定在 AI 面板与编辑器挂载处，**未触碰截图/标注/OCR/历史/保存的任何既有逻辑**。

## 4. 用户体验闭环
用户现在可以在 AI 生成文案后：
1. 点「📝 回写截图」→ 文案作为文字标注出现在截图左上角；
2. 拖动改位置、在标注工具栏改字号/粗体/底衬/颜色；
3. 与打码、箭头、序号等标注自由组合；
4. 最后「保存」导出一张**自带 AI 文案的成图**，或「导出 .docx/.pdf」产出完整文档（截图 + AI 文案并列）。

## 5. 后续候选（未做，纯增量，不影响现有功能）
- **对话历史持久化（SQLite）**：多轮「搭建一份文档」会话可跨重启恢复。
- **后端代理隐藏 Key**：Rust 侧代理 LLM 调用，密钥不下发渲染层。
- **批量截图一键成报告**：多选历史截图 → 自动分段、按序成稿（多图自动报告）。
- **.xlsx 导出**：AI 输出的表格数据直接落 Excel。
- **AI 多轮 Agent（记忆/压缩）**：参考 privdoc-ai 的 `plugins/ai_agent/`（memory.rs / compact.rs / engine.rs），让 AI 跨多张截图持续「记»前面说过什么」，产出更连贯的长文档。

---
生效方式：`./start.sh dev`（开发热更）或 `pnpm build && ./start.sh app`（内嵌 dist，无 HMR 时序问题）。
