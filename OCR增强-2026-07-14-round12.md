# OCR 功能增强 · 第十二轮（需求攫取 → 智能实体提取）

> 实施时间：2026-07-14。状态：代码完成，`pnpm build`（前端 tsc + vite）通过（125 模块，exit 0），**后端零改动**，未触碰截图/标注/历史/钉图/主题等任何其它功能。

## 一、需求攫取与发觉（本轮起点）

对前 11 轮已落地的 OCR 能力做盘点，并按「日常截图取字场景」重新挖掘缺口：

| 已具备 | 说明 |
|---|---|
| 结构化识别 + 逐行/选区/贴回 | 坐标 + 置信度全用起来 |
| 语言选择 / 内联纠错 / 搜索高亮 + 跳转 | 长文本快速定位 |
| 置信度阈值 / 合并一行 / 阅读顺序重排 | 质量与排版 |
| 偏好持久化 / 自动复制 / 耗时统计 / 本地历史 | 效率 |
| 重识别 / 快捷键 / 字号可调 / 逐行勾选 | 体验 |
| 空间预览联动 / 结果条取字 / 历史网格取字 / 剪贴板取字 | 4 个取字入口 |

**新发现的可用性缺口（按「可用性 × 风险」排序）：**

1. **智能实体提取（URL / 邮箱 / 电话）** —— 高可用 / 低风险。用户常截取报错页、名片、收据、网页，里面混着网址/邮箱/电话，逐项手选复制很累。
2. 翻译 —— 中可用 / 高风险（需网络或离线模型，超出本地工具定位）。
3. 文本清洗（全角半角/去空行） —— 低可用。
4. 多区域批量 OCR —— 中可用 / 中风险（需改造框选模式，改动面较大）。

**结论：** 选中 **①智能实体提取**。它是真正「攫取」到的高频痛点，且纯前端、零后端改动、视图层辅助、可 opt-in 默认关闭（零回归），完美满足「集成/改善/优化且不影响其它功能」的约束。

## 二、实现（增量、零回归）

### 1. 纯函数 `ocrExtractEntities(text): OcrEntity`
- 位置：紧随 `ocrHighlightParts` 之后，模块级纯函数，无副作用、无正则注入风险。
- 三类独立安全正则 + `Set` 去重，按出现顺序保留：
  - **URL**：`https?://` 或 `www.` 开头，截到空白/引号/中文标点，并去除结尾标点。
  - **邮箱**：标准 `user@domain.tld` 形态。
  - **电话**：7–14 位数字，可含空格/括号/连字符/点；**显式过滤纯日期（YYYY-MM-DD 等）**规避误判；纯数字串仅接受 11 位（中国大陆手机号启发式），规避价格/编号等长数字。

### 2. 偏好与状态（收敛在既有 OCR 面板 + 隔离 localStorage）
- `OcrPrefs` 新增 `extract: boolean`（默认 `false` = 历史观感零回归），`loadOcrPrefs` 默认值同步。
- 新增 `ocrExtract` 状态（从偏好初始化）；持久化 `useEffect` 写入 `snapcraft.ocr.prefs`，跨会话记住。
- 渲染期 `const ocrEnt = ocrExtract ? ocrExtractEntities(ocrDisplayText) : null;` —— **提取自当前可见/纳入集合文字（WYSIWYG）**，受搜索/置信度/勾选/合并影响；关闭时为 `null` = 零渲染。

### 3. 面板 UI
- 选项行新增 checkbox **「提取链接/邮箱/电话」**（`.ocr-extract`，与其它开关同风格）。
- 选项行下方新增实体区块（仅 `ocrEnt` 非 null 时渲染）：
  - 标题 + 总数 badge；
  - 未识别到 → 空态提示；
  - 按「网址 / 邮箱 / 电话」分组，每组：每条可**单独复制**，组头「复制全部」一键复制整组；
  - 长实体省略号截断 + title 悬浮看全文。

### 4. i18n（zh-CN / en-US 同步）
新增 `ocr.extract` / `extractTitle` / `entUrls` / `entEmails` / `entPhones` / `entCopyAll` / `noEntity`；逐条复制复用既有 `ocr.copy`。

### 5. CSS
新增 `.ocr-extract` / `.ocr-entity*` 系列，沿用 `--accent` / `--surface-*` / `--border` 主题变量，明暗主题自适应。

## 三、关键设计（保障「不影响其它功能」）

- **纯视图层辅助**：实体提取**不进入**复制全部 / 导出 / 贴回标注 / 历史的数据链（与第八轮搜索高亮同一设计原则），复制/导出行为与原有一致。
- **opt-in 默认关**：`extract` 默认 `false`，旧用户打开面板外观与行为完全不变 = 零回归。
- **后端零改动**：仅前端；`runOcr` 依赖与重置逻辑未动（实体偏好不像合并/自动复制那样需在重识别时重置，它跨识别持续有效）。
- **隔离存储**：复用 `snapcraft.ocr.prefs` 单 key，与截图历史 store 完全隔离。

## 四、验证

- `pnpm build` → exit 0（tsc 通过，125 模块，vite 产物正常）。
- `dist/assets/*.js` 含 `noEntity`/`entCopyAll`/`entUrls`/`extractTitle`/`ocr-entity`；`dist/assets/*.css` 含 `.ocr-entity`/`.ocr-extract`/`.ocr-entity-copyall`。
- 后端 `src-tauri/src/lib.rs` 未改（仍仅 `ocr_image` + `read_clipboard_image` 两条 OCR 命令）。

## 五、仍留后续评估（延续，本轮不做）

- 翻译（网络/离线边界）；
- 文本清洗（低可用）；
- 多区域批量 OCR（需改造框选模式，改动面较大，待放宽约束再做）。

OCR 取字现已覆盖：四个入口（截图/结果条/历史网格/剪贴板）+ 结构化/语言/选区/纠错/搜索高亮/置信度/合并/持久化/自动复制/统计/历史/阅读顺序/重识别/快捷键/字号/勾选/空间联动 + **本轮机上实体一键提取**。面板内纯前端增强已高度收敛；后续如要再突破需先放宽「不影响其它功能」约束或引入离线资源。
