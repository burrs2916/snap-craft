# AI 助手「生成文档」元信息修复（2026-07-17 第三轮）

> 接上一轮（增强×修复）用户"好继续"。本轮落地"发现未落地"清单第 ③ 项：**导出文件元信息打通**。
> 经源码实证，澄清一个关键误判：原以为"docx 元信息硬编码"，实际** docx 用 docxjs 库自动填默认空值，真正手写死假值的是 pptx**。

## 根因（源码实证）
- **pptx**（`markdownPptx.ts`）：`CORE_XML` 模板硬编码 `<dc:title>SnapCraft AI</dc:title>`、`<dcterms:created>2026-07-16T00:00:00Z</dcterms:created>`、modified 同值。**每一份导出的 PPTX 在 PowerPoint「文件信息」里都显示错的标题与固定日期** → 典型"假实现"。
- **docx**（`markdownDocx.ts`，docx 9.7.1）：库默认 `creator`/`lastModifiedBy` = `"Un-named"`、`title` 空白 → 文档属性失真（非我们写死，但同样是"失真"）。
- **xlsx**（SheetJS 库）：仅库默认、无手写假值 → **不改动**（避免越界）。

## 修复
1. **pptx**（`markdownPptx.ts`）：
   - `CORE_XML` 常量改为 `coreXml(title, nowIso)` 函数。
   - 标题取 `opts.title`（AIPanel 已传 `presetLabel(activePreset)`），经 `esc()` 防 XML 注入；时间取导出时刻 `new Date().toISOString().split('.')[0]+'Z'`（W3CDTF 格式，对齐原格式）。
   - 调用点 `files.push({ name:'docProps/core.xml', data: encStr(coreXml(opts.title ?? '', ...)) })`。
2. **docx**（`markdownDocx.ts`）：`new Document({...})` 改用 **docx 9.7.1 正确的顶层字段**（非 `coreProperties`，该版本类型未暴露且私有）：`title` / `creator:'SnapCraft'` / `lastModifiedBy:'SnapCraft'` / `description: subtitle`。库会把它们写入 core.xml，`created`/`modified` 由库默认填导出时刻。

## 改动文件
| 文件 | 改动 |
|---|---|
| `src/features/ai/markdownPptx.ts` | `CORE_XML` 常量 → `coreXml(title, nowIso)` 函数；push 处注入真实标题+时间 |
| `src/features/ai/markdownDocx.ts` | `new Document` 顶层加 `title`/`creator`/`lastModifiedBy`/`description`（修复前为库默认 "Un-named"/空白） |

## 验证
- `pnpm build`（tsc + vite）**绿**，0 error 0 warning。
- 关键类型陷阱已排：初版误用 `coreProperties`（docx 9.7.1 该字段私有且类型未暴露）→ 改用顶层 `title`/`creator` 等（已对照 `node_modules/docx/dist/index.d.ts` 的 `IPropertiesOptions` 与 iife 运行时确认 `options.title/creator/lastModifiedBy` 会写入 core.xml）。

## 仍待你拍板（未做）
- ④ doc 图文穿插锚点（`doc` 预设不产生 `<!--SNAP:k-->`，无图文混排）
- ⑤ 面板内富文本二次编辑后重导
- （元信息项已覆盖 docx/pptx；xlsx 为库默认无假值，未动）
