# AI「生成文档」(第一个功能标签) 复盘×修复 第四轮（2026-07-17 ~22:33）

> 范围：对 AI 助手**第一个功能标签「生成文档」(doc 预设)** 的前端已有功能做代码级复盘回溯，聚焦「附带截图 / 附带识别文字 / 导出 6 格式 / 文档主题样式 / 润色 / 二次编辑回写 / 复制为富文本 / 历史库导出」整条链路，找空实现、假实现、实现不完美、达不到预期的项，直接修复，并挖掘新需求。
>
> 方法：直读 `AIPanel.tsx` / `markdownHtml.ts` / `markdownPptx.ts` 等源码（file:line 实证），与前三轮已修项交叉比对，确认**本轮新发现** vs **前三轮漏改**。

## 一、本轮落地（5 项真实修复，全部纯前端、0 Rust、0 新依赖）

### 🔴 缺陷1（P1，前三轮漏改）—— 历史库导出 html/pdf 截图位置仍在底部
- **证据**：前三轮（第三轮 21:18）只修了**实时导出**三处（`handleExport` html / `handleExportPdf` / `handlePreview`），把截图从 `html.replace('</body>', …)` 改为 `frontImageBlockHtml()` 前置到 `<main class="doc-main">` 之前（顶部）。
- **但历史库导出路径** `handleHistoryExport` 的 `pdf`（AIPanel.tsx:480）与 `html`（AIPanel.tsx:507）分支**仍用旧写法** `html.replace('</body>', …)` → 截图被丢在正文底部（footer 之后）。
- **后果**：同一篇文档，从「生成文档」实时导出（截图在顶部）与从历史库导出（截图在底部）位置错乱，达不到「附带截图」位置一致的预期。
- **修复**：两处统一改为 `frontImageBlockHtml(coverImages)` 前置到 `<main class="doc-main">` 之前，与实时导出完全对齐。

### 🔴 缺陷2（P1，前三轮漏改）—— 实时 PPTX 封面标题写死预设名
- **证据**：第三轮给 html/docx/pdf/preview + 历史库 docx/html/pdf 都改用了 `firstHeading(...) || 预设名`（让 AI 自拟标题上封面），**唯独漏了实时 PPTX**：`handleExportPptx`（AIPanel.tsx:983）的 `title` 仍是 `presetLabel(activePreset)`（如"文档"）。
- **后果**：导出 PPTX 时封面永远是通用预设名，AI 写的真实标题被吞，与 docx/html/pdf 行为不一致。
- **修复**：改为 `firstHeading(useSections ? output : stripSnapMarkers(output)) || presetLabel(activePreset)`，与另四种产物一致。`markdownPptx` 本就用 `opts.title` 作首屏标题（markdownPptx.ts:731-732），改动即生效。

### 🟠 缺陷3（P2，前三轮已发掘未落地）—— 历史库导出不支持 PPTX
- **证据**：`handleHistoryExport` 类型（AIPanel.tsx:427）与历史阅读器 UI（AIPanel.tsx:2265）只有 md/txt/html/docx/pdf/xlsx，**无 pptx**。
- **修复**：① 类型加 `'pptx'`；② `coverImages` 条件纳入 `pptx`（缩略图作封面前置块）；③ 新增 `else if (fmt === 'pptx')` 分支，调 `markdownToPptx(md, { title: firstHeading(md)||预设名, subtitle: firstGoal, theme, images: coverImages||[] })`；④ 历史导出行加 `.pptx` 按钮（带 `exportPptxTitle` 提示）。

### 🟠 缺陷4（P2，前三轮已发掘未落地）—— 历史库导出后无「在 Finder 显示 / 打开文件」双按钮
- **证据**：实时导出区有 reveal/open 双按钮（AIPanel.tsx:1689 / 2172，由共享 `lastExportedPath` 驱动）。但历史库导出只 `setLastExportedPath`、只显示 `historyMsg`，**reveal/open 双按钮只出现在实时区**（用户从历史阅读器导出后，要在另一块 UI 找按钮，割裂、易找不到）。
- **修复**：新增 `historyExportedPath` 状态（与 `lastExportedPath` 解耦），历史 md/txt/html/docx/xlsx/pptx 五个落盘分支写入它；历史阅读器导出区在 `historyMsg` 下方渲染专属 reveal/open 双按钮（复用 `exportReveal` / `openApp` 文案与 `revealInFolder` / `openExported`）。PDF 为打印式不落盘，仍只给诚实提示、不显示双按钮（与实时区一致）。

### 🟡 缺陷5（P2，前三轮已发掘未落地）—— 实时 md/txt/html/pdf/预览按钮缺 exporting 禁用
- **证据**：仅 xlsx/docx/pptx 按钮有 `disabled={exporting}`（AIPanel.tsx:1605/1613/1621），而 md/txt/html/pdf/预览（AIPanel.tsx:1593/1596/1599/1626/1630 主区；2107/2108/2109/2146/2148 弹窗区）**无禁用**，导出进行中仍可重复点击/并行触发。
- **修复**：① 三个 handler（`handleExport` / `handleExportPdf` / `handlePreview`）顶部加 `if (exporting) return` 守卫；② 上述 5 类按钮（主区+弹窗区共 10 处）加 `disabled={exporting}`。杜绝导出期重复点击与并行触发，与重量级导出行为一致。

## 二、本轮重新核定的「非缺陷 / 已正常」（刻意不改动，避免误伤）
- `attachImage` / `attachOcr` 复选框**真控**模型输入（aiStore 中 `orderedImages` / OCR 文本拼接），非空实现。
- 6 格式导出（含第三轮修好的 docx/pptx 内嵌截图、xlsx、md/txt/html）全可用。
- 文档主题样式贯通：docx/pptx 用强调色 token、html/pdf 用全 CSS（DOC_THEMES）、复制富文本同款 figure 样式。
- `refine` 润色（更短/更长/正式/口语/英文）真闭环；`setOutput` 二次编辑写回同步会话+落盘，重开恢复同源。
- PDF 诚实提示（调起系统打印、不写假路径），不误导「在 Finder 中显示」。
- 第 3 轮的 `frontImageBlockHtml` / `firstHeading` 辅助函数已被本轮历史路径正确复用，无重复实现。

## 三、新需求点发掘（仅记录，未落地，按 ROI 排序）
1. **doc / prd 单图「图文穿插锚点」**：当前报告类（多图）已能按 `<!--SNAP:k-->` 把截图嵌到对应小节；但单图 doc/prd 预设仍是「截图整块堆在顶部」。`markdownDocx/Pptx` 内嵌管线已支持，缺在预设 prompt 引导 AI 埋 SNAP 锚点 → 让单图文档正文读起来「边读边看图」。（卖点：长文档可读性）
2. **导出格式记忆 / 上次路径记忆跨窗持久**：`lastExportedPath` 仅在内存，关窗即丢；可落 localStorage 供下次默认打开同目录。（卖点：少点一次）
3. **导出历史持久化 + 最近 N 次导出列表**：目前导出后仅靠 `historyMsg` 临时提示，关窗即不可追溯；可建 `snapcraft-ai-export-history` 落盘（路径/格式/时间/来源会话），主面板「最近导出」一键 reveal/open。（卖点：工作流闭环）
4. **AI 文档「替换图 / 续写 / 另存为模板」入口**：成稿后缺「换张图重生成这一段」「接着写」「存成本次风格的模板」三个高频动作。（卖点：迭代效率）
5. **批量导出队列**：大文档串行 await 导出卡主线程，可加轻量任务队列 + 进度。（卖点：大批量不卡）

## 四、验证
- `npx tsc --noEmit`：0 错 0 警 ✅
- `pnpm build`（tsc && vite build，双入口 index.html / ai-panel.html）：exit 0，~1.93s ✅
- i18n 键 `exportPptxTitle` / `exportReveal` / `openApp` / `exportRevealTitle` / `openAppTitle` 等全部存在于 `src/locales/zh-CN.json` + `en-US.json` ✅
- 铁律保持：纯前端增量、0 Rust 改动、0 新依赖、未破坏任何现有功能。

## 五、改动文件
- `src/features/ai/AIPanel.tsx`（仅此一个文件，约 +40 / -20 行）
  - 历史库导出 pdf/html 截图顶部插入（对齐实时）
  - 实时 PPTX 封面用 `firstHeading`
  - 历史库导出新增 pptx 分支 + UI 按钮
  - 新增 `historyExportedPath` 状态 + 历史区 reveal/open 双按钮
  - 实时 md/txt/html/pdf/预览 按钮 `disabled={exporting}` + handler 守卫
