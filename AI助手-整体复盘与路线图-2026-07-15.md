# SnapCraft AI 助手 · 整体复盘、缺陷核查与增强路线图（2026-07-15）

> 范围：对 Phase 1–12 已落地的全部 AI 能力做**真值核查**（有无空实现/假实现/前后端断联/bug），并对照顶级项目 `claw-code-main` 与 `openclaw-main`，发掘 AI 成为产品卖点的新需求与底层增强机会。
> 本轮已**实修 2 类真实缺陷**（见 §3），纯前端、零 Rust 改动、构建全绿。

---

## 0. 一句话结论

**地基非常扎实，没有"假实现"。** 12 个 Phase 声称的能力全部可从 UI 点击到达真实后端/输出；18 个前端 `invoke` 命令与 Rust `#[tauri::command]` 100% 对齐，无参数错位、无返回形状错配。**真正的问题不在"有没有"，而在"稳不稳、智不智、卖点够不够锋利"**——这正是下一阶段要补的。

---

## 1. 空实现 / 假实现核查（逐文件）

| 模块 | 结论 | 证据 |
|---|---|---|
| `AIPanel.tsx` | **无死代码、无空实现** | 所有 handler 均被按钮/回调调用 |
| `aiStore.ts` | 全部方法被调用；1 处**不可达死分支**（`buildUrl` 永不抛错，L111-115 catch 永不触发） | `aiStore.ts:111` |
| `aiClient.ts` | OpenAI(`choices[0].delta.content`) + Anthropic(`content_block_delta`) SSE 均**正确** | `aiClient.ts:167 / :162` |
| `aiPresets.ts` / `aiTypes.ts` / `aiMarkdown.tsx` | 无空实现；`SNAP_MARKER_RE`/`stripSnapMarkers`/`hasSnapMarkers` 均被使用 | — |
| `markdownHtml.ts` | `MdToHtmlOpts.subtitle` 字段已声明但**永不通**（副标题分支为死代码，L278-324） | `AIPanel` 调用点均未传 `subtitle` |
| `markdownDocx.ts` / `markdownXlsx.ts` | 无空实现；双回退路径完整 | — |

**核查的 10 项声明功能全部 [WORKS]**（多轮对话 / 记忆压缩 / 相关性检索 / 跨截图历史库 / OCR 跟随编辑图 / 编辑后图喂模型 / 5 主题+预览 / 6 格式导出 / 批量成报告 / 回写标注）——可直接从 UI 到达，无 FAKE/EMPTY。

---

## 2. 前后端联动核查（18 个 invoke 全绿）

| 类别 | 结果 |
|---|---|
| 命令存在性 | 18/18 命中 `lib.rs:172-200` 的 `generate_handler!`，无悬空调用 |
| camelCase 参数匹配 | `image_data`/`file_path`/`ocr_text`/`annotations_json` ↔ `imageData`/`filePath`/`ocrText`/`annotationsJson` 全部对齐；嵌套 `HistoryItem` 字段 snake_case 一致 |
| 返回形状 | `OcrResult{text,blocks[]}`、`HistoryItem{data_url,created_at,...}` 前后端吻合 |
| 异步卫生 | 全部 `async fn`，错误以 `Result<_,String>` 中文回传；OCR / 剪贴板读取会短暂占住 tokio worker（**不影响正确性**，仅吞吐） |
| `tauri.conf.json` | `csp:null`（AI 直连必需）✅；`infoPlist:"Info.plist"` 字符串路径✅ |

**唯一架构事实**（非 bug）：AI 对话历史仅存前端 `localStorage`（按截图 hash 分桶），与 Rust 后端 `history.rs` **完全解耦**——换机/重装即丢，无后端落库。

---

## 3. 真实缺陷清单（本轮已修 + 待修）

### ✅ 本轮已实修（2 处，构建全绿 / 136 模块 / exit 0）
1. **聊天区泄漏原始 `<!--SNAP:k-->` 锚点标记**：流式/历史渲染时 AI 输出里的报告章节标记直接裸显给用户。
   → 修复：`aiMarkdown.tsx` 在解析前用 `^<!--SNAP:\d+-->\s*$/` 过滤掉这些行（导出仍由 `stripSnapMarkers` 真剥离，行为不变）。
2. **历史阅读器导出能力断档 + 成功提示残缺**：
   → 修复：`handleHistoryExport` 从 `md|txt|html|xlsx` 扩展到 `docx|pdf`，补齐与主流水线一致的 6 格式按钮；并修复 `setHistoryMsg(t('ai.exportOk',{path:''}))` 永远传空路径导致提示残缺的问题（现取文件名）。注：历史线程不存来源截图的完整像素，docx/pdf 走**纯文本**回退（主面板仍内嵌截图）。

### 🟡 待修（小，建议并入下个 Phase）
- `aiStore.ts:111-115` 不可达 catch 分支 → 删除或让 `buildUrl` 真正抛错。
- `markdownHtml.ts` `subtitle` 死代码 → 真正接入（让用户给文档封面加副标题/品牌色），或删除字段。
- 本地化孤儿键：`ai.styleTitle`/`ai.outputEmpty` 定义未用；主面板 `.docx`/`.pdf` 按钮硬编码标签未用 `ai.exportDocx`/`ai.exportPdf`。建议二选一（调用 or 删键），保持 i18n 干净。
- OCR / 剪贴板读取在 tokio worker 线程同步阻塞 → 包 `spawn_blocking` 提升吞吐（Rust 侧小改）。

---

## 4. 对照顶级项目：可落地的底层增强模式

参考 `claw-code-main`（Rust 编排：`crates/runtime`+`crates/api`）与 `openclaw-main`（TS 包装层 + 上下文引擎 + 记忆后端），把它们的招牌能力映射到我们的纯前端架构：

| 顶级项目能力 | 我们现状 | 差距 / 增强机会 | 优先级 |
|---|---|---|---|
| **claw-code**：`SystemPromptBuilder` 预算截断（单文件≤4000、总≤12000、`[truncated]` 标记） | 无预算控制，多图+OCR+记忆累积会让 prompt 膨胀甚至爆窗 | 加 **prompt 预算守卫**：拼接 system+记忆+OCR 时按字符/估算 token 截断并标注 | 🔴高 |
| **claw-code**：压缩式记忆（token>阈值自动把旧消息摘要成一条 System 消息，保留最近 N） | Phase 6 `compactMemory` 已用非流式 `chatOnce` 摘要最早 4 轮 | **已基本对齐**；可加"二次压缩合并历史摘要"让长会话更稳 | 🟢已有 |
| **claw-code**：SSE 增量解析（处理 TCP 分片、`[DONE]`、`ping`、`:` 注释） | `aiClient` 按 `\n` 切分，基本可用但脆弱（`data:` 可能跨 chunk 被切断） | 加**健壮 buffer 解析器**，`push(chunk)` 累积到完整 `data:` 再解析 | 🔴高 |
| **claw-code**：`ProviderClient` 别名解析 + base_url 路由（如 `opus`→`claude-opus-4`、DashScope 特殊路由） | `aiClient` 直接拼 url，无别名/路由 | 加**模型别名 + 供应商自动识别**（用户填 `claude-3.5` 自动路由 anthropic） | 🟡中 |
| **claw-code**：`send_with_retry` 指数退避 1s→128s / 最多 8 次 + 429 分类 | **无重试**，一次网络抖动就断、直接报错 | 加**流式重试 + 指数退避**；429 时提示"X 秒后重试" | 🔴高 |
| **claw-code / openclaw**：Agent loop（`tool_use`↔`tool_result` 循环 + hooks + 权限） | **无 tool calling** | **🔥 最大卖点机会**：让 AI 直接**操作截图**（见 §5-A） | 🔴高 |
| **openclaw**：记忆 `search(score)`→`get(range)` 两段式 + embeddings + citations | Phase 9 启发式 `tokenize`（CJK 二元组）/`relevanceScore` | 可升级为**语义检索**（本地 embedding 模型）或至少更鲁棒的语义打分 + 引用标注 | 🟡中 |
| **openclaw**：`sessionKey`+`lane` 隔离（每文档线程互不污染） | `convHash` 按截图 hash 分桶（等价设计） | **已对齐** | 🟢已有 |
| **openclaw**：流式 `TransportContentBlock` 联合类型 + 80ms 节流 + 围栏安全分块 | 直接渲染 `output` 字符串 | 加**节流防抖**（长输出渐进可见、不被截断在代码围栏中间） | 🟡中 |
| **openclaw**：`AbortSignal` 取消 / `FallbackSummaryError`（带 attempts+冷却到期） | `AbortController` 已接 fetch（`aiStore:568/706/740`），`stop()` 可用 | **取消已扎实**；缺的是"限流时给结构化重试 UI" | 🟢已有取消 |

> 结论：我们 Phase 6/9/取消 已对齐顶级项目的记忆压缩、记忆隔离、取消能力；**真正的短板是"流式可靠性"（重试/buffer/限流）与"AI 从聊天进化为操作截图的 Agent"**。

---

## 5. 新需求发掘：让 AI 成为真正的卖点

### A. 🔥 AI Agent 化——直接"操作截图"（最大差异化）
竞品（含 privdoc-ai）的 AI 只"产出文字"，而截图工具的独有价值是**图即画布**。给 AI 一组"工具"，让它从"说"变成"做"：
- **AI 自动标注**：识别图中按钮/标题/价格/报错，自动加框+箭头+文字（如"把『立即购买』圈出来"）
- **AI 智能打码**："把右上角头像和手机号打码" → 自动模糊/遮挡（隐私刚需，且先于人工）
- **AI 区域圈选**："圈出这段报错""框住这个图表" → 自动生成标注区域
- **AI 视觉编辑**："把图里中文翻译成英文叠加""把这张表格重排成横向"
- 实现：在 `aiClient` 之上加一层 **tool-calling 循环**（`tool_use`→执行 Rust/前端标注命令→`tool_result`回灌→续生成），对齐 claw-code 的 `run_turn` agent loop。这是把"截图工具"升维成"AI 视觉协作助手"的关键一跃。

### B. AI × 现有功能的化学反应
- **AI + OCR**：截图→结构化抽取（表格/表单/名片）→ 直接落 `xlsx`/可编辑 JSON（Phase 2c/7 已铺路，可加"一键结构化"预设）
- **AI + 历史截图**：跨多次截图生成"时间线文档"（如 bug 复现步骤、需求演进），把 `history.rs` 的截图当作多源素材
- **AI + 多屏/长截图**：自动拼接分屏截图再成稿
- **AI 批量工厂**：对 N 张截图「逐张生成 + 自动汇总总览」（当前是合并成一篇，加"逐张+总览"模式）
- **AI 实时划词翻译**：截图后框选文字 → 视觉+翻译叠加层
- **AI 隐私哨兵**：自动检测身份证/手机号/金额并预打码

### C. 文档生成润色（让"导出"更像"成品"）
- **副标题/品牌色**：接入 `markdownHtml.subtitle` 死代码，封面可填副标题+主色（premium 感）
- **更多专用版式**：简历 / 周报 / 白皮书 / 产品手册 专用模板（Phase 12 主题是通用风格，可加领域版式）
- **docx 套主题封面**：当前 docx 无主题，与 html/pdf 观感割裂
- **自动目录(TOC)** + 长文档页码
- **中英混排 typography**：CJK 标点挤压、行距、字号阶梯优化
- **导出前页面尺寸/边距选择**（A4/Letter），所见即所得更彻底

### D. 会话/历史深化
- **后端落库 AI 历史**（已知候选②，需 Rust）：跨设备/备份不丢；顺带做"跨截图文档库"语义搜索（升级 Phase 11 的字符串搜索为嵌入检索）
- **会话分支(fork)**：同一截图的不同成稿方向互不覆盖
- **项目级长期记忆**：多次截图的成稿沉淀为"项目知识库"，写新文档时自动引用（openclaw citations 模式）

---

## 6. 页面易用性继续优化（清单）
- ✅ 聊天区 `<!--SNAP:k-->` 标记已隐藏（本轮修复）
- ✅ 历史阅读器 docx/pdf 导出已补齐（本轮修复）
- 🟡 **首次使用引导(onboarding)**：API Key/模型选择入口过深，建议首次打开 AI 面板给"三步配 Key + 示例 prompt"引导
- 🟡 **入口更浅**：截图完成后能否一键"AI 生成文档"而不必先进编辑器再点 ✨
- 🟡 **空状态引导**：AI 面板首次打开给 3 个示例目标（写缺陷单 / 翻译 / 提取表格）
- 🟡 **流式"停止"按钮**更醒目；生成中禁用其他操作防误触
- 🟢 主面板样式 chips + 预览按钮已位于滚动容器内，不溢出（Phase 12 已处理）

---

## 7. 优先级路线图（建议 Phase 13+）

| 阶段 | 内容 | 价值 | 风险 | 依赖 |
|---|---|---|---|---|
| **Phase 13** | **流式可靠性**：健壮 SSE buffer 解析 + 指数退避重试 + 429 友好提示 + prompt 预算截断 | 🔴 高（稳） | 低（纯前端） | 无 |
| **Phase 14** | **AI Agent 工具层**：自动标注 / 智能打码 / 区域圈选（tool-calling loop） | 🔥🔴 最高（卖点） | 中（需设计标注命令契约） | Phase 13 |
| **Phase 15** | **记忆升级**：语义检索(本地 embedding) + 项目级长期记忆 + 引用标注 | 🔴 高（智） | 中（本地模型/算力） | 无 |
| **Phase 16** | **文档润色**：副标题/品牌色 + docx 主题封面 + TOC + 中英 typography + 页面尺寸 | 🟡 中（成品感） | 低 | 无 |
| **Phase 17** | **后端落库 AI 历史 + 语义搜索 + 会话 fork**（需 Rust） | 🟡 中（持久化） | 中（Rust） | 无 |
| **Phase 18** | **AI 批量工厂 + 实时划词翻译 + 隐私哨兵** | 🟡 中（场景） | 中 | Phase 14 |

> 建议：Phase 13 立即做（低成本高收益、零架构风险）；**Phase 14 是"AI 成为卖点"的胜负手**，建议在动手前先确认工具契约（AI 可调用的标注/打码/圈选命令清单与参数），按项目约定"增量增强、不推翻重构"。

---

## 8. 本轮交付物
- 修复：`src/features/ai/aiMarkdown.tsx`（聊天隐藏 SNAP 标记）
- 修复：`src/features/ai/AIPanel.tsx`（`handleHistoryExport` 扩 docx/pdf + 修正成功路径提示 + 历史导出按钮补齐）
- 验证：`pnpm build` exit 0，136 模块，零类型错误，无回归
- 本报告（复盘 + 对照顶级项目的能力映射 + 卖点挖掘 + Phase 13+ 路线图）
