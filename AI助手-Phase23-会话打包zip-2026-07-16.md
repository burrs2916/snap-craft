# AI 助手 Phase 23 — P1 会话打包 zip（便携归档）

> 接 Phase 22（会话 fork），继续推进路线图 P1。把 AI 会话导出成**零依赖、跨平台可打开**的便携 zip 归档包，与 History 阅读器的 6 格式导出天然衔接。

## 背景
B 端用户常常需要把「AI 成稿 + 来源截图 + 原始对话」一起交付给同事 / 客户 / 审计方。单文件（docx/pdf/md）无法同时携带来源截图证据与可重放的对话；一个**自包含 zip** 是最通用的交付单元——双击即可在 macOS/Windows/7z 打开，无需任何专用软件。

## 决策：零依赖手写 ZIP vs 引入库
- 直接引入 `jszip` 类库：体积大（~90KB gzip）、且会话归档**本就不需要压缩**（PNG 已压缩、md/json 很小）
- **采用 store 模式 ZIP（method=0，不压缩）+ 手写 CRC32**：~120 行纯 TS，无任何依赖，生成标准 zip，系统 `unzip` 校验通过

## 实施（2 文件新增 + 1 文件改 + 2 i18n 键，零 Rust）
1. **`src/features/ai/zipStore.ts`（新增）**
   - `crc32(buf)` —— 标准 IEEE CRC32（预计算 256 项表）
   - `buildZip(files)` —— 标准 store 模式 ZIP：本地文件头 + 中央目录 + EOCD，全部小端 `DataView` 写入
   - 文件名用 **UTF-8 标志位（bit11）** 声明，中文文件名不乱码
   - `dataUrlToBytes(dataUrl)` —— base64 dataURL → Uint8Array（把缩略图塞进 zip）
2. **`src/features/ai/AIPanel.tsx`（改）**
   - 新增 `handleHistoryZip()`：构建 4 个成员 → `conversation.md`（去标记成稿）、`conversation.json`（原始对话，可重放/可审计）、`source.png`（来源截图缩略图）、`README.txt`（预设/首轮目标/消息数/更新时间/分支来源）→ `buildZip` → `save_binary_file`
   - History 阅读器导出按钮行追加「📦 .zip」按钮（`exportZipTitle` tooltip）
3. **`locales`（`zh-CN.json` / `en-US.json`）** —— `exportZipTitle`（打包会话为 zip 归档说明）
4. **`scripts/test-zip.mjs`（新增 sanity）** —— CRC32 标准向量 + 生成真实 zip 交系统 `unzip -t` 校验

## 打包内容结构
```
snapcraft-ai-<ts>.zip
├── conversation.md   # 末轮 AI 成稿（去 <!--SNAP:k--> 标记）
├── conversation.json # 完整对话线程（role/content），可重放 / 合规审计
├── source.png        # 来源截图缩略图（若有）
└── README.txt        # 会话元信息（预设 / 首轮目标 / 消息数 / 更新时间 / 分支来源）
```

## 验证
- `tsc --noEmit` → **0 错**
- `pnpm build` → **exit 0（1.96s，139 模块）**
- `test-tool-call-parser.mjs` → **50/50 绿**（Phase 16 无回归）
- `test-zip.mjs` → **7/7 绿**
  - CRC32("123456789") === 0xCBF43926 ✅
  - 系统 `unzip -l` / `unzip -t` → **No errors detected**，两文件 OK ✅

## 价值
- B 端「交付 / 审计 / 分享」一键闭环：成稿 + 证据图 + 原始对话同包带走
- 与 Phase 22 fork 联动：`README.txt` 自动记录 `分支自: <parent>`，分支会话也能追溯源头
- 零依赖、零 Rust、零运行时成本

## 路线图进度
- ✅ P0-α 操作可视化（Phase 20）/ ✅ P0-β 隐私哨兵（Phase 21）/ ✅ P1 会话 fork（Phase 22）/ ✅ P1 会话打包 zip（Phase 23）
- ⬜ P0-γ 后端代理隐藏 Key（需 Rust）
- ⬜ P1 本地语义记忆 / docx 高级排版
- ⬜ P2 选区划词 / 批量 Agent 队列 / 工具集扩充(arrow/callout/crop) / PPT 导出
