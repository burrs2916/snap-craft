# AI 助手 Phase 9 — 记忆相关性检索（对齐 privdoc-ai searchMemories）

> 自动化轮次：2026-07-15｜参考 `privdoc-ai` 的 `searchMemories` 能力
> 原则：纯前端增量、零 Rust 改动、不影响截图 / 标注 / OCR / 历史 / 导出

## 一、需求发掘（本轮起点）

复盘 Phase 1–8 后，AI 助手已具备完整闭环：编辑截图 → 多模态理解 → 多轮迭代成稿 →
记忆压缩 → 导出完整文档（.md/.txt/.html/.docx/.pdf/.xlsx）+ 文案回写截图。

唯一遗留的质量断点：**Phase 6 的记忆是「全量注入」**——每轮对话都把该截图上下文下的
*全部* 长期记忆作为 system 消息灌入。当用户围绕一张截图做了很多轮、积累了数条记忆后：

1. 早期记忆与「当前这轮追问」可能无关，稀释注意力；
2. 记忆越多越挤占上下文窗口，反而逼近溢出阈值；
3. 与 privdoc-ai 的招牌能力 `searchMemories`（按语义相关性检索记忆）相比存在差距。

结论：补上「**按当前追问语义，只注入最相关的记忆**」，让截图工具的
「截图即文档上下文 + 长迭代」模式在记忆膨胀时依旧稳健。

## 二、方案设计（低风险、启发式、零额外 API 调用）

不引入向量库 / embedding（需后端、超当前架构边界），改用**轻量 token 重叠启发式**：

- `tokenize(text)`：切出检索 token = 拉丁/数字词（小写） + CJK 单字 + **CJK 二元组**
  （中文无空格，二元组显著提升短语匹配召回）。
- `relevanceScore(input, summary)`：用户输入 token 在记忆摘要中的**命中率**（0~1）。
- `selectMemories(input, memories)`：
  - 记忆不多（≤5 条）→ 全量注入，**行为与原版完全一致（零回归）**；
  - 否则：重要性 ≥ 4 的记忆**恒注入**（关键事实不丢，防措辞不同而漏掉）；
    其余按相关性降序补满 5 条配额；返回按 `createdAt` 升序，保持时间线可读。

安全性网：高重要性记忆永远注入，避免「相关性低但很关键」的记忆被误筛掉。

## 三、改动清单（纯前端）

| 文件 | 改动 |
| --- | --- |
| `src/features/ai/aiTypes.ts` | `AiMemory` 新增 `id?`（UI 高亮锚点） |
| `src/features/ai/aiStore.ts` | 新增 `tokenize` / `relevanceScore` / `selectMemories` + 常量 `MAX_RELEVANT=5` / `ALWAYS_IMPORTANCE=4`；`chat()` 每轮按当前输入筛选注入；新增状态 `activeMemoryIds`；`loadMemories` 补合成 id；`compactMemory` 赋值 `genMemId()`；`setConvKey` / `clearConversation` 重置 |
| `src/features/ai/AIPanel.tsx` | 记忆区「相关」徽标高亮 `activeMemoryIds` 命中项；过滤生效时显示「本次注入 k 条相关」提示 |
| `src/locales/zh-CN.json` / `en-US.json` | `ai.memActive` / `ai.memRel` |
| `src/index.css` | `.ai-mem-hint` / `.ai-mem-item.active` / `.ai-mem-rel` |

## 四、与 privdoc-ai 的差异

- privdoc-ai：`searchMemories` 由 Rust + SQLite 支撑，可对记忆做语义检索；
- 本方案：前端 `localStorage` 按截图哈希分桶（更轻、零后端风险），截图哈希即记忆锚点；
  用启发式重叠代替 embedding，**零额外网络请求、零密钥风险**，在「长文档迭代」场景下
  足够有效，且完全不改 Rust / 不影响现有功能。

## 五、验证

- `pnpm build` → exit 0（136 模块，零类型错误，无回归）；
- `dist` 含新 token：`activeMemoryIds` / `memActive` / `memRel` / `ai-mem-rel` / `ai-mem-hint`；
- 行为兜底：记忆 ≤ 5 条时与原版一致（全量注入）；> 5 条时才触发筛选。

## 六、后续候选（未做，按优先级）

1. 跨截图会话列表（浏览 / 恢复其它截图的对话线程）
2. 后端代理隐藏 Key（改 Rust + 服务端，需用户确认）
3. 拖拽重排多截图选择顺序（报告章节顺序控制）
4. OCR 文字改取编辑后图（refresh 时重跑 OCR，使 AI 读到打码后文字）

> 生效方式：`./start.sh dev` 或 `pnpm build && ./start.sh app`（内嵌 dist）重启后生效。
