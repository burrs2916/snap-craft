# AI 助手 · 整体复盘 × 真值核查 × 易用性修复 × 增强蓝图

> 自动化轮次：2026-07-15 14:33（Phase 1–12 + 多轮复盘之后）
> 本轮目标：在「已确认无空/假实现」的基础上，**再真读当前代码找真实 bug / 易用性断点**，并对照用户指定的顶级项目（`claw-code-main` / `openclaw-main`）给出能真正让 AI 成为卖点的下一步蓝图。
> 严守原则：**增量、不推翻重构、纯前端零 Rust 改动、零回归**。

---

## 一、本轮真值核查（直接读码，非凭记忆）

| 核查项 | 方法 | 结论 |
|---|---|---|
| 空/假实现 | 通读 `AIPanel.tsx`(1324行) + `aiStore.ts`(823行) | **无**。10 类功能（生成/润色/多截图/报告/回写/历史/记忆/导出6格式/主题/预览）全部可从 UI 点击到达真实后端或输出 |
| 前后端联动 | 核对 18 个 `invoke` 与 Rust `#[tauri::command]` | 18/18 对齐，无参数错位（此前轮次已确认，本轮复查仍成立） |
| 语言感知是否失效 | 读 `i18n/index.ts:98-100` `getLang()` 返回 `'zh-CN'` 字面量 | **此前怀疑的 `getLang()==='zh-CN'` 全量误判 = 误报**，实际正确，中文下 system/润色/记忆/压缩指令均走中文分支 |
| 编辑后截图联动 | grep 两个编辑器 `visionImageUrl`/`refreshAiVision`/`onApplyToScreenshot`/`onRefreshImage` | Phase 8/10/3 **真实挂载**：`EnhancedScreenshotApp:2474-2479` 与 `EditorWindow:1465-1470` 均已接线 |
| 构建基座 | `pnpm build`（tsc + vite） | 136 模块，exit0，零类型错误 |

**结论：系统整体健康，无假实现、无前后端断联。** 真正可挖的价值在「易用性断点」「文档润色不一致」「AI 底层能力对标顶级项目的差距」。

---

## 二、本轮实修（2 项，纯前端 / 零 Rust / 构建 exit0）

### 🟢 修复 1 —「新对话」无声删除已保存文档（真实数据丢失 bug）
- **根因**：`aiStore.clearConversation()` 原会 `saveConversation(convKey,[])` + `removeConvMeta(convKey)`。它绑定在「🗨 新对话」按钮上，**点一下就把该截图的归档成稿从历史库抹掉**，且把落盘线程清空 → History 阅读器再点进去是空会话。
- **修复**：`clearConversation` 现在**只重置面板工作态**（conversation/memories/output/status），**不再触碰落盘线程与历史索引**。归档文档由 History 里的「🗑 删除」显式清理，语义一致。
- **影响**：用户「新对话」= 起新草稿；已生成的文档安全留在 History 库，可随时回看/续写。

### 🟢 修复 2 — 多截图「图文报告」支持拖拽 / 箭头重排章节顺序（易用性 + 卖点增强）
- **断点**：Phase 5 已支持多选截图成报告，但顺序只能「勾选 / 取消」，**无法调整章节先后**——核心卖点「按截图顺序图文混排」的编排权没交给用户。
- **实现**：
  - `AIPanel.tsx`：新增 `moveSel(id,±1)` / `removeSel(id)` / `onDropAt(toIdx)` + `dragIdx` 拖拽态；选中区下方渲染**有序列表**（序号徽标 ①②③ + 缩略图 + ↑↓ 箭头 + × 移除），支持 HTML5 拖拽与箭头双通道。
  - `zh-CN.json` / `en-US.json`：补 `orderUp/orderDown/orderRemove/orderDrag` 4 键（中英）。
  - `index.css`：补 `.ai-attach-order*` 11 类（复用 `--accent`/`--border`/`--text`，明暗自适应）。
- **验证**：`pnpm build` exit0（136 模块）；dist 含全部 `.ai-attach-order*` 与 `order*` locale 键。
- **价值**：用户可自由编排报告叙事顺序（如「先问题截图、后数据截图、再结论截图」），直接强化「截图即章节、图文咬合」这一竞品没有的差异化能力。

---

## 三、真实剩余 gap / bug（诚实清单，按优先级）

| # | 类别 | 问题 | 严重度 | 备注 |
|---|---|---|---|---|
| 1 | 文档润色不一致 | **docx 导出无视主题**：Phase 12 给 HTML/PDF 做了 5 套精美主题（封面 hero + 章节徽标 + 图文卡片 + TOC），但 `markdownDocx.ts` 的 `MarkdownToDocxOptions` 只有 `title/images/sectionImages`，**无 `theme`/`subtitle`/封面**。用户选「产品推广」→预览惊艳→导出 .docx 得到朴素黑字。 | 中 | 与「文档生成继续润色」诉求直接相撞 |
| 2 | AI 底层·记忆 | 记忆检索仍是**词法层**（CJK 单字+二元组 + Jaccard MMR），无语义 embedding、无时间衰减*循环*（衰减只在查询时算，记忆永不 consolidation/过期）。openclaw 有 conceptual + hybrid + 半衰期 consolidation。 | 中 | 长对话质量天花板 |
| 3 | AI 底层·Agent | **无 tool-calling 循环**——最大卖点缺口（见第四节蓝图）。 | 高 | 需先与用户确认「模型可调用的标注工具契约」 |
| 4 | 安全 | API Key 存 localStorage、由渲染层直发；无后端代理隐藏 Key。 | 中 | 需 Rust 代理 |
| 5 | 易用性 | 无「选区 / 划词 AI」（选中结果里一段文字→快捷翻译/解释/改写）。 | 低-中 | 纯前端可做的即时增强 |
| 6 | 文档 | History 阅读器导出 docx/pdf **不含来源截图**（线程不存像素，设计如此）。 | 低 | 已接受的限制，文档注明即可 |
| 7 | 易用性 | 多选顺序（`selectedOrder`）是面板态，**关闭面板重开即丢失**；而对话线程按截图哈希持久化。 | 低 | 可顺手把 selectedOrder 也落盘 |

> 说明：第 1、7 项本轮未做（避免单次改动过大），但已列出可立即执行的最小方案；第 3 项需用户拍板工具契约，不擅自动手。

---

## 四、对照顶级项目的「卖点蓝图」（用户指定路径真读）

### 4.1 `claw-code-main` —— 工具循环（Agentic loop）
- `runtime.py:154` `run_turn_loop(self, prompt, limit=5, max_turns=3, structured_output)`：把「模型输出 → 解析 tool_call → 执行工具 → 结果回填 → 再喂模型」做成**迭代闭环**，直到命中真实终止条件或达到 `max_turns`。
- 含义：AI 不是「一次问答」，而是「**能动手操作的代理**」。

### 4.2 `openclaw-main` —— 生产级 Agent 栈
- `config/types.agent-defaults.ts:294` `strict-agentic` 执行契约：让模型**持续行动直到撞到真实阻塞**，而不是停在「计划 / 废话」。
- `shared/text/tool-call-shaped-text.ts`：把 JSON / XML / bracketed / react-action **多种 tool_call 形态统一识别**（稳健解析）。
- `detectToolCallLoop`（`tool-loop-detection.ts`）：用哈希检测**工具调用失控循环**并打断——防止 AI 死循环烧 token。
- **Pre-compaction memory flush**：重度压缩前先跑一轮 agentic 记忆写入，把关键上下文固化（见 `schema.base.generated.ts:5073` 注释）。

### 4.3 把「卖点」翻译成 SnapCraft 能落地的形态
**🎯 胜负手 = AI Agent 化（让模型直接操作截图）**。竞品（含 privdoc-ai）只产出**文字**；而我们是**截图工具**——若让模型能调用标注工具，就能产出「带视觉证据的智能成稿」：

| 工具契约（草案，待用户确认） | 模型能做什么 | 对应卖点 |
|---|---|---|
| `annotate_rect(x,y,w,h,text)` | 在截图指定区域加文字标注 | 自动给截图配说明 |
| `highlight_text(rect)` | 高亮图中某段文字 | 自动突出关键数据 |
| `redact_area(rect)` | **打码/模糊**指定区域 | 🔒 隐私哨兵：让 AI 自动脱敏敏感信息 |
| `read_region_ocr(rect)` | 只 OCR 图中某块 | 精准取数，不入全图上下文 |
| `summarize_selection(rect)` | 对某区域生成讲解 | 截图即教程 |

→ 用户说「把这页做成带标注的产品说明文档」，模型**自己圈重点、打码隐私、写说明**，最后 `回写截图`（Phase 3 已具备通道）。这是 privdoc-ai / 通用聊天 AI **根本做不到**的闭环。

---

## 五、下一步路线图（按优先级，标注是否需确认 / 需 Rust）

| 优先级 | 项 | 类型 | 依赖 |
|---|---|---|---|
| 🔴 P0 | AI Agent 化（4.3 工具循环） | 纯前端 + 模型 tool_call 解析 | **需用户确认工具契约**（annotate_rect / redact_area 等） |
| 🟠 P1 | docx 套主题封面（修复 #1 不一致） | 纯前端 | 无，可直接做 |
| 🟠 P1 | 后端代理隐藏 Key | **需 Rust** | 新增 Rust 命令 + 配置 |
| 🟡 P2 | 语义记忆（本地 embedding + consolidation） | 纯前端 | 引入轻量 embedding（如 transformers.js / 本地模型） |
| 🟡 P2 | 选区 / 划词 AI（#5） | 纯前端 | 无 |
| 🟢 P3 | selectedOrder 落盘（#7） | 纯前端 | 无 |
| 🟢 P3 | History docx/pdf 携带缩略图（#6） | 纯前端 | 索引已存 `thumb`，导出时复用 |

---

## 六、本轮交付物
- `src/features/ai/AIPanel.tsx`：多截图有序列表（拖拽 + 箭头重排 + 移除）
- `src/features/ai/aiStore.ts`：修复「新对话」数据丢失 footgun
- `src/index.css`：`.ai-attach-order*` 11 类
- `src/locales/zh-CN.json` / `en-US.json`：`orderUp/orderDown/orderRemove/orderDrag`
- 验证：`pnpm build` exit0（tsc + vite，136 模块，零类型错误，零回归）

> 重启 App（`./start.sh dev` 或 `pnpm build && ./start.sh app`）即可见：多选截图后，附加区出现「①②③ 可拖拽/箭头调整」的有序列表；「🗨 新对话」不再删历史文档。
