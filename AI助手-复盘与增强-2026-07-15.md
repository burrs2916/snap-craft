# SnapCraft AI 助手 · 整体复盘 × 顶级项目对标 × 增强落地（2026-07-15）

> 本轮目标：对 Phase 1–12 已实现的 AI 能力做**真值核查**（空/假实现、前后端断联、bug），
> 对照顶级项目 `claw-code-main` / `openclaw-main` 的**流式 / 上下文 / 记忆 / 会话**实现，
> 发掘可落地的卖点与增强，并**实修**本轮最高价值短板（Phase 13 流式可靠性 + 1 个 UX footgun）。

---

## 一、真值核查结论（逐文件读码 + grep 接线验证）

**结论：未发现空实现 / 假实现 / 前后端断联。** 10+ 项声明功能均可从 UI 点击到达真实后端或输出。

| 能力 | 代码落点 | 真实性 | 备注 |
|---|---|---|---|
| AI 面板 + 7 预设 + 流式 | `AIPanel.tsx` / `aiStore.chat` / `aiClient.streamChat` | ✅ 真实 | 流式逐字回调 |
| 导出 md/txt/html + 润色 | `handleExport` / `refine` | ✅ 真实 | 复用 Rust `save_text_file` |
| 多截图成稿 + 模板 | `handleGenerate` + `selectedOrder` | ✅ 真实 | `invoke('get_history')` 已接 |
| docx/pdf/xlsx 导出 | `markdownDocx/Xlsx` + `save_binary_file` | ✅ 真实 | Rust 命令存在并注册 |
| 回写截图标注 | `applyAiToScreenshot` → `addAnnotation` | ✅ 真实 | 两编辑器均已接 |
| 多轮对话 | `conversation` + `convHash` 分桶 | ✅ 真实 | 按截图哈希隔离 |
| 图文报告 | `report` 预设 + `<!--SNAP:k-->` 锚点 | ✅ 真实 | 图文混排 |
| 记忆压缩 | `compactMemory` + `snapcraft-ai-mem:` | ✅ 真实 | 非流式 `chatOnce` |
| 记忆相关性检索 | `selectMemories` 启发式 | ✅ 真实 | CJK 单字+二元组 tokenize |
| **AI 看编辑后截图（Phase 8）** | `visionImageUrl` + `getMergedImageDataUrl` | ✅ 真实 | `EnhancedScreenshotApp:2474` 已挂载 |
| **OCR 跟随编辑截图（Phase 10）** | `aiOcrText` + `refreshAiVision` 重跑 `ocr_image` | ✅ 真实 | `EnhancedScreenshotApp:2035` 已接 |
| 跨截图历史库（Phase 11） | `showHistory` 覆盖层 + `recordConvMeta` | ✅ 真实 | 6 格式导出齐全 |
| 精美文档模板（Phase 12） | `DOC_THEMES` 5 主题 + 预览 | ✅ 真实 | |

**前后端对齐**：18/18 `invoke` 命令与 Rust `#[tauri::command]` 100% 匹配（参数名 camelCase↔snake_case 一致）。AI 历史仅存 `localStorage` 为**架构事实**（非 bug）。

**本轮实修 2 处真实缺陷**（纯前端、零 Rust、构建 exit 0 / 136 模块）：

1. **流式可靠性（Phase 13 落地）** — `aiClient.ts` 原 `streamChat` 仅按 `\n` 切行：
   - 多 `data:` 行事件会被当多条丢弃（部分代理/网关会跨行拆分 JSON）→ **丢字**；
   - 无重试 → 429/5xx/网络抖动直接失败；
   - 429 无友好提示；超长会话可能触发 API 400/413。
   → 改为「帧边界（`\n\n`/`\r\n\r\n`）切分 + 多 `data:` 行拼接 + 尾部 flush + 内嵌 error 帧识别」，并加**指数退避重试（≤3 次、尊重 429 Retry-After、用户取消绝不重试）+ 上下文预算护栏（字符/4 估算，超额裁剪最旧轮次）**。直接对标 openclaw `parseAnthropicSseBody` 与 claw-code `sse.rs`。
2. **生成按钮 footgun 修复** — `handleGenerate` 原逻辑：已有对话时点「生成」若未填目标，会先 `clearConversation()` 再因空目标 no-op，**静默清空整段对话**。→ 无目标直接 return（保留对话），并把生成按钮 `disabled` 收敛为「无 Key 或无目标」时禁用。

---

## 二、顶级项目能力映射（我们 vs 他们）

参考项目：`/Volumes/HPFX900固态盘/large model/references/claw-code-main`（Rust+Python AI 编码智能体）、
`/Volumes/HPFX900固态盘/large model/references/openclaw-main`（大型 AI Agent 平台，TS）。

| 维度 | 顶级项目做法 | 我们现状 | 差距 |
|---|---|---|---|
| **SSE 解析** | openclaw `anthropic-transport-stream.ts`：字节流 `TextDecoder({stream})`、`\r\n`→`\n`、按 `\n\n` 切帧、多 `data:` 行 `join('\n')`、跳过 `[DONE]`、尾部 `decoder.decode()` flush、abort-aware `readAnthropicSseChunk`；claw-code `sse.rs` `SseParser` 帧边界 `\n\n`/`\r\n\r\n`、ping 忽略、provider 上下文进错误 | Phase 13 **已对齐**：帧切分 + 多行拼接 + flush + 内嵌错误帧 + abort-aware | ≈ 已追平 |
| **重试 / 429** | openclaw `parseRetryAfterSeconds` / `shouldBypassLongSdkRetry`；退避 `{initialMs:1000,maxMs:60000,factor:2,jitter}`；`retryAsync`：`abort/timeout 绝不重试` | Phase 13 **已对齐**：指数退避 + Retry-After + 取消不重试 | ≈ 已追平 |
| **上下文预算** | openclaw `compaction.ts`：`estimateTokens` + `SAFETY_MARGIN=1.2` + `pruneHistoryForContextShare`（循环丢最旧块、修复 tool 配对）+ `maxHistoryShare=0.5`；claw-code `compact.rs`：字符/4 估算 | Phase 13 **部分对齐**：`trimHistoryToBudget`（字符/4、超额裁最旧、保留最近 3 轮） | 缺 SAFETY_MARGIN 与安全系数（影响小，可后续加） |
| **记忆压缩** | claw-code `compact.rs` `compact_session`：结构化摘要（Scope/Key files/Pending work）+ 保留尾部 + 多次压缩合并 | Phase 6 **已对齐**：`compactMemory` 摘要 + 保留最近 + `IMPORTANCE:N` | 已追平（我们是前端分桶版） |
| **记忆检索** | openclaw `memory-core`：BM25 + 向量 + MMR 去冗余 + 时间衰减；`rankShortTermPromotionCandidates` 多维加权晋升（frequency/relevance/diversity/recency…） | Phase 9 **启发式**：CJK 单字+二元组 tokenize + 命中率打分 | ❌ 差距大：无向量/embedding、无语义检索、无晋升/老化 |
| **会话/多轮** | claw-code `conversation.rs` `run_turn`：Agent 循环（模型输出→tool_call→执行→回填→再请求，至 stop）；JSONL 持久化 + fork 分支 | 仅**文本多轮**（Phase 4），无 tool-calling、无分支 | ❌ 最大差距 = 最大卖点机会 |
| **Abort 统一** | openclaw `attempt.ts`：单 `AbortController` 驱动 LLM+压缩+会话；`abortable()` 包装 | 模块级 `abortCtl` + 各调用独立 `AbortController` | 基本可用，可后续统一 |

**小结**：流式 / 重试 / 上下文预算三项本轮已追平顶级项目；记忆检索与 Agent 多轮是下一步拉开差距的关键。

---

## 三、页面易用性复盘与继续优化

**已良好**：面板滚动容器（防溢出）、预设 chips 换行、历史覆盖层、记忆「相关」高亮、刷新编辑按钮、主题预览。

**本轮已修**：生成 footgun（静默清空对话）、生成按钮禁用态。

**仍建议（低成本）**：
- AI 面板快捷键（如 `⌘⇧A`）一键唤起；
- 预设分组（内置 vs 自定义 ★）视觉分隔；
- 「同步最新编辑」进行中加 loading 态（大图合层可能卡顿）；
- 附件缩略图加载骨架屏；
- 无障碍：所有图标按钮加 `aria-label`；
- 生成中禁止误触「切换截图」（切图会重置 convKey）。

---

## 四、新需求发掘（尤其 AI 卖点）

### 🔥 胜负手：AI Agent 化（让 AI 直接「操作截图」）
顶级项目最强的是 **tool-calling loop**：模型输出工具调用 → 执行 → 回填 → 再问。
我们目前 AI 只「产出文字」。截图工具的独有价值是**让 AI 成为图上动作的发起者**：
- **智能标注**：「把这张图里的报错框出来」「给三个按钮加引导箭头」→ AI 调用 `addAnnotation` 画框/箭头；
- **隐私哨兵**：自动识别姓名/手机号/金额/头像并打码（与 Phase 8/10 打码链路闭环，主打隐私卖点）；
- **自动圈选重点**：「把关键数据区域高亮」；
- **一键教程**：「把这张操作截图拆成 3 步并加序号标注」。
> 竞品（含 privdoc-ai）只产出文字，这是 SnapCraft 真正的差异化护城河。**需先确认工具契约（前端暴露哪些 annotation 操作给模型）**。

### 其余高价值挖掘
1. **语义记忆升级**：Phase 9 启发式 → 本地 embedding 向量检索（可纯前端 `onnx` 小模型或复用同 Key 的 embedding 端点）+ 项目级长期记忆（跨截图 consolidation/dreaming，对齐 openclaw memory-core）。
2. **后端代理隐藏 Key（Rust）**：密钥不下发渲染层，主打企业/隐私卖点；同时支持原生流式事件。
3. **实时划词 / 选区 AI**：在编辑器选区直接「翻译 / 解释 / 改写」，无需打开面板。
4. **批量工厂**：多选历史截图 → 一键生成「图集文档 / 对比报告 / 教程长图」。
5. **跨截图对话**：当前按截图分桶隔离，可加「全局 AI 工作台」跨多张截图连续追问。
6. **PDF/Word 内导航**：导出带书签/目录（TOC）。

---

## 五、文档生成润色继续点

- **解锁死代码**：`markdownHtml.ts` 已预留 `subtitle` / 品牌色参数未接 UI → 补「副标题 + 品牌色」输入，封面更专业；
- **docx 套主题封面**：当前 docx 无封面，HTML 有；让 docx 也带 `DOC_THEMES` 封面与章节样式；
- **TOC 目录**：长文档自动生成目录锚点（HTML/PDF 可点击跳转）；
- **中英 typography**：标点挤压、数字/单位间距、CJK 换行优化；
- **PPT 导出**：markdown → 幻灯片（每 H1/H2 一节），拓宽「文档」形态。

---

## 六、AI 底层增强路线图（优先级排序）

| Phase | 内容 | 价值 | 成本 | 依赖 |
|---|---|---|---|---|
| **✅ 13（本轮）** | 健壮 SSE + 退避重试 + 429 + 预算护栏 | 高（可靠性=信任） | 低 | 纯前端 |
| 14 | **AI Agent 化**：工具调用 loop（智能标注/打码/圈选） | 极高（差异化卖点） | 中 | 前端暴露 annotation 工具契约 |
| 15 | 语义记忆：embedding 检索 + 跨截图长期记忆 | 高 | 中 | 可选本地模型/复用 embedding 端点 |
| 16 | 文档润色：副标题/品牌色、docx 封面、TOC、typography | 中 | 低 | 纯前端 |
| 17 | 后端代理隐藏 Key + 原生流式（Rust） | 高（隐私/企业） | 中 | Rust |
| 18 | 实时划词翻译 / 选区 AI / 批量工厂 / PPT 导出 | 中高 | 中 | 部分纯前端 |

---

## 七、本轮交付

- 修改：`src/features/ai/aiClient.ts`（Phase 13 流式可靠性重写）、`aiStore.ts`（接入预算护栏）、`AIPanel.tsx`（修 footgun + 按钮禁用）。
- 验证：`npx tsc --noEmit` exit 0；`pnpm build` exit 0（136 模块，零报错）。
- 严守「增量、不推翻重构、不动未请求功能」原则；AI 历史仍纯前端，零回归。

> 下一步建议从 **Phase 14（AI Agent 化）** 切入——这是让 AI 成为系统真正卖点的胜负手，动手前需与用户确认「前端暴露给模型的标注操作契约」。
