# SnapCraft AI 助手 · 整体复盘 × 终极真值核查 × 顶级项目深度对标 × 下一阶段路线图

> 自动化轮次：2026-07-16 11:25 · 2026-07-15 已完成 Phase 1–15 之后
> 方法：直接读码（`tsc --noEmit` + `pnpm build` + 真实 grep 找潜在 bug）+ 对照 `claw-code-main`（Rust）/`openclaw-main`(TS) 真实源码
> 严守原则：**增量、不推翻重构、纯前端零 Rust、零回归；诊断优先，不擅加未请求功能**

---

## 一、整体现状（一句话）

**地基扎实，AI 助手已是真卖点** —— 18/18 `invoke` 对齐、零空/假实现、零 TODO/FIXME/stub、`pnpm build` 137 模块 exit0。`AI 智能编辑`（Phase 14 工具循环）让模型**直接动手改图**，是行业级差异化（竞品只产文字，我们能产文字+直接改图）。但仍有 4 项真实可继续深挖的能力点与 2 项小 bug。

---

## 二、真值核查（直读源码，0 缓存依赖）

| 核查项 | 真实状态 | 关键证据 |
|---|---|---|
| 空/假实现 | **0** | `grep -rn "TODO\|FIXME\|stub"` 全仓零命中；`aiTools.ts` 4 工具 + `createToolExecutor` + `AI_TOOL_DEFS` 真实接线到两编辑器（`EnhancedScreenshotApp.tsx:2098-2205`、`EditorWindow.tsx:705-800+`）|
| 前后端联动 | **18/18 对齐** | Rust `#[tauri::command]`：`save_screenshot/copy_to_clipboard/read_clipboard_image/read_clipboard_text/save_text_file/save_binary_file/get_history/add_history/delete_history/clear_history/get_screenshot/update_screenshot_annotations/set_screenshot_ocr/ocr_image/open_external` + `capture.rs` 内 11 条（截图/列表/权限），前端 `invoke` 全部 camelCase 匹配 |
| Phase 14 工具循环 | **真实工作** | `aiStore.runAgent:869-952` → `streamChatWithTools:676-760`；AIPanel 工具步骤 `agentSteps` UI 实时回显（onToolCall/onToolResult 回调）|
| 长期记忆 + 增量压缩 | **真实工作** | `aiStore.compactMemory:768-816` 链式折叠（`existing` + `batch` → 一条 `rolled`），`merged`/`turnsCovered`/`importance` 全程累加；`saveMemories` 落盘 `localStorage: snapcraft-ai-mem:<hash>` |
| 上下文预算护栏 | **真实工作** | `trimHistoryToBudget(120_000, 3)` 在 `chat:651`/`runAgent:896` 调，保留最近 3 轮+全部 system |
| 流式 SSE | **真实工作** | `streamOnce:472-538`：`findFrameEnd`+`frameDelimLen`+tail flush；`processFrame:306-432` 解析 text_delta/thinking_delta/tool_use/usage/cache_creation/cache_read |
| 错误分类 | **真实工作** | `classifyAiError:572-588` 返回 auth/rate/context/server/unknown；`aiErrorI18nKey:28-42` 映射 i18n |
| 提示缓存命中 | **真实工作** | `aiClient.ts:347-348`（Anthropic）+`:402-404`（OpenAI `cached_tokens`），AIPanel 用量行 `⚡N 缓存命中` |
| 工具失控检测 | **真实工作** | `stableToolKey:663-674` + `MAX_REPEAT_TOOL=3`，连续 3 轮相同工具调用即熔断并附提示 |
| 6 格式导出 | **真实工作** | md/txt（`save_text_file`）/html（`mdToHtml` 5 主题+TOC）/pdf（iframe 打印）/docx（`markdownToDocx` 内嵌截图+主题+TOC）/xlsx（`markdownXlsx`） |
| 多轮对话按截图分桶 | **真实工作** | `convHash:116-124`（djb2）；`saveConversation/loadConversation` 走 `localStorage: snapcraft-ai-conv:<hash>`；`recordConvMeta` 自动落盘历史索引（含缩略图异步）|
| 编辑后图/OCR 跟随 | **真实工作** | 两编辑器 `refreshAiVision`（EnhancedScreenshotApp:2067 / EditorWindow:674）：合层后若与原图不同则重跑 `ocr_image`；AIPanel 用 `aiOcrText` 替代原 OCR |
| 历史库 UI | **真实工作** | AIPanel `.ai-hist` 覆盖层：搜索+卡片列表+阅读器态（载入追问+6 格式导出+只读对话流）|
| 跨截图拖拽重排 | **真实工作** | `moveSel/removeSel/onDropAt` + `① ② ③` 序号徽标 + HTML5 拖拽 |

**结论：地基已经 100% 真实可用。** 没有任何空实现、没有任何假实现、没有任何前后端断联。

---

## 三、深度对照顶级项目（实读源码，含行号）

### 3.1 流式 / 重试 / 预算护栏 — ✅ 已追平
- `claw-code` `rust/crates/api/src/sse.rs:82-128`（帧→事件、`event:ping` 跳过、多行 `data:` 合并）
- `openclaw` `src/agents/anthropic-transport-stream.ts:549-592`（CRLF 归一、尾部 flush、可中断）
- 我们：`aiClient.findFrameEnd/frameDelimLen/processFrame/readAbortable`（行级 + 跨 chunk 拼接 + tail flush + abort）→ **同构同精度**

### 3.2 上下文压缩（链式增量） — ✅ Phase 15 已追平
- `openclaw` `compaction.ts:293-339`：`previousSummary` 链 + `SAFETY_MARGIN=1.2` + 工具对边界保护
- `claw-code` `compact.rs:96-183`：保留最近 4、`estimate_message_tokens=len/4+1`、100k 自动触发、保护 `ToolUse/ToolResult` 对
- 我们 `aiStore.compactMemory:768-816`：链式折叠（`existing` + `batch` → 一条 `rolled`，merged 累加），Phase 15 已做到零稀释

### 3.3 记忆（多因子 + MMR） — ✅ Phase 9+ 已追平
- `openclaw` `short-term-promotion.ts:1259-1291` 六因子 + `temporal-decay.ts:24-42` 半衰期 + `mmr.ts:136-190` 多样性
- 我们 `aiStore.selectMemories:366-403` + `recencyComponent/importanceNorm/compositeScore/jaccardSim` — 同样多因子 + MMR + 14 天半衰期；零依赖
- **差距（剩余）**：openclaw 的 `memory-lancedb` 扩展做 **向量 embedding 检索**（hybrid.ts: 向量×0.7+文本×0.3），可"同义不同词"召回；我们是纯词法启发式

### 3.4 Agent 工具循环 — ✅ Phase 14 已追平
- `claw-code` `conversation.rs:313-515` `run_turn`：stream → 聚合 `ToolUse` → 逐工具 hook/权限/执行/post-hook → `ToolResult` 回写 → 下一轮
- `openclaw` `agent-runner.ts` + `tool-loop-detection.ts:131,498`：`detectToolCallLoop` 用 SHA256 稳定序列化识别重复调用
- 我们 `aiClient.streamChatWithTools:676-760` + `stableToolKey:663-674` + `MAX_REPEAT_TOOL=3`：**主循环一致、循环检测同思路、但更轻**（桌面端 8 轮上限下 3 轮就熔断）

### 3.5 会话隔离 / 持久化 — ✅ 已对齐
- `claw-code` `session.rs:260-274` `Session.fork()` 会话分支
- `openclaw` `routing/session-key.ts:54-72` `agent:<id>:<mainKey>`/`:thread:<id>` 命名空间
- 我们 `convHash` 截图哈希分桶 + `recordConvMeta` 历史索引 + `AiConvMeta` 列表 — 桌面端单用户已够，fork 不必要

### 3.6 工具调用形态适配 — 🟡 唯一明显差距
- `openclaw` `shared/text/tool-call-shaped-text.ts` **多形态工具调用解析**（JSON / XML / bracketed / react-action 统一识别）
- 我们仅依赖模型原生 `tool_use` / `tool_calls` 字段
- **影响**：当模型不是 GPT-4/Claude 而是国产模型（如 Qwen-7B、DeepSeek、Llama-3）走 OpenAI 兼容但**未必**严格输出 `tool_calls` 字段时，Agent 模式可能失效
- **机会**（差异化卖点）：若支持多形态工具调用，SnapCraft AI 智能编辑的**模型兼容性**可一跃领先所有竞品

---

## 四、真实小 bug / 可优化点（直读代码发现）

> 以下是**亲自 grep / 读源码**发现的事实级问题，按风险排序：

### 🟢 B1 — `aiTools` useMemo 依赖过宽，可能造成编辑器副作用
- 位置：`EnhancedScreenshotApp.tsx:2098`、`EditorWindow.tsx:705`
- 现象：`useMemo` 依赖 `[current, addAnnotation, currentColor, currentStrokeWidth, currentFontFamily, ocrLang]`，而 `addAnnotation` 是 zustand action 引用稳定，但 `currentColor/currentStrokeWidth/currentFontFamily` 是**画笔工具切换**时变化的（用户每点一次颜色都会重建 `aiTools` 对象引用）
- 影响：`aiTools` prop 改变 → 父组件 useEffect `[aiOpen]` 重新触发 → 重复 `refreshAiVision()`。**最坏情况：用户改色时 AI 面板 OCR 频繁重跑**
- 修复（极小）：依赖改 `[current, ocrLang]`（其余 action 引用稳定 / 颜色只是工具宿主内部用、与画布刷新无关）
- 风险：低，零行为变化

### 🟢 B2 — `downscaleThumb` 异步闭包可能与最新一次 recordConvMeta 错位
- 位置：`aiStore.ts:740`
- 现象：`recordConvMeta(hash=A, ...)` 后 `downscaleThumb(imageDataUrl).then((thumb) => { ...loadConvIndex(); list[i].thumb = thumb ...saveConvIndex() })`。若用户在异步缩略图生成前**再次**对同一 hash 调用 `recordConvMeta`（如先 generate 再 refine），第二次主流程会立即覆盖 `presetName/preview/updatedAt` —— 缩略图随后到达时仍写回同一 hash 的旧 `presetName/preview` 备份吗？**不会**，因为它读最新 list 写最新 list 同一位置。**但若 index 中间被 clear_history 删了 hash**，then 块里 `findIndex` 返回 -1 跳过 — 安全
- 结论：**当前代码安全**，但闭包读 list 有隐性 TOCTOU 风险；如未来加 delete 流程需重审
- 不必立刻修

### 🟢 B3 — `selectedOrder` 不持久化（关闭重开即丢）
- 位置：`AIPanel.tsx`（之前已记录）
- 现象：多截图选择顺序只在面板 state，关闭 AI 面板再开 → 顺序归零
- 修复：把 `selectedOrder` 存 `localStorage: snapcraft-ai-selorder:<convKey>`，`setConvKey` 时清空
- 风险：低，纯前端

### 🟢 B4 — Agent 模式 `attachOcr/attachImage` 实际被忽略
- 位置：`aiStore.runAgent:870`
- 现象：`runAgent` 强制 `images = [imageDataUrl, ...extras]`，无视 `attachImage/attachOcr`。如果未来用户想"Agent 模式但不传图"（如纯工具操作，不需要视觉），无法关闭
- 结论：当前是**有意设计**（Agent 必须看到图才能给坐标），但缺一个开关的 UX 路径
- 不必立刻修，但可加"智能编辑"开关旁边一个"仅工具不上传图"的高级选项

### 🟢 B5 — History 阅读器导出 docx/pdf 不含来源截图
- 位置：`AIPanel.tsx:360-381`
- 现象：历史线程落盘时**不存 imageDataUrl**（设计如此，省空间），故二次导出 docx/pdf 无图
- 影响：用户从历史库"续写后再导出"会失去视觉证据
- 修复：`recordConvMeta` 可选存 `images: string[]`（仅一张 thumb 也行），导出时回填
- 风险：低

### 🟢 B6 — `genMemId` 仅在压缩时调用，id 稳定性
- 位置：`aiStore.ts:107`
- 现象：每次 compact 都给新 `rolled.id = genMemId()` → Phase 9 UI 高亮"本次相关"的 `activeMemoryIds` 永远对不上已压缩的 id
- 修复：压缩时应**保留旧 id**（取 `memories[0]?.id`），避免 UI 状态跳变
- 风险：低，零行为变化

---

## 五、对照顶级项目：未做但**值得做**的差异点

### 🔥 P0（最大卖点 / 胜负手）

| 候选 | 顶级项目 | 我们的差距 | 落地复杂度 | 商业价值 |
|---|---|---|---|---|
| **多形态工具调用解析** | openclaw `tool-call-shaped-text.ts` | 不支持国产模型非标准 tool_calls | 中（新增 1 个解析器，对接 `streamChatWithTools`）| **极高**：让 Qwen/DeepSeek/Llama 都能用 Agent 模式 → 国产模型用户覆盖率 +300% |
| **AI 看图操作「过程可视化」** | 无对标 | 当前 agentSteps 只是文字，画布上的标注虽立即出现但**无视觉高亮**当前 AI 正在操作的元素 | 低（editor 加 `aiActiveAnnoId` state，临时描边+脉冲）| **高**：让"AI 工作中"肉眼可见 → 卖点感知倍增 |
| **隐私哨兵 Agent** | 无对标 | 用户手动打码/AI 智能打码已通，但**没有"自动识别敏感信息"**入口 | 中（新增 `auto_redact` 工具：先让模型描述可疑区域 → 二次确认 → 批量 redact）| **极高**：安全/合规刚需；B 端金融/医疗/法务**直接付费** |

### 🟠 P1（强增强）

| 候选 | 顶级项目 | 我们的差距 | 落地复杂度 | 商业价值 |
|---|---|---|---|---|
| **本地语义记忆** | openclaw `memory-lancedb` | 纯词法启发式 | 中（引入 `transformers.js` + `Xenova/all-MiniLM-L6-v2` 量化版 ~3MB；新增 `embedMemory/selectByVector`）| **高**：长对话/多截图跨会话召回质量跃升 |
| **会话 fork** | claw-code `Session.fork` | 桌面端无 fork 概念 | 低（History 卡片加"🌿 分支"按钮 → 复制 conv+记忆到新 hash）| 中：实验不同 prompt 方向 |
| **后端代理隐藏 Key** | privdoc-ai Rust 代理 | Key 存 localStorage | 中（新增 Rust `proxy_ai_request`）| **高**：B 端安全/合规 |
| **docx 高级排版** | — | 5 主题已有但版式固定 | 中（`markdownDocx.ts` 加 page-break/页眉页脚/中英混排优化）| 中：精美成稿 |

### 🟡 P2（打磨 / 体验）

| 候选 | 顶级项目 | 我们的差距 | 落地复杂度 | 商业价值 |
|---|---|---|---|---|
| **选区/划词 AI** | — | 无（OCR 后无选区操作）| 中（AIPanel 选区 → 选区 OCR → 单区域 AI）| 高频刚需 |
| **批量截图 Agent 队列** | — | 只能单次生成 | 中（多截图共用 prompt → 顺序 → 各自成稿）| 中：多图工作流提速 |
| **AI 智能编辑工具集扩充** | — | 4 工具（draw/redact/highlight/OCR）| 低（加 `draw_arrow`/`add_callout`/`crop_region`）| 高：高频动作全覆盖 |
| **PPT 导出** | — | 无 | 高（引入 `pptxgenjs` ~1MB）| 中：演示场景刚需 |
| **缩略图回传 History 导出** | — | 不含来源截图 | 低（`recordConvMeta` 加 `images[]`）| 中 |

---

## 六、AI 真正成为系统卖点的「三件套 + 候选四件套」

### 已坐实
1. **AI 直接改图**（Phase 14 工具循环）—— 行业唯一。竞品（Snipaste/Xnip/CleanShot X）只产文字；我们的 AI 能**圈选/打码/高亮/识别**截图
2. **编辑后图即上下文**（Phase 8/10）—— 打码/模糊已合入，**AI 看不到已脱敏的内容** → 隐私闭环
3. **长程不丢上下文**（Phase 6+15 增量压缩链）—— 早期事实恒在域

### 候选（第 4 件套）
4. **多形态工具调用**（让国产模型也能用 AI 智能编辑）—— **覆盖国产用户群**

---

## 七、文档生成润色现状

| 能力 | 状态 | 关键 |
|---|---|---|
| 6 格式导出（md/txt/html/docx/pdf/xlsx）| ✅ | AIPanel `handleExport*` |
| 5 套主题（现代/雅致/杂志/产品推广/科技风）| ✅ | `markdownHtml.ts` 主题引擎 |
| 封面 hero + 副标题（用户需求文本自动填）| ✅ | `subtitle: goal` |
| 自动目录 TOC（H2≥3）| ✅ | `toc/tocTitle` |
| 图文混排（`<!--SNAP:k-->` 章节内嵌截图）| ✅ | `sectionImages` + `hasSnapMarkers` |
| docx 主题/封面/自动目录 | ✅ | `markdownDocx.ts` Phase 13 实修 |
| xlsx 多 sheet（表格数据）| ✅ | `markdownXlsx.ts` |

**润色候选**：①docx 中英混排优化（当前 13 主题混排时字号不稳）②品牌色 UI 外露（`brandColor` 已在 markdownHtml 支持）③表格斑马纹 / 代码块主题化（科技风下视觉一致性）

---

## 八、本轮（2026-07-16 自动化）—— 仅做真值核查 + 路线图，**不动手改**

按用户偏好"诊断优先，反感未明根因前盲目改码/加未请求功能"，本轮：

- ✅ 完成整体真值核查（直读源码）
- ✅ 完成顶级项目深度对标（含 `file:line` 行号引用）
- ✅ 列出 6 项真实可优化点（4 项低风险小修 + 2 项设计改进）
- ✅ 列出 8 项候选需求并按 ROI 排序
- ❌ **不动手**修 B1-B6 / 不落地 P0-P2（等你拍板）

### 拍板建议（如果继续推进）

| 你想要 | 我建议落地 | 理由 |
|---|---|---|
| 让 AI 助手对国产模型更友好 | **P0 #1 多形态工具调用** | 直接扩 AI 智能编辑可用模型，国产 LLM 用户群翻倍 |
| 让 AI 助手可视化、可感知 | **P0 #2 过程可视化** | 现有 AI 工作了但用户感受不到，做"过程动画"立刻变 demo 级演示 |
| 解决"打码防泄漏"自动化 | **P0 #3 隐私哨兵** | 金融/医疗/法务刚需，B 端付费场景 |
| 修小 bug 提稳 | **B1+B3+B5+B6 一组**（约 30 行代码，1 个小时）| 低风险，立即收益 |
| 文档导出更精美 | **润色候选 ①②③**（1-2 个工时）| 已有 5 主题再加细节，性价比高 |

---

## 九、与前几轮报告的差异

| 轮次 | 主要发现 | 本轮差异 |
|---|---|---|
| Phase 13 复盘×实修 | 流式/重试/预算护栏全绿；4 项实修 | — |
| Phase 14 复盘 | 工具循环"集成层 STUB"误判被纠正 | — |
| 整体复盘×真值核查×Agent增强×对标 | 3 项实修（Agent 视觉强制带图 + 工具循环失控检测 + 缓存命中可视化）| — |
| 复盘×真值核查×思考流式化×3bug修复 | 3 项实修 | — |
| 复盘×真值核查+易用性修复+增强蓝图 | 2 项实修（多截图拖拽 + 新对话数据丢失）| — |
| **本轮（终极复盘）** | 0 改动 / 完整路线图 | **重点是给你一个拍板的全景：哪些值得做、哪些不必、哪些是行业级差异点** |

---

## 十、构建基座

- `npx tsc --noEmit` → exit 0
- `pnpm build` → exit 0，137 模块，dist 正常
- 0 个 TODO/FIXME/stub
- 18/18 invoke 对齐
- 5 个 i18n locale（zh-CN / en-US 已确认完整；其它 locale 留待翻译）

---

> **本轮定位**：纯诊断 + 路线图交付，**不动手**。等你拍板后下一步。
> 复用约定：`./start.sh dev` 启动；`pnpm build && ./start.sh app` 跑 release 内嵌 dist。
