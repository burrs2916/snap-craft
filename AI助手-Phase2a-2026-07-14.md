# AI 助手集成 · Phase 2a：导出文档 + 快速润色迭代

> 参考项目：`/Users/liwenchao/GithubProSpace/privdoc-ai`（其具备 Provider/Model 配置、文档管理、模板管理、对话持久化、文档导出）
> 本轮目标：把「截图 → AI 生成完整文档/文案」从「面板内展示」推进到「可落盘成文件 + 可迭代润色」，且**不影响任何现有功能**。

## 需求发掘（对照 privdoc-ai 找差距）
| # | 断点 | 现状 | 理想（privdoc-ai 启示） |
|---|------|------|------------------------|
| 1 | 结果无法落盘 | 仅面板展示 + 复制剪贴板 | 导出为 .md/.txt 文档文件 |
| 2 | 文案无法迭代 | 改一次要重打整段 goal | 一键「更简洁/更详细/更正式/更口语/译英」润色 |
| 3 | 多截图合并 | 仅单张截图上下文 | 多张截图合并成一份报告（**后续待办**） |
| 4 | 结果回写 | 无 | AI 结果贴回为标注（**后续待办**） |
| 5 | 对话历史 | 无 | 持久化多轮对话（**后续待办**） |

本轮落地 **#1 与 #2**；#3/#4/#5 列入后续，避免改动过大影响现有功能。

## 本轮改动（纯前端，零 Rust，零现有功能影响）
- **`src/features/ai/aiStore.ts`**
  - 新增 `refining` 状态 + `refine(instruction)` 方法：以上一轮 `output` 为输入、追加润色指令流式重生成；系统指令语言自适应（中文界面用中文、英文界面用英文）。
- **`src/features/ai/AIPanel.tsx`**
  - 结果区新增「**导出**」：`.md` / `.txt` 两个按钮，复用既有 `saveDialog` + Rust `save_text_file`（零新后端）；`.txt` 走 `mdToPlainText` 轻量去标记。
  - 结果区新增「**润色**」chips：更简洁 / 更详细 / 更正式 / 更口语 / 译英文，点击即 `refine`，无需重打 goal。
- **i18n（zh-CN + en-US）**：新增 `ai.export / exportOk / exportFail / refine / refineShorter / refineLonger / refineFormal / refineCasual / refineEn`。
- **`src/index.css`**：新增 `.ai-post` / `.ai-post-row` / `.ai-post-label` / `.ai-refine-chip` / `.ai-export-msg` / `.ai-btn-sm`，深浅色自适应。

## 验证
- `pnpm build`（tsc + vite）exit 0，131 modules，零报错。
- dist 含全部新 token：CSS 类 `ai-post` / `ai-refine-chip` / `ai-export-msg` / `ai-btn-sm`；locale 键 `exportOk` / `exportFail` / `refineShorter` / `refineLonger` / `refineFormal` / `refineCasual` / `refineEn`；导出名前缀 `snapcraft-ai-`。
- 纯前端改动，重启 App（`./start.sh dev` 或 `pnpm build && ./start.sh app`）即生效。

## 后续候选（待确认，不在此轮范围）
1. 多截图合并成报告（历史网格多选 → 送入 AI 生成一份整合文档）
2. AI 结果「一键贴回为标注」（落到 AnnotationCanvas）
3. 对话历史持久化（SQLite，跨会话保留多轮）
4. Rust 后端代理（密钥不下发渲染层）
5. 自定义 prompt 模板（复用 privdoc-ai 的 TemplateManage 思路）
