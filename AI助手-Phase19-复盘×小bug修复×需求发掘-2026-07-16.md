# AI 助手 Phase 19 — 复盘 × 小 Bug 修复 × 需求发掘

> 2026-07-16 · Senior Developer · 纯前端、零 Rust、构建 exit0 · 双 sanity 全绿

## 一、背景与范围

本轮延续 Phase 15/16/17/18 的实施节奏，在完成"三连击修复"（popup 主区 / OCR 清洗 / 小面板折叠）后，
针对 [整体复盘×终极真值核查] 报告中列出的 4 项低风险 bug 与需求发掘继续推进：

- **B1** `aiTools` useMemo 依赖过宽 → 画笔颜色/字号切换即重建对象引用 → 潜在多余重渲染
- **B3** 多截图 `selectedOrder` 章节顺序不持久化（复核后为**误判**，实际早已持久化）
- **B5** History 阅读器导出 docx / pdf / html 不含来源截图（无证据链）
- **B6** `genMemId` 每次压缩换新 id → Phase 9「本次注入」高亮跳变

同时挖掘**下一阶段可落地的需求**并给出优先级建议。

## 二、修复清单

### B1 · aiTools useMemo 依赖收敛（EnhancedScreenshotApp.tsx / EditorWindow.tsx）

**根因**：`aiTools` 对象的 `useMemo` 依赖数组把 `currentColor / currentStrokeWidth / currentFontFamily` 全部纳入。
用户切换画笔颜色/字号，即触发 `aiTools` 引用变化。虽然 AIPanel 目前**未把 aiTools 用作 useEffect 依赖**（因此当前**未出现** refreshAiVision 循环），
但依赖过宽是**未来加 useEffect 时的隐患**，也让 React DevTools 分析变得嘈杂。

**修复**：用 `useRef` 持有画笔/字号/字体最新值，`aiTools` 内部读 `ref.current`，`useMemo` 依赖数组只保留真正影响工具语义的字段
（`current / addAnnotation / ocrLang / imgWidth / imgHeight / imageData`）。

```ts
// Phase 19-B1：画笔/字号/字体等 UI 快照用 ref 隔离
const currentColorRef = useRef(currentColor);
const currentStrokeWidthRef = useRef(currentStrokeWidth);
const currentFontFamilyRef = useRef(currentFontFamily);
useEffect(() => { currentColorRef.current = currentColor; }, [currentColor]);
// ...
const aiTools: AiToolHost = useMemo(
  () => ({
    drawRectangle: (r, opts) => {
      addAnnotation({
        color: opts?.color || currentColorRef.current,    // ← ref 读
        lineWidth: currentStrokeWidthRef.current,          // ← ref 读
        // ...
      });
    },
    // ...
  }),
  // 只依赖真正决定工具语义的字段；画笔从 ref 读，不参与依赖
  [current, addAnnotation, ocrLang],
);
```

**顺手强化**：`summarizeRegion` 的返回值原是 `res?.text?.trim()`，本次统一走 `cleanOcrText(res?.text).trim()` —— **区域 OCR 也享受 Phase 18 的乱码清洗**。

### B3 · selectedOrder 持久化（误判，已核销）

复核代码后发现，`selectedOrder` 已完整实现持久化：
- `SEL_PREFIX = 'snapcraft-ai-sel:'` + `saveSel(hash, ids)` / `loadSel(hash)`
- 所有 5 处 `setSelectedOrder` 写路径都同步落盘
- `useEffect([imageDataUrl])` 切换截图时 `loadSel` 恢复

原复盘的"不持久化"结论**为误判**，本轮已核销。**用户可继续放心使用多截图章节顺序功能。**

### B5 · History 阅读器导出附带来源截图（AIPanel.tsx）

**根因**：`recordConvMeta` 早已异步生成缩略图（`downscaleThumb` → `AiConvMeta.thumb`），
但 `handleHistoryExport` 三条导出路径（docx/pdf/html）都没有把这张图注入 `sectionImages`，
导致 History 阅读器导出的文档**看不到来源截图**——审计/合规场景缺少证据链。

**修复**：把 `activeConv.meta.thumb` 作为 `sectionImages[0]` 注入：

```ts
const thumb = activeConv.meta.thumb;
const coverImages =
  thumb && (fmt === 'docx' || fmt === 'pdf' || fmt === 'html')
    ? [{ dataUrl: thumb, caption: activeConv.meta.firstGoal || undefined }]
    : undefined;
// docx / pdf / html 三条路径统一传 sectionImages: coverImages
```

- **docx**：走 `markdownToDocx({ title, sectionImages })` —— 首章插入缩略图 + 目标 caption
- **pdf**：走 `mdToHtml({ theme, sectionImages })` → `printHtmlViaIframe` —— 主题化封面首章
- **html**：走 `mdToHtml(...)` —— 直接可读的静态页
- **md / txt / xlsx**：保持纯文本，不涉及图（这些格式对图形嵌入不友好）

**收益**：从此 History 每一条会话的导出结果都自带来源截图 → **审计闭环 + 复现路径**。

### B6 · genMemId 稳定化（aiStore.ts）

**根因**：`compactMemory` 里的 `rolled` 记忆条目每次压缩都用 `genMemId()` 生成新 id，
但 Phase 9 UI 通过 `activeMemoryIds` 高亮"本次注入的相关记忆"，压缩发生瞬间：
- 旧 id 从 `activeMemoryIds` 消失 → 高亮闪一下
- 新 id 出现 → 高亮又亮起来

用户视觉上感受到"记忆卡片跳变"。

**修复**：如果已有一条历史记忆，就复用它的 id（同一条滚动摘要跨压缩保持稳定）：

```ts
const rolled: AiMemory = {
  // Phase 19-B6：稳定 id——若已有历史记忆则复用，保证 activeMemoryIds 不因压缩而跳变
  id: memories[0]?.id || genMemId(),
  summary: text,
  // ...
};
```

只有**首次压缩**（`memories` 为空）时才 `genMemId()`；后续所有压缩都复用同一 id → **UI 高亮永远稳定**。

## 三、验证

| 检查 | 结果 |
|---|---|
| `npx tsc --noEmit` | ✅ exit 0（零类型错误） |
| `pnpm build` | ✅ exit 0（139 模块稳） |
| `scripts/test-tool-call-parser.mjs`（Phase 16） | ✅ **50/50** 全绿 |
| `scripts/test-ocr-clean.mjs`（Phase 18） | ✅ **19/19** 全绿 |

## 四、需求发掘（下一阶段路线图）

复盘现状后，按 **ROI × 用户呼声 × 技术难度**排序：

### 🔥 P0 · 战略级（保持第一梯队差异化）

#### P0-α · AI 操作过程可视化（Canvas 临时描边 + 脉冲）

- **痛点**：目前 AI 智能编辑执行完，标注瞬间"啪"出现在画布，用户不知道是 AI 干的还是自己手抖误触。
- **方案**：`agentSteps` 变化时，最新一步对应的标注在画布上**临时脉冲 2 秒**（金色描边 + 缩放呼吸 + 淡出），
  然后回归常态。同时 popup 步骤时间线上鼠标 hover 一条步骤时，画布上高亮对应标注。
- **代码量**：~150 行 CSS/hook，纯前端。
- **卖点收益**：demo 级演示效果、教学价值、"AI 在工作"肉眼可见。

#### P0-β · 隐私哨兵 Agent（金融/医疗/法务 B 端刚需）

- **痛点**：目前 AI 智能编辑要用户主动说"帮我把手机号打码"。企业客户需要一键批量识别 + 二次确认。
- **方案**：新 preset「隐私扫描」→ 调用 `summarize_region` + 一个新工具 `list_sensitive_regions`（模型返回 0~1 区域列表 + 类型），
  用户在 popup 里勾选 → 批量应用 `redact_area`。类型：手机 / 身份证 / 邮箱 / 车牌 / 姓名 / 金额 / 头像 / 二维码 / 银行卡号。
- **代码量**：~350 行，纯前端 + 一个新 preset。
- **卖点收益**：直接对标 Snagit / Cleanshot X 的商业化痛点，B 端付费转化点。

#### P0-γ · 后端代理隐藏 Key（Rust 侧新加）

- **痛点**：API Key 目前只存在 localStorage → 团队部署 / MDM 场景下暴露。
- **方案**：`src-tauri/src/commands/ai_proxy.rs` 新增 `ai_proxy_stream` 命令，Rust 侧持有 Key（keychain / DPAPI），
  前端只发**目标 URL + payload**，Rust 加 header + 转发流。零架构大改，只在 aiClient.ts 加一层 `useNativeProxy` 开关。
- **代码量**：Rust ~200 行 + 前端 ~50 行。
- **卖点收益**：企业版必备。

### 🟠 P1 · 增强级

- **P1-a · 本地语义记忆（transformers.js + MiniLM 量化）**：替代 Phase 9 词法 MMR，让"上次那个报告"能被同义词召回。首包 ~4MB WASM，冷启动 +200ms，但记忆命中率翻倍。
- **P1-b · 会话 fork（对齐 claw-code Session.fork）**：从任意历史线程的某一轮 fork 出新对话线程，实验"如果当时那样问会怎样"，不污染主线。
- **P1-c · docx 高级排版**：三线表 / 斑马纹 / 目录悬挂缩进 / 中英混排字距。
- **P1-d · 会话导出打包 zip**：把 History 一条会话的所有导出格式 + 缩略图 + OCR 原文一次性打包 zip，方便邮件发送。

### 🟡 P2 · 打磨级

- 选区/划词 AI（用户拖框圈选后弹菜单「让 AI 解释这块」）
- 批量 Agent 队列（一次投喂 20 张，逐个走同一 preset）
- 工具集扩充：`draw_arrow` / `draw_callout` / `crop_image`
- PPT 导出（`markdownToPptx`）
- 缩略图回传 History 阅读器卡片顶部（B5 已存图，UI 只需读）

## 五、AI 卖点四件套（Phase 19 后仍成立）

1. **AI 直接改图**（Phase 14，行业唯一）
2. **编辑后图即上下文**（Phase 8/10 隐私闭环）
3. **长程不丢上下文**（Phase 6+15 增量压缩链，零稀释）
4. **全模型通用**（Phase 16 多形态解析）

Phase 19 让这四件套更**扎实、平顺、可审计**，为下一阶段冲击 P0-α/β/γ 打好地基。

## 六、变更文件清单

| 文件 | 变更 |
|---|---|
| `src/features/screenshot/EnhancedScreenshotApp.tsx` | B1 · ref 隔离画笔状态 + summarizeRegion 走 cleanOcrText |
| `src/features/screenshot/components/EditorWindow.tsx` | B1 · 同上 |
| `src/features/ai/aiStore.ts` | B6 · genMemId 稳定化（memories[0]?.id 复用） |
| `src/features/ai/AIPanel.tsx` | B5 · handleHistoryExport 注入 sectionImages 缩略图 |

零 Rust 改动。共 4 文件、~40 行净改动。

---

**下一步候选（等你拍板）**：
- ①P0-α AI 操作过程可视化（150 行，2 小时，demo 效果爆炸）
- ②P0-β 隐私哨兵（350 行，半天，B 端付费点）
- ③P0-γ 后端代理（Rust + 前端，1 天，企业版必备）
- ④P1-a 本地语义记忆（500 行 + 4MB WASM 首包，1 天，记忆能力升级）
