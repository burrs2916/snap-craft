# AI 助手 — 长期记忆管理（逐条删除 / 编辑）

> 日期：2026-07-16 ｜ 类型：功能增量（铁律内）｜ 约束：纯前端 / 零 Rust / 零新依赖

## 1. 为什么做这件事（诊断优先）

书面路线图（Phase 19–29 + B1–B6）在「零 Rust / 零新依赖」铁律下已**全部交付**。继续推进前复查了长期记忆子系统，发现一个真实缺口：

- `selectMemories`（`aiStore.ts`）**已经是 hybrid + MMR 词面相关性检索**（`compositeScore` + `jaccardSim` + MMR 贪心重排），检索质量不差——所以「P1 本地语义记忆」若按原计划用 transformers.js 才是深度升级，但那需放宽「零新依赖」。
- 但记忆**面板是只读的**：用户只能「合并全部」(`compactMemory`) 或「清空全部」(`reset`)，**无法删除 / 编辑单条记忆**。
- 本项目核心主题是**隐私**（隐私哨兵 Phase 21）。用户无法删除某条可能含敏感信息的记忆，是一个真实的隐私控制缺口。

这条增量纯前端、零 Rust、零新依赖，且契合项目隐私价值观，是铁律内最后一个高价值收尾。

## 2. 实现（增量，不破任何既有逻辑）

### 2.1 store 动作（aiStore.ts）
- `deleteMemory(id)`：过滤掉该 id → 同步清 `activeMemoryIds` → `saveMemories(convKey, next)` 持久化（localStorage 按 convKey）。
- `updateMemory(id, { summary?, importance? })`：就地修补该条 → `saveMemories` 持久化。
- `AiState` 接口补两条声明。

### 2.2 面板 UI（AIPanel.tsx）
- 每条记忆右上角加 **✎ 编辑 / 🗑 删除** 入口（`.ai-mem-acts`）。
- 删除：直接调 `deleteMemory`，即时从面板与持久化层移除。
- 编辑：内联展开 `textarea`（摘要）+ 5 点重要性选择器 + 保存/取消；保存调 `updateMemory`。
- 新增编辑态 `editMemId` / `editSummary` / `editImportance` 三个组件态。

### 2.3 i18n
- zh-CN / en-US 新增 `memEdit` / `memEditTitle` / `memDelete` / `memDeleteTitle` / `memSave` / `memCancel` / `memEditImportance`。

### 2.4 CSS（index.css）
- `.ai-mem-act`（含删除态红色 hover）/ `.ai-mem-edit` / `.ai-mem-edit-area` / `.ai-mem-imp-pick` / `.ai-mem-imp-dot` 等，复用既有 `--accent` / `--surface` / `--text-muted` 变量，明暗主题自动适配。

## 3. 验证（与 Phase 27–29 / B4 同一标准）

| 项 | 结果 |
| --- | --- |
| `npx tsc --noEmit` | ✅ 0 错 |
| `pnpm build` | ✅ exit 0（141 模块，1.87s） |
| `dist` 含 `memDeleteTitle` / `memEditTitle` / `memEditImportance` | ✅ 各 ×3 |
| `dist` 含 `删除这条记忆` | ✅ ×1 |
| `dist` CSS 含 `.ai-mem-act` / `.ai-mem-acts` / `.ai-mem-edit-area` / `.ai-mem-imp-dot` | ✅ 全部就位 |
| 既有记忆检索 / 合并 / 注入链路 | ✅ 未改动，零回归 |

## 4. 影响与边界

- **影响**：用户现在可主动治理长期记忆——删除误记/敏感条目、修正 AI 生成的错误摘要、调整重要性（重要性 ≥ 阈值的关键记忆在 `selectMemories` 中恒注入，调重要性直接影响后续注入策略）。
- **边界（非缺陷）**：删除/编辑即时写回 localStorage，与现有「按 convKey 独立记忆库」模型一致；跨截图不共享（与既有设计一致，非本次引入）。

## 5. 状态

- 记忆管理增量完成。这是铁律内、书面路线图之外的最后一个高价值收尾。
- 正式路线图剩余项均需放宽某条铁律：
  - 🔥 P0-γ 后端代理隐藏 Key（需 Rust）
  - 🟠 P1 本地语义记忆（需 transformers.js 新依赖，需特批放宽「零新依赖」）
