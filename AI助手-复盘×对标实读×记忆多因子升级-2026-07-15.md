# SnapCraft AI 助手 · 复盘 × 真读顶级项目源码 × 记忆多因子升级（2026-07-15）

> 本轮方法升级：**不再凭记忆复述顶级项目**，而是直接读 `claw-code-main` 与 `openclaw-main` 的真实源码，提取可引用的 `file:line` 模式，并据此实修一项差距最大的能力——**记忆相关性检索**。
> 交付：① 两仓库真实模式对照表（带源码引用）；② 记忆选择器多因子 + MMR 升级（已落地，构建全绿）；③ AI 卖点挖掘与工具契约草案。

---

## 0. 一句话结论

**地基依旧扎实（无空/假实现、18/18 invoke 对齐、流式/重试/预算护栏已齐）**；本轮真正往前推了一步——把我们的记忆检索从「单一词法重叠」升级为 **对齐 openclaw 的多因子加权 + MMR 多样性重排**。下一阶段最大的胜负手仍是 **AI Agent 化（让模型直接操作截图）**，动手前需与用户确认工具契约。

---

## 1. 真读源码：两仓库关键文件定位

| 仓库 | 关键文件 | 用途 |
|---|---|---|
| **claw-code** | `rust/crates/runtime/src/sse.rs:26-101` | 通用增量 SSE 解析（`push_chunk` 按行、尾部 `finish()` flush） |
| | `rust/crates/api/src/sse.rs:82-128` | API 层帧→事件，跳过 `event:ping`/`[DONE]`，多行 `data:` 合并 |
| | `rust/crates/runtime/src/compact.rs:96-183` | 自动压缩（保留最近 4、保护 ToolUse/ToolResult 对不被拆散） |
| | `rust/crates/runtime/src/conversation.rs:555-578` | 累计 input≥100k token 触发 `maybe_auto_compact` |
| | `rust/crates/api/src/providers/anthropic.rs:401-464,569-585` | 指数退避重试（initial×2^(n-1)、抖动、可重试状态 408/409/429/5xx） |
| | `rust/crates/runtime/src/conversation.rs:313-515` | **`run_turn` agent 循环**：模型→`tool_use`→执行（含 hook+权限）→`tool_result` 回填 |
| | `rust/crates/runtime/src/session.rs:260-274` | `Session.fork()` 会话分支隔离 |
| **openclaw** | `src/agents/anthropic-transport-stream.ts:549-592` | `parseAnthropicSseBody`（帧分隔、CRLF 归一、尾部 flush、可中断） |
| | `src/agents/compaction.ts:293-339` | 增量 `previousSummary` 链 + `SAFETY_MARGIN=1.2` + 工具对边界保护 |
| | `src/infra/retry.ts:69-137` + `src/infra/backoff.ts` | `retryAsync`（`shouldRetry`/`retryAfterMs`/jitter）+ `computeBackoff` |
| | `extensions/memory-core/src/short-term-promotion.ts:1259-1291` | **六因子加权**：`freq·rel·div·recency·consolidation·conceptual + phaseBoost` |
| | `extensions/memory-core/src/temporal-decay.ts:24-42` | 时间衰减 `exp(-λ·ageDays)`（λ=ln2/halfLife） |
| | `extensions/memory-core/src/mmr.ts:136-190` | **MMR 多样性重排**：`λ·rel − (1−λ)·maxSim`，贪心挑选 |
| | `src/agents/tool-loop-detection.ts:131,498` | `detectToolCallLoop`：SHA256 稳定序列化参数识别重复调用 + 4 类 detector |
| | `src/routing/session-key.ts:54-72` | `agent:<id>:<mainKey>`/`:thread:<id>` 命名空间隔离 |

> 关键发现：**claw-code 没有记忆层**（仅 `compact`/`fork`），明确建议前端自建 embedding+重要性检索；**openclaw 的记忆系统是行业级参考实现**——这正是我们 Phase 9 启发式与顶级实现的真实差距所在，也是本轮升级的目标。

---

## 2. 我们 vs 顶级项目（4 维对照）

| 能力 | 我们现状 | claw-code | openclaw | 差距 |
|---|---|---|---|---|
| **流式 SSE** | 帧边界切分 + 尾部 flush + 内嵌 error 帧（Phase 13，`aiClient.ts`） | 行级 `push_chunk`+`finish` | 帧分隔 + CRLF 归一 + 节流 | ✅ 已追平 |
| **重试/退避** | 指数退避≤3、尊重 429、取消不重试 | 指数+抖动、可重试状态表 | `retryAsync`+jitter | ✅ 已追平 |
| **上下文预算** | `trimHistoryToBudget` 裁最旧+保最近 3 | 100k 自动压缩、保最近 4、工具对边界保护 | 增量摘要链+`SAFETY_MARGIN` | 🟢 基本齐（缺增量链/降级） |
| **记忆检索** | **Phase 9 仅词法重叠**（无衰减/多样性） | 无 | **六因子加权+半衰期+MMR** | 🔴 本轮已补（见 §3） |
| **会话隔离** | 截图 hash 分桶（localStorage） | `Session.fork` | `sessionKey` 命名空间 | ✅ 已对齐（桌面端足够） |
| **Agent 工具循环** | 无（仅产出文字） | `run_turn` 迭代+权限 | `runTurn`+循环检测熔断 | 🔴 **最大差距=最大卖点** |

---

## 3. 本轮实修：记忆多因子 + MMR（Phase 9+）

**根因**：旧 `selectMemories`（`aiStore.ts`）只按「用户输入与记忆摘要的词法命中率」排序，存在三个问题——① 记忆永不衰减，半年前的旧要点与昨天刚压缩的同等权重；② 完全忽略 `importance`（仅作硬阈值，未进打分）；③ 可能一次注入 5 条高度雷同的记忆，挤占窗口。

**对齐 openclaw**：`short-term-promotion.ts` 的六因子加权 + `mmr.ts` 多样性重排。我们取与之同构但零依赖的形态：

```
composite = 0.6·relevance   // 词法命中率（保留原信号）
          + 0.2·recency      // exp(-ln2/14 · ageDays)  半衰期 14 天
          + 0.2·importance   // (imp-1)/4  归一 0~1
候选池 = 复合分 Top(2×配额)
注入集 = MMR 贪心：max( composite − 0.7·maxSimToPicked )   // Jaccard 相似度
```

- `≤MAX_RELEVANT(5)` 条时**行为不变**（全部注入，零回归）；
- 记忆增多后：重要性≥4 恒注入，其余经「复合分→ MMR 多样性」补满配额；
- 无查询时（相关性=0）自动退化为「近期+重要」优先，比旧版全 0 更合理；
- 返回仍按 `createdAt` 升序，时间线可读；UI 的「相关」徽标（`activeMemoryIds`）坐标不变，零 UI 改动。

**改动文件**：`src/features/ai/aiStore.ts`（`relevanceScore` 保留，新增 `recencyComponent`/`importanceNorm`/`compositeScore`/`jaccardSim`，重写 `selectMemories`）。
**验证**：`pnpm build` exit 0（tsc + vite，136 模块，零类型错误，无回归）。

---

## 4. 已确认无问题的项（本轮复核）

- ✅ **i18n 孤儿键已清**：`ai.styleTitle`/`ai.outputEmpty` 与硬编码 `ai.exportDocx`/`ai.exportPdf` 此前报告标记「待修」，核对当前 `AIPanel.tsx:966,970` 已改用 `ai.exportDocxTitle`/`ai.exportPdfTitle`，无需再动。
- ✅ **前后端 18/18 invoke 对齐**：Rust `#[tauri::command]` 与前端 `invoke` 参数（snake→camel）仍 100% 吻合，无悬空调用。
- ✅ **无空/假实现**：Phase 1–12 声明能力均可从 UI 点击到达真实后端/输出。
- ✅ **流式/重试/预算护栏**（Phase 13）确认在 `aiClient.ts` 真实生效，非注释。

---

## 5. 新需求发掘：让 AI 成为真正卖点

### 🔥 A. AI Agent 化（直接"操作截图"）—— 胜负手
竞品（含 privdoc-ai）的 AI 只"产出文字"；截图工具的独有价值是**图即画布**。给 AI 一组"工具"，从「说」变「做」（对齐 claw-code `run_turn` / openclaw `runTurn` + `detectToolCallLoop`）：
- **AI 自动标注**："把『立即购买』圈出来" → 调 `addAnnotation` 加框+箭头+文字
- **AI 智能打码**："把右上角头像和手机号打码" → 自动模糊/遮挡（隐私刚需）
- **AI 区域圈选**："框住这个图表" → 生成标注区域
- **AI 视觉编辑**："把图里中文翻成英文叠加"

**工具契约草案（动手前需用户确认）**：

| 工具 | 参数 | 落地 |
|---|---|---|
| `annotate_rect` | `{x,y,w,h,label?,color?}` | 现有 `addAnnotation`（rect） |
| `annotate_arrow` | `{from,to,label?}` | 现有 arrow 标注 |
| `redact_area` | `{x,y,w,h,mode:'blur'\|'block'}` | 现有打码/模糊标注 |
| `highlight_text` | `{x,y,w,h}` | 现有高亮标注 |
| `read_region_ocr` | `{x,y,w,h}` | 现有 `ocr_image` 区域模式 |

循环：模型 `tool_use` → 前端/后端执行 → `tool_result`（含坐标回执/截图回读）→ 续生成；配 openclaw 式循环检测（SHA256 参数哈希 + 滑动窗口）防失控。建议纯前端先跑（调用现有 React `addAnnotation`），不强制改 Rust。

### B. AI × 现有功能的化学反应
- **AI + 历史截图**：跨多次截图生成"时间线文档"（bug 复现步骤 / 需求演进），把历史当多源素材
- **AI 批量工厂**：N 张截图逐张生成 + 自动汇总总览（当前是合并成一篇，加"逐张+总览"模式）
- **AI 实时划词翻译**：截图后框选文字 → 视觉+翻译叠加层
- **AI 隐私哨兵**：自动检测身份证/手机号/金额并预打码

### C. 文档生成润色（剩余项，按优先级）
1. **docx 套主题封面**：当前 docx 无主题，与 html/pdf 观感割裂（`markdownDocx.ts` 加封面 hero）
2. **导出前页面尺寸/边距选择**（A4/Letter）+ 打印保真
3. **中英混排 typography**：CJK 标点挤压、行距、字号阶梯
4. **会话分支(fork)**：同一截图不同成稿方向互不覆盖（openclaw `Session.fork` 模式）
5. **项目级长期记忆**：多次截图成稿沉淀为"项目知识库"，写新文档自动引用（openclaw citations）

### D. 会话/历史深化
- **后端落库 AI 历史**（需 Rust）：跨设备不丢；升级 Phase 11 字符串搜索为嵌入检索
- **语义记忆升级**：本地 embedding 模型做真·语义检索（替代词法重叠的终极形态）

---

## 6. 精炼路线图

| 阶段 | 内容 | 价值 | 风险 | 依赖 |
|---|---|---|---|---|
| **Phase 9+（本轮）** | 记忆多因子 + MMR | 🟢 已落地 | 低 | 无 |
| **Phase 14** | **AI Agent 工具层**（自动标注/打码/圈选，tool-calling loop） | 🔥🔴 最高（卖点） | 中 | 工具契约确认 |
| **Phase 15** | 语义记忆（本地 embedding）+ 项目级长期记忆 | 🔴 高（智） | 中 | 无 |
| **Phase 16b** | docx 主题封面 + 页面尺寸 + 中英 typography | 🟡 中（成品感） | 低 | 无 |
| **Phase 17** | 后端落库 AI 历史 + 语义搜索 + fork（需 Rust） | 🟡 中（持久化） | 中 | 无 |
| **Phase 18** | 批量工厂 + 实时划词 + 隐私哨兵 | 🟡 中（场景） | 中 | Phase 14 |

> 建议：Phase 14 是"AI 成为卖点"的胜负手，按项目约定"增量增强、不推翻重构"，**动手前先确认工具契约**（§5-A 草案可作为讨论起点）。

---

## 7. 本轮交付物
- 实修：`src/features/ai/aiStore.ts`（记忆多因子加权 + MMR 多样性重排）
- 验证：`pnpm build` exit 0 / 136 模块 / 零类型错误 / 无回归
- 本报告：真读 `claw-code-main` + `openclaw-main` 源码的对照表（带 file:line）+ 记忆升级 + AI 卖点挖掘 + 工具契约草案 + 路线图
