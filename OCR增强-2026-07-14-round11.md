# OCR 增强 第十一轮：从系统剪贴板取字（第四个 OCR 入口）

> 执行时间：2026-07-14｜状态：代码完成，`pnpm build`(前端 tsc+vite，125 模块) 与 `cargo check`(后端) 均 exit 0。

## 一、需求攫取（Requirement Discovery）

对前 10 轮已落地的 OCR 能力做缺口复盘，按「可用性 × 风险」矩阵重新评估：

| 候选需求 | 可用性 | 风险/成本 | 结论 |
|---|---|---|---|
| **从剪贴板取字**（识别已复制到系统剪贴板的任意图片） | 高 | 低（纯增量、零后端改动可加命令） | ✅ **本轮落地** |
| 翻译（中译英等） | 中 | 高（需联网或离线词库，超出本地工具定位） | 留评 |
| 文本清洗（去噪/规范化） | 低 | 中（面板膨胀、收益有限） | 留评 |
| 一次框选多区域批量 OCR | 中 | 中（牵动区域截图流程） | 留评 |

**核心洞察**：此前 OCR 的三个入口（截图取字 / 结果条取字 / 历史网格取字）都要求图片「先进入 SnapCraft 内部」。但真实高频场景是——用户从浏览器、微信、PPT、其它截图工具**复制了一张图到剪贴板**，只想立刻把上面的字提出来，而不想先存盘再导入。这是一个未被覆盖的日常断点，且实现完全增量、零回归。

## 二、实现方案（集成 · 改善 · 优化）

### 1. 后端新增命令 `read_clipboard_image`（`src-tauri/src/commands/edit.rs`）
- 复用既有 `arboard`（剪贴板读写）与 `image`（PNG 编码）依赖、`base64`（data URL 编码）依赖，**不引入任何新 crate**。
- 逻辑：`arboard::Clipboard::get_image()` → 取 RGBA 像素 → `image` 编码为 PNG → 包成 `data:image/png;base64,...` 返回。
- **只读不写**，与 `copy_to_clipboard` 平级、互不干扰；不读取屏幕、**无需 TCC 屏幕录制权限**；剪贴板无图片时返回 `Err`，由前端友好提示。
- 在 `lib.rs` 的 `invoke_handler` 注册新命令（一行）。

### 2. 前端新增入口 `startOcrFromClipboard`（`EnhancedScreenshotApp.tsx`）
- 调 `invoke('read_clipboard_image')` → 解码图片尺寸 → 设置 `current`/`currentView='edit'` → 直接 `runOcr(dataUrl)`。
- 复用已有的 `runOcr`（接收 imageData 入参、不依赖 current 闭包），因此**全部既有能力（语言/区域/置信度/阅读顺序/导出/历史/空间预览联动…）对剪贴板图自动生效**。
- 设置 `setCurrentScreenshot(null)`，避免把剪贴板图误当成本机历史条目（影响钉图等依赖历史 id 的功能）。

### 3. UI 入口（两处，均不破坏现有布局）
- **主屏**：在 4 张截图卡片之后新增独立次级操作行 `📋 从剪贴板取字`（`.home-secondary`/`.home-sec-btn`，中性主题色，不与截图卡片抢视觉权重，**不改动 4 列网格避免中英漂移**）。
- **编辑器工具栏**：在「框选区域」按钮后追加 `📋 从剪贴板取字`，与现有「取字」并列，图标/语义清晰区分（当前截图 vs 剪贴板图）。

### 4. 国际化（zh-CN / en-US）
- `ocr.clipboard` / `ocr.clipTitle` / `ocr.clipEmpty` / `ocr.clipFailed`（支持 `{msg}` 插值，沿用既有 i18n 机制）。

### 5. 样式（`index.css`）
- 新增 `.home-secondary`/`.home-sec-btn`，中性胶囊按钮 + hover 轻微上浮/强调色，遵循「忌大面积刺眼红色」的用户偏好。

## 三、零回归保证（不影响其它功能）

- **后端**：仅新增一个独立 command，未改动 `copy_to_clipboard`/`save_text_file`/`ocr_image` 等任何现有命令与其签名。
- **前端**：新增独立函数与两处按钮，未触碰截图流程、标注体系、历史网格、钉图、滚动长截图、主题切换等任何其它功能的状态或渲染。
- **OCR 面板内既有增强全部复用**：语言选择、区域识别、内联纠错、搜索高亮、置信度过滤、阅读顺序、导出格式、字号、勾选、空间预览联动、历史——对剪贴板图一律生效，行为一致。
- **权限**：不触碰屏幕录制 TCC；剪贴板读取为用户主动点击触发，无后台读取。

## 四、验证

| 检查项 | 结果 |
|---|---|
| `pnpm build`（tsc + vite） | exit 0，125 模块 |
| `cargo check`（后端） | exit 0 |
| dist JS 含新文案 `从剪贴板取字` / `Clipboard OCR` | ✅ |
| dist CSS 含 `.home-sec-btn` | ✅ |
| 后端命令 `read_clipboard_image` 已编译进 target | ✅ |

> 注：新命令需重新编译 Rust 二进制方可调用。本地验证用 `./start.sh dev`（连 vite，改完前端 `pnpm build` 后重开 dev 窗口）或 `./start.sh app`（内嵌 dist 的 .app，改完需重编后端）。本轮未做可交互运行时截图验证（无显示器环境），仅静态构建通过。

## 五、仍留评估（延续）

翻译、文本清洗、多区域批量 OCR——成本/跨组件/超出本地工具定位，暂不做。

OCR 取字入口现已覆盖四个场景：**截图取字 · 结果条取字 · 历史网格取字 · 剪贴板取字**，纯前端+单增量命令的增强已高度收敛。
