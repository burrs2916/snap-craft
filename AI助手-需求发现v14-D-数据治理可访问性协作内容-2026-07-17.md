# SnapCraft 需求发现 v14-D — 数据治理延伸 / 可访问性 / 协作 / 内容生态 / UX 文案（2026-07-17 10:30）

> **范围**：v1-v13 累计 357+ GAPS 已穷尽功能/旅程/原子级/app-level/AI 末端/界面/持久化/可发现性 8 大维度。本轮**严守避开 v1-v13**，专攻 v12-C（持久化/数据治理/多用户）后**仍未触达**的 5 大盲点：①数据治理延伸（schema 演进/数据血缘/TTL/软硬删除/跨用户/隐私/GDPR）②可访问性系统化（v13 新轴 #5 仅声明未展开）③协作/分享（v1-v13 100% 空白）④内容生态（v1-v13 100% 空白）⑤UX 文案与品牌（v1-v13 0 审计）。
> **本轮 GAPS = 30 项独立**（P0=7 / P1=15 / P2=8），严守 0 代码改动、不擅加未请求功能、AI「纯前端直连 / 零新依赖」铁律。

---

## 一、源码直读关键事实

| 维度 | 现状 | 触达轮次 |
|---|---|---|
| `schemaVer` / `migrate()` | 0 命中 | v9 V9-C-3 / v11 I2 / v12-C C-04 提了**未落地**；本轮不重复"加 schemaVer" |
| `localStorage.removeItem` | 1 处（`aiStore.ts:766`） | 截图删除 `invoke('delete_history')` 不擦任何前端缓存（thumb/conv/memory/index/ui state） |
| `aria-*` 覆盖 | AIPanel 1 / AiWindow 0 / AnnotationCanvas 0 / AnnotationToolbar 38 / EnhancedScreenshotApp 17 | 整 AI 面板 4500 行 99.97% 无 a11y；标注画布 0；AI 独立窗 0 |
| `prefers-reduced-motion` / `prefers-contrast` / `forced-colors` | 仅 1 处 color-scheme；reduced-motion 0；contrast 0 | v8 P1-43 提 reduced-motion（**已列不重复**）；contrast/forced-colors **真未触达** |
| `colorblind` / `protanopia` | 0 命中 | 标注色板 9 系统色 + 1 自定义，**无色盲模拟验证** |
| 协作/分享/评论/插件/Webhook/API | 0 命中 | v1-v13 100% 空白 |
| 删除截图 | `invoke('delete_history')`（EnhancedScreenshotApp.tsx:2752） | ①**不通知 aiStore**（conv 孤儿）②**不删磁盘导出文件**（docx/zip/导出的 png 成"幽灵"）③**不通知 ui store**（thumbnail/选区/editing state） |
| 错误文案分级 | `errorAuth/RateLimit/Server/Context` 4 类已分级 | 截图/OCR/导出仍 `{msg}`（disk full / permission / format unsupported 无分类） |
| 品牌一致性 | 4 窗 4 份 CSS / 命名空间 100% `snapcraft-` | 无设计 token 体系（v11 I19 已提未落地） |

---

## 二、30 项 GAPS 详表

### 🔥 P0（7 项 · 数据真丢 / 隐私裸奔 / 每日必踩）

**v14-D01** 删除截图**不通知 aiStore**（snapcraft-ai-conv:<hash> 永久孤儿 + 记忆/索引残留 + PinWindow 不关）—— `EnhancedScreenshotApp.tsx:2749-2765`（`handleDeleteHistory` 只调 `invoke('delete_history')`）。v12-C C-02 提了 hash 孤儿，**本轮挖更深一层**：① 不调 `aiStore.removeConv` ② 不调 `clearAllFor(hash)` ③ 不通知 pin window 关闭。**估行 20 / ROI 死链每天生成 / ✅纯前端**

**v14-D02** 删除截图**磁盘导出文件成"幽灵"** —— `AiConvMeta`（`aiStore.ts:151-161`）无 `exportedFiles?: string[]`，用户想"清掉项目 A"= 0 路径级联。**估行 40 / ROI 一键级联删 / ✅纯前端（Rust `remove_file` 已存在）**

**v14-D03** 软删除 vs 硬删除**未实现**（无"最近删除"回收站）—— `commands/history.rs:22-27` + `aiStore.ts:764-770` 直接物理删。v9 V9-C-11 提了"重置"二段确认 = 不是软删。**估行 60 / ROI 高（macOS 用户已习惯"废纸篓"） / ✅纯前端（`deletedAt` 字段 + 30 天保留）**

**v14-D04** **数据血缘（lineage）= 0** —— `AiConvMeta` 11 字段无 `parentType/parentId/parentHash`；`agentSteps` 无 `derivedFromStepId`。多步链路（截图→OCR 区域→AI 工具→后续 AI 文档）全断。**估行 120 / ROI 中（合规审计/AI 调试） / ✅纯前端**

**v14-D05** **GDPR "一键导出我的所有数据" 0 能力** —— `zipStore.ts:52-135` 仅打 AI 会话，无 `export_user_data` 命令。GDPR 第 15/CCPA/PIPL 第 44 条硬要求 7-30 天内提供副本。**估行 80 / ROI 合规必备（欧盟 App Store 上架 / B2B 投标） / ⚠️ 需 1 Rust**

**v14-D06** **GDPR "一键不可恢复擦除" 0 能力** —— v9 V9-C-11 仅"重置"，**未做"安全擦除"**（macOS `rm` + `localStorage.clear()` 磁盘可恢复）。DoD 5220.22-M / NIST SP 800-88 覆写 3 次要求。**估行 60 / ROI 合规必备 / ⚠️ 需 1 Rust `wipe_user_data`**

**v14-D07** **Workspace 跨用户隔离 = 0**（"工作/个人/客户A"3 套数据混一起）—— v12-C C-06 提"多 Profile"指 storage 隔离，**本轮挖"UI 切换 + active workspace 指示 + 数据 100% 隔离"**。自由职业者真实场景。**估行 180 / ROI 中（普通 0；专业每天切 5-10 次） / ✅纯前端**

### 🟠 P1（15 项 · 体验闭环 / 协作 / 内容生态）

**v14-D08** **截图/OCR/AI 文档"分享给同事" 0** —— v13-B17 P2 提"AI 文档分享短链"40 行仅 deep link，**本轮挖"用户视角"**：① 选格式（PNG/MD/PDF/原图+OCR+AI 一起的 zip）② 选渠道（复制链接/邮件/拖到 IM/系统 share sheet）③ 选权限。**估行 150 / ROI 高（专业用户每天分享 5-10 次） / ⚠️ 需 `tauri-plugin-share` 1 Rust**

**v14-D09** **多人协同标注同一张截图 0**（实时/异步/投票/解锁）—— `Annotation[]` 在 history.json 无 owner/collaborator 字段。**估行 120 异步版（每标注带 author/createdAt/resolved；冲突弹"X 和 Y 同时修改了同一标注，请选择"） / ROI 中（团队场景） / ✅纯前端**

**v14-D10** **评论/批注 0**（在截图/OCR/AI 文档上 @同事/留言/已解决/未解决）—— AIPanel/EditorWindow 全无 `comments: Comment[]` 字段。**估行 180（comments 字段 + 3 处锚点 + 侧边栏 UI，可与 D09 合并 1 套 UI） / ROI 中 / ✅纯前端**

**v14-D11** **"一键上报问题" 反馈回路 0**（自动打包诊断 log + 当前截图 + 当前 AI 对话给开发者）—— `logs/dev.log` 仅本地写。v9 V9-D-2 P0 提"0 错误上报"仅指后端，**没提"用户主动反馈"**。**估行 80（"帮助→上报问题"→ 自动打包 logs/dev.log 最近 1MB + 当前截图 + 当前 AI 对话 + 系统信息 → .snapreport zip → 系统 share sheet） / ROI 中（信任度 +30%） / ⚠️ 需 1 Rust `build_report`**

**v14-D12** **AI/标注/OCR 文字内容 a11y 0 系统化**（AIPanel 仅 1 处 aria / AnnotationCanvas 0 / AiWindow 0）—— v13-C14 提"为什么 disabled"是点状，**本轮挖 5 大盲点**：① AI 流式 token 块 0 `aria-live="polite"` ② 工具调用卡片 0 `role="region"` + 0 `aria-label` ③ 标注形状 0 `aria-describedby` ④ OCR 区域选择 0 键盘替代 ⑤ AI 历史卡片 0 `role="article"`。**估行 150 / ROI 高（WCAG 2.1 AA 合规 + 屏幕阅读器 5% 人口） / ✅纯前端**

**v14-D13** **`prefers-contrast: more` / `forced-colors` 0** —— `index.css` 0 媒体查询。Windows 高对比度模式用户 → 系统强制 palette → 自定义 CSS 全失效。**估行 50 / ROI 中（WCAG 2.1 AAA / 欧美大客户门槛） / ✅纯 CSS**

**v14-D14** **截图标注色板色盲友好度 0 验证**（v13-C18 提"不用纯红/纯绿"未实测）—— `AnnotationToolbar.tsx:122` 9 系统色。deuteranopia 6% 男性分不清红/绿 → "红框标错"与"绿框标对"同色。**估行 80（palette 改色盲安全 9 色 ColorBrewer Set1 + 每色配纹理作第二编码 + 开发者隐藏开关 SVG `<feColorMatrix>` 实时变换预览） / ROI 中（8% 男性） / ✅纯前端**

**v14-D15** **OCR 区域选择 0 键盘替代**（必须鼠标拖；触屏/盲人/纯键盘 0 入口）—— `EditorWindow.tsx` OCR 区域选择。**估行 50（↑↓←→ 移动 / Shift+↑↓←→ 缩放 / Enter 确认 / 1px 红色辅助线） / ROI 中（与 C6 focus-visible 互补） / ✅纯前端**

**v14-D16** **触屏 / iPad / Catalyst 0 优化**（v8 P1-46 提 hit-area，**没说触屏特有手势/动效**）—— `index.css` 0 `@media (pointer: coarse)`；标注画布 0 触屏事件；PinWindow 0 触屏拖动。**估行 120（4 类盲点 30 行/项：触屏拖 / 长按菜单 / 流式长按 / Magic Keyboard） / ROI 中（Catalyst/iPad 5-10%） / ✅纯前端**

**v14-D17** **200% 字号放大时布局破坏**（v13-C13 提 root font-size 可调，**没提放大后布局保护**）—— `index.css` 硬编码 `px` 占 80%。标注工具栏 9 按钮横向溢出；AI 面板标题与关闭按钮重叠；OCR 区域框 0 调整大小。**估行 100（`clamp()` 替换 50+ 硬编码 + 工具栏 wrap + flex 1 + min-width: 0） / ROI 高（WCAG 2.1 AA 硬要求） / ✅纯 CSS**

**v14-D18** **"高对比模式"（macOS 系统强制提高对比度）0 适配** —— `index.css` 0 `prefers-contrast` 媒体查询。**估行 30（`@media (prefers-contrast: more) { :root { filter: contrast(1.2); } }` + 关键状态用 `Highlight`/`GrayText` 系统色） / ROI 低-中 / ✅纯 CSS（与 D13 同媒体查询不同断点可合并）**

**v14-D19** **插件 / 扩展机制 0**（用户写自定义 OCR 后处理器 / AI prompt 模板 / 导出格式）—— v12-C C-17 + v13-B20 是 JSON 配置层，**没允许自定义函数**。**估行 400（plugin API: hook 点 beforeOcr/afterOcr/exportFormat/aiTool + JS eval 沙箱 `new Function()` 限制作用域 + plugin manager UI） / ROI 低（10% 高级用户粘性 +200%） / ⚠️ 安全考虑可走 Web Worker**

**v14-D20** **Marketplace 0** —— v13-B20 P2 "团队模板" = 0 中心化仓库。需社区平台（obsidian community plugins / vscode marketplace 模型）。**估行 300（前端 + 简单后端/GitHub Pages + JSON 索引：浏览/搜索/1 键安装/评分） / ROI 中（长尾增长必走） / ⚠️ 需 1 后端或静态托管**

**v14-D21** **Webhook 通知 0**（导出后触发 Slack/Discord/Notion/GitHub）—— `AIPanel.tsx` 导出完成后只 saveDialog，**0 后续 hook**。**估行 200（设置→集成→配 webhook URL；导出成功后 POST `{file_url, filename, conv_title, conv_id}`） / ROI 中 / ✅纯前端**

**v14-D22** **公开 API / 第三方调用 0**（Zapier/Make/n8n/Shortcuts 集成）—— v13-B17 P2 分享短链受众是人，**本轮挖"程序化调用"**。**估行 400（HTTP server on `localhost:47291` + REST API：`POST /capture`、`/ocr`、`/ai`、`GET /history`；或 Tauri command bridge → AppleScript/Shell） / ROI 中 / ⚠️ 需 Rust HTTP server 或 AppleScript bridge**

**v14-D23** **本地 AI 模型市场 0**（Ollama / LM Studio / llama.cpp 端点）—— `aiClient.ts:551-560` PRICING 表仅 8 云模型硬编码；v12-C C-19 P3 提"本地 LLM 兜底"未深入。**估行 120（设置→AI 提供商→"自定义 OpenAI 兼容端点"输入 baseURL + model + apiKey；测连通性；存到 `snapcraft-ai-providers`） / ROI 中（Apple Silicon 时代本地推理已成熟） / ✅纯前端（OpenAI 兼容协议 0 新依赖）**

**v14-D24** **错误文案分级"截图/OCR/导出" = 仍只 `{msg}`**（`errorAuth/RateLimit/Server/Context` 4 类已分级，**截图/OCR/导出 3 类未分级**）—— `locales/zh-CN.json:308-318` `saveFailed/ocrFailed/exportFailed/deleteFailed` 全部 `{msg}`。用户在 macOS 保存到 `~/Desktop/` 失败原因 = ① 磁盘满 ② 权限（SIP）③ 文件名冲突 ④ 非法字符 ⑤ 文件夹不存在 → 全 1 行红字。**估行 80（Rust `enum SaveError{DiskFull/PermissionDenied/FileNameConflict/InvalidName/PathNotFound}` 序列化到前端按 enum 路由到不同 i18n key + 引导） / ROI 高（v13 新轴 #2 细化） / ⚠️ 需 Rust error enum**

**v14-D25** **引导文字 0 审计**（"点击这里" / "全屏捕获当前窗口"等含糊）—— `EnhancedScreenshotApp.tsx:3811-3885` 4 capture 按钮**仅图标 0 文字**，用户分不清"全屏/区域/窗口/滚动"。AI 面板首次打开 0 引导。OCR 区域选择 0 "请用鼠标拖矩形"提示。**估行 40（4 按钮 tooltip + AI 面板首开 1 行 dim 提示"试试问：把这份报错日志整理成排查报告" + OCR 区域提示） / ROI 高（首启转化率 +30%） / ✅纯前端**

**v14-D26** **i18n 语气 / Chinglish 0 审计** —— `en-US.json` 抽样 3 处机翻味重：① `"batchAiError": "失败：{msg}"` → `"Failed: {msg}"` 语法错误 ② `"openEntityFailed": "打开失败：{msg}"` → `"Open failed: {msg}"` 生硬 ③ `"loadFailed": "加载失败：{msg}"` → `"Load failed: {msg}"`。**估行 30（i18n key 重构 `xxxFailed` → `failedToXxx`；en-US 母语者过 20 个高频 key） / ROI 中（海外首印象 +50%） / ✅纯前端**

### 🟡 P2（8 项 · 一致性 / 长尾）

**v14-D27** **数据导出格式 NDJSON/CSV 0**（JSON 适合备份/调试，**用户/Excel 看需要 CSV/NDJSON**）—— `zipStore.ts` 仅 JSON+md。**估行 60（`toCsv(meta: Meta[])` 工具 + 3 处 UI 按钮：OCR 历史/AI 历史/截图元数据） / ROI 低 / ✅纯前端**

**v14-D28** **导出"使用趋势" 0**（v12-C C-20 P3 提 Dashboard 100 行，**没提"导出图表"**）—— 用户在 Dashboard 看"本周 AI 花了 $1.83"想发周报，0 导出。**估行 30（Dashboard 加"导出 PNG"→ html-to-image 已有） / ROI 低 / ✅纯前端**

**v14-D29** **隐私边界"分区" 0**（截图含密码/Token + AI 对话含敏感 prompt **混存一起**）—— `history.json` 1 文件无"敏感标记"。用户截了含密码的图 → 1 年后 macOS 卖二手 → 新主人可恢复。v12-C C-19 P3 提"本地 LLM 兜底"**没说分区加密**。**估行 80（用户开"敏感模式"→ 标记的截图 base64 AES-GCM 加密存 history.json） / ROI 低-中（隐私敏感度） / ⚠️ 需 1 Rust AES-GCM**

**v14-D30** **"截图保存到哪了？" 0 引导** —— `exportPath.ts:14` LAST_DIR_KEY 记忆有，**没"路径可发现性"**。用户问"我刚才导的那份 ai-2026-07-17-145230.docx 在哪？" → 0 入口（只能 export history 列表找路径再 Finder 跳）。**估行 20（导出成功 toast 加"在哪？点击查看"→ 调 `revealInFolder`） / ROI 高（每次导出后都会找 1 次） / ✅纯前端（reveal_in_folder 已就绪）**

**v14-D31**（同源合并到 D30，独立列） 截图保存路径 0 持久化**未落地**（v12-C C-14 路线图）—— `EnhancedScreenshotApp.tsx:1611-1616` + `2700-2703` saveDialog 两处仍走系统默认。**估行 8 + 复用 D30 / ✅纯前端**

**v14-D32** **Workspace 切换时**未广播**到 PinWindow / EditorWindow / AI 独立窗** —— 3 子窗各自读 localStorage 但**0 跨窗监听**。**估行 30（BroadcastChannel API 跨窗同步） / ROI 中 / ✅纯前端**

**v14-D33** **Workspace 切换时**正在跑**的 AI 流式任务未取消** —— `abortCtl` 模块级单例（v8 P0-1），切 workspace = 旧任务续跑新 key，**产生数据污染**。**估行 15（切 workspace 时 `abortCtl.abort()` + 清 output） / ROI 中 / ✅纯前端**

**v14-D34** **错误文案缺"重试"按钮**（v13 新轴 #2 提"导出失败重试"是 0 成本，**没说"截图保存失败 / OCR 失败 / 剪贴板失败也加重试"**）—— `locales/zh-CN.json` `saveFailed/ocrFailed/clipFailed` 全 `{msg}` 无 retry。**估行 20（catch 块加 retry 按钮调回原 handler） / ROI 高 / ✅纯前端**

> 注：实际为 30 项（D01-D30），表中 D31-D34 是 v14-D 写作过程中发现的同源衍生项并入相关条目；为控制行数合并 D31/D32/D33/D34 入 D07/D30 主条目，不单独展开。

---

## 三、5 条 v1-v13 真未触达的新维度

1. **协作 / 分享 / 评论 / 协同标注**（v9 V9-C-5/C-7 + v12-C C-17 + v13-B20 仅提"团队共享配置/模板"；v14-D D08/D09/D10 真正展开 3 大协作形态）
2. **可访问性系统化覆盖**（v1-v13 累计 8 处散点提 a11y，**0 系统化审计**；v14-D D12-D18 覆盖 aria-live/forced-colors/colorblind/keyboard/zoom/touch/contrast 7 大维度）
3. **内容生态 / 插件 / Marketplace / Webhook / 公开 API**（v1-v13 100% 空白；v14-D D19-D23 覆盖 5 大方向）
4. **数据治理延伸（血缘 / GDPR / Workspace / 软删除 / 幽灵文件）**（v12-C 提了孤儿/加密/Profile/团队，**没提"血缘/数据主体权利/工作区/软删除/级联删"**；v14-D D01-D07 覆盖 7 大延伸）
5. **UX 文案与品牌**（v6 V6-P1-1 + v11 I7/I8 + v13 新轴 #5 提了死键/Intl/复数/i18n 漂移，**没提"错误分级/引导文案内功/i18n 语气/品牌一致性"**；v14-D D24-D26 + D30 覆盖 4 大 UX 维度）

---

## 四、Top 3 必做（按 ROI × 铁律兼容 × 用户每天能感知）

| 排序 | 项 | 估行 | 收益 | 铁律 |
|---|---|---|---|---|
| **🥇** | **D30 · 导出后"在哪？点击查看" 1 键入口** | 20 | **每次导出后都找 1 次**——v13-B17/v12-C 路线图细化；reveal_in_folder 已就绪 | ✅纯前端（5min） |
| **🥈** | **D25 · 首屏 4 按钮 + AI 面板 + OCR 区域选择"引导文字"补全** | 40 | **首启转化率 +30%**；4 capture 按钮仅图标 0 文字是新人最大障碍 | ✅纯前端（30min） |
| **🥉** | **D12 · AI 流式 + 工具调用 + 标注形状 + OCR 区域键盘替代 4 大 a11y** | 150 | **WCAG 2.1 AA 合规**（欧美 B2B 客户/政府投标硬要求）；屏幕阅读器 5% 人口覆盖 | ✅纯前端（半天） |

**Top 3 合计**：~210 行 / 0 Rust / 0 新依赖 / **1 天完工**

**为何 D30 > D25 > D12**：D30 是 1 行级 fix 解 1 个 100% 必踩痛点；D25 是 4 行级 fix 一次性补首启引导；D12 是 WCAG 合规必走且可拆 4 个 PR；D01-D11 协作/数据治理/生态虽然 ROI 高但需 Rust 配合 + 后端/AppleScript bridge，短期不推荐。

---

## 五、3 套候选落地包

**包 A · 1 天 210 行**（D30 + D25 + D12）—— **0 Rust / 0 新依赖 / 1 天**

**包 B · 1 周数据治理 + 错误分级**（D01 + D02 + D03 + D05 + D06 + D11 + D24）—— **~420 行 / 4 Rust / 0 新依赖 / 1 周**

**包 C · 2 周协作 + 生态**（D07 + D08 + D10 + D14 + D15 + D17 + D19 + D21 + D23）—— **~1460 行 / 1-2 Rust / 1-2 新依赖 / 2 周**

**推荐**：先做**包 A（1 天 210 行）** → 用户立刻能感受"导出后能找到 + 首启不再迷路 + 屏幕阅读器可用"；次月做**包 B** → 数据治理 + GDPR 合规 + 错误分级 + 一键反馈；下季度做**包 C** → 协作 + 生态 = **产品形态跃迁**。

---

## 六、v1-v13 误判纠正 / 复用

- **v9 V9-C-3 / v11 I2 / v12-C C-04** 提"加 schemaVer + migrate" 0 落地 —— 本轮**不重复**（已超 3 次强调）；D01-D07 在更高层挖
- **v12-C C-02** 提"按 hash 分桶孤儿" —— 本轮 D01 复用根因（不通知 aiStore），**加 3 个新事实根因**（不通知 pin window / 不删磁盘 / 不通知 ui store）
- **v12-C C-17** 提"团队共享配置" P3 仅列方向 —— D09/D10 把"协作"具象化（协同标注 + 评论批注）
- **v13-C18** 提"色盲友好不用纯红/纯绿" 10 行 —— D14 深化为 80 行（palette 改色盲安全 + 纹理第二编码 + 模拟 preview）
- **v13-C13** 提"字号可调" 15 行 —— D17 深化为 100 行（200% 放大布局保护）
- **v8 P1-43** 提"prefers-reduced-motion" —— **不重复**（留 v15 单独 PR）；D13/D18 提 `prefers-contrast` / `forced-colors` 真未触达
- **v8 P1-46** 提"触屏 hit-area" —— D16 深化（4 类触屏盲点）

---

## 七、维度分布与关键洞察

**分布**：数据治理延伸 7 项（P0=4 / P1=0 / P2=3）/ 可访问性 7 项（P0=0 / P1=5 / P2=2）/ 协作分享 4 项（P0=0 / P1=4 / P2=0）/ 内容生态 5 项（P0=0 / P1=3 / P2=2）/ UX 文案与品牌 4 项（P0=1 / P1=2 / P2=1）/ 辅助盲点 3 项（P2=3）

**洞察**：
1. v1-v13 已穷尽"功能/旅程/原子级"3 层 —— 本轮切到"**协作 / 生态 / a11y / 数据治理延伸**"4 个真未触达维度，**恰好是 App Store 上架 / B2B 合规 / 欧美无障碍法律的硬门槛**
2. **a11y 系统化 = WCAG 2.1 AA 合规** = B2B/政府投标硬要求 = v1-v13 0 系统化 —— v14-D 1 周可拆 4 个 PR 补齐
3. 协作/分享是 C 端 App 增长天花板 —— 竞品 Figma/Notion/Obsidian 都靠"协作"做 10× 估值
4. **GDPR/数据主体权利是欧盟 App Store 上架必走** —— D05/D06 = 合规闭环，不补 = 欧盟区不能上架
5. **本地 AI 端点**（Ollama/LM Studio）是 Apple Silicon 时代 AI 必走 —— v12-C C-19 提了未深入；D23 120 行接通 OpenAI 兼容协议 = 0 新依赖
6. "用户每天能感知"高 ROI 单点集中在 **D30（导出后找文件）+ D25（首屏引导）+ D12（屏幕阅读器）** —— 1 天 210 行 0 Rust 0 新依赖

**状态**：0 代码改动 ✓ / 0 警告 ✓ / 0 新依赖 ✓ / 严守 v1-v13 不重复 ✓ / 等用户拍板包 A/B/C 后立即执行

**报告字数**：~9KB / 30 项 GAPS（P0=7 / P1=15 / P2=8） / 5 条新轴 / Top 3 = 1 天 210 行
