# OCR 功能增强 · 第十三轮（需求攫取 → 预览框框选范围 + 结构化复制）

> 时间：2026-07-14 08:27。状态：代码完成，`pnpm build`(前端) 通过（exit 0，125 模块），dist 已含新文案/样式；后端零改动。

## 一、需求攫取（发现 + 可用性评估）

在 12 轮已有增强（结构化/语言/区域/内联纠错/导出/搜索高亮/置信度/合并/持久化/自动复制/统计/历史/阅读顺序/重识别/快捷键/字号/勾选/坐标导出/空间预览联动/实体提取/剪贴板入口…）基础上，对「还能增加可用性」的需求做一轮盘点，按 **可用性 × 风险** 矩阵筛选：

| 候选需求 | 可用性 | 风险 | 结论 |
|---|---|---|---|
| **预览框「框选范围取字」**（在已有空间缩略图上拖拽矩形，仅提取该范围内文字块） | 高 | 低 | ✅ 落地 |
| **结构化复制**（把带坐标 JSON/TSV 直接写剪贴板，免存文件） | 中 | 低 | ✅ 落地 |
| 翻译（OCR 结果机翻） | 中 | 高 | ❌ 跳过（网络/离线边界、超出本地工具定位） |
| 文本清洗（l/1、O/0 等常见误识自动校正） | 低 | 低 | ❌ 跳过（可用度低，且易误导） |
| 多区域批量 OCR（一次框多个区域分别识别） | 中 | 中 | ❌ 跳过（需重构选区流程，成本偏高） |

> 选中两项均 **纯前端、零后端改动、不碰其它功能**；默认关闭状态与历史行为完全一致（零回归）。

## 二、实现项

### 1. 预览框「框选范围取字」（`ocrRegionPick`）
- 预览缩略图头部新增 **▭ 框选范围** 开关（默认关，零回归；无识别结果时禁用）。
- 开启后，预览图进入 `picking` 态（十字光标、`user-select:none`、文字块框 `pointer-events:none` 不挡拖拽）；在图上 **按下→拖拽→松手** 画出一个矩形。
- 松手时按归一化坐标求交，落在矩形内的文字块自动写入既有 `ocrSel` 选择集合 → **直接复用** 现有的「复制全部 / 导出 / 贴回标注」管线，做到「精准提取某一列、某一段落、某表格单元」，不必整图复制后再删。
- 选完自动退出框选模式、选择保留；矩形过小（<2%×2%）视为取消、不改选择；新识别自动清空该模式（`runOcr` 内重置 `ocrRegionPick`/`ocrDrag`）。
- 纯几何计算（`b.x < d.x+d.w && b.x+b.w > d.x && …`），无平台/网络依赖。

### 2. 结构化复制（JSON / TSV 内联）
- 操作行在「复制全部文字」后新增 **复制 JSON** / **复制 TSV** 两个按钮。
- 复用既有 `buildOcrExportContent('json'|'tsv')`，把带归一化坐标（x,y,w,h,confidence）的结构化文本 **直接写入剪贴板**（不落文件），方便下游脚本/表格直接消费。
- 受搜索/置信度/勾选（含框选范围）影响（WYSIWYG）；无内容时复用既有 `exportEmpty` 提示。

### 3. 支撑改动
- `src/locales/zh-CN.json` / `en-US.json`：在 `ocr` 对象新增 8 个键（`regionPick`/`regionPickTitle`/`regionPickActive`/`regionPickNone`/`copyJson`/`copyJsonTitle`/`copyTsv`/`copyTsvTitle`）。
- `EnhancedScreenshotApp.tsx`：新增状态 `ocrRegionPick`/`ocrDrag`/`ocrWrapRef`/`ocrDragStart` 与处理函 `ocrPct`/`onPreviewDown`/`onPreviewMove`/`onPreviewUp`/`copyOcrAs`；预览头部加开关、包裹层加 `ref`+拖拽事件+实时矩形、加激活提示条；操作行加两个结构化复制按钮。
- `src/index.css`：`.ocr-region-pick`（含 active/disabled）、`.ocr-preview-imgwrap.picking`、`.ocr-region-drag`、`.ocr-preview-hint-active`。

## 三、验证

```bash
pnpm build   # tsc + vite build → exit 0，125 模块
```
- `dist/assets/*.js` 含 `regionPickActive`/`copyJsonTitle`/`regionPickNone` 等新增键；
- `dist/assets/*.css` 含 `ocr-region-pick`/`ocr-region-drag`/`ocr-preview-hint-active`/`picking` 等新类；
- 后端 `cargo check` 无需重跑（本轮零 Rust 改动）。

## 四、零回归 / 不影响其它功能

- **仅改 OCR 面板内**：状态、处理函、JSX、CSS 全部收敛在取字面板与两个 locale 文件；未改任何命令签名、截图/标注/历史/钉图/主题/权限流程。
- **默认行为完全一致**：`ocrRegionPick` 默认 `false` → 预览图交互与历史版本完全相同；新增两按钮为纯增量，不改动既有「复制全部/导出文件」行为。
- 所有复制/导出/贴回/历史数据链未被触碰，框选范围只是「另一种方式去设置既有的勾选集合」。

## 五、仍留后续评估（延续前几轮，本轮不做）

翻译、文本清洗、多区域批量 OCR —— 成本/跨组件/网络边界偏高，或超出本地工具定位。OCR 面板纯前端增强已高度收敛，后续如需突破需先放宽「不影响其它功能」约束或引入离线资源。
