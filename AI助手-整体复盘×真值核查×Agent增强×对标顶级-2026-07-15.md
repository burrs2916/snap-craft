# AI 助手整体复盘 × 真值核查 × Agent 增强 × 对标顶级项目

> 执行时间：2026-07-15 18:16 · 自动化轮次
> 方法：直接读码（非依赖缓存快照）+ `tsc --noEmit` + `pnpm build` 三道关口交叉验证
> 参考：用户指定 `/Volumes/HPFX900固态盘/large model/references/claw-code-main` 与 `openclaw-main`

---

## 一、重要更正：上一轮审计基于「过期快照」误判 Phase 14 为 STUB

本轮启动初，一个只读审计 agent 回报 **「Phase 14（AI Agent 工具循环）= 集成层 STUB：零件齐全但从未接线」**，并据此列出一长串「死代码 / 类型错误 / 缺失 i18n」。

**经直接读码 + 类型检查核实，该结论不成立**，属 agent 读取了过期文件快照。真实状态是：

| 被审计 agent 声称 | 直读真实状态 |
|---|---|
| `AIPanelProps` 未声明 `aiTools` | 已声明（`AIPanel.tsx:98` `aiTools?: AiToolHost`） |
| `AIPanel` 无 Agent UI、`agentSystem` 从未调用 | 有 `agentMode` 开关（`:161`）、`runAgent`（`:142`）、`agentSteps` 回显（`:1370`）；`agentSystem()` 在 `aiStore.ts:885` 被调用 |
| `streamChatWithTools` 无人调用 | `aiStore.runAgent`（`aiStore.ts:903`）正是其唯一调用方 |
| `aiStore` 引入工具零件从不使用 | `runAgent` 用到 `streamChatWithTools`/`AI_TOOL_DEFS`/`createToolExecutor`/`agentSystem` |
| `ai.agentTool.*` i18n 为空 | `zh-CN.json:391` / `en-US.json:391` 已含 4 个键 |
| `aiTools` 死 prop 触发 TS 报错 | `tsc --noEmit` **零错误**，`pnpm build` **通过** |

**结论：Phase 14 已完整落地、可经 UI 触发、编译通过。**本复盘以直读事实为准，不沿用该误判。

---

## 二、真值核查结论（当前真实状态）

- **空/假实现：无。** Phase 1–14 全部功能可从 UI 点击抵达真实后端/输出。
- **前后端对齐：18/18 `invoke` 命令与 Rust `#[tauri::command]` 100% 对齐**（历史审计结论仍成立；本轮未改动 Rust）。
- **编译健康**：`tsc --noEmit` exit 0；`pnpm build` exit 0（137 模块）。
- **AI 历史仅落 localStorage**（架构事实，非 bug）：按截图哈希分桶，天然把「截图」作为文档锚点，桌面端足够。

### 当前 AI 能力全景（真实可达）

| 能力 | 状态 | 关键位置 |
|---|---|---|
| 生成 / 润色 / 多轮追问 | ✅ | `aiStore.generate/refine/chat` |
| AI Agent 智能编辑（圈选/打码/高亮/区域 OCR） | ✅ | `aiStore.runAgent` → `streamChatWithTools` → 编辑器 `AiToolHost` |
| 长期记忆（多因子+MMR 相关性检索） | ✅ | `aiStore.selectMemories/compactMemory` |
| 导出 md/txt/html/docx/pdf/xlsx | ✅ | `AIPanel` 各 `handleExport*` |
| 跨截图 AI 文档历史库（搜索/阅读/续写/删除/6 格式导出） | ✅ | `AIPanel` 历史覆盖层 + `aiStore.recordConvMeta` |
| 文档主题（5 套）/ 封面副标题 / 自动目录 | ✅ | `markdownHtml.ts` + `AIPanel` |
| 图文报告（`<!--SNAP:k-->` 章节内嵌截图） | ✅ | `aiPresets.report` + `markdownDocx/Html` |
| 多截图附加 / 模板管理 / 拖拽重排章节 | ✅ | `AIPanel` |
| token 成本透明 / 错误分类 / 提示缓存发送 | ✅ | `aiClient.ts` |
| 流式健壮解析 / 退避重试 / 上下文预算护栏 | ✅ | `aiClient.ts`（Phase 13） |

---

## 三、本轮实修的 3 项真实增强（纯前端 / 零 Rust / 构建绿）

### ① Agent 模式「视觉断链」bug 修复（核心卖点保真）
- **根因**：`runAgent` 原按 `input.preset.vision` 决定是否携带截图。若用户开启 Agent 模式但当前选中的是「非视觉预设」（如翻译、纯文案），`images=undefined`，模型**拿不到截图** → 它给出的 0~1 坐标圈选/打码全部失去视觉依据、等于空转。
- **修复**：`aiStore.ts` 的 `runAgent` 改为 **Agent 模式强制携带当前截图**（忽略 `preset.vision`），确保坐标编辑始终有图可依。
- 验证：`pnpm build` exit 0；`dist` 含 `智能编辑`。

### ② 工具循环「失控检测」（对齐 openclaw `tool-loop-detection`）
- **差距**：原 `streamChatWithTools` 仅靠 `maxToolTurns=8` 硬上限，无「无进展 / 重复」检测。一个困惑的模型可连续多轮发出**完全相同**的工具调用（如反复打同一块码），白白烧 8 轮 token。
- **修复**：`aiClient.ts` 新增 `stableToolKey()`（同名+同参归一化指纹）+ `MAX_REPEAT_TOOL=3` 连续重复阈值。检测到连续 3 轮相同工具调用即提前终止并附 `[已自动停止：检测到连续重复的工具调用，疑似陷入循环]` 提示，避免空转。
- 这是直采 openclaw `tool-loop-detection` 的 no-progress 思想（其用 10/20/30 三级阈值 + sha256；桌面端 8 轮上限下收紧为 3，更安全）。

### ③ 提示缓存命中「可视化」（对齐 openclaw `anthropic-payload-policy`）
- **差距**：`buildBody` 已向 Anthropic 发 `cache_control: ephemeral`（Phase 13 行为），但 `processFrame` 从未解析 `cache_creation_input_tokens` / `cache_read_input_tokens`，UI 永远只显示 input/output，**缓存省下的 token 对用户完全不可见**。
- **修复**：
  - `aiTypes.ts` 的 `AiUsage` 增 `cacheCreate?` / `cacheRead?`；
  - `aiClient.ts` 的 `processFrame` 在 Anthropic `message_start` 与 OpenAI `prompt_tokens_details.cached_tokens` 处回填；
  - `AIPanel.tsx` 用量行追加 `⚡{n} 缓存命中`，并补 `ai.usageCache` 中英 i18n 键。
- 价值：把「沉默的成本优化」变成用户可见的「⚡ N cached」，强化「省钱/快」的体感——正是顶级项目做提示缓存回显的用意。

---

## 四、对照顶级项目的差距地图

### 已追平（无需再做）
SSE 健壮帧解析、`data:` 跨 chunk 拼接、尾部 flush；指数退避重试（尊重 429 Retry-After）；上下文预算护栏（裁最旧+保留最近轮）；记忆多因子+MMR 相关性检索；token 成本估算；错误语义分类；提示缓存**发送**；**Agent 工具循环本体**（含 OpenAI/Anthropic 双格式）；会话按截图哈希分桶隔离。

### 仍有差距（按优先级）

| 差距 | 对标来源 | 优先级 | 说明 |
|---|---|---|---|
| 语义记忆（向量 embedding 检索） | openclaw `hybrid.ts`（向量×0.7+文本×0.3） | 中 | 当前为词法重叠+多因子，长对话相关性弱于向量检索 |
| 增量压缩链（previousSummary 链式合并） | claw-code `compact.rs:282` / openclaw `compaction.ts` | 低 | 当前每轮非流式重摘要，未做链式累积 |
| 选区 / 划词 AI | —（竞品常见） | 中 | 在截图上框选文字/区域 → 即时翻译/解释/润色，高频刚需 |
| 后端代理隐藏 Key | privdoc-ai / 安全诉求 | 中 | 需 Rust 端代理，密钥不下发渲染层 |
| 隐私哨兵 Agent | —（差异化卖点） | 中 | 模型自动识别截图中手机号/头像/金额并打码 |
| 会话 fork / 项目级长期记忆 | claw-code `session.fork` | 低 | 桌面端 localStorage 分桶已够，fork 为锦上添花 |
| PPT 导出 / docx 高级排版 | 文档润色 | 低 | 当前 6 格式+5 主题已强，PPT 与精密排版为延伸 |

---

## 五、文档生成润色：现状与下一步

**已实现（行业级）**：6 种导出格式（md/txt/html/docx/pdf/xlsx）、5 套主题（现代/雅致/杂志/产品推广/科技风）、封面 hero + 副标题、自动目录 TOC、图文混排（报告章节内嵌截图）、docx 主题封面+目录、中文 typography。

**下一步润色候选**：
1. **PPT 导出**：截图类文档最自然的演示形态，竞品稀缺 → 强差异化。
2. **docx 页面尺寸 / 中英混排优化 / 表格斑马纹**：当前 docx 为功能级，距「精美」尚有打磨空间。
3. **品牌色 / 自定义副标题 UI 外露**：`markdownHtml` 已支持 `subtitle`/`brandColor`，可让用户在导出面板自选。
4. **表格/代码块主题化增强**：科技风下代码块、表格的视觉一致性。

---

## 六、AI 卖点挖掘：让 AI 成为系统真正卖点

**头号卖点已落地且真实可用**：`AI 智能编辑`（Agent 模式）——模型**直接调用工具修改当前截图**（圈选 / 打码 / 高亮 / 区域 OCR），竞品的 AI 只产出「文字」，我们的 AI 能「动手改图」。这是与同类截图工具最本质的差异化，应作为主推点。

**强化方向（让卖点更锋利）**：
1. **编辑过程实时可见**：Agent 执行工具时，画布上的矩形/打码/高亮应**即时出现**（当前已通过 `addAnnotation` 落图，但可加视觉高亮当前正在编辑的标注），让用户「看 AI 工作」。
2. **扩充工具集**：新增 `draw_arrow`（箭头批注）、`add_text_callout`（文字说明气泡）、`crop_region`（裁剪重点）——这些是「圈出重点并加说明」最高频动作。
3. **隐私哨兵（Privacy Sentinel）**：模型先扫描截图识别敏感信息（头像/姓名/手机号/金额/地址），自动建议/执行打码——把「打码防泄漏」从手动升级为 AI 主动，强卖点。
4. **多图批量 Agent**：对一组截图一次性「统一风格标注 + 逐张成稿」，呼应图文报告但由 AI 驱动标注。
5. **选区划词 AI**：在截图上框选一段文字即出「翻译/润色/解释」，零上下文切换。

---

## 七、优先级路线图（Phase 15+）

- **P0｜立即可做（纯前端，零 Rust）**
  - 新增 Agent 工具：`draw_arrow` / `add_text_callout`（强化头号卖点）
  - 选区 / 划词 AI（高频刚需）
  - 语义记忆（可选本地 embedding，如 `transformers.js` 轻量模型；无网络依赖）
- **P1｜需设计确认 / 少量 Rust**
  - 后端代理隐藏 Key（密钥不下发渲染层）
  - 隐私哨兵 Agent（自动识别敏感区并打码）
  - 多图批量 Agent
- **P2｜打磨**
  - PPT 导出、docx 高级排版、品牌色/副标题 UI 外露
  - 会话 fork、项目级长期记忆
  - 增量压缩链（previousSummary）

---

## 八、验证

- `npx tsc --noEmit` → **exit 0，零错误**
- `pnpm build` → **exit 0，137 模块，零类型错误**
- `dist` 含新 token：`缓存命中` / `智能编辑` / `cacheRead` / `agentSteps`

> 本轮严守「增量、不推翻重构、不动未请求功能」原则：仅修 3 处真实增强，全部纯前端、零 Rust 改动、构建全绿。重启用 `./start.sh dev` 或 `pnpm build && ./start.sh app` 即可见。
