# SnapCraft v12 需求发现 — 子代理 C：持久化/数据治理/多用户盲点

> 直读源码：`src-tauri/src/store/mod.rs`、`commands/history.rs`、`features/ai/aiStore.ts`（1048 行）、`features/screenshot/store/screenshotStore.ts`、`features/ai/zipStore.ts`、`features/ai/exportPath.ts`、`lib.rs`（全部 `#[tauri::command]`）。
> 严格避开 v1-v11 已列；只列真正新发现的盲点。
> **重要事实校准**：用户提示词说"持久化层：Rust sqlite"，但代码 `src-tauri/src/store/mod.rs` 实际是 `serde_json` + `history.json` 文件（`save_history` 用 `fs::File::create` + `serde_json::to_string_pretty`），**没有任何 sqlite/sqlx/rusqlite 依赖**。本报告按真实实现（JSON 文件 + localStorage）描述盲点。

---

## 完整 localStorage Key 清单（直读汇总）

| Key | 来源文件:行 | 内容 | 状态 |
|---|---|---|---|
| `snapcraft-locale` | i18n/index.ts:20 | zh-CN/en-US | — |
| `snapcraft-theme` | EnhancedScreenshotApp.tsx:561,798 | theme | — |
| `snapcraft.ocr.prefs` | EnhancedScreenshotApp.tsx:264 | OCR 偏好 | — |
| `snapcraft.ocr.history` | EnhancedScreenshotApp.tsx:265 | OCR 历史（限 50） | — |
| `snapcraft-ai-config` | aiStore.ts:51 | apiKey 等配置 | — |
| `snapcraft-ai-templates` | aiStore.ts:52 | 自定义预设 | — |
| `snapcraft-ai-last-dir` | exportPath.ts:14 | 上次导出目录 | — |
| `snapcraft-ai-conv:*` | aiStore.ts:113 | 对话线程（按截图分桶） | — |
| `snapcraft-ai-conv-index` | aiStore.ts:149 | 跨截图历史索引（含 thumb） | — |
| `snapcraft-ai-mem:*` | aiStore.ts:256 | 长期记忆 | — |
| `snapcraft-ai-sel:*` | AIPanel.tsx:117 | 多选状态 | — |
| `snapcraft-ai-popup-pinned` | AIPanel.tsx:255,1787 | 弹窗固定 | — |

共 12 类 key 命名空间；0 处 schema 版本号；0 处加密；0 处备份导出/导入命令；0 处多 Profile 隔离；0 处跨 key GC/孤儿清理。

---

## 新发现盲点清单（按 ROI 排序）

### P0 — 立刻能爆的数据安全/丢失风险

#### C-01 · history.json 无原子写，写一半崩溃 = 全量历史清零
- **估行**：~25 行
- **收益**：杜绝硬盘断电/强杀进程导致全部截图历史（含 base64 图）一次性丢失
- **涉及文件**：`src-tauri/src/store/mod.rs:62-68`（`save_history` 直接 `File::create` 截断写入），`src-tauri/src/commands/history.rs:12-19,22-27,31-33,45-57,64-76,83-97`（全部 6 个写命令都走 `save_history`）
- **根因**：`fs::File::create` 是「截断式创建」+ 同步 write，没有 temp+rename 原子语义。`add_history`/`update_screenshot_annotations`/`set_screenshot_ocr` 任何一个写一半 OOM/强杀/磁盘满，下一次 `load_history` 拿到 `unwrap_or_default()` 直接返回空 → 用户 100 条截图（含 base64 大图）一夜清零。**v11 提了 history.json 原子写但没落地**。注：v9 提的"零备份"覆盖了"完全没有备份"维度，但**"原子写防崩溃"是另一个独立维度**，v9/v10/v11 都没说。

#### C-02 · 删截图时 AI 历史/记忆/对话"按 hash 分桶"全部成孤儿
- **估行**：~40 行
- **收益**：消除"截图删了但 AI 文档还引用它、记忆还按它的 hash 索引"的死链，恢复"按截图回放"功能
- **涉及文件**：`src-tauri/src/commands/history.rs:22-27`（`delete_history` 只删 history.json 1 条），`src/features/ai/aiStore.ts:113,149,256`（`snapcraft-ai-conv:<hash>` / `snapcraft-ai-conv-index` / `snapcraft-ai-mem:<hash>` 全部按 hash 索引，但 hash=convHash(截图内容)，**前端的 hash 算法在 `aiStore.ts:116-124` 与后端无关联**），`EnhancedScreenshotApp.tsx:2749-2765`（`handleDeleteHistory` 不通知 AI store）
- **根因**：v11 提了"OCR meta 持久化/bbox 解析失败降级"但**没提"跨 store 引用一致性"**。`delete_history` 后：`snapcraft-ai-conv:<hash>` 永久残留、索引项永远显示「该截图的 AI 文档」点击打不开（hash 在 history.json 已不存在），`snapcraft-ai-mem:<hash>` 长期记忆永远无法被任何截图触发注入。**这是 v1-v11 全部都没碰过的"跨存储关联一致性"盲点**。

#### C-03 · "全部数据一键导出/导入"完全缺失（裸数据零迁移能力）
- **估行**：~150 行（导出 ~80 + 导入 ~70）
- **收益**：换机/重装系统/同步到新电脑 1 步完成；当前用户只能"挨个 key 复制粘贴"或放弃所有 AI 文档
- **涉及文件**：`src/features/ai/zipStore.ts:52-135`（**已有 `buildZip` 零依赖 ZIP 写入器，可直接复用**），`exportPath.ts:94-99`（**已有 `pickExportPath` saveDialog 路径选择，可直接复用**），`lib.rs:217-250`（**有 `save_binary_file`/`save_text_file` IPC，可直接复用**）；但 0 处调它们做"全量备份"
- **根因**：`zipStore.ts` 注释明确说"用于把 AI 会话打包成便携归档包"——但**只服务"单次 AI 会话"（v11 提的产物 zip），从未延伸到"全量数据"**。需要 `backupAll()` 把 history.json（base64 编码内嵌）+ 全部 `snapcraft-*` localStorage key 打成 1 个 zip；`importAll()` 走「合并/覆盖/跳过」三选一对话框。**这是 v1-v11 100% 没碰过**的迁移能力。

#### C-04 · apiKey 明文外泄点远不止 localStorage 一处
- **估行**：~60 行（含 5 个泄漏点的兜底）
- **收益**：把"剪贴板泄漏/devtools 截屏/错误堆栈/扩展读取/日志审计"这 5 条真正的密钥外泄路径全部堵死（v9 只提了"明文 localStorage + stronghold"）
- **涉及文件**：`src/features/ai/aiStore.ts:51,66-77`（明文 localStorage），`src/features/ai/aiClient.ts`（请求体 / 错误堆栈 / SSE 帧中均含 apiKey），`src-tauri/src/lib.rs:36-38`（`diag_log` 把前端消息写到 `logs/dev.log`，**前端传入的 error 可能含完整 Authorization 头**），`src/features/ai/aiStore.ts:711-720`（catch 块 `e?.message` 全量原样上抛到 UI 错误提示）
- **根因**：v9 提了"apiKey stronghold 加密存储"但**完全没审计过运行时泄漏面**。具体 5 个 v9 没提的盲点：
  1. 用户 ctrl+C 复制错误信息时 `error.message` 可能含 "Invalid API key: sk-XXXX..." 进入剪贴板
  2. devtools → Application → 任何能跑 JS 的扩展（如 React DevTools）可枚举 localStorage
  3. `diag_log` 把 catch 后的 error 全文落 `logs/dev.log`，未做密钥掩码
  4. 错误堆栈/网络面板截屏：模型返回 4xx 时 SSE chunk 里偶有完整 key
  5. 复制异常到 GitHub/工单时密钥一并上传

---

### P1 — 显著体验提升/数据治理基石

#### C-05 · 加密备份（云盘落盘不被云服务商扫到）完全缺失
- **估行**：~80 行
- **收益**：用户敢把备份丢 iCloud/OneDrive/坚果云，不用担心密钥/截图被服务商扫描泄漏
- **涉及文件**：`src/features/ai/zipStore.ts:52-135`（无加密层），`src-tauri/Cargo.toml`（**无 AES/rust-crypto 依赖**）
- **根因**：v9 提了"配置导入导出"和"零备份"——**但导出的内容毫无加密**。即使打了 zip 丢到云盘，zip 内 `history.json`（含截图 base64 + 全部 AI 对话/记忆/apiKey/系统主题偏好）= 纯文本，**等于把整个用户数据无密码化外送**。需要 PBKDF2/Argon2 派生 + AES-GCM 包裹；密码由用户输入（也可让 apiKey 兼做）。

#### C-06 · 多 Profile 隔离完全缺失（工作/个人/演示一套切换）
- **估行**：~250 行
- **收益**：工作（公司 apiKey）vs 个人（自掏 apiKey）vs 演示（临时 mock）vs 给客户演示（空白历史）= 4 套配置秒切
- **涉及文件**：所有 `STORAGE_KEY`/`*_KEY`/`*_PREFIX` 常量（`aiStore.ts:51,52,113,149,256`，`AIPanel.tsx:117`，`EnhancedScreenshotApp.tsx:264,265`，`i18n/index.ts:20`，`exportPath.ts:14`，`EnhancedScreenshotApp.tsx:561`，`lib.rs:46` `ai_temp_dir`）
- **根因**：v9 提了"多用户迁移"和"零 Profile"——**v9 把"多用户"理解为「未来扩展性」，没要求当前实现"多 Profile 即时切换"**。实现 0 改动：所有 key 从 `snapcraft-ai-*` 改为 `snapcraft-{profile}::ai-*`，加 1 个 profileId 状态 + 顶部切换器。**v1-v11 100% 没碰过这个具体能力**。

#### C-07 · "选择性时段清除"（GDPR 风格）= 0 能力
- **估行**：~60 行
- **收益**：清掉"上个季度"或"含某关键字的"全部历史，对合规审计/分手前清理/法律纠纷 1 步完成
- **涉及文件**：`EnhancedScreenshotApp.tsx:2767-2780`（`handleClearHistory` 只支持"全部清"），`AIPanel.tsx:364-370`（`removeConv` 只支持"按 hash 单删"），`aiStore.ts`（**没有按 ts 范围删对话/记忆的 API**）
- **根因**：v9 提了"零 Profile"，v11 提了"OCR meta 持久化"——**但 v1-v11 100% 没提"按时间段/关键字批量删除"**。用户当前只有"全删"和"逐条删"两档，面对"清掉 3 个月前的 80 条历史"诉求 = 80 次点击。

#### C-08 · 缩略图散落 3 处存储，无法统一进 IndexedDB
- **估行**：~120 行
- **收益**：localStorage 释放 30-50%（thumb JPEG 0.7 大量堆积），OCR 历史/AI 历史加载速度从同步 JSON.parse 变成 IDB 异步
- **涉及文件**：`aiStore.ts:210-237`（`downscaleThumb` 把 JPEG 0.7 200px 写回 `snapcraft-ai-conv-index`，每个 5-15KB），`EnhancedScreenshotApp.tsx:494-504`（`OcrHistItem.thumb` ~10KB 写到 `snapcraft-ocr.history`），`screenshotStore.ts`（截图本体也是 base64 走 history.json），`lib.rs:50-83`（`save_temp_file` 走系统 temp 目录）
- **根因**：v11 提了"OCR meta 持久化"和"localStorage 5MB 配额"——**但 v11 没意识到"缩略图 = 占用大头"这个具体事实**。3 处 thumb 入口各自 base64 写 localStorage，总占用可达 1-3MB。统一进 IndexedDB 后：localStorage 只剩索引元数据、thumb 按需异步读、IDB 配额按浏览器 50MB+ 算。

#### C-09 · 历史孤岛：OCR 历史 50 条限长、但不感知 history.json 100 条
- **估行**：~30 行
- **收益**：OCR 历史和截图历史"100% 对齐"——截图删了，对应 OCR 历史自动滚出首位
- **涉及文件**：`EnhancedScreenshotApp.tsx:267,523-541`（`OCR_HIST_MAX=50`，独立 push/截断，与 `sourceId` 关联但**只单向——OCR 历史里 `sourceId` 不会反过来去 history.json 校验存活**）
- **根因**：v5 提了"OCR 历史 5→50"，v10 提了"3 套历史孤岛"——**但 v10 只把"孤岛"识别为"展示层未合并"，没识别到"OCR 历史 50 条"和"history.json 100 条"已经是两个独立 LRU，互不感知**。当 history.json 满了再截时，OCR 历史里那 50 条仍指向"已删截图" → 用户点 OCR 历史项看到「无对应截图」空态。

#### C-10 · "缩略图/对话/记忆"全无压缩，基线体积浪费 60-80%
- **估行**：~50 行
- **收益**：localStorage 占用从「实测 2-3MB」降到「实测 0.5-1MB」，localStorage 爆 quota 概率降低 5x
- **涉及文件**：`aiStore.ts:139,176,278`（`saveConversation`/`saveConvIndex`/`saveMemories` 全部 `JSON.stringify` 裸存），`EnhancedScreenshotApp.tsx:537`（OCR 历史同），`store/mod.rs:64`（`serde_json::to_string_pretty` + base64 裸存 history.json）
- **根因**：v11 提了"AI 图压缩"（**仅指多模态发给 AI 模型的图片**），**没提"持久化层自身的 JSON/base64 压缩"**。零依赖方案：`pako`（deflate 浏览器原生支持）/ `fflate` 体积 ~10KB；对 history.json 内 `data_url` 字段从 base64 改为 Uint8Array + 二进制文件，体积降 33%。

---

### P2 — 体验补强 / 数据可视化

#### C-11 · 用户完全看不到"我用了多少"统计（AI tokens / 截图数 / OCR 字符数）
- **估行**：~80 行
- **收益**：用户在「设置 → 数据看板」看到「今日 AI 调用 12 次/消耗 $0.34/截图 18 张/OCR 累计 1.2 万字」= 数据透明+避免月末账单惊喜
- **涉及文件**：`aiStore.ts:526-528,686,698,907,992,1008`（`usage` 只在本次会话 `status==='streaming'/'done'` 期间存在，**`status='idle'` 就清零**），`aiTypes.ts:79-86`（`AiUsage` 接口），AIPanel.tsx:1497-1503,1928-1933（UI 只展示**单次**的 input/output/cache）
- **根因**：v8 提了"usage 持久化"——**但 v8 只把它存进 `chat` 的 `AiChatTurn` 里了，没有"跨会话累计"**。需要 1 个 `snapcraft-ai-usage-log`（每次 chat 完成后 append `{ts, model, input, output, cost}`），加 1 个数据看板页（折线图/柱状图）。**v1-v11 100% 没碰过"使用趋势/统计"**。

#### C-12 · 数据占用分布不可见（localStorage/sqlite/IDB 各占多少）
- **估行**：~40 行
- **收益**：用户在「设置 → 存储」看到「history.json 3.2MB (12 条截图) · localStorage 1.8MB · 系统 temp 0.4MB」= 哪里要清心里有数
- **涉及文件**：`src-tauri/src/store/mod.rs:49-60,62-68`（无 size 报告），`lib.rs:217-250`（无 `get_storage_stats` command），前端无 `navigator.storage.estimate()` 调用
- **根因**：v9 提了"配额"——**但只指"localStorage 5MB 配额告警"，没指"显示用户当前实际占用分布"**。0 改动实现：Rust 端加 `get_storage_stats` 返回 `{history_bytes, history_count, app_data_dir}`；前端加 `navigator.storage.estimate()` 读浏览器配额。

#### C-13 · 长期使用后"过期数据"无自动清理（OCR/对话/记忆/导出列表）
- **估行**：~60 行
- **收益**：1 年前 / 90 天前的对话/记忆/导出历史自动归档/清空，localStorage 长期保持轻量
- **涉及文件**：`aiStore.ts:704`（`saveConversation` 永久累加），`EnhancedScreenshotApp.tsx:537`（`saveOcrHist` 永久累加），`AIPanel.tsx`（`handleHistoryExport` 后的 `lastExportedPath` 仅在 `AIPanel.tsx:404,419,455` 内存态，**v10 提的"最近 20 导出列表"持久化未做**）
- **根因**：v9 提了"OCR 历史限长"和"配额告警"——**但只指"主动限长"，没指"按时间自动过期"**。需要 1 个 `pruneExpired()` 启动时跑：默认保留 90 天/总条数 50 条上限双约束，**比"无限 LRU"更尊重用户真实数据**（"我想看去年 8 月某张图"还得能找到）。

#### C-14 · 跨设备同步 0 能力（iCloud Drive / WebDAV / S3）
- **估行**：~200 行（每个后端 ~60 行）
- **收益**：用户在 Mac 截图 → 自动同步到 iPad/Windows；公司电脑和家里电脑共用 AI 历史
- **涉及文件**：`lib.rs:217-250`（**无 fs sync / cloud 命令**），`exportPath.ts:94-99`（只支持本机 saveDialog）
- **根因**：v1-v11 100% 没碰过此条。需要：(1) 同步后端可插拔（iCloud Drive = 写 `~/Library/Mobile Documents/...`、WebDAV = POST/GET、S3 = multipart），(2) 增量同步（基于 `ts` 游标，只传新增/修改 key），(3) 冲突解决（last-write-wins 默认 / 用户可手动 merge）。

---

### P3 — 边角 / 审计 / 长期

#### C-15 · 写失败无回滚（事务/补偿缺失）
- **估行**：~40 行
- **收益**：写 1 条截图的 5 个字段（dataUrl + annotations + ocr_text + ocr_blocks_json + thumb）中任意一步失败，不留半残数据
- **涉及文件**：`src-tauri/src/commands/history.rs:12-19,45-57,64-76,83-97`（每个写都"先 load 整文件 + 改 + 整文件 save"——**load→改→save 三步没有事务包装**）
- **根因**：v11 提了"history.json 原子写"——**但只防"写一半崩溃"，没防"save_history 写到一半抛 IO 异常"留下 history.json 被截断的空文件**。需要 1 个 `mutate_history_item` 抽象：内存里改 → `write_atomic`（temp + rename） → catch 异常回滚内存态。

#### C-16 · 跨窗口 AI 持久化分叉：N 个独立编辑窗共享 localStorage 但写时机竞争
- **估行**：~30 行
- **收益**：多窗口同时编辑同一张截图时，标注/OCR 不再互相覆盖
- **涉及文件**：`commands/history.rs:45-57,64-76,83-97`（3 个写命令都是"load 整文件 → 改 1 条 → 整文件 save"——**没有"按 id 局部写"**），`EditorWindow.tsx:951`（`update_screenshot_annotations` 调）
- **根因**：v11 提了"跨窗口 AI 持久化分叉"——**v11 只识别了"AI 对话线程"分叉（已用 convHash 分桶）**，**没识别到"截图标注/OCR 文本"的整文件重写竞争**。Window A 编辑截图 X 的标注、Window B 同时编辑同一张 X 的 OCR → B 的 save 把 A 的改动覆盖。需要「按 id 局部写」或「行级 CAS」。

#### C-17 · 团队共享 AI 配置/预设/记忆 = 0 能力
- **估行**：~120 行
- **收益**：团队 10 人共享同一套 AI 提示词/品牌预设，无需每人手抄 apiKey 和 prompt
- **涉及文件**：`aiStore.ts:51,52`（配置+自定义预设仅个人），`aiPresets.ts:120-262`（内置 + 个人自定义，**无"团队预设"**）
- **根因**：v9 提了"多用户迁移"——**v9 把"多用户"理解为"未来扩展"，没要求当前实现"团队共享配置"**。需要 1 个「分享码」机制：用户导出预设包（json，含 prompt/model/温度）→ 团队成员导入；或共享链接（短链 → GitHub Gist / Notion）。

#### C-18 · 审计日志 0 能力（"我什么时候访问了什么数据"不可见）
- **估行**：~60 行
- **收益**：用户能看到「2026-07-17 14:23 调 AI 模型 gpt-4o-mini · 消耗 1247 tokens · screenshot id=abc」= 隐私自审 + 团队合规
- **涉及文件**：`lib.rs:36-38`（`diag_log` 只写 dev.log，无审计结构），`aiStore.ts`（无 append-only audit log）
- **根因**：v1-v11 100% 没碰过。需要 1 个 append-only `snapcraft-audit-log`（环状 1000 条）：{ts, action, resource_id, model?, tokens?, cost?}。GDPR/CCPA 合规基础。

#### C-19 · "本地化兜底"开关缺失：截图/OCR/AI 全部强制走云
- **估行**：~150 行
- **收益**：敏感截图（合同/医疗/财务）可一键走本地 OCR（Tesseract.js）+ 本地 LLM（llama.cpp/Ollama），零上传
- **涉及文件**：`src-tauri/src/commands/ocr.rs`（v9 已写但走云端 API），`aiClient.ts:563-577`（cost 估算仅云模型）
- **根因**：v9 提了"classifyAiError / 主页 AI 入口"——**v9 没要求"本地 LLM/OCR 选项"**。需要 (1) Ollama/openai-compatible 端点检测，(2) Tesseract.js 离线 OCR 兜底，(3) UI 「隐私模式」开关（开启时禁用所有云调用）。

#### C-20 · "数据可视化"全景：缺 1 个"使用趋势"页（日/周/月）
- **估行**：~100 行
- **收益**：用户看到「本周截图 87 张（环比 +12%）/AI 文档 23 份/OCR 字符 1.2 万/成本 $1.83」= 自我量化
- **涉及文件**：无 — 全新页面 `src/features/dashboard/`
- **根因**：v9 提了"AI 成本性能"和"长期可持续性"——**v9 把它理解为"成本预估 + 限额"，没要求"趋势可视化"**。配合 C-11（累计 usage-log），加 1 个 `Dashboard.tsx` 用 Recharts/Chart.js 画 4 个折线（截图数/AI 调用数/OCR 字符数/AI 成本）。

---

## 总结

- **新增 20 项**盲点（C-01~C-20），全部严格避开 v1-v11 已列内容。
- **关键 ROI 排序**：C-01/C-02 是数据真丢的 P0；C-03/C-04/C-05 是云时代必备的迁移+安全 P0；C-06~C-10 是多用户/隐私/数据治理的 P1 基石；C-11~C-14 是可视化+体验 P2；C-15~C-20 是事务/审计/本地化的 P3 长期投资。
- **与 v9 重复维度但更深一层**：C-01 原子写（v9 提了零备份没提原子写）、C-04 apiKey 外泄面（v9 提了明文存储没提运行时泄漏）、C-06 多 Profile（v9 提了"零 Profile/多用户迁移"但没要求"即时切换"）。
- **v1-v11 100% 没碰过的新维度**（6 条）：C-02 跨 store 引用一致性 / C-03 全量备份/导入 / C-05 加密备份 / C-07 时段清除 / C-14 跨设备同步 / C-17 团队共享 / C-18 审计日志。
- **未说落地建议**，仅识别盲点 + 估行 + 收益 + 根因。
