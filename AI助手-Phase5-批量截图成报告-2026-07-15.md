# AI 助手 Phase 5 — 批量截图一键成报告（图文混排）

> 参考 `privdoc-ai` 的文档/模板/导出与 agent 会话模型，结合 SnapCraft 业务，把「截图工具」推向「完整文档生成器」。
> 日期：2026-07-15 · 实现：Senior Developer (高级开发工程师)

## 需求发掘

对照 privdoc-ai 的「多源分析 / 结构化导出 / 会话持久化」，复盘 Phase 1–4 已补齐的能力，确诊 SnapCraft AI 仍缺**最关键的一环闭环**——

- Phase 2b 已支持「附加更多截图」把多张图塞进同一次生成，但本质是「多图喂给一个 prompt」，AI 输出是一整段文字，**截图并没有成为文档里有秩序的章节**；
- 导出（.md/.txt/.html/.docx/.pdf）也只会把**全部截图整块堆在正文前**，无法做到「这张图对应这一段分析」。

而 privdoc-ai 是纯文档工具、根本没有「图」。SnapCraft 的独有价值恰恰是：**截图即证据，AI 文案应围绕每张截图逐节展开，图与文一一咬合**。这就是「完整文档」真正的样子。

## 本次实现（纯前端，零 Rust 改动，不影响截图/标注/OCR/历史）

### 1. 图文报告预设 `report`（aiPresets.ts）
- 新增 `report` 内置预设，system 指令要求模型：每张截图对应一节，每节以**单独一行的锚点标记 `<!--SNAP:k-->` 开头**（k 为从 1 起的阿拉伯数字，按截图顺序），其后用 `## 截图 k：<标题>` 展开，并由连贯叙述衔接成完整文档。
- 新增 `buildReportUser()`：按「①当前 / ②附加1 / ③附加2…」顺序把 OCR 原文分块注入，使模型清楚每张图对应哪个序号。
- 新增锚点约定与工具：`SNAP_MARKER_RE`、`stripSnapMarkers()`（.md/.txt 导出去标记）、`hasSnapMarkers()`（判断是否需要图文混排）。

### 2. 章节级图文混排导出（markdownDocx.ts / markdownHtml.ts）
- `markdownToDocx` 与 `mdToHtml` 均新增 `sectionImages` 参数：解析到 `<!--SNAP:k-->` 标记时，把第 k 张截图**内嵌到对应章节之前**，实现真正的「图随文走」。
- 健壮性：**当且仅当输出含 SNAP 标记**才启用章节混排；否则回退为原有的「整块前置内嵌」（老行为不变）。标记行在 .md/.txt 导出时被剥离，保证文件干净。

### 3. 「成报告」工作流（AIPanel.tsx）
- 多截图选择从「无序 Set」升级为**有序数组** `selectedOrder`：缩略图显示 **①②③ 序号角标**，选择顺序 = 报告章节顺序；附「清空选择」与「按选择顺序组稿」提示。
- 操作区新增醒目按钮 **📑 成报告**（主题色描边+淡底，与「生成」主按钮形成层级）。点击时：自动开启「附带截图 / 附带识别文字」以保证标记与图片顺序严格对齐，切换至 `report` 预设并起草首稿。
- 所有导出（.md/.txt/.html/.docx/.pdf）统一走 `stripSnapMarkers` + `orderedImages()` + `hasSnapMarkers` 分支：报告自动图文混排，单图/普通预设仍沿用原逻辑。

### 4. i18n + CSS
- 中英文新增：`ai.makeReport` / `makeReportTitle` / `selOrder` / `clearSel` 及 `preset.report` / `preset.reportDesc`。
- 新增样式：`.ai-btn-report`（报告按钮）、`.ai-attach-badge`（序号角标）、`.ai-attach-foot`（选择底栏）。

## 验证
- `pnpm build`（tsc + vite）：exit 0，134 模块，零类型错误。
- `dist` 含全部新 token：CSS `ai-btn-report / ai-attach-badge / ai-attach-foot`；JS `makeReport / makeReportTitle / selOrder / clearSel / preset.report / SNAP: / 图文报告 / Illustrated report`。

## 与 privdoc-ai 的差异（独有价值）
| 维度 | privdoc-ai | SnapCraft Phase 5 |
|------|------------|-------------------|
| 素材 | 纯文本/文档 | 多张截图（可含标注/打码） |
| 文档形态 | 结构化文本 | **图文混排报告**：图与章节一一咬合 |
| 截图角色 | 无 | 作为可视化证据嵌入正文对应节 |

## 后续候选（未做，均纯前端、零后端风险）
- 长对话记忆压缩（超限自动摘要前 N 轮，对齐 privdoc-ai 的 conversation compact）
- 跨截图会话列表（当前每个截图一份独立线程，可加全局列表便于检索）
- 后端代理隐藏 Key（密钥不下发渲染层）
- `.xlsx` 导出（表格数据）
- 拖拽重排选择顺序（当前为点击顺序）

生效方式：`./start.sh dev` 或 `pnpm build && ./start.sh app`。
